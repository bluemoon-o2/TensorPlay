# @generated by tools/codegen/gen_pyi.py from functional.pyi.in
# mypy: disable-error-code="type-arg"
# mypy: allow-untyped-defs
# ruff: noqa: F401,PYI054

from typing import Any, overload

import tensorplay
from tensorplay import (
    Generator,
    MemoryFormat,
    contiguous_format,
)
from tensorplay.types import (
    Number,
    _bool,
    _complex,
    _device,
    _dtype,
    _float,
    _int,
    _size,
)
from tensorplay._C import (
    Device,
    DType,
    TensorBase,
)

__all__ = []

def abs_(input: TensorBase) -> TensorBase: ...

__all__ += ["abs_"]

def add(input: TensorBase, other: TensorBase | Number | _complex, alpha: Number | _complex = 1) -> TensorBase: ...

__all__ += ["add"]

def add_(input: TensorBase, other: TensorBase | Number | _complex, alpha: Number | _complex = 1) -> TensorBase: ...

__all__ += ["add_"]

def absolute_(input: TensorBase) -> TensorBase: ...

__all__ += ["absolute_"]

def addcmul_(input: TensorBase, tensor1: TensorBase, tensor2: TensorBase, value: Number | _complex = 1) -> TensorBase: ...

__all__ += ["addcmul_"]

def addcdiv_(input: TensorBase, tensor1: TensorBase, tensor2: TensorBase, value: Number | _complex = 1) -> TensorBase: ...

__all__ += ["addcdiv_"]

@overload
def arange(end: Number, *, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...
@overload
def arange(start: Number, end: Number, *, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...
@overload
def arange(start: Number, end: Number, step: Number, *, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["arange"]

def bernoulli_(input: TensorBase) -> TensorBase: ...

__all__ += ["bernoulli_"]

def cauchy_(input: TensorBase, median: _float = 0.0, sigma: _float = 1.0) -> TensorBase: ...

__all__ += ["cauchy_"]

def clone(input: TensorBase, memory_format: MemoryFormat | None = None) -> TensorBase: ...

__all__ += ["clone"]

def contiguous(input: TensorBase, memory_format: MemoryFormat = contiguous_format) -> TensorBase: ...

__all__ += ["contiguous"]

def copy_(input: TensorBase, src: TensorBase, non_blocking: _bool = False) -> TensorBase: ...

__all__ += ["copy_"]

def div(input: TensorBase, other: TensorBase | Number | _complex) -> TensorBase: ...

__all__ += ["div"]

def div_(input: TensorBase, other: TensorBase | Number | _complex) -> TensorBase: ...

__all__ += ["div_"]

@overload
def empty(*size: _int, dtype: _dtype = ..., device: Device | None = None, pin_memory: _bool = False, requires_grad: _bool = False) -> TensorBase: ...
@overload
def empty(size: _size, *, dtype: _dtype = ..., device: Device | None = None, pin_memory: _bool = False, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["empty"]

def empty_like(input: TensorBase, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["empty_like"]

def expand(input: TensorBase, size: _size, implicit: _bool = False) -> TensorBase: ...

__all__ += ["expand"]

def expand_as(input: TensorBase, other: TensorBase) -> TensorBase: ...

__all__ += ["expand_as"]

def exponential_(input: TensorBase, lambd: _float = 1.0) -> TensorBase: ...

__all__ += ["exponential_"]

def fill_(input: TensorBase, value: Number | _complex) -> TensorBase: ...

__all__ += ["fill_"]

def full(size: _size, fill_value: Number | _complex, dtype: _dtype = ..., device: Device | None = None, pin_memory: _bool = False, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["full"]

def full_like(input: TensorBase, fill_value: Number | _complex, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["full_like"]

def geometric_(input: TensorBase, p: _float) -> TensorBase: ...

__all__ += ["geometric_"]

def item(input: TensorBase) -> Number | _complex: ...

__all__ += ["item"]

def kaiser_window(window_length: _int, periodic: _bool = True, beta: _float = 12.0, *, dtype: _dtype | None = None, device: Device | None = None, pin_memory: _bool = False) -> TensorBase: ...

__all__ += ["kaiser_window"]

def linear(input: TensorBase, weight: TensorBase, bias: TensorBase | None = None) -> TensorBase: ...

__all__ += ["linear"]

def lerp_(input: TensorBase, end: TensorBase, weight: TensorBase | Number | _complex) -> TensorBase: ...

__all__ += ["lerp_"]

def log_normal_(input: TensorBase, mean: _float = 1.0, std: _float = 2.0) -> TensorBase: ...

__all__ += ["log_normal_"]

def logspace(start: Number, end: Number, steps: _int, base: _float = 10.0, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["logspace"]

def linspace(start: Number, end: Number, steps: _int, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["linspace"]

def matmul(input: TensorBase, other: TensorBase | Number | _complex, *, out: TensorBase | None = None) -> TensorBase: ...

__all__ += ["matmul"]

def mm(input: TensorBase, mat2: TensorBase) -> TensorBase: ...

__all__ += ["mm"]

def mul(input: TensorBase, other: TensorBase | Number | _complex) -> TensorBase: ...

__all__ += ["mul"]

def mul_(input: TensorBase, other: TensorBase | Number | _complex) -> TensorBase: ...

__all__ += ["mul_"]

def neg_(input: TensorBase) -> TensorBase: ...

__all__ += ["neg_"]

def normal_(input: TensorBase, mean: _float = 0.0, std: _float = 1.0) -> TensorBase: ...

__all__ += ["normal_"]

@overload
def rand(*size: _int, dtype: _dtype | None = None, device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...
@overload
def rand(size: _size, *, dtype: _dtype | None = None, device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["rand"]

def rand_like(input: TensorBase, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["rand_like"]

def randint(low: _int, high: _int, size: _size, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["randint"]

def randint_like(input: TensorBase, low: _int, high: _int, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["randint_like"]

@overload
def randn(*size: _int, dtype: _dtype | None = None, device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...
@overload
def randn(size: _size, *, dtype: _dtype | None = None, device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["randn"]

def randn_like(input: TensorBase, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["randn_like"]

def random_(input: TensorBase, low: _int = 0, high: _int = 0) -> TensorBase: ...

__all__ += ["random_"]

def repeat(input: TensorBase, repeats: _size) -> TensorBase: ...

__all__ += ["repeat"]

def resize_(input: TensorBase, size: _size) -> TensorBase: ...

__all__ += ["resize_"]

def rsqrt_(input: TensorBase) -> TensorBase: ...

__all__ += ["rsqrt_"]

def select(input: TensorBase, dim: _int, index: _int) -> TensorBase: ...

__all__ += ["select"]

def slice(input: TensorBase, dim: _int = 0, start: _int | None = None, end: _int | None = None, step: _int = 1) -> TensorBase: ...

__all__ += ["slice"]

def sqrt_(input: TensorBase) -> TensorBase: ...

__all__ += ["sqrt_"]

def squeeze_copy(input: TensorBase, dim: _size | None = None) -> TensorBase: ...

__all__ += ["squeeze_copy"]

def sub(input: TensorBase, other: TensorBase | Number | _complex, alpha: Number | _complex = 1) -> TensorBase: ...

__all__ += ["sub"]

def sub_(input: TensorBase, other: TensorBase | Number | _complex, alpha: Number | _complex = 1) -> TensorBase: ...

__all__ += ["sub_"]

def trapz(y: TensorBase, x: TensorBase | None = None, dx: _float | None = None, dim: _int = -1) -> TensorBase: ...

__all__ += ["trapz"]

def unique_consecutive(input: TensorBase, return_inverse: _bool = False, return_counts: _bool = False, dim: _int | None = None) -> Any: ...

__all__ += ["unique_consecutive"]

def uniform_(input: TensorBase, from_: _float = 0.0, to: _float = 1.0) -> TensorBase: ...

__all__ += ["uniform_"]

def view(input: TensorBase, shape: _size) -> TensorBase: ...

__all__ += ["view"]

def where(condition: TensorBase, input: TensorBase | Number | _complex, other: TensorBase | Number | _complex) -> TensorBase: ...

__all__ += ["where"]

def zero_(input: TensorBase) -> TensorBase: ...

__all__ += ["zero_"]

def ones(*size: _int, dtype: _dtype = ..., device: Device | None = None, pin_memory: _bool = False, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["ones"]

def ones_like(input: TensorBase, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["ones_like"]

def zeros(*size: _int, dtype: _dtype = ..., device: Device | None = None, pin_memory: _bool = False, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["zeros"]

def zeros_like(input: TensorBase, dtype: _dtype = ..., device: Device | None = None, requires_grad: _bool = False) -> TensorBase: ...

__all__ += ["zeros_like"]

def abs(input: TensorBase) -> TensorBase: ...
def absolute(input: TensorBase) -> TensorBase: ...
def acos(input: TensorBase) -> TensorBase: ...
def acos_(input: TensorBase) -> TensorBase: ...
def acosh(input: TensorBase) -> TensorBase: ...
def acosh_(input: TensorBase) -> TensorBase: ...
def adaptive_avg_pool1d(
    input: TensorBase,
    output_size: _size,
) -> TensorBase: ...
def adaptive_avg_pool2d(
    input: TensorBase,
    output_size: _size,
) -> TensorBase: ...
def adaptive_avg_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
def adaptive_avg_pool3d(
    input: TensorBase,
    output_size: _size,
) -> TensorBase: ...
@overload
def adaptive_avg_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
@overload
def adaptive_avg_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def adaptive_max_pool1d(
    input: TensorBase,
    output_size: _size,
) -> tuple[TensorBase, TensorBase]: ...
def adaptive_max_pool2d(
    input: TensorBase,
    output_size: _size,
) -> TensorBase: ...
@overload
def adaptive_max_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
@overload
def adaptive_max_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    indices: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def adaptive_max_pool2d_with_indices(
    input: TensorBase,
    output_size: _size,
) -> tuple[TensorBase, TensorBase]: ...
def adaptive_max_pool2d_with_indices_backward(
    grad_output: TensorBase,
    input: TensorBase,
    output_size: _size,
    indices: TensorBase,
) -> TensorBase: ...
def adaptive_max_pool3d(
    input: TensorBase,
    output_size: _size,
) -> TensorBase: ...
@overload
def adaptive_max_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
@overload
def adaptive_max_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    indices: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def add_relu(input: TensorBase, other: TensorBase) -> TensorBase: ...
def addbmm(
    input: TensorBase,
    batch1: TensorBase,
    batch2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def addbmm_(
    input: TensorBase,
    batch1: TensorBase,
    batch2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def addcdiv(
    input: TensorBase,
    tensor1: TensorBase,
    tensor2: TensorBase,
    *,
    value: Number | _complex = 1,
) -> TensorBase: ...
def addcmul(
    input: TensorBase,
    tensor1: TensorBase,
    tensor2: TensorBase,
    *,
    value: Number | _complex = 1,
) -> TensorBase: ...
@overload
def addmm(
    input: TensorBase,
    mat1: TensorBase,
    mat2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
@overload
def addmm(
    input: TensorBase,
    mat1: TensorBase,
    mat2: TensorBase,
    out_dtype: _dtype,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def addmm_(
    input: TensorBase,
    mat1: TensorBase,
    mat2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def addmv(
    input: TensorBase,
    mat: TensorBase,
    vec: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def addmv_(
    input: TensorBase,
    mat: TensorBase,
    vec: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def addr(
    input: TensorBase,
    vec1: TensorBase,
    vec2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def addr_(
    input: TensorBase,
    vec1: TensorBase,
    vec2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def adjoint(input: TensorBase) -> TensorBase: ...
def affine_grid_generator(
    theta: TensorBase,
    size: _size,
    align_corners: _bool,
) -> TensorBase: ...
def affine_grid_generator_backward(
    grad: TensorBase,
    size: _size,
    align_corners: _bool,
) -> TensorBase: ...
def airy_ai(input: TensorBase) -> TensorBase: ...
def alias(input: TensorBase) -> TensorBase: ...
def alias_copy(input: TensorBase) -> TensorBase: ...
@overload
def all(input: TensorBase) -> TensorBase: ...
@overload
def all(input: TensorBase, dim: _int, keepdim: _bool = False) -> TensorBase: ...
@overload
def all(
    input: TensorBase,
    dim: _size | None = None,
    keepdim: _bool = False,
) -> TensorBase: ...
def allclose(
    input: TensorBase,
    other: TensorBase,
    rtol: _float = 1e-05,
    atol: _float = 1e-08,
    equal_nan: _bool = False,
) -> _bool: ...
def alpha_dropout(
    input: TensorBase,
    p: _float = 0.5,
    train: _bool = True,
) -> TensorBase: ...
def alpha_dropout_(
    input: TensorBase,
    p: _float = 0.5,
    train: _bool = True,
) -> TensorBase: ...
def amax(
    input: TensorBase,
    dim: _size = (),
    keepdim: _bool = False,
) -> TensorBase: ...
def amin(
    input: TensorBase,
    dim: _size = (),
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def aminmax(
    input: TensorBase,
    dim: _size = (),
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def aminmax(
    input: TensorBase,
    dim: _int | None = None,
    keepdim: _bool = False,
    *,
    min: TensorBase,
    max: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def angle(input: TensorBase) -> TensorBase: ...
@overload
def any(input: TensorBase) -> TensorBase: ...
@overload
def any(input: TensorBase, dim: _int, keepdim: _bool = False) -> TensorBase: ...
@overload
def any(
    input: TensorBase,
    dim: _size | None = None,
    keepdim: _bool = False,
) -> TensorBase: ...
def arccos(input: TensorBase) -> TensorBase: ...
def arccos_(input: TensorBase) -> TensorBase: ...
def arccosh(input: TensorBase) -> TensorBase: ...
def arccosh_(input: TensorBase) -> TensorBase: ...
def arcsin(input: TensorBase) -> TensorBase: ...
def arcsin_(input: TensorBase) -> TensorBase: ...
def arcsinh(input: TensorBase) -> TensorBase: ...
def arcsinh_(input: TensorBase) -> TensorBase: ...
def arctan(input: TensorBase) -> TensorBase: ...
def arctan2(input: TensorBase, other: TensorBase) -> TensorBase: ...
def arctan_(input: TensorBase) -> TensorBase: ...
def arctanh(input: TensorBase) -> TensorBase: ...
def arctanh_(input: TensorBase) -> TensorBase: ...
def argmax(
    input: TensorBase,
    dim: _int | None = None,
    keepdim: _bool = False,
) -> TensorBase: ...
def argmin(
    input: TensorBase,
    dim: _int | None = None,
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def argsort(
    input: TensorBase,
    dim: _int = -1,
    descending: _bool = False,
) -> TensorBase: ...
@overload
def argsort(
    input: TensorBase,
    *,
    stable: _bool,
    dim: _int = -1,
    descending: _bool = False,
) -> TensorBase: ...
def argwhere(input: TensorBase) -> TensorBase: ...
def as_strided(
    input: TensorBase,
    size: _size,
    stride: _size,
    storage_offset: _int | None = None,
) -> TensorBase: ...
def as_strided_(
    input: TensorBase,
    size: _size,
    stride: _size,
    storage_offset: _int | None = None,
) -> TensorBase: ...
def as_strided_copy(
    input: TensorBase,
    size: _size,
    stride: _size,
    storage_offset: _int | None = None,
) -> TensorBase: ...
def as_strided_scatter(
    input: TensorBase,
    src: TensorBase,
    size: _size,
    stride: _size,
    storage_offset: _int | None = None,
) -> TensorBase: ...
def asin(input: TensorBase) -> TensorBase: ...
def asin_(input: TensorBase) -> TensorBase: ...
def asinh(input: TensorBase) -> TensorBase: ...
def asinh_(input: TensorBase) -> TensorBase: ...
def atan(input: TensorBase) -> TensorBase: ...
def atan2(input: TensorBase, other: TensorBase) -> TensorBase: ...
def atan2_(input: TensorBase, other: TensorBase) -> TensorBase: ...
def atan_(input: TensorBase) -> TensorBase: ...
def atanh(input: TensorBase) -> TensorBase: ...
def atanh_(input: TensorBase) -> TensorBase: ...
@overload
def atleast_1d(input: TensorBase) -> TensorBase: ...
@overload
def atleast_1d(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> tuple[TensorBase, ...]: ...
@overload
def atleast_2d(input: TensorBase) -> TensorBase: ...
@overload
def atleast_2d(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> tuple[TensorBase, ...]: ...
@overload
def atleast_3d(input: TensorBase) -> TensorBase: ...
@overload
def atleast_3d(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> tuple[TensorBase, ...]: ...
def avg_pool1d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    ceil_mode: _bool = False,
    count_include_pad: _bool = True,
) -> TensorBase: ...
def avg_pool2d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0]),
    ceil_mode: _bool = False,
    count_include_pad: _bool = True,
    divisor_override: _int | None = None,
) -> TensorBase: ...
@overload
def avg_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0]),
    ceil_mode: _bool = False,
    count_include_pad: _bool = True,
    divisor_override: _int | None = None,
) -> TensorBase: ...
@overload
def avg_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size,
    padding: _size,
    ceil_mode: _bool,
    count_include_pad: _bool,
    divisor_override: _int | None,
    grad_input: TensorBase,
) -> TensorBase: ...
def avg_pool3d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0, 0]),
    ceil_mode: _bool = False,
    count_include_pad: _bool = True,
    divisor_override: _int | None = None,
) -> TensorBase: ...
@overload
def avg_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0, 0]),
    ceil_mode: _bool = False,
    count_include_pad: _bool = True,
    divisor_override: _int | None = None,
) -> TensorBase: ...
@overload
def avg_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size,
    padding: _size,
    ceil_mode: _bool,
    count_include_pad: _bool,
    divisor_override: _int | None,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def baddbmm(
    input: TensorBase,
    batch1: TensorBase,
    batch2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
@overload
def baddbmm(
    input: TensorBase,
    batch1: TensorBase,
    batch2: TensorBase,
    out_dtype: _dtype,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
@overload
def bartlett_window(
    window_length: _int,
    periodic: _bool = True,
    dtype: _dtype | None = None,
) -> TensorBase: ...
@overload
def bartlett_window(
    window_length: _int,
    periodic: _bool,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def batch_norm(
    input: TensorBase,
    weight: TensorBase | None,
    bias: TensorBase | None,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    training: _bool,
    momentum: _float,
    eps: _float,
) -> TensorBase: ...
def batch_norm_backward(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase | None = None,
    running_mean: TensorBase | None = None,
    running_var: TensorBase | None = None,
    training: _bool = True,
    eps: _float = 1e-5,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def batch_norm_backward_elemt(
    grad_out: TensorBase,
    input: TensorBase,
    mean: TensorBase,
    invstd: TensorBase,
    weight: TensorBase | None,
    sum_dy: TensorBase,
    sum_dy_xmu: TensorBase,
    count: TensorBase,
) -> TensorBase: ...
def batch_norm_backward_reduce(
    grad_out: TensorBase,
    input: TensorBase,
    mean: TensorBase,
    invstd: TensorBase,
    weight: TensorBase | None,
    input_g: _bool,
    weight_g: _bool,
    bias_g: _bool,
) -> tuple[TensorBase, TensorBase, TensorBase, TensorBase]: ...
def batch_norm_elemt(
    input: TensorBase,
    weight: TensorBase | None,
    bias: TensorBase | None,
    mean: TensorBase,
    invstd: TensorBase,
    eps: _float,
) -> TensorBase: ...
def batch_norm_gather_stats(
    input: TensorBase,
    mean: TensorBase,
    invstd: TensorBase,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    momentum: _float,
    eps: _float,
    count: _int,
) -> tuple[TensorBase, TensorBase]: ...
def batch_norm_gather_stats_with_counts(
    input: TensorBase,
    mean: TensorBase,
    invstd: TensorBase,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    momentum: _float,
    eps: _float,
    counts: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def batch_norm_stats(
    input: TensorBase,
    eps: _float,
) -> tuple[TensorBase, TensorBase]: ...
def batch_norm_update_stats(
    input: TensorBase,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    momentum: _float,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def bernoulli(
    input: TensorBase,
    *,
    generator: Generator | None = None,
) -> TensorBase: ...
@overload
def bernoulli(
    input: TensorBase,
    p: _float,
    *,
    generator: Generator | None = None,
) -> TensorBase: ...
def bessel_j0(input: TensorBase) -> TensorBase: ...
def bessel_j1(input: TensorBase) -> TensorBase: ...
def bessel_y0(input: TensorBase) -> TensorBase: ...
def bessel_y1(input: TensorBase) -> TensorBase: ...
def bilinear(
    input1: TensorBase,
    input2: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
) -> TensorBase: ...
def binary_cross_entropy(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = 1,
) -> TensorBase: ...
@overload
def binary_cross_entropy_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = 1,
) -> TensorBase: ...
@overload
def binary_cross_entropy_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = Mean,
    grad_input: TensorBase,
) -> TensorBase: ...
def binary_cross_entropy_with_logits(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    pos_weight: TensorBase | None = None,
) -> TensorBase: ...
def bincount(
    input: TensorBase,
    weights: TensorBase | None = None,
    minlength: _int = 0,
) -> TensorBase: ...
def binomial(
    count: TensorBase,
    prob: TensorBase,
    generator: Generator | None = None,
) -> TensorBase: ...
@overload
def bitwise_and(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def bitwise_and(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def bitwise_and(input: Number | _complex, other: TensorBase) -> TensorBase: ...
@overload
def bitwise_left_shift(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def bitwise_left_shift(
    input: TensorBase,
    other: Number | _complex,
) -> TensorBase: ...
@overload
def bitwise_left_shift(
    input: Number | _complex,
    other: TensorBase,
) -> TensorBase: ...
def bitwise_not(input: TensorBase) -> TensorBase: ...
def bitwise_not_(input: TensorBase) -> TensorBase: ...
@overload
def bitwise_or(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def bitwise_or(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def bitwise_or(input: Number | _complex, other: TensorBase) -> TensorBase: ...
@overload
def bitwise_right_shift(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def bitwise_right_shift(
    input: TensorBase,
    other: Number | _complex,
) -> TensorBase: ...
@overload
def bitwise_right_shift(
    input: Number | _complex,
    other: TensorBase,
) -> TensorBase: ...
@overload
def bitwise_xor(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def bitwise_xor(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def bitwise_xor(input: Number | _complex, other: TensorBase) -> TensorBase: ...
@overload
def blackman_window(
    window_length: _int,
    periodic: _bool = True,
    dtype: _dtype | None = None,
) -> TensorBase: ...
@overload
def blackman_window(
    window_length: _int,
    periodic: _bool,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def block_diag(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
@overload
def bmm(input: TensorBase, mat2: TensorBase) -> TensorBase: ...
@overload
def bmm(
    input: TensorBase,
    mat2: TensorBase,
    out_dtype: _dtype,
) -> TensorBase: ...
def broadcast_tensors(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> tuple[TensorBase, ...]: ...
def broadcast_to(input: TensorBase, size: _size) -> TensorBase: ...
@overload
def bucketize(
    input: TensorBase,
    boundaries: TensorBase,
    *,
    out_int32: _bool = False,
    right: _bool = False,
) -> TensorBase: ...
@overload
def bucketize(
    input: Number | _complex,
    boundaries: TensorBase,
    *,
    out_int32: _bool = False,
    right: _bool = False,
) -> TensorBase: ...
def can_cast(from_: _dtype, to: _dtype) -> _bool: ...
def cartesian_prod(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def cat(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
    dim: _int = 0,
) -> TensorBase: ...
def ccol_indices_copy(input: TensorBase) -> TensorBase: ...
def cdist(
    x1: TensorBase,
    x2: TensorBase,
    p: _float = 2,
    *,
    compute_mode: _int | None = None,
) -> TensorBase: ...
def ceil(input: TensorBase) -> TensorBase: ...
def ceil_(input: TensorBase) -> TensorBase: ...
def celu(input: TensorBase, alpha: Number | _complex = 1.0) -> TensorBase: ...
def celu_(input: TensorBase, alpha: Number | _complex = 1.0) -> TensorBase: ...
def chain_matmul(
    matrices: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def channel_shuffle(input: TensorBase, groups: _int) -> TensorBase: ...
def chebyshev_polynomial_t(x: TensorBase, n: TensorBase) -> TensorBase: ...
def chebyshev_polynomial_u(x: TensorBase, n: TensorBase) -> TensorBase: ...
def chebyshev_polynomial_v(x: TensorBase, n: TensorBase) -> TensorBase: ...
def chebyshev_polynomial_w(x: TensorBase, n: TensorBase) -> TensorBase: ...
def cholesky(input: TensorBase, upper: _bool = False) -> TensorBase: ...
def cholesky_inverse(input: TensorBase, upper: _bool = False) -> TensorBase: ...
def cholesky_solve(
    input: TensorBase,
    input2: TensorBase,
    upper: _bool = False,
) -> TensorBase: ...
def choose_qparams_optimized(
    input: TensorBase,
    numel: _int,
    n_bins: _int,
    ratio: _float,
    bit_width: _int,
) -> tuple[TensorBase, TensorBase]: ...
def chunk(
    input: TensorBase,
    chunks: _int,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
def circular_pad_nd(input: TensorBase, pad: _size) -> TensorBase: ...
def circular_pad_nd_backward(
    grad_output: TensorBase,
    input: TensorBase,
    pad: _size,
) -> TensorBase: ...
@overload
def clamp(
    input: TensorBase,
    min: Number | _complex | None = None,
    max: Number | _complex | None = None,
) -> TensorBase: ...
@overload
def clamp(
    input: TensorBase,
    min: TensorBase | None = None,
    max: TensorBase | None = None,
) -> TensorBase: ...
@overload
def clamp_(
    input: TensorBase,
    min: Number | _complex | None = None,
    max: Number | _complex | None = None,
) -> TensorBase: ...
@overload
def clamp_(
    input: TensorBase,
    min: TensorBase | None = None,
    max: TensorBase | None = None,
) -> TensorBase: ...
def clamp_backward(
    grad_output: TensorBase,
    input: TensorBase,
    min: Number | _complex | None = None,
    max: Number | _complex | None = None,
) -> TensorBase: ...
def clamp_max(input: TensorBase, max: Number | _complex) -> TensorBase: ...
@overload
def clamp_max_(input: TensorBase, max: Number | _complex) -> TensorBase: ...
@overload
def clamp_max_(input: TensorBase, max: TensorBase) -> TensorBase: ...
def clamp_min(input: TensorBase, min: Number | _complex) -> TensorBase: ...
@overload
def clamp_min_(input: TensorBase, min: Number | _complex) -> TensorBase: ...
@overload
def clamp_min_(input: TensorBase, min: TensorBase) -> TensorBase: ...
@overload
def clip(
    input: TensorBase,
    min: Number | _complex | None = None,
    max: Number | _complex | None = None,
) -> TensorBase: ...
@overload
def clip(
    input: TensorBase,
    min: TensorBase | None = None,
    max: TensorBase | None = None,
) -> TensorBase: ...
@overload
def clip_(
    input: TensorBase,
    min: Number | _complex | None = None,
    max: Number | _complex | None = None,
) -> TensorBase: ...
@overload
def clip_(
    input: TensorBase,
    min: TensorBase | None = None,
    max: TensorBase | None = None,
) -> TensorBase: ...
def col2im(
    input: TensorBase,
    output_size: _size,
    kernel_size: _size,
    dilation: _size = (),
    padding: _size = (),
    stride: _size = (),
) -> TensorBase: ...
def col2im_backward(
    grad_output: TensorBase,
    input_size: _size,
    output_size: _size,
    kernel_size: _size,
    dilation: _size,
    padding: _size,
    stride: _size,
) -> TensorBase: ...
def col_indices_copy(input: TensorBase) -> TensorBase: ...
def column_stack(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def combinations(
    input: TensorBase,
    r: _int = 2,
    with_replacement: _bool = False,
) -> TensorBase: ...
def complex(real: TensorBase, imag: TensorBase) -> TensorBase: ...
def concat(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
    dim: _int = 0,
) -> TensorBase: ...
def concatenate(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
    dim: _int = 0,
) -> TensorBase: ...
def conj(input: TensorBase) -> TensorBase: ...
def conj_physical(input: TensorBase) -> TensorBase: ...
def conj_physical_(input: TensorBase) -> TensorBase: ...
def constant_pad_nd(
    input: TensorBase,
    pad: _size,
    value: Number | _complex,
) -> TensorBase: ...
def constant_pad_nd_backward(
    grad_output: TensorBase,
    pad: _size,
) -> TensorBase: ...
@overload
def conv1d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _size = ([1]),
    padding: _size = ([0]),
    dilation: _size = ([1]),
    groups: _int = 1,
) -> TensorBase: ...
@overload
def conv1d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: str = "valid",
    dilation: _int | _size = 1,
    groups: _int = 1,
) -> TensorBase: ...
def conv1d_grad_bias(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def conv1d_grad_input(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def conv1d_grad_weight(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
@overload
def conv2d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _size = ([1, 1]),
    padding: _size = ([0, 0]),
    dilation: _size = ([1, 1]),
    groups: _int = 1,
) -> TensorBase: ...
@overload
def conv2d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: str = "valid",
    dilation: _int | _size = 1,
    groups: _int = 1,
) -> TensorBase: ...
def conv2d_grad_bias(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def conv2d_grad_input(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def conv2d_grad_weight(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def conv2d_relu(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _size = ([1, 1]),
    padding: _size = ([0, 0]),
    dilation: _size = ([1, 1]),
    groups: _int = 1,
) -> TensorBase: ...
@overload
def conv3d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _size = ([1, 1, 1]),
    padding: _size = ([0, 0, 0]),
    dilation: _size = ([1, 1, 1]),
    groups: _int = 1,
) -> TensorBase: ...
@overload
def conv3d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: str = "valid",
    dilation: _int | _size = 1,
    groups: _int = 1,
) -> TensorBase: ...
def conv3d_grad_bias(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def conv3d_grad_input(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def conv3d_grad_weight(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def conv_depthwise3d(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
    dilation: _size,
) -> TensorBase: ...
def conv_tbc(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase,
    pad: _int = 0,
) -> TensorBase: ...
def conv_tbc_backward(
    input: TensorBase,
    input1: TensorBase,
    weight: TensorBase,
    bias: TensorBase,
    pad: _int,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def conv_transpose1d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _size = (),
    padding: _size = (),
    output_padding: _size = (),
    groups: _int = 1,
    dilation: _size = (),
) -> TensorBase: ...
def conv_transpose1d_grad_bias(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
def conv_transpose1d_grad_input(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
def conv_transpose1d_grad_weight(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
@overload
def conv_transpose2d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _size = ([1, 1]),
    padding: _size = ([0, 0]),
    output_padding: _size = ([0, 0]),
    groups: _int = 1,
    dilation: _size = ([1, 1]),
) -> TensorBase: ...
@overload
def conv_transpose2d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: _int | _size = 0,
    output_padding: _int | _size = 0,
    groups: _int = 1,
    dilation: _int | _size = 1,
) -> TensorBase: ...
def conv_transpose2d_grad_bias(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
def conv_transpose2d_grad_input(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
def conv_transpose2d_grad_weight(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
@overload
def conv_transpose3d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _size = ([1, 1, 1]),
    padding: _size = ([0, 0, 0]),
    output_padding: _size = ([0, 0, 0]),
    groups: _int = 1,
    dilation: _size = ([1, 1, 1]),
) -> TensorBase: ...
@overload
def conv_transpose3d(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: _int | _size = 0,
    output_padding: _int | _size = 0,
    groups: _int = 1,
    dilation: _int | _size = 1,
) -> TensorBase: ...
def conv_transpose3d_grad_bias(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
def conv_transpose3d_grad_input(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
def conv_transpose3d_grad_weight(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    output_padding: _size,
    groups: _int,
    dilation: _size,
) -> TensorBase: ...
def convolution(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
    dilation: _size,
    transposed: _bool,
    output_padding: _size,
    groups: _int,
) -> TensorBase: ...
def convolution_backward(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    bias_sizes: _size | None,
    stride: _size,
    padding: _size,
    dilation: _size,
    transposed: _bool,
    output_padding: _size,
    groups: _int,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def convolution_backward_overrideable(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    stride: _size,
    padding: _size,
    dilation: _size,
    transposed: _bool,
    output_padding: _size,
    groups: _int,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def convolution_overrideable(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
    dilation: _size,
    transposed: _bool,
    output_padding: _size,
    groups: _int,
) -> TensorBase: ...
def copy(
    input: TensorBase,
    src: TensorBase,
    non_blocking: _bool = False,
) -> TensorBase: ...
def copy_sparse_to_sparse_(
    input: TensorBase,
    src: TensorBase,
    non_blocking: _bool = False,
) -> TensorBase: ...
def corrcoef(input: TensorBase) -> TensorBase: ...
def cos(input: TensorBase) -> TensorBase: ...
def cos_(input: TensorBase) -> TensorBase: ...
def cosh(input: TensorBase) -> TensorBase: ...
def cosh_(input: TensorBase) -> TensorBase: ...
def cosine_embedding_loss(
    x1: TensorBase,
    x2: TensorBase,
    target: TensorBase,
    margin: Number | _complex = 0,
) -> TensorBase: ...
def cosine_similarity(
    x1: TensorBase,
    x2: TensorBase,
    dim: _int = 1,
    eps: _float = 1e-8,
) -> TensorBase: ...
@overload
def count_nonzero(input: TensorBase, dim: _size = ()) -> TensorBase: ...
@overload
def count_nonzero(input: TensorBase, dim: _size) -> TensorBase: ...
def cov(
    input: TensorBase,
    *,
    correction: _int = 1,
    fweights: TensorBase | None = None,
    aweights: TensorBase | None = None,
) -> TensorBase: ...
def cross(
    input: TensorBase,
    other: TensorBase,
    dim: _int | None = None,
) -> TensorBase: ...
def cross_entropy_loss(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = Mean,
    ignore_index: _int = -100,
    label_smoothing: _float = 0.0,
) -> TensorBase: ...
def crow_indices_copy(input: TensorBase) -> TensorBase: ...
@overload
def ctc_loss(
    log_probs: TensorBase,
    targets: TensorBase,
    input_lengths: _size,
    target_lengths: _size,
    blank: _int = 0,
    reduction: _int = Mean,
    zero_infinity: _bool = False,
) -> TensorBase: ...
@overload
def ctc_loss(
    log_probs: TensorBase,
    targets: TensorBase,
    input_lengths: TensorBase,
    target_lengths: TensorBase,
    blank: _int = 0,
    reduction: _int = Mean,
    zero_infinity: _bool = False,
) -> TensorBase: ...
def cudnn_affine_grid_generator(
    theta: TensorBase,
    N: _int,
    C: _int,
    H: _int,
    W: _int,
) -> TensorBase: ...
def cudnn_affine_grid_generator_backward(
    grad: TensorBase,
    N: _int,
    C: _int,
    H: _int,
    W: _int,
) -> TensorBase: ...
@overload
def cudnn_batch_norm(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    training: _bool,
    exponential_average_factor: _float,
    epsilon: _float,
) -> tuple[TensorBase, TensorBase, TensorBase, TensorBase]: ...
@overload
def cudnn_batch_norm(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    training: _bool,
    exponential_average_factor: _float,
    epsilon: _float,
    out0: TensorBase,
    out1: TensorBase,
    out2: TensorBase,
    out3: TensorBase,
) -> tuple[TensorBase, TensorBase, TensorBase, TensorBase]: ...
def cudnn_batch_norm_backward(
    input: TensorBase,
    grad_output: TensorBase,
    weight: TensorBase,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    save_mean: TensorBase | None,
    save_var: TensorBase | None,
    epsilon: _float,
    reserveSpace: TensorBase,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def cudnn_convolution(
    input: TensorBase,
    weight: TensorBase,
    padding: _size,
    stride: _size,
    dilation: _size,
    groups: _int,
    benchmark: _bool,
    deterministic: _bool,
    allow_tf32: _bool,
) -> TensorBase: ...
def cudnn_convolution_add_relu(
    input: TensorBase,
    weight: TensorBase,
    z: TensorBase,
    alpha: Number | _complex | None,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def cudnn_convolution_relu(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def cudnn_convolution_transpose(
    input: TensorBase,
    weight: TensorBase,
    padding: _size,
    output_padding: _size,
    stride: _size,
    dilation: _size,
    groups: _int,
    benchmark: _bool,
    deterministic: _bool,
    allow_tf32: _bool,
) -> TensorBase: ...
def cudnn_grid_sampler(input: TensorBase, grid: TensorBase) -> TensorBase: ...
def cudnn_grid_sampler_backward(
    input: TensorBase,
    grid: TensorBase,
    grad_output: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def cudnn_is_acceptable(input: TensorBase) -> _bool: ...
@overload
def cummax(input: TensorBase, dim: _int) -> tuple[TensorBase, TensorBase]: ...
@overload
def cummax(
    input: TensorBase,
    dim: _int,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def cummaxmin_backward(
    grad: TensorBase,
    input: TensorBase,
    indices: TensorBase,
    dim: _int,
) -> TensorBase: ...
@overload
def cummin(input: TensorBase, dim: _int) -> tuple[TensorBase, TensorBase]: ...
@overload
def cummin(
    input: TensorBase,
    dim: _int,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def cumprod(
    input: TensorBase,
    dim: _int,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def cumprod_(
    input: TensorBase,
    dim: _int,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def cumprod_backward(
    grad: TensorBase,
    input: TensorBase,
    dim: _int,
    output: TensorBase,
) -> TensorBase: ...
def cumsum(
    input: TensorBase,
    dim: _int = 0,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def cumsum_(
    input: TensorBase,
    dim: _int = 0,
    dtype: _dtype | None = None,
) -> TensorBase: ...
@overload
def cumulative_trapezoid(
    y: TensorBase,
    x: TensorBase | None = None,
    *,
    dx: Number | _complex = 1,
    dim: _int = -1,
) -> TensorBase: ...
@overload
def cumulative_trapezoid(
    y: TensorBase,
    x: TensorBase,
    *,
    dim: _int = -1,
) -> TensorBase: ...
@overload
def cumulative_trapezoid(
    y: TensorBase,
    *,
    dx: Number | _complex = 1,
    dim: _int = -1,
) -> TensorBase: ...
def deg2rad(input: TensorBase) -> TensorBase: ...
def deg2rad_(input: TensorBase) -> TensorBase: ...
@overload
def dequantize(input: TensorBase) -> TensorBase: ...
@overload
def dequantize(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> tuple[TensorBase, ...]: ...
def dequantize_per_channel(
    input: TensorBase,
    scales: TensorBase,
    zero_points: TensorBase,
    axis: _int = 0,
) -> TensorBase: ...
def dequantize_per_tensor(
    input: TensorBase,
    scale: _float,
    zero_point: _int,
) -> TensorBase: ...
def det(input: TensorBase) -> TensorBase: ...
def detach(input: TensorBase) -> TensorBase: ...
def detach_(input: TensorBase) -> TensorBase: ...
def detach_copy(input: TensorBase) -> TensorBase: ...
def diag(input: TensorBase, diagonal: _int = 0) -> TensorBase: ...
def diag_embed(
    input: TensorBase,
    offset: _int = 0,
    dim1: _int = -2,
    dim2: _int = -1,
) -> TensorBase: ...
def diagflat(input: TensorBase, offset: _int = 0) -> TensorBase: ...
def diagonal(
    input: TensorBase,
    offset: _int = 0,
    dim1: _int = 0,
    dim2: _int = 1,
) -> TensorBase: ...
def diagonal_backward(
    grad_output: TensorBase,
    input_sizes: _size,
    offset: _int,
    dim1: _int,
    dim2: _int,
) -> TensorBase: ...
def diagonal_copy(
    input: TensorBase,
    offset: _int = 0,
    dim1: _int = 0,
    dim2: _int = 1,
) -> TensorBase: ...
def diagonal_scatter(
    input: TensorBase,
    src: TensorBase,
    offset: _int = 0,
    dim1: _int = 0,
    dim2: _int = 1,
) -> TensorBase: ...
def diff(
    input: TensorBase,
    n: _int = 1,
    dim: _int = -1,
    prepend: TensorBase | None = None,
    append: TensorBase | None = None,
) -> TensorBase: ...
def digamma(input: TensorBase) -> TensorBase: ...
def dist(
    input: TensorBase,
    other: TensorBase,
    p: Number | _complex = 2,
) -> TensorBase: ...
def dot(input: TensorBase, tensor: TensorBase) -> TensorBase: ...
def dropout(
    input: TensorBase,
    p: _float = 0.5,
    train: _bool = True,
) -> TensorBase: ...
def dropout_(
    input: TensorBase,
    p: _float = 0.5,
    train: _bool = True,
) -> TensorBase: ...
@overload
def dsplit(input: TensorBase, sections: _int) -> tuple[TensorBase, ...]: ...
@overload
def dsplit(input: TensorBase, indices: _size) -> tuple[TensorBase, ...]: ...
def dstack(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def einsum(
    equation: str,
    operands: tuple[TensorBase, ...] | list[TensorBase],
    path: _size = ([]),
) -> TensorBase: ...
def elu(
    input: TensorBase,
    alpha: Number | _complex = 1,
    scale: Number | _complex = 1,
    input_scale: Number | _complex = 1,
) -> TensorBase: ...
def elu_(
    input: TensorBase,
    alpha: Number | _complex = 1,
    scale: Number | _complex = 1,
    input_scale: Number | _complex = 1,
) -> TensorBase: ...
@overload
def elu_backward(
    grad_output: TensorBase,
    alpha: Number | _complex,
    scale: Number | _complex,
    input_scale: Number | _complex,
    is_result: _bool,
    self_or_result: TensorBase,
) -> TensorBase: ...
@overload
def elu_backward(
    grad_output: TensorBase,
    alpha: Number | _complex,
    scale: Number | _complex,
    input_scale: Number | _complex,
    is_result: _bool,
    self_or_result: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def embedding(
    weight: TensorBase,
    indices: TensorBase,
    padding_idx: _int = -1,
    scale_grad_by_freq: _bool = False,
    sparse: _bool = False,
) -> TensorBase: ...
def embedding_backward(
    grad: TensorBase,
    indices: TensorBase,
    num_weights: _int,
    padding_idx: _int,
    scale_grad_by_freq: _bool,
    sparse: _bool,
) -> TensorBase: ...
@overload
def embedding_bag(
    weight: TensorBase,
    indices: TensorBase,
    offsets: TensorBase,
    scale_grad_by_freq: _bool = False,
    mode: _int = 0,
    sparse: _bool = False,
    per_sample_weights: TensorBase | None = None,
    include_last_offset: _bool = False,
) -> tuple[TensorBase, TensorBase, TensorBase, TensorBase]: ...
@overload
def embedding_bag(
    weight: TensorBase,
    indices: TensorBase,
    offsets: TensorBase,
    scale_grad_by_freq: _bool,
    mode: _int,
    sparse: _bool,
    per_sample_weights: TensorBase | None,
    include_last_offset: _bool,
    padding_idx: _int | None,
) -> tuple[TensorBase, TensorBase, TensorBase, TensorBase]: ...
def embedding_dense_backward(
    grad_output: TensorBase,
    indices: TensorBase,
    num_weights: _int,
    padding_idx: _int,
    scale_grad_by_freq: _bool,
) -> TensorBase: ...
def embedding_renorm_(
    input: TensorBase,
    indices: TensorBase,
    max_norm: _float,
    norm_type: _float,
) -> TensorBase: ...
def embedding_sparse_backward(
    grad: TensorBase,
    indices: TensorBase,
    num_weights: _int,
    padding_idx: _int,
    scale_grad_by_freq: _bool,
) -> TensorBase: ...
def empty_permuted(
    size: _size,
    physical_layout: _size,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def empty_quantized(
    size: _size,
    qtensor: TensorBase,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
    memory_format: MemoryFormat | None = None,
) -> TensorBase: ...
def empty_strided(
    size: _size,
    stride: _size,
    *,
    dtype: _dtype | None = None,
    device: Device | None = None,
    pin_memory: _bool = False,
) -> TensorBase: ...
def entr(input: TensorBase) -> TensorBase: ...
@overload
def eq(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def eq(input: TensorBase, other: Number | _complex) -> TensorBase: ...
def equal(input: TensorBase, other: TensorBase) -> _bool: ...
def erf(input: TensorBase) -> TensorBase: ...
def erf_(input: TensorBase) -> TensorBase: ...
def erfc(input: TensorBase) -> TensorBase: ...
def erfc_(input: TensorBase) -> TensorBase: ...
def erfcx(input: TensorBase) -> TensorBase: ...
def erfinv(input: TensorBase) -> TensorBase: ...
def erfinv_(input: TensorBase) -> TensorBase: ...
def exp(input: TensorBase) -> TensorBase: ...
def exp2(input: TensorBase) -> TensorBase: ...
def exp2_(input: TensorBase) -> TensorBase: ...
def exp_(input: TensorBase) -> TensorBase: ...
def expand_copy(
    input: TensorBase,
    size: _size,
    *,
    implicit: _bool = False,
) -> TensorBase: ...
def expm1(input: TensorBase) -> TensorBase: ...
def expm1_(input: TensorBase) -> TensorBase: ...
@overload
def eye(
    n: _int,
    m: _int = -1,
    *,
    dtype: _dtype = DType.float32,
    device: Device | None = None,
    requires_grad: _bool = False,
) -> TensorBase: ...
@overload
def eye(
    n: _int,
    m: _int,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def fake_quantize_per_channel_affine(
    input: TensorBase,
    scale: TensorBase,
    zero_point: TensorBase,
    axis: _int,
    quant_min: _int,
    quant_max: _int,
) -> TensorBase: ...
def fake_quantize_per_channel_affine_cachemask(
    input: TensorBase,
    scale: TensorBase,
    zero_point: TensorBase,
    axis: _int,
    quant_min: _int,
    quant_max: _int,
) -> tuple[TensorBase, TensorBase]: ...
def fake_quantize_per_channel_affine_cachemask_backward(
    grad: TensorBase,
    mask: TensorBase,
) -> TensorBase: ...
@overload
def fake_quantize_per_tensor_affine(
    input: TensorBase,
    scale: _float,
    zero_point: _int,
    quant_min: _int,
    quant_max: _int,
) -> TensorBase: ...
@overload
def fake_quantize_per_tensor_affine(
    input: TensorBase,
    scale: TensorBase,
    zero_point: TensorBase,
    quant_min: _int,
    quant_max: _int,
) -> TensorBase: ...
def fake_quantize_per_tensor_affine_cachemask(
    input: TensorBase,
    scale: _float,
    zero_point: _int,
    quant_min: _int,
    quant_max: _int,
) -> tuple[TensorBase, TensorBase]: ...
def fake_quantize_per_tensor_affine_cachemask_backward(
    grad: TensorBase,
    mask: TensorBase,
) -> TensorBase: ...
@overload
def fbgemm_linear_fp16_weight(
    input: TensorBase,
    packed_weight: TensorBase,
    bias: TensorBase,
) -> TensorBase: ...
@overload
def fbgemm_linear_fp16_weight(
    input: TensorBase,
    packed_weight: TensorBase,
    bias: TensorBase,
    output: TensorBase,
) -> TensorBase: ...
@overload
def fbgemm_linear_fp16_weight_fp32_activation(
    input: TensorBase,
    packed_weight: TensorBase,
    bias: TensorBase | None,
) -> TensorBase: ...
@overload
def fbgemm_linear_fp16_weight_fp32_activation(
    input: TensorBase,
    packed_weight: TensorBase,
    bias: TensorBase | None,
    output: TensorBase,
) -> TensorBase: ...
def fbgemm_linear_int8_weight(
    input: TensorBase,
    weight: TensorBase,
    packed: TensorBase,
    col_offsets: TensorBase,
    weight_scale: Number | _complex,
    weight_zero_point: Number | _complex,
    bias: TensorBase,
) -> TensorBase: ...
def fbgemm_linear_int8_weight_fp32_activation(
    input: TensorBase,
    weight: TensorBase,
    packed: TensorBase,
    col_offsets: TensorBase,
    weight_scale: Number | _complex,
    weight_zero_point: Number | _complex,
    bias: TensorBase,
) -> TensorBase: ...
def fbgemm_linear_quantize_weight(
    input: TensorBase,
) -> tuple[TensorBase, TensorBase, _float, _int]: ...
def fbgemm_pack_gemm_matrix_fp16(input: TensorBase) -> TensorBase: ...
@overload
def fbgemm_pack_quantized_matrix(input: TensorBase) -> TensorBase: ...
@overload
def fbgemm_pack_quantized_matrix(
    input: TensorBase,
    K: _int,
    N: _int,
) -> TensorBase: ...
def feature_alpha_dropout(
    input: TensorBase,
    p: _float = 0.5,
    train: _bool = True,
) -> TensorBase: ...
def feature_alpha_dropout_(
    input: TensorBase,
    p: _float = 0.5,
    train: _bool = True,
) -> TensorBase: ...
def feature_dropout(
    input: TensorBase,
    p: _float = 0.5,
    train: _bool = True,
) -> TensorBase: ...
def feature_dropout_(
    input: TensorBase,
    p: _float = 0.5,
    train: _bool = True,
) -> TensorBase: ...
def fft_fft(
    input: TensorBase,
    n: _int = -1,
    dim: _int = -1,
    norm: str = "backward",
) -> TensorBase: ...
def fft_fft2(
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str = "backward",
) -> TensorBase: ...
def fft_fft2_backward(
    grad: TensorBase,
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str = "backward",
) -> TensorBase: ...
def fft_fft_backward(
    grad: TensorBase,
    input: TensorBase,
    dim: _int,
    norm: str,
) -> TensorBase: ...
def fft_fftfreq(
    n: _int,
    d: _float = 1.0,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def fft_fftn(
    input: TensorBase,
    s: _size | None = None,
    dim: _size | None = None,
    norm: str | None = None,
) -> TensorBase: ...
def fft_fftshift(input: TensorBase, dim: _size | None = None) -> TensorBase: ...
def fft_hfft(
    input: TensorBase,
    n: _int | None = None,
    dim: _int = -1,
    norm: str | None = None,
) -> TensorBase: ...
def fft_hfft2(
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str | None = None,
) -> TensorBase: ...
def fft_hfftn(
    input: TensorBase,
    s: _size | None = None,
    dim: _size | None = None,
    norm: str | None = None,
) -> TensorBase: ...
def fft_ifft(
    input: TensorBase,
    n: _int = -1,
    dim: _int = -1,
    norm: str = "backward",
) -> TensorBase: ...
def fft_ifft2(
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str = "backward",
) -> TensorBase: ...
def fft_ifft2_backward(
    grad: TensorBase,
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str = "backward",
) -> TensorBase: ...
def fft_ifft_backward(
    grad: TensorBase,
    input: TensorBase,
    dim: _int,
    norm: str,
) -> TensorBase: ...
def fft_ifftn(
    input: TensorBase,
    s: _size | None = None,
    dim: _size | None = None,
    norm: str | None = None,
) -> TensorBase: ...
def fft_ifftshift(
    input: TensorBase,
    dim: _size | None = None,
) -> TensorBase: ...
def fft_ihfft(
    input: TensorBase,
    n: _int | None = None,
    dim: _int = -1,
    norm: str | None = None,
) -> TensorBase: ...
def fft_ihfft2(
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str | None = None,
) -> TensorBase: ...
def fft_ihfftn(
    input: TensorBase,
    s: _size | None = None,
    dim: _size | None = None,
    norm: str | None = None,
) -> TensorBase: ...
def fft_irfft(
    input: TensorBase,
    n: _int = -1,
    dim: _int = -1,
    norm: str = "backward",
) -> TensorBase: ...
def fft_irfft2(
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str = "backward",
) -> TensorBase: ...
def fft_irfft2_backward(
    grad: TensorBase,
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str = "backward",
) -> TensorBase: ...
def fft_irfft_backward(
    grad: TensorBase,
    input: TensorBase,
    dim: _int,
    norm: str,
) -> TensorBase: ...
def fft_irfftn(
    input: TensorBase,
    s: _size | None = None,
    dim: _size | None = None,
    norm: str | None = None,
) -> TensorBase: ...
def fft_rfft(
    input: TensorBase,
    n: _int = -1,
    dim: _int = -1,
    norm: str = "backward",
) -> TensorBase: ...
def fft_rfft2(
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str = "backward",
) -> TensorBase: ...
def fft_rfft2_backward(
    grad: TensorBase,
    input: TensorBase,
    s: _size | None = None,
    dim: _size = (-2, -1),
    norm: str = "backward",
) -> TensorBase: ...
def fft_rfft_backward(
    grad: TensorBase,
    input: TensorBase,
    dim: _int,
    norm: str,
) -> TensorBase: ...
def fft_rfftfreq(
    n: _int,
    d: _float = 1.0,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def fft_rfftn(
    input: TensorBase,
    s: _size | None = None,
    dim: _size | None = None,
    norm: str | None = None,
) -> TensorBase: ...
@overload
def fill(input: TensorBase, value: Number | _complex) -> TensorBase: ...
@overload
def fill(input: TensorBase, value: TensorBase) -> TensorBase: ...
def fix(input: TensorBase) -> TensorBase: ...
def fix_(input: TensorBase) -> TensorBase: ...
def flatten(
    input: TensorBase,
    start_dim: _int = 0,
    end_dim: _int = -1,
) -> TensorBase: ...
def flatten_dense_tensors(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def flip(input: TensorBase, dims: _size = ()) -> TensorBase: ...
def fliplr(input: TensorBase) -> TensorBase: ...
def flipud(input: TensorBase) -> TensorBase: ...
@overload
def float_power(input: TensorBase, exponent: TensorBase) -> TensorBase: ...
@overload
def float_power(input: TensorBase, exponent: TensorBase) -> TensorBase: ...
@overload
def float_power(
    input: Number | _complex,
    exponent: TensorBase,
) -> TensorBase: ...
@overload
def float_power(
    input: TensorBase,
    exponent: Number | _complex,
) -> TensorBase: ...
def floor(input: TensorBase) -> TensorBase: ...
def floor_(input: TensorBase) -> TensorBase: ...
def fmax(input: TensorBase, other: TensorBase) -> TensorBase: ...
def fmin(input: TensorBase, other: TensorBase) -> TensorBase: ...
def forward_add(
    a: TensorBase,
    da: TensorBase,
    b: TensorBase,
    db: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_cos(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_div(
    a: TensorBase,
    da: TensorBase,
    b: TensorBase,
    db: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_exp(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_log(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_mm(
    a: TensorBase,
    da: TensorBase,
    b: TensorBase,
    db: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_mul(
    a: TensorBase,
    da: TensorBase,
    b: TensorBase,
    db: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_neg(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_pow(
    a: TensorBase,
    da: TensorBase,
    b: TensorBase,
    db: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_relu(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_sigmoid(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_sin(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_sqrt(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_sub(
    a: TensorBase,
    da: TensorBase,
    b: TensorBase,
    db: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def forward_tanh(
    a: TensorBase,
    da: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def frac(input: TensorBase) -> TensorBase: ...
def frac_(input: TensorBase) -> TensorBase: ...
@overload
def fractional_max_pool2d(
    input: TensorBase,
    kernel_size: _size,
    output_size: _size,
    random_samples: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def fractional_max_pool2d(
    input: TensorBase,
    kernel_size: _size,
    output_size: _size,
    random_samples: TensorBase,
    output: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def fractional_max_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    output_size: _size,
    indices: TensorBase,
) -> TensorBase: ...
@overload
def fractional_max_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    output_size: _size,
    indices: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def fractional_max_pool3d(
    input: TensorBase,
    kernel_size: _size,
    output_size: _size,
    random_samples: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def fractional_max_pool3d(
    input: TensorBase,
    kernel_size: _size,
    output_size: _size,
    random_samples: TensorBase,
    output: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def fractional_max_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    output_size: _size,
    indices: TensorBase,
) -> TensorBase: ...
@overload
def fractional_max_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    output_size: _size,
    indices: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def frexp(input: TensorBase) -> tuple[TensorBase, TensorBase]: ...
@overload
def frexp(input: TensorBase) -> tuple[TensorBase, TensorBase]: ...
@overload
def frexp(
    input: TensorBase,
    mantissa: TensorBase,
    exponent: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def frobenius_norm(
    input: TensorBase,
    dim: _size,
    keepdim: _bool = False,
) -> TensorBase: ...
def from_file(
    filename: str,
    shared: _bool | None = None,
    size: _int | None = 0,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def fused_moving_avg_obs_fake_quant(
    input: TensorBase,
    observer_on: TensorBase,
    fake_quant_on: TensorBase,
    running_min: TensorBase,
    running_max: TensorBase,
    scale: TensorBase,
    zero_point: TensorBase,
    averaging_const: _float,
    quant_min: _int,
    quant_max: _int,
    ch_axis: _int,
    per_row_fake_quant: _bool = False,
    symmetric_quant: _bool = False,
) -> TensorBase: ...
@overload
def fused_mul_add(
    input: TensorBase,
    other: TensorBase,
    addend: TensorBase,
) -> TensorBase: ...
@overload
def fused_mul_add(
    input: TensorBase,
    other: Number | _complex,
    addend: Number | _complex,
) -> TensorBase: ...
def fused_rope(
    query: TensorBase,
    key: TensorBase,
    cos: TensorBase,
    sin: TensorBase,
    position_offset: _int = 0,
) -> tuple[TensorBase, TensorBase]: ...
def fused_swiglu(gate: TensorBase, up: TensorBase) -> TensorBase: ...
def gammainc(a: TensorBase, x: TensorBase) -> TensorBase: ...
def gammaincc(a: TensorBase, x: TensorBase) -> TensorBase: ...
def gather(input: TensorBase, dim: _int, index: TensorBase) -> TensorBase: ...
def gather_backward(
    grad: TensorBase,
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    sparse_grad: _bool,
) -> TensorBase: ...
def gcd(input: TensorBase, other: TensorBase) -> TensorBase: ...
def gcd_(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def ge(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def ge(input: TensorBase, other: Number | _complex) -> TensorBase: ...
def gelu(input: TensorBase, approximate: str = "none") -> TensorBase: ...
def gelu_(input: TensorBase, approximate: str = "none") -> TensorBase: ...
@overload
def gelu_backward(
    grad_output: TensorBase,
    input: TensorBase,
    approximate: str = "none",
) -> TensorBase: ...
@overload
def gelu_backward(
    grad_output: TensorBase,
    input: TensorBase,
    approximate: str = 'none',
    *,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def geqrf(
    input: TensorBase,
    a: TensorBase,
    tau: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def geqrf(input: TensorBase) -> tuple[TensorBase, TensorBase]: ...
def ger(input: TensorBase, vec2: TensorBase) -> TensorBase: ...
def get_device(input: TensorBase) -> _int: ...
def glu(input: TensorBase, dim: _int = -1) -> TensorBase: ...
@overload
def glu_backward(
    grad_output: TensorBase,
    input: TensorBase,
    dim: _int = -1,
) -> TensorBase: ...
@overload
def glu_backward(
    grad_output: TensorBase,
    input: TensorBase,
    dim: _int,
    grad_input: TensorBase,
) -> TensorBase: ...
def glu_backward_jvp(
    grad_x: TensorBase,
    grad_glu: TensorBase,
    x: TensorBase,
    dgrad_glu: TensorBase,
    dx: TensorBase,
    dim: _int,
) -> TensorBase: ...
def glu_jvp(
    glu: TensorBase,
    x: TensorBase,
    dx: TensorBase,
    dim: _int,
) -> TensorBase: ...
@overload
def gradient(
    input: TensorBase,
    spacing: tuple[TensorBase, ...] | list[TensorBase] = (),
    dim: _size = (),
    edge_order: _int = 1,
) -> tuple[TensorBase, ...]: ...
@overload
def gradient(
    input: TensorBase,
    *,
    spacing: Number | _complex | None = None,
    dim: _int | None = None,
    edge_order: _int = 1,
) -> tuple[TensorBase, ...]: ...
@overload
def gradient(
    input: TensorBase,
    *,
    spacing: Number | _complex,
    dim: _size,
    edge_order: _int = 1,
) -> tuple[TensorBase, ...]: ...
@overload
def gradient(
    input: TensorBase,
    *,
    dim: _size,
    edge_order: _int = 1,
) -> tuple[TensorBase, ...]: ...
@overload
def gradient(
    input: TensorBase,
    *,
    spacing: Sequence[Number | _complex],
    dim: _int | None = None,
    edge_order: _int = 1,
) -> tuple[TensorBase, ...]: ...
@overload
def gradient(
    input: TensorBase,
    *,
    spacing: Sequence[Number | _complex],
    dim: _size,
    edge_order: _int = 1,
) -> tuple[TensorBase, ...]: ...
@overload
def gradient(
    input: TensorBase,
    *,
    spacing: tuple[TensorBase, ...] | list[TensorBase],
    dim: _int | None = None,
    edge_order: _int = 1,
) -> tuple[TensorBase, ...]: ...
@overload
def gradient(
    input: TensorBase,
    *,
    spacing: tuple[TensorBase, ...] | list[TensorBase],
    dim: _size,
    edge_order: _int = 1,
) -> tuple[TensorBase, ...]: ...
@overload
def greater(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def greater(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def greater(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def greater_equal(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def greater_equal(
    input: TensorBase,
    other: Number | _complex,
) -> TensorBase: ...
@overload
def greater_equal(input: TensorBase, other: TensorBase) -> TensorBase: ...
def grid_sampler(
    input: TensorBase,
    grid: TensorBase,
    interpolation_mode: _int,
    padding_mode: _int,
    align_corners: _bool,
) -> TensorBase: ...
def grid_sampler_2d(
    input: TensorBase,
    grid: TensorBase,
    interpolation_mode: _int,
    padding_mode: _int,
    align_corners: _bool,
) -> TensorBase: ...
def grid_sampler_2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    grid: TensorBase,
    interpolation_mode: _int,
    padding_mode: _int,
    align_corners: _bool,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase]: ...
def grid_sampler_3d(
    input: TensorBase,
    grid: TensorBase,
    interpolation_mode: _int,
    padding_mode: _int,
    align_corners: _bool,
) -> TensorBase: ...
def grid_sampler_3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    grid: TensorBase,
    interpolation_mode: _int,
    padding_mode: _int,
    align_corners: _bool,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase]: ...
def group_norm(
    input: TensorBase,
    num_groups: _int,
    weight: TensorBase | None = None,
    bias: TensorBase | None = None,
    eps: _float = 1e-5,
) -> TensorBase: ...
def group_norm_backward(
    grad_output: TensorBase,
    input: TensorBase,
    num_groups: _int,
    weight: TensorBase | None = None,
    bias: TensorBase | None = None,
    eps: _float = 1e-5,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def grouped_mm(
    input: TensorBase,
    mat2: TensorBase,
    offs: TensorBase,
) -> TensorBase: ...
@overload
def gru(
    input: TensorBase,
    hx: tuple[TensorBase, ...] | list[TensorBase],
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool = True,
    num_layers: _int = 1,
    dropout_p: _float = 0.0,
    training: _bool = False,
    bidirectional: _bool = False,
    batch_first: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def gru(
    input: TensorBase,
    hx: TensorBase,
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
    batch_first: _bool,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def gru(
    data: TensorBase,
    batch_sizes: TensorBase,
    hx: TensorBase,
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
) -> tuple[TensorBase, TensorBase]: ...
def gru_cell(
    input: TensorBase,
    hx: TensorBase,
    w_ih: TensorBase,
    w_hh: TensorBase,
    b_ih: TensorBase | None = None,
    b_hh: TensorBase | None = None,
) -> TensorBase: ...
@overload
def gt(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def gt(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def hamming_window(
    window_length: _int,
    periodic: _bool = True,
    alpha: _float = 0.54,
    beta: _float = 0.46,
    dtype: _dtype | None = None,
) -> TensorBase: ...
@overload
def hamming_window(
    window_length: _int,
    periodic: _bool,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
@overload
def hamming_window(
    window_length: _int,
    periodic: _bool,
    alpha: _float,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
@overload
def hamming_window(
    window_length: _int,
    periodic: _bool,
    alpha: _float,
    beta: _float,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
@overload
def hann_window(
    window_length: _int,
    periodic: _bool = True,
    dtype: _dtype | None = None,
) -> TensorBase: ...
@overload
def hann_window(
    window_length: _int,
    periodic: _bool,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def hardshrink(
    input: TensorBase,
    lambd: Number | _complex = 0.5,
) -> TensorBase: ...
@overload
def hardshrink_backward(
    grad_out: TensorBase,
    input: TensorBase,
    lambd: Number | _complex,
) -> TensorBase: ...
@overload
def hardshrink_backward(
    grad_out: TensorBase,
    input: TensorBase,
    lambd: Number | _complex,
    grad_input: TensorBase,
) -> TensorBase: ...
def hardsigmoid(input: TensorBase) -> TensorBase: ...
def hardsigmoid_(input: TensorBase) -> TensorBase: ...
@overload
def hardsigmoid_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
@overload
def hardsigmoid_backward(
    grad_output: TensorBase,
    input: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def hardswish(input: TensorBase) -> TensorBase: ...
def hardswish_(input: TensorBase) -> TensorBase: ...
def hardswish_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
def hardtanh(
    input: TensorBase,
    min_val: Number | _complex = -1,
    max_val: Number | _complex = 1,
) -> TensorBase: ...
def hardtanh_(
    input: TensorBase,
    min_val: Number | _complex = -1,
    max_val: Number | _complex = 1,
) -> TensorBase: ...
@overload
def hardtanh_backward(
    grad_output: TensorBase,
    input: TensorBase,
    min_val: Number | _complex,
    max_val: Number | _complex,
) -> TensorBase: ...
@overload
def hardtanh_backward(
    grad_output: TensorBase,
    input: TensorBase,
    min_val: Number | _complex,
    max_val: Number | _complex,
    grad_input: TensorBase,
) -> TensorBase: ...
def hash_tensor(
    input: TensorBase,
    dim: _size = (),
    *,
    keepdim: _bool = False,
    mode: _int = 0,
) -> TensorBase: ...
def heaviside(input: TensorBase, values: TensorBase) -> TensorBase: ...
def heaviside_(input: TensorBase, values: TensorBase) -> TensorBase: ...
def hermite_polynomial_h(x: TensorBase, n: TensorBase) -> TensorBase: ...
def hermite_polynomial_he(x: TensorBase, n: TensorBase) -> TensorBase: ...
def hinge_embedding_loss(
    input: TensorBase,
    target: TensorBase,
    margin: Number | _complex = 1.0,
) -> TensorBase: ...
def histc(
    input: TensorBase,
    bins: _int = 100,
    min: Number | _complex = 0,
    max: Number | _complex = 0,
) -> TensorBase: ...
@overload
def histogram(
    input: TensorBase,
    bins: TensorBase,
    *,
    weight: TensorBase | None = None,
    density: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def histogram(
    input: TensorBase,
    bins: _int = 100,
    *,
    range: Sequence[_float] | None = None,
    weight: TensorBase | None = None,
    density: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def histogram(
    input: TensorBase,
    bins: TensorBase,
    weight: TensorBase | None = None,
    density: _bool = False,
    *,
    hist: TensorBase,
    bin_edges: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def histogram(
    input: TensorBase,
    bins: _int = 100,
    range: Sequence[_float] | None = None,
    weight: TensorBase | None = None,
    *,
    density: _bool = False,
    hist: TensorBase,
    bin_edges: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def histogramdd(
    input: TensorBase,
    bins: _size,
    range: Sequence[_float] | None = None,
    weight: TensorBase | None = None,
    density: _bool = False,
) -> tuple[TensorBase, tuple[TensorBase, ...]]: ...
@overload
def histogramdd(
    input: TensorBase,
    bins: _int,
    range: Sequence[_float] | None = None,
    weight: TensorBase | None = None,
    density: _bool = False,
) -> tuple[TensorBase, tuple[TensorBase, ...]]: ...
@overload
def histogramdd(
    input: TensorBase,
    bins: tuple[TensorBase, ...] | list[TensorBase],
    range: Sequence[_float] | None = None,
    weight: TensorBase | None = None,
    density: _bool = False,
) -> tuple[TensorBase, tuple[TensorBase, ...]]: ...
@overload
def hsplit(input: TensorBase, sections: _int) -> tuple[TensorBase, ...]: ...
@overload
def hsplit(input: TensorBase, indices: _size) -> tuple[TensorBase, ...]: ...
def hspmm(mat1: TensorBase, mat2: TensorBase) -> TensorBase: ...
def hstack(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def huber_loss(
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
    delta: _float = 1.0,
) -> TensorBase: ...
@overload
def huber_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    delta: _float,
) -> TensorBase: ...
@overload
def huber_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    delta: _float,
    grad_input: TensorBase,
) -> TensorBase: ...
def hypot(input: TensorBase, other: TensorBase) -> TensorBase: ...
def hypot_(input: TensorBase, other: TensorBase) -> TensorBase: ...
def i0(input: TensorBase) -> TensorBase: ...
def i0_(input: TensorBase) -> TensorBase: ...
def i0e(input: TensorBase) -> TensorBase: ...
def i1(input: TensorBase) -> TensorBase: ...
def i1e(input: TensorBase) -> TensorBase: ...
def igamma(a: TensorBase, x: TensorBase) -> TensorBase: ...
def igammac(a: TensorBase, x: TensorBase) -> TensorBase: ...
def im2col(
    input: TensorBase,
    kernel_size: _size,
    dilation: _size = (),
    padding: _size = (),
    stride: _size = (),
) -> TensorBase: ...
def im2col_backward(
    grad_output: TensorBase,
    input_size: _size,
    kernel_size: _size,
    dilation: _size,
    padding: _size,
    stride: _size,
) -> TensorBase: ...
def imag(input: TensorBase) -> TensorBase: ...
def index(
    input: TensorBase,
    indices: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def index_add(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    source: TensorBase,
) -> TensorBase: ...
def index_add_(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    source: TensorBase,
) -> TensorBase: ...
def index_copy(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    source: TensorBase,
) -> TensorBase: ...
def index_copy_(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    source: TensorBase,
) -> TensorBase: ...
@overload
def index_fill(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: Number | _complex,
) -> TensorBase: ...
@overload
def index_fill(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: TensorBase,
) -> TensorBase: ...
@overload
def index_fill(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: Number | _complex,
) -> TensorBase: ...
@overload
def index_fill(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: TensorBase,
) -> TensorBase: ...
@overload
def index_fill_(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: Number | _complex,
) -> TensorBase: ...
@overload
def index_fill_(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: TensorBase,
) -> TensorBase: ...
def index_put(
    input: TensorBase,
    indices: tuple[TensorBase, ...] | list[TensorBase],
    values: TensorBase,
    accumulate: _bool = False,
) -> TensorBase: ...
def index_put_(
    input: TensorBase,
    indices: tuple[TensorBase, ...] | list[TensorBase],
    values: TensorBase,
    accumulate: _bool = False,
) -> TensorBase: ...
def index_reduce(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    source: TensorBase,
    reduce: str,
    *,
    include_self: _bool = True,
) -> TensorBase: ...
def index_reduce_(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    source: TensorBase,
    reduce: str,
    *,
    include_self: _bool = True,
) -> TensorBase: ...
def index_select(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
) -> TensorBase: ...
def index_select_backward(
    grad: TensorBase,
    self_sizes: _size,
    dim: _int,
    index: TensorBase,
) -> TensorBase: ...
def indices_copy(input: TensorBase) -> TensorBase: ...
def infinitely_differentiable_gelu_backward(
    grad: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
def inner(input: TensorBase, other: TensorBase) -> TensorBase: ...
def inner_backward_other(
    grad_output: TensorBase,
    input: TensorBase,
    other: TensorBase,
) -> TensorBase: ...
def inner_backward_self(
    grad_output: TensorBase,
    input: TensorBase,
    other: TensorBase,
) -> TensorBase: ...
def instance_norm(
    input: TensorBase,
    weight: TensorBase | None = None,
    bias: TensorBase | None = None,
    running_mean: TensorBase | None = None,
    running_var: TensorBase | None = None,
    use_input_stats: _bool = True,
    momentum: _float = 0.1,
    eps: _float = 1e-5,
) -> TensorBase: ...
def instance_norm_backward(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase | None = None,
    bias: TensorBase | None = None,
    running_mean: TensorBase | None = None,
    running_var: TensorBase | None = None,
    use_input_stats: _bool = True,
    eps: _float = 1e-5,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def int_repr(input: TensorBase) -> TensorBase: ...
def inverse(input: TensorBase) -> TensorBase: ...
def is_complex(input: TensorBase) -> _bool: ...
def is_conj(input: TensorBase) -> _bool: ...
def is_distributed(input: TensorBase) -> _bool: ...
def is_floating_point(input: TensorBase) -> _bool: ...
def is_inference(input: TensorBase) -> _bool: ...
def is_neg(input: TensorBase) -> _bool: ...
def is_nonzero(input: TensorBase) -> _bool: ...
def is_same_size(input: TensorBase, other: TensorBase) -> _bool: ...
def is_signed(input: TensorBase) -> _bool: ...
def is_vulkan_available() -> _bool: ...
def isclose(
    input: TensorBase,
    other: TensorBase,
    rtol: _float = 1e-05,
    atol: _float = 1e-08,
    equal_nan: _bool = False,
) -> TensorBase: ...
def isfinite(input: TensorBase) -> TensorBase: ...
@overload
def isin(
    elements: TensorBase,
    test_elements: TensorBase,
    *,
    assume_unique: _bool = False,
    invert: _bool = False,
) -> TensorBase: ...
@overload
def isin(
    elements: TensorBase,
    test_element: Number | _complex,
    *,
    assume_unique: _bool = False,
    invert: _bool = False,
) -> TensorBase: ...
@overload
def isin(
    element: Number | _complex,
    test_elements: TensorBase,
    *,
    assume_unique: _bool = False,
    invert: _bool = False,
) -> TensorBase: ...
def isinf(input: TensorBase) -> TensorBase: ...
def isnan(input: TensorBase) -> TensorBase: ...
def isneginf(input: TensorBase) -> TensorBase: ...
def isposinf(input: TensorBase) -> TensorBase: ...
def isreal(input: TensorBase) -> TensorBase: ...
def istft(
    input: TensorBase,
    n_fft: _int,
    hop_length: _int | None = None,
    win_length: _int | None = None,
    window: TensorBase | None = None,
    center: _bool = True,
    normalized: _bool = False,
    onesided: _bool = True,
    length: _int | None = None,
    return_complex: _bool = False,
) -> TensorBase: ...
def kl_div(input: TensorBase, target: TensorBase) -> TensorBase: ...
def kron(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def kthvalue(
    input: TensorBase,
    k: _int,
    dim: _int = -1,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def kthvalue(
    input: TensorBase,
    k: _int,
    dim: _int = -1,
    keepdim: _bool = False,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def l1_loss(input: TensorBase, target: TensorBase) -> TensorBase: ...
def laguerre_polynomial_l(x: TensorBase, n: TensorBase) -> TensorBase: ...
def layer_norm(
    input: TensorBase,
    normalized_shape: _size,
    weight: TensorBase | None = None,
    bias: TensorBase | None = None,
    eps: _float = 1e-5,
) -> TensorBase: ...
def layer_norm_backward(
    grad_output: TensorBase,
    input: TensorBase,
    normalized_shape: _size,
    weight: TensorBase | None = None,
    bias: TensorBase | None = None,
    eps: _float = 1e-5,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def lcm(input: TensorBase, other: TensorBase) -> TensorBase: ...
def lcm_(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def ldexp(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def ldexp(input: TensorBase, other: TensorBase) -> TensorBase: ...
def ldexp_(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def le(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def le(input: TensorBase, other: Number | _complex) -> TensorBase: ...
def leaky_relu(
    input: TensorBase,
    negative_slope: Number | _complex = 0.01,
) -> TensorBase: ...
def leaky_relu_(
    input: TensorBase,
    negative_slope: Number | _complex = 0.01,
) -> TensorBase: ...
@overload
def leaky_relu_backward(
    grad_output: TensorBase,
    input: TensorBase,
    negative_slope: Number | _complex,
    self_is_result: _bool,
) -> TensorBase: ...
@overload
def leaky_relu_backward(
    grad_output: TensorBase,
    input: TensorBase,
    negative_slope: Number | _complex,
    self_is_result: _bool,
    grad_input: TensorBase,
) -> TensorBase: ...
def legendre_polynomial_p(x: TensorBase, n: TensorBase) -> TensorBase: ...
@overload
def lerp(
    input: TensorBase,
    end: TensorBase,
    weight: Number | _complex,
) -> TensorBase: ...
@overload
def lerp(
    input: TensorBase,
    end: TensorBase,
    weight: TensorBase,
) -> TensorBase: ...
@overload
def lerp(
    input: TensorBase,
    end: TensorBase,
    weight: Number | _complex,
) -> TensorBase: ...
@overload
def less(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def less(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def less(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def less_equal(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def less_equal(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def less_equal(input: TensorBase, other: TensorBase) -> TensorBase: ...
def lgamma(input: TensorBase) -> TensorBase: ...
def lgamma_(input: TensorBase) -> TensorBase: ...
def lift(input: TensorBase) -> TensorBase: ...
def lift_fresh(input: TensorBase) -> TensorBase: ...
def lift_fresh_copy(input: TensorBase) -> TensorBase: ...
def linear_backward(
    input: TensorBase,
    grad_output: TensorBase,
    weight: TensorBase,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def log(input: TensorBase) -> TensorBase: ...
def log10(input: TensorBase) -> TensorBase: ...
def log10_(input: TensorBase) -> TensorBase: ...
def log1p(input: TensorBase) -> TensorBase: ...
def log1p_(input: TensorBase) -> TensorBase: ...
def log2(input: TensorBase) -> TensorBase: ...
def log2_(input: TensorBase) -> TensorBase: ...
def log_(input: TensorBase) -> TensorBase: ...
def log_ndtr(input: TensorBase) -> TensorBase: ...
def log_sigmoid(input: TensorBase) -> TensorBase: ...
@overload
def log_sigmoid_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
@overload
def log_sigmoid_backward(
    grad_output: TensorBase,
    input: TensorBase,
    buffer: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def log_sigmoid_forward(
    input: TensorBase,
    output: TensorBase,
    buffer: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def log_sigmoid_forward(input: TensorBase) -> tuple[TensorBase, TensorBase]: ...
@overload
def log_softmax(
    input: TensorBase,
    dim: _int,
    dtype: _dtype = DType.undefined,
) -> TensorBase: ...
@overload
def log_softmax(
    input: TensorBase,
    dim: _int,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def logaddexp(input: TensorBase, other: TensorBase) -> TensorBase: ...
def logaddexp2(input: TensorBase, other: TensorBase) -> TensorBase: ...
def logcumsumexp(
    input: TensorBase,
    dim: _int,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def logdet(input: TensorBase) -> TensorBase: ...
def logical_and(input: TensorBase, other: TensorBase) -> TensorBase: ...
def logical_and_(input: TensorBase, other: TensorBase) -> TensorBase: ...
def logical_not(input: TensorBase) -> TensorBase: ...
def logical_not_(input: TensorBase) -> TensorBase: ...
def logical_or(input: TensorBase, other: TensorBase) -> TensorBase: ...
def logical_or_(input: TensorBase, other: TensorBase) -> TensorBase: ...
def logical_xor(input: TensorBase, other: TensorBase) -> TensorBase: ...
def logical_xor_(input: TensorBase, other: TensorBase) -> TensorBase: ...
def logit(
    input: TensorBase,
    eps: Number | _complex | None = None,
) -> TensorBase: ...
def logit_(
    input: TensorBase,
    eps: Number | _complex | None = None,
) -> TensorBase: ...
@overload
def logit_backward(
    grad_output: TensorBase,
    input: TensorBase,
    eps: Number | _complex | None = None,
) -> TensorBase: ...
@overload
def logit_backward(
    grad_output: TensorBase,
    input: TensorBase,
    eps: _float | None = None,
    grad_input: TensorBase,
) -> TensorBase: ...
def logsumexp(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def lstm(
    input: TensorBase,
    hx: tuple[TensorBase, ...] | list[TensorBase],
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool = True,
    num_layers: _int = 1,
    dropout_p: _float = 0.0,
    training: _bool = False,
    bidirectional: _bool = False,
    batch_first: _bool = False,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
@overload
def lstm(
    input: TensorBase,
    hx: tuple[TensorBase, ...] | list[TensorBase],
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
    batch_first: _bool,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
@overload
def lstm(
    data: TensorBase,
    batch_sizes: TensorBase,
    hx: tuple[TensorBase, ...] | list[TensorBase],
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def lstm_cell(
    input: TensorBase,
    hx: TensorBase,
    cx: TensorBase,
    w_ih: TensorBase,
    w_hh: TensorBase,
    b_ih: TensorBase | None = None,
    b_hh: TensorBase | None = None,
) -> tuple[TensorBase, TensorBase]: ...
def lstm_mps_backward(
    grad_y: TensorBase | None,
    grad_hy: TensorBase | None,
    grad_cy: TensorBase | None,
    z_state: TensorBase,
    cell_state_fwd: TensorBase,
    input: TensorBase,
    layersOutputs: TensorBase,
    hx: tuple[TensorBase, ...] | list[TensorBase],
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
    batch_first: _bool,
) -> tuple[TensorBase, tuple[TensorBase, ...], tuple[TensorBase, ...]]: ...
@overload
def lt(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def lt(input: TensorBase, other: Number | _complex) -> TensorBase: ...
def lu_solve(
    input: TensorBase,
    LU_data: TensorBase,
    LU_pivots: TensorBase,
) -> TensorBase: ...
@overload
def lu_unpack(
    LU_data: TensorBase,
    LU_pivots: TensorBase,
    unpack_data: _bool = True,
    unpack_pivots: _bool = True,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
@overload
def lu_unpack(
    LU_data: TensorBase,
    LU_pivots: TensorBase,
    unpack_data: _bool = True,
    unpack_pivots: _bool = True,
    P: TensorBase,
    L: TensorBase,
    U: TensorBase,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def margin_ranking_loss(
    input1: TensorBase,
    input2: TensorBase,
    target: TensorBase,
    margin: Number | _complex = 0,
) -> TensorBase: ...
@overload
def masked_fill(
    input: TensorBase,
    mask: TensorBase,
    value: TensorBase,
) -> TensorBase: ...
@overload
def masked_fill(
    input: TensorBase,
    mask: TensorBase,
    value: Number | _complex,
) -> TensorBase: ...
@overload
def masked_fill(
    input: TensorBase,
    mask: TensorBase,
    value: Number | _complex,
) -> TensorBase: ...
@overload
def masked_fill_(
    input: TensorBase,
    mask: TensorBase,
    value: TensorBase,
) -> TensorBase: ...
@overload
def masked_fill_(
    input: TensorBase,
    mask: TensorBase,
    value: Number | _complex,
) -> TensorBase: ...
def masked_scatter(
    input: TensorBase,
    mask: TensorBase,
    source: TensorBase,
) -> TensorBase: ...
def masked_scatter_(
    input: TensorBase,
    mask: TensorBase,
    source: TensorBase,
) -> TensorBase: ...
def masked_scatter_backward(
    grad_output: TensorBase,
    mask: TensorBase,
    sizes: _size,
) -> TensorBase: ...
def masked_select(input: TensorBase, mask: TensorBase) -> TensorBase: ...
def masked_select_backward(
    grad: TensorBase,
    input: TensorBase,
    mask: TensorBase,
) -> TensorBase: ...
def matmul_backward(
    grad: TensorBase,
    input: TensorBase,
    other: TensorBase,
    mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase]: ...
def matmul_backward_other(
    grad_output: TensorBase,
    input: TensorBase,
    other: TensorBase,
) -> TensorBase: ...
def matmul_backward_self(
    grad_output: TensorBase,
    input: TensorBase,
    other: TensorBase,
) -> TensorBase: ...
def matrix_exp(input: TensorBase) -> TensorBase: ...
def matrix_exp_backward(input: TensorBase, grad: TensorBase) -> TensorBase: ...
def matrix_power(input: TensorBase, n: _int) -> TensorBase: ...
@overload
def max(input: TensorBase) -> TensorBase: ...
@overload
def max(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def max(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
    max: TensorBase,
    max_values: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def max(input: TensorBase, other: TensorBase) -> TensorBase: ...
def max_pool1d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> TensorBase: ...
def max_pool1d_with_indices(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
def max_pool2d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0]),
    dilation: _size = ([1, 1]),
    ceil_mode: _bool = False,
) -> TensorBase: ...
def max_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0]),
    dilation: _size = ([1, 1]),
    ceil_mode: _bool = False,
) -> TensorBase: ...
def max_pool2d_with_indices(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0]),
    dilation: _size = ([1, 1]),
    ceil_mode: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def max_pool2d_with_indices_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0]),
    dilation: _size = ([1, 1]),
    ceil_mode: _bool = False,
    indices: TensorBase | None = None,
) -> TensorBase: ...
@overload
def max_pool2d_with_indices_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size,
    padding: _size,
    dilation: _size,
    ceil_mode: _bool,
    indices: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def max_pool3d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0, 0]),
    dilation: _size = ([1, 1, 1]),
    ceil_mode: _bool = False,
) -> TensorBase: ...
def max_pool3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0, 0]),
    dilation: _size = ([1, 1, 1]),
    ceil_mode: _bool = False,
) -> TensorBase: ...
def max_pool3d_with_indices(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0, 0]),
    dilation: _size = ([1, 1, 1]),
    ceil_mode: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def max_pool3d_with_indices_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size = ([]),
    padding: _size = ([0, 0, 0]),
    dilation: _size = ([1, 1, 1]),
    ceil_mode: _bool = False,
    indices: TensorBase | None = None,
) -> TensorBase: ...
@overload
def max_pool3d_with_indices_backward(
    grad_output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size,
    padding: _size,
    dilation: _size,
    ceil_mode: _bool,
    indices: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def max_unpool2d(
    input: TensorBase,
    indices: TensorBase,
    output_size: _size,
) -> TensorBase: ...
def max_unpool2d_backward(
    grad_output: TensorBase,
    indices: TensorBase,
    output_size: _size,
) -> TensorBase: ...
def max_unpool3d(
    input: TensorBase,
    indices: TensorBase,
    output_size: _size,
    stride: _size,
    padding: _size,
) -> TensorBase: ...
def max_unpool3d_backward(
    grad_output: TensorBase,
    indices: TensorBase,
    output_size: _size,
) -> TensorBase: ...
def maximum(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def mean(
    input: TensorBase,
    *,
    dtype: _dtype = DType.undefined,
) -> TensorBase: ...
@overload
def mean(
    input: TensorBase,
    dim: _size,
    keepdim: _bool = False,
    *,
    dtype: _dtype = DType.undefined,
) -> TensorBase: ...
def mean_dim_backward(
    grad_output: TensorBase,
    input: TensorBase,
    dim: _size,
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def median(input: TensorBase) -> TensorBase: ...
@overload
def median(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def median(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def meshgrid(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
    indexing: str = "ij",
) -> tuple[TensorBase, ...]: ...
@overload
def meshgrid(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
    *,
    indexing: str,
) -> tuple[TensorBase, ...]: ...
@overload
def min(input: TensorBase) -> TensorBase: ...
@overload
def min(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def min(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
    min: TensorBase,
    min_indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def min(input: TensorBase, other: TensorBase) -> TensorBase: ...
def minimum(input: TensorBase, other: TensorBase) -> TensorBase: ...
def miopen_batch_norm(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    training: _bool,
    exponential_average_factor: _float,
    epsilon: _float,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def miopen_batch_norm_backward(
    input: TensorBase,
    grad_output: TensorBase,
    weight: TensorBase,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    save_mean: TensorBase | None,
    save_var: TensorBase | None,
    epsilon: _float,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def miopen_convolution(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    padding: _size,
    stride: _size,
    dilation: _size,
    groups: _int,
    benchmark: _bool,
    deterministic: _bool,
) -> TensorBase: ...
def miopen_convolution_add_relu(
    input: TensorBase,
    weight: TensorBase,
    z: TensorBase,
    alpha: Number | _complex | None,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def miopen_convolution_relu(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def miopen_convolution_transpose(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    padding: _size,
    output_padding: _size,
    stride: _size,
    dilation: _size,
    groups: _int,
    benchmark: _bool,
    deterministic: _bool,
) -> TensorBase: ...
@overload
def miopen_ctc_loss(
    log_probs: TensorBase,
    targets: TensorBase,
    input_lengths: _size,
    target_lengths: _size,
    blank: _int,
    deterministic: _bool,
    zero_infinity: _bool,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def miopen_ctc_loss(
    log_probs: TensorBase,
    targets: TensorBase,
    input_lengths: TensorBase,
    target_lengths: TensorBase,
    blank: _int,
    deterministic: _bool,
    zero_infinity: _bool,
) -> tuple[TensorBase, TensorBase]: ...
def miopen_depthwise_convolution(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    padding: _size,
    stride: _size,
    dilation: _size,
    groups: _int,
    benchmark: _bool,
    deterministic: _bool,
) -> TensorBase: ...
def miopen_rnn(
    input: TensorBase,
    weight: tuple[TensorBase, ...] | list[TensorBase],
    weight_stride0: _int,
    hx: TensorBase,
    cx: TensorBase | None,
    mode: _int,
    hidden_size: _int,
    num_layers: _int,
    batch_first: _bool,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
    batch_sizes: _size,
    dropout_state: TensorBase | None,
) -> tuple[TensorBase, TensorBase, TensorBase, TensorBase, TensorBase]: ...
def miopen_rnn_backward(
    input: TensorBase,
    weight: tuple[TensorBase, ...] | list[TensorBase],
    weight_stride0: _int,
    weight_buf: TensorBase,
    hx: TensorBase,
    cx: TensorBase | None,
    output: TensorBase,
    grad_output: TensorBase | None,
    grad_hy: TensorBase | None,
    grad_cy: TensorBase | None,
    mode: _int,
    hidden_size: _int,
    num_layers: _int,
    batch_first: _bool,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
    batch_sizes: _size,
    dropout_state: TensorBase | None,
    reserve: TensorBase,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase, tuple[TensorBase, ...]]: ...
def mish(input: TensorBase) -> TensorBase: ...
def mish_(input: TensorBase) -> TensorBase: ...
def mish_backward(grad_output: TensorBase, input: TensorBase) -> TensorBase: ...
def mkldnn_adaptive_avg_pool2d(
    input: TensorBase,
    output_size: _size,
) -> TensorBase: ...
def mkldnn_adaptive_avg_pool2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
def mkldnn_convolution(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None,
    padding: _size,
    stride: _size,
    dilation: _size,
    groups: _int,
) -> TensorBase: ...
def mkldnn_linear(
    input: TensorBase,
    weight: TensorBase,
    bias: TensorBase | None = None,
) -> TensorBase: ...
def mkldnn_linear_backward(
    input: TensorBase,
    grad_output: TensorBase,
    weight: TensorBase,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def mkldnn_linear_backward_input(
    input_size: _size,
    grad_output: TensorBase,
    weight: TensorBase,
) -> TensorBase: ...
def mkldnn_linear_backward_weights(
    grad_output: TensorBase,
    input: TensorBase,
    weight: TensorBase,
    bias_defined: _bool,
) -> tuple[TensorBase, TensorBase]: ...
def mkldnn_max_pool2d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> TensorBase: ...
def mkldnn_max_pool2d_backward(
    grad_output: TensorBase,
    output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> TensorBase: ...
def mkldnn_max_pool3d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> TensorBase: ...
def mkldnn_max_pool3d_backward(
    grad_output: TensorBase,
    output: TensorBase,
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> TensorBase: ...
def mkldnn_reorder_conv2d_weight(
    input: TensorBase,
    padding: _int | _size = 0,
    stride: _int | _size = 1,
    dilation: _int | _size = 1,
    groups: _int = 1,
    input_size: _size | None = None,
) -> TensorBase: ...
def mkldnn_reorder_conv3d_weight(
    input: TensorBase,
    padding: _int | _size = 0,
    stride: _int | _size = 1,
    dilation: _int | _size = 1,
    groups: _int = 1,
    input_size: _size | None = None,
) -> TensorBase: ...
def mkldnn_rnn_layer(
    input: TensorBase,
    weight0: TensorBase,
    weight1: TensorBase,
    weight2: TensorBase,
    weight3: TensorBase,
    hx_: TensorBase,
    cx_: TensorBase,
    reverse: _bool,
    batch_sizes: _size,
    mode: _int,
    hidden_size: _int,
    num_layers: _int,
    has_biases: _bool,
    bidirectional: _bool,
    batch_first: _bool,
    train: _bool,
) -> tuple[TensorBase, TensorBase, TensorBase, TensorBase]: ...
def mkldnn_rnn_layer_backward(
    input: TensorBase,
    weight1: TensorBase,
    weight2: TensorBase,
    weight3: TensorBase,
    weight4: TensorBase,
    hx_: TensorBase,
    cx_tmp: TensorBase,
    output: TensorBase,
    hy_: TensorBase,
    cy_: TensorBase,
    grad_output: TensorBase | None,
    grad_hy: TensorBase | None,
    grad_cy: TensorBase | None,
    reverse: _bool,
    mode: _int,
    hidden_size: _int,
    num_layers: _int,
    has_biases: _bool,
    train: _bool,
    bidirectional: _bool,
    batch_sizes: _size,
    batch_first: _bool,
    workspace: TensorBase,
) -> tuple[TensorBase, TensorBase, TensorBase, TensorBase, TensorBase, TensorBase, TensorBase]:  # fmt: skip
    ...
@overload
def mode(
    input: TensorBase,
    dim: _int = -1,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def mode(
    input: TensorBase,
    dim: _int = -1,
    keepdim: _bool = False,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def modified_bessel_i0(input: TensorBase) -> TensorBase: ...
def modified_bessel_i1(input: TensorBase) -> TensorBase: ...
def modified_bessel_k0(input: TensorBase) -> TensorBase: ...
def modified_bessel_k1(input: TensorBase) -> TensorBase: ...
@overload
def moveaxis(
    input: TensorBase,
    source: _size,
    destination: _size,
) -> TensorBase: ...
@overload
def moveaxis(
    input: TensorBase,
    source: _int,
    destination: _int,
) -> TensorBase: ...
@overload
def movedim(
    input: TensorBase,
    source: _size,
    destination: _size,
) -> TensorBase: ...
@overload
def movedim(
    input: TensorBase,
    source: _size,
    destination: _size,
) -> TensorBase: ...
@overload
def movedim(
    input: TensorBase,
    source: _int,
    destination: _int,
) -> TensorBase: ...
def mps_convolution_backward(
    input: TensorBase,
    grad_output: TensorBase,
    weight: TensorBase,
    padding: _size,
    stride: _size,
    dilation: _size,
    groups: _int,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def mps_convolution_transpose_backward(
    input: TensorBase,
    grad_output: TensorBase,
    weight: TensorBase,
    padding: _size,
    output_padding: _size,
    stride: _size,
    dilation: _size,
    groups: _int,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase]: ...
def mse_loss(
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
) -> TensorBase: ...
@overload
def mse_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
) -> TensorBase: ...
@overload
def mse_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    grad_input: TensorBase,
) -> TensorBase: ...
def msort(input: TensorBase) -> TensorBase: ...
def multi_margin_loss(
    input: TensorBase,
    target: TensorBase,
    p: Number | _complex = 1,
    margin: Number | _complex = 1,
    weight: TensorBase | None = None,
    reduction: _int = 1,
) -> TensorBase: ...
@overload
def multi_margin_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    p: Number | _complex,
    margin: Number | _complex,
    weight: TensorBase | None = None,
    reduction: _int = 1,
) -> TensorBase: ...
@overload
def multi_margin_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    p: Number | _complex,
    margin: Number | _complex,
    weight: TensorBase | None = None,
    reduction: _int = Mean,
    grad_input: TensorBase,
) -> TensorBase: ...
def multilabel_margin_loss(
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
) -> TensorBase: ...
@overload
def multilabel_margin_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    is_target: TensorBase,
) -> TensorBase: ...
@overload
def multilabel_margin_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    is_target: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def multilabel_margin_loss_forward(
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def multilabel_margin_loss_forward(
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    output: TensorBase,
    is_target: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def multilabel_soft_margin_loss(
    input: TensorBase,
    target: TensorBase,
) -> TensorBase: ...
def multinomial(
    input: TensorBase,
    num_samples: _int,
    replacement: _bool = False,
    impl: _int = 0,
) -> TensorBase: ...
def mv(input: TensorBase, vec: TensorBase) -> TensorBase: ...
def mvlgamma(input: TensorBase, p: _int) -> TensorBase: ...
def nan_to_num(
    input: TensorBase,
    nan: Number | _complex = 0.0,
    posinf: Number | _complex | None = None,
    neginf: Number | _complex | None = None,
) -> TensorBase: ...
def nan_to_num_(
    input: TensorBase,
    nan: Number | _complex = 0.0,
    posinf: Number | _complex | None = None,
    neginf: Number | _complex | None = None,
) -> TensorBase: ...
def nanmean(
    input: TensorBase,
    dim: _int | None = None,
    keepdim: _bool = False,
    *,
    dtype: _dtype | None = None,
) -> TensorBase: ...
@overload
def nanmedian(input: TensorBase) -> TensorBase: ...
@overload
def nanmedian(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def nanmedian(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def nanquantile(
    input: TensorBase,
    q: TensorBase,
    dim: _int | None = None,
    keepdim: _bool = False,
    *,
    interpolation: str = "linear",
) -> TensorBase: ...
@overload
def nanquantile(
    input: TensorBase,
    q: _float,
    dim: _int | None = None,
    keepdim: _bool = False,
    *,
    interpolation: str = 'linear',
) -> TensorBase: ...
def nansum(
    input: TensorBase,
    dim: _size = (),
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def narrow(
    input: TensorBase,
    dim: _int,
    start: _int,
    length: _int,
) -> TensorBase: ...
@overload
def narrow(
    input: TensorBase,
    dim: _int,
    start: TensorBase,
    length: _int,
) -> TensorBase: ...
def narrow_copy(
    input: TensorBase,
    dim: _int,
    start: _int,
    length: _int,
) -> TensorBase: ...
def native_alpha_dropout(
    input: TensorBase,
    p: _float,
) -> tuple[TensorBase, TensorBase]: ...
def native_batch_norm(
    input: TensorBase,
    weight: TensorBase | None,
    bias: TensorBase | None,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    training: _bool,
    momentum: _float,
    eps: _float,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def native_batch_norm_backward(
    grad_out: TensorBase,
    input: TensorBase,
    weight: TensorBase | None,
    running_mean: TensorBase | None,
    running_var: TensorBase | None,
    save_mean: TensorBase | None,
    save_invstd: TensorBase | None,
    train: _bool,
    eps: _float,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def native_channel_shuffle(input: TensorBase, groups: _int) -> TensorBase: ...
def native_dropout(
    input: TensorBase,
    p: _float,
) -> tuple[TensorBase, TensorBase]: ...
def native_dropout_backward(
    grad_output: TensorBase,
    mask: TensorBase,
    scale: _float,
) -> TensorBase: ...
def native_feature_dropout(
    input: TensorBase,
    p: _float,
) -> tuple[TensorBase, TensorBase]: ...
def native_group_norm(
    input: TensorBase,
    weight: TensorBase | None,
    bias: TensorBase | None,
    N: _int,
    C: _int,
    HxW: _int,
    group: _int,
    eps: _float,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def native_group_norm_backward(
    grad_out: TensorBase,
    input: TensorBase,
    mean: TensorBase,
    rstd: TensorBase,
    weight: TensorBase | None,
    N: _int,
    C: _int,
    HxW: _int,
    group: _int,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def native_layer_norm(
    input: TensorBase,
    normalized_shape: _size,
    weight: TensorBase | None,
    bias: TensorBase | None,
    eps: _float,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def native_layer_norm_backward(
    grad_out: TensorBase,
    input: TensorBase,
    normalized_shape: _size,
    mean: TensorBase,
    rstd: TensorBase,
    weight: TensorBase | None,
    bias: TensorBase | None,
    output_mask: Sequence[_bool],
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
@overload
def native_norm(input: TensorBase, p: Number | _complex = 2) -> TensorBase: ...
@overload
def native_norm(
    input: TensorBase,
    p: Number | _complex | None,
    dim: _size,
    keepdim: _bool,
    dtype: _dtype | None,
) -> TensorBase: ...
def ndtr(input: TensorBase) -> TensorBase: ...
def ndtri(input: TensorBase) -> TensorBase: ...
@overload
def ne(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def ne(input: TensorBase, other: Number | _complex) -> TensorBase: ...
def neg(input: TensorBase) -> TensorBase: ...
def negative(input: TensorBase) -> TensorBase: ...
def negative_(input: TensorBase) -> TensorBase: ...
def nested_to_padded_tensor(
    input: TensorBase,
    padding: _float,
    output_size: _size | None = None,
) -> TensorBase: ...
def nextafter(input: TensorBase, other: TensorBase) -> TensorBase: ...
def nextafter_(input: TensorBase, other: TensorBase) -> TensorBase: ...
def nll_loss(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = 1,
    ignore_index: _int = -100,
) -> tuple[TensorBase, TensorBase]: ...
def nll_loss2d(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = 1,
    ignore_index: _int = -100,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def nll_loss2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = 1,
    ignore_index: _int = -100,
    total_weight: TensorBase | None = None,
) -> TensorBase: ...
@overload
def nll_loss2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None,
    reduction: _int,
    ignore_index: _int,
    total_weight: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def nll_loss2d_forward(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None,
    reduction: _int,
    ignore_index: _int,
    output: TensorBase,
    total_weight: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def nll_loss2d_forward(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None,
    reduction: _int,
    ignore_index: _int,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def nll_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = 1,
    ignore_index: _int = -100,
    total_weight: TensorBase | None = None,
) -> TensorBase: ...
@overload
def nll_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None,
    reduction: _int,
    ignore_index: _int,
    total_weight: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def nll_loss_forward(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None,
    reduction: _int,
    ignore_index: _int,
    output: TensorBase,
    total_weight: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def nll_loss_forward(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None,
    reduction: _int,
    ignore_index: _int,
) -> tuple[TensorBase, TensorBase]: ...
def nll_loss_nd(
    input: TensorBase,
    target: TensorBase,
    weight: TensorBase | None = None,
    reduction: _int = Mean,
    ignore_index: _int = -100,
) -> TensorBase: ...
def nonzero(input: TensorBase) -> TensorBase: ...
def nonzero_numpy(input: TensorBase) -> tuple[TensorBase, ...]: ...
def nonzero_static(
    input: TensorBase,
    *,
    size: _int | None = None,
    fill_value: _int = -1,
) -> TensorBase: ...
@overload
def norm(input: TensorBase, p: _float = 2.0) -> TensorBase: ...
@overload
def norm(
    input: TensorBase,
    dim: _size,
    p: _float = 2.0,
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def norm(
    input: TensorBase,
    p: Number | _complex | None,
    *,
    dtype: _dtype,
) -> TensorBase: ...
@overload
def norm(input: TensorBase, p: Number | _complex = 2) -> TensorBase: ...
@overload
def norm(
    input: TensorBase,
    p: Number | _complex | None,
    dim: _size,
    keepdim: _bool,
    *,
    dtype: _dtype,
) -> TensorBase: ...
@overload
def norm(
    input: TensorBase,
    p: Number | _complex | None,
    dim: _size,
    keepdim: _bool = False,
) -> TensorBase: ...
def norm_except_dim(
    v: TensorBase,
    pow: _int = 2,
    dim: _int = 0,
) -> TensorBase: ...
@overload
def normal(mean: TensorBase, std: TensorBase) -> TensorBase: ...
@overload
def normal(
    mean: TensorBase,
    std: _float = 1,
    *,
    generator: Generator | None = None,
) -> TensorBase: ...
@overload
def normal(
    mean: _float,
    std: TensorBase,
    *,
    generator: Generator | None = None,
) -> TensorBase: ...
@overload
def normal(
    mean: TensorBase,
    std: TensorBase,
    *,
    generator: Generator | None = None,
) -> TensorBase: ...
@overload
def normal(
    mean: _float,
    std: _float,
    size: _size,
    *,
    generator: Generator | None = None,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def normal_functional(
    input: TensorBase,
    mean: _float = 0,
    std: _float = 1,
    *,
    generator: Generator | None = None,
) -> TensorBase: ...
@overload
def not_equal(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def not_equal(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def not_equal(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def nuclear_norm(input: TensorBase, keepdim: _bool = False) -> TensorBase: ...
@overload
def nuclear_norm(
    input: TensorBase,
    dim: _size,
    keepdim: _bool = False,
) -> TensorBase: ...
def one_hot(input: TensorBase, num_classes: _int = -1) -> TensorBase: ...
def orgqr(input: TensorBase, input2: TensorBase) -> TensorBase: ...
def ormqr(
    input: TensorBase,
    input2: TensorBase,
    input3: TensorBase,
    left: _bool = True,
    transpose: _bool = False,
) -> TensorBase: ...
def outer(input: TensorBase, vec2: TensorBase) -> TensorBase: ...
def pad(
    input: TensorBase,
    pad: _size,
    mode: str = "constant",
    value: _float | None = None,
) -> TensorBase: ...
def pad_sequence(
    sequences: tuple[TensorBase, ...] | list[TensorBase],
    batch_first: _bool = False,
    padding_value: _float = 0.0,
    padding_side: str = "right",
) -> TensorBase: ...
def pairwise_distance(
    x1: TensorBase,
    x2: TensorBase,
    p: _float = 2.0,
    eps: _float = 1e-06,
    keepdim: _bool = False,
) -> TensorBase: ...
def pdist(input: TensorBase, p: _float = 2.0) -> TensorBase: ...
def permute(input: TensorBase, dims: _size) -> TensorBase: ...
def permute_backward(
    grad_output: TensorBase,
    input: TensorBase,
    dims: _size,
) -> TensorBase: ...
def permute_copy(input: TensorBase, dims: _size) -> TensorBase: ...
def pinverse(input: TensorBase, rcond: _float = 1e-15) -> TensorBase: ...
def pixel_shuffle(input: TensorBase, upscale_factor: _int) -> TensorBase: ...
def pixel_unshuffle(
    input: TensorBase,
    downscale_factor: _int,
) -> TensorBase: ...
def poisson(input: TensorBase) -> TensorBase: ...
def poisson_nll_loss(
    input: TensorBase,
    target: TensorBase,
    log_input: _bool = True,
    full: _bool = False,
    eps: _float = 1e-08,
) -> TensorBase: ...
def polar(abs: TensorBase, angle: TensorBase) -> TensorBase: ...
def polygamma(n: _int, input: TensorBase) -> TensorBase: ...
def positive(input: TensorBase) -> TensorBase: ...
@overload
def pow(input: TensorBase, exponent: Number | _complex) -> TensorBase: ...
@overload
def pow(input: TensorBase, exponent: TensorBase) -> TensorBase: ...
@overload
def pow(input: Number | _complex, exponent: TensorBase) -> TensorBase: ...
def prelu(input: TensorBase, weight: TensorBase) -> TensorBase: ...
@overload
def prod(
    input: TensorBase,
    *,
    dtype: _dtype = DType.undefined,
) -> TensorBase: ...
@overload
def prod(
    input: TensorBase,
    dim: _size,
    keepdim: _bool = False,
    *,
    dtype: _dtype = DType.undefined,
) -> TensorBase: ...
@overload
def prod(
    input: TensorBase,
    dim: _int,
    keepdim: _bool = False,
    *,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def promote_types(type1: _dtype, type2: _dtype) -> _dtype: ...
def put(
    input: TensorBase,
    index: TensorBase,
    source: TensorBase,
    accumulate: _bool = False,
) -> TensorBase: ...
def q_per_channel_axis(input: TensorBase) -> _int: ...
def q_per_channel_scales(input: TensorBase) -> TensorBase: ...
def q_per_channel_zero_points(input: TensorBase) -> TensorBase: ...
def q_scale(input: TensorBase) -> _float: ...
def q_zero_point(input: TensorBase) -> _int: ...
@overload
def quantile(
    input: TensorBase,
    q: TensorBase,
    dim: _int | None = None,
    keepdim: _bool = False,
    *,
    interpolation: str = "linear",
) -> TensorBase: ...
@overload
def quantile(
    input: TensorBase,
    q: _float,
    dim: _int | None = None,
    keepdim: _bool = False,
    *,
    interpolation: str = 'linear',
) -> TensorBase: ...
def quantize_per_channel(
    input: TensorBase,
    scales: TensorBase,
    zero_points: TensorBase,
    axis: _int = 0,
) -> TensorBase: ...
@overload
def quantize_per_tensor(
    input: TensorBase,
    scale: _float,
    zero_point: _int,
    quant_min: _int = -128,
    quant_max: _int = 127,
) -> TensorBase: ...
@overload
def quantize_per_tensor(
    input: TensorBase,
    scale: TensorBase,
    zero_point: TensorBase,
    dtype: _dtype,
) -> TensorBase: ...
@overload
def quantize_per_tensor(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
    scales: TensorBase,
    zero_points: TensorBase,
    dtype: _dtype,
) -> tuple[TensorBase, ...]: ...
def quantize_per_tensor_dynamic(
    input: TensorBase,
    dtype: _dtype,
    reduce_range: _bool,
) -> TensorBase: ...
def quantized_batch_norm(
    input: TensorBase,
    weight: TensorBase | None,
    bias: TensorBase | None,
    mean: TensorBase,
    var: TensorBase,
    eps: _float,
    output_scale: _float,
    output_zero_point: _int,
) -> TensorBase: ...
def quantized_gru_cell(
    input: TensorBase,
    hx: TensorBase,
    w_ih: TensorBase,
    w_hh: TensorBase,
    b_ih: TensorBase,
    b_hh: TensorBase,
    packed_ih: TensorBase,
    packed_hh: TensorBase,
    col_offsets_ih: TensorBase,
    col_offsets_hh: TensorBase,
    scale_ih: Number | _complex,
    scale_hh: Number | _complex,
    zero_point_ih: Number | _complex,
    zero_point_hh: Number | _complex,
) -> TensorBase: ...
def quantized_linear(
    input: TensorBase,
    weight: TensorBase,
    input_scale: _float,
    input_zero_point: _int,
    weight_scales: TensorBase,
    weight_zero_points: TensorBase,
    bias: TensorBase | None = None,
) -> TensorBase: ...
def quantized_lstm_cell(
    input: TensorBase,
    hx: tuple[TensorBase, ...] | list[TensorBase],
    w_ih: TensorBase,
    w_hh: TensorBase,
    b_ih: TensorBase,
    b_hh: TensorBase,
    packed_ih: TensorBase,
    packed_hh: TensorBase,
    col_offsets_ih: TensorBase,
    col_offsets_hh: TensorBase,
    scale_ih: Number | _complex,
    scale_hh: Number | _complex,
    zero_point_ih: Number | _complex,
    zero_point_hh: Number | _complex,
) -> tuple[TensorBase, TensorBase]: ...
def quantized_max_pool1d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> TensorBase: ...
def quantized_max_pool2d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> TensorBase: ...
def quantized_max_pool3d(
    input: TensorBase,
    kernel_size: _size,
    stride: _size = (),
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
    ceil_mode: _bool = False,
) -> TensorBase: ...
def quantized_rnn_relu_cell(
    input: TensorBase,
    hx: TensorBase,
    w_ih: TensorBase,
    w_hh: TensorBase,
    b_ih: TensorBase,
    b_hh: TensorBase,
    packed_ih: TensorBase,
    packed_hh: TensorBase,
    col_offsets_ih: TensorBase,
    col_offsets_hh: TensorBase,
    scale_ih: Number | _complex,
    scale_hh: Number | _complex,
    zero_point_ih: Number | _complex,
    zero_point_hh: Number | _complex,
) -> TensorBase: ...
def quantized_rnn_tanh_cell(
    input: TensorBase,
    hx: TensorBase,
    w_ih: TensorBase,
    w_hh: TensorBase,
    b_ih: TensorBase,
    b_hh: TensorBase,
    packed_ih: TensorBase,
    packed_hh: TensorBase,
    col_offsets_ih: TensorBase,
    col_offsets_hh: TensorBase,
    scale_ih: Number | _complex,
    scale_hh: Number | _complex,
    zero_point_ih: Number | _complex,
    zero_point_hh: Number | _complex,
) -> TensorBase: ...
def rad2deg(input: TensorBase) -> TensorBase: ...
def rad2deg_(input: TensorBase) -> TensorBase: ...
@overload
def randperm(
    n: _int,
    *,
    dtype: _dtype = DType.int64,
    device: Device | None = None,
    requires_grad: _bool = False,
) -> TensorBase: ...
@overload
def randperm(
    n: _int,
    *,
    generator: Generator | None,
    dtype: _dtype | None = long,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
@overload
def range(
    start: Number | _complex,
    end: Number | _complex,
    step: Number | _complex = 1,
    *,
    dtype: _dtype | None = None,
    device: Device | None = None,
) -> TensorBase: ...
@overload
def range(
    start: Number | _complex,
    end: Number | _complex,
    step: Number | _complex = 1,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
) -> TensorBase: ...
def ravel(input: TensorBase) -> TensorBase: ...
def real(input: TensorBase) -> TensorBase: ...
def reciprocal(input: TensorBase) -> TensorBase: ...
def reciprocal_(input: TensorBase) -> TensorBase: ...
def reflection_pad1d(input: TensorBase, padding: _size) -> TensorBase: ...
@overload
def reflection_pad1d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def reflection_pad1d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
) -> TensorBase: ...
def reflection_pad2d(input: TensorBase, padding: _size) -> TensorBase: ...
@overload
def reflection_pad2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def reflection_pad2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
) -> TensorBase: ...
def reflection_pad3d(input: TensorBase, padding: _size) -> TensorBase: ...
@overload
def reflection_pad3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def reflection_pad3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
) -> TensorBase: ...
def reflection_pad_nd(input: TensorBase, pad: _size) -> TensorBase: ...
def reflection_pad_nd_backward(
    grad_output: TensorBase,
    input: TensorBase,
    pad: _size,
) -> TensorBase: ...
def relu(input: TensorBase) -> TensorBase: ...
def relu6(input: TensorBase) -> TensorBase: ...
def relu6_(input: TensorBase) -> TensorBase: ...
def relu_(input: TensorBase) -> TensorBase: ...
@overload
def remainder(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def remainder(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def remainder(input: Number | _complex, other: TensorBase) -> TensorBase: ...
def renorm(
    input: TensorBase,
    p: Number | _complex,
    dim: _int,
    maxnorm: Number | _complex,
) -> TensorBase: ...
def renorm_(
    input: TensorBase,
    p: Number | _complex,
    dim: _int,
    maxnorm: Number | _complex,
) -> TensorBase: ...
@overload
def repeat_interleave(
    repeats: TensorBase,
    *,
    output_size: _int | None = None,
) -> TensorBase: ...
@overload
def repeat_interleave(
    input: TensorBase,
    repeats: TensorBase,
    dim: _int | None = None,
    *,
    output_size: _int | None = None,
) -> TensorBase: ...
@overload
def repeat_interleave(
    input: TensorBase,
    repeats: _int,
    dim: _int | None = None,
    *,
    output_size: _int | None = None,
) -> TensorBase: ...
def replication_pad1d(input: TensorBase, padding: _size) -> TensorBase: ...
@overload
def replication_pad1d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def replication_pad1d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
) -> TensorBase: ...
def replication_pad2d(input: TensorBase, padding: _size) -> TensorBase: ...
@overload
def replication_pad2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def replication_pad2d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
) -> TensorBase: ...
def replication_pad3d(input: TensorBase, padding: _size) -> TensorBase: ...
@overload
def replication_pad3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def replication_pad3d_backward(
    grad_output: TensorBase,
    input: TensorBase,
    padding: _size,
) -> TensorBase: ...
def replication_pad_nd(input: TensorBase, pad: _size) -> TensorBase: ...
def replication_pad_nd_backward(
    grad_output: TensorBase,
    input: TensorBase,
    pad: _size,
) -> TensorBase: ...
def reshape(input: TensorBase, shape: _size) -> TensorBase: ...
def reshape_as(input: TensorBase, other: TensorBase) -> TensorBase: ...
def resize_as_(
    input: TensorBase,
    other: TensorBase,
    *,
    memory_format: _int = 0,
) -> TensorBase: ...
def resize_as_sparse_(
    input: TensorBase,
    the_template: TensorBase,
) -> TensorBase: ...
def resolve_conj(input: TensorBase) -> TensorBase: ...
def resolve_neg(input: TensorBase) -> TensorBase: ...
@overload
def result_type(tensor: TensorBase, other: TensorBase) -> _dtype: ...
@overload
def result_type(tensor: TensorBase, other: Number | _complex) -> _dtype: ...
@overload
def result_type(scalar: Number | _complex, tensor: TensorBase) -> _dtype: ...
@overload
def result_type(
    scalar1: Number | _complex,
    scalar2: Number | _complex,
) -> _dtype: ...
def rms_norm(
    input: TensorBase,
    normalized_shape: _size,
    weight: TensorBase | None = None,
    eps: _float | None = None,
) -> TensorBase: ...
@overload
def rnn_relu(
    input: TensorBase,
    hx: tuple[TensorBase, ...] | list[TensorBase],
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool = True,
    num_layers: _int = 1,
    dropout_p: _float = 0.0,
    training: _bool = False,
    bidirectional: _bool = False,
    batch_first: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def rnn_relu(
    input: TensorBase,
    hx: TensorBase,
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
    batch_first: _bool,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def rnn_relu(
    data: TensorBase,
    batch_sizes: TensorBase,
    hx: TensorBase,
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
) -> tuple[TensorBase, TensorBase]: ...
def rnn_relu_cell(
    input: TensorBase,
    hx: TensorBase,
    w_ih: TensorBase,
    w_hh: TensorBase,
    b_ih: TensorBase | None = None,
    b_hh: TensorBase | None = None,
) -> TensorBase: ...
@overload
def rnn_tanh(
    input: TensorBase,
    hx: tuple[TensorBase, ...] | list[TensorBase],
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool = True,
    num_layers: _int = 1,
    dropout_p: _float = 0.0,
    training: _bool = False,
    bidirectional: _bool = False,
    batch_first: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def rnn_tanh(
    input: TensorBase,
    hx: TensorBase,
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
    batch_first: _bool,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def rnn_tanh(
    data: TensorBase,
    batch_sizes: TensorBase,
    hx: TensorBase,
    params: tuple[TensorBase, ...] | list[TensorBase],
    has_biases: _bool,
    num_layers: _int,
    dropout: _float,
    train: _bool,
    bidirectional: _bool,
) -> tuple[TensorBase, TensorBase]: ...
def rnn_tanh_cell(
    input: TensorBase,
    hx: TensorBase,
    w_ih: TensorBase,
    w_hh: TensorBase,
    b_ih: TensorBase | None = None,
    b_hh: TensorBase | None = None,
) -> TensorBase: ...
def roll(input: TensorBase, shifts: _size, dims: _size = ()) -> TensorBase: ...
def rot90(
    input: TensorBase,
    k: _int = 1,
    dims: _size = (0, 1),
) -> TensorBase: ...
def rotary_embedding(
    input: TensorBase,
    cos: TensorBase,
    sin: TensorBase,
    position_offset: _int = 0,
) -> TensorBase: ...
@overload
def round(input: TensorBase) -> TensorBase: ...
@overload
def round(input: TensorBase, *, decimals: _int) -> TensorBase: ...
@overload
def round_(input: TensorBase) -> TensorBase: ...
@overload
def round_(input: TensorBase, *, decimals: _int) -> TensorBase: ...
def row_indices_copy(input: TensorBase) -> TensorBase: ...
def row_stack(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def rrelu(
    input: TensorBase,
    lower: Number | _complex = 0.125,
    upper: Number | _complex = 0.3333333333333333,
    training: _bool = False,
) -> TensorBase: ...
def rrelu_(
    input: TensorBase,
    lower: Number | _complex = 0.125,
    upper: Number | _complex = 0.3333333333333333,
    training: _bool = False,
) -> TensorBase: ...
def rrelu_with_noise(
    input: TensorBase,
    noise: TensorBase,
    lower: Number | _complex = 0.125,
    upper: Number | _complex = 0.3333333333333333,
    training: _bool = False,
) -> TensorBase: ...
def rrelu_with_noise_(
    input: TensorBase,
    noise: TensorBase,
    lower: Number | _complex = 0.125,
    upper: Number | _complex = 0.3333333333333333,
    training: _bool = False,
    generator: Generator | None = None,
) -> TensorBase: ...
def rrelu_with_noise_backward(
    grad_output: TensorBase,
    input: TensorBase,
    noise: TensorBase,
    lower: Number | _complex,
    upper: Number | _complex,
    training: _bool,
    self_is_result: _bool,
) -> TensorBase: ...
def rsqrt(input: TensorBase) -> TensorBase: ...
@overload
def rsub(
    input: TensorBase,
    other: TensorBase,
    *,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
@overload
def rsub(
    input: TensorBase,
    other: Number | _complex,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def sample(
    logits: TensorBase,
    temperature: _float = 1.0,
    top_k: _int = 0,
    top_p: _float = 1.0,
    impl: _int = 0,
) -> TensorBase: ...
def scalar_tensor(
    s: Number | _complex,
    *,
    dtype: _dtype | None = None,
    device: Device | None = None,
    pin_memory: _bool = False,
) -> TensorBase: ...
def scaled_dot_product_attention(
    query: TensorBase,
    key: TensorBase,
    value: TensorBase,
    is_causal: _bool = False,
    impl: _int = 0,
) -> TensorBase: ...
def scaled_dot_product_attention_backward(
    grad_output: TensorBase,
    query: TensorBase,
    key: TensorBase,
    value: TensorBase,
    is_causal: _bool = False,
    impl: _int = 0,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def scaled_modified_bessel_k0(input: TensorBase) -> TensorBase: ...
def scaled_modified_bessel_k1(input: TensorBase) -> TensorBase: ...
@overload
def scatter(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    src: TensorBase,
) -> TensorBase: ...
@overload
def scatter(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: Number | _complex,
) -> TensorBase: ...
@overload
def scatter(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    src: TensorBase,
    *,
    reduce: str,
) -> TensorBase: ...
@overload
def scatter(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: Number | _complex,
    *,
    reduce: str,
) -> TensorBase: ...
@overload
def scatter_(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    src: TensorBase,
) -> TensorBase: ...
@overload
def scatter_(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    value: Number | _complex,
) -> TensorBase: ...
def scatter_add(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    src: TensorBase,
) -> TensorBase: ...
def scatter_add_(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    src: TensorBase,
) -> TensorBase: ...
@overload
def scatter_reduce(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    src: TensorBase,
    reduce: str,
    *,
    include_self: _bool = True,
) -> TensorBase: ...
@overload
def scatter_reduce(
    input: TensorBase,
    dim: _int,
    index: TensorBase,
    src: TensorBase,
    reduce: str,
    *,
    include_self: _bool = True,
) -> TensorBase: ...
@overload
def searchsorted(
    sorted_sequence: TensorBase,
    input: TensorBase,
    *,
    out_int32: _bool = False,
    right: _bool = False,
) -> TensorBase: ...
@overload
def searchsorted(
    sorted_sequence: TensorBase,
    input: Number | _complex,
    *,
    out_int32: _bool = False,
    right: _bool = False,
    side: str | None = None,
    sorter: TensorBase | None = None,
) -> TensorBase: ...
def segment_reduce(
    data: TensorBase,
    reduce: str,
    *,
    lengths: TensorBase | None = None,
    indices: TensorBase | None = None,
    offsets: TensorBase | None = None,
    axis: _int = 0,
    unsafe: _bool = False,
    initial: Number | _complex | None = None,
) -> TensorBase: ...
def select_backward(
    grad_output: TensorBase,
    input: TensorBase,
    dim: _int,
    index: _int,
) -> TensorBase: ...
def select_copy(input: TensorBase, dim: _int, index: _int) -> TensorBase: ...
def select_scatter(
    input: TensorBase,
    src: TensorBase,
    dim: _int,
    index: _int,
) -> TensorBase: ...
def selu(input: TensorBase) -> TensorBase: ...
def selu_(input: TensorBase) -> TensorBase: ...
def sgn(input: TensorBase) -> TensorBase: ...
def sgn_(input: TensorBase) -> TensorBase: ...
def shifted_chebyshev_polynomial_t(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
def shifted_chebyshev_polynomial_u(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
def shifted_chebyshev_polynomial_v(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
def shifted_chebyshev_polynomial_w(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
def sigmoid(input: TensorBase) -> TensorBase: ...
def sigmoid_(input: TensorBase) -> TensorBase: ...
@overload
def sigmoid_backward(
    grad_output: TensorBase,
    output: TensorBase,
) -> TensorBase: ...
@overload
def sigmoid_backward(
    grad_output: TensorBase,
    output: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def sign(input: TensorBase) -> TensorBase: ...
def sign_(input: TensorBase) -> TensorBase: ...
def signbit(input: TensorBase) -> TensorBase: ...
def silu(input: TensorBase) -> TensorBase: ...
def silu_(input: TensorBase) -> TensorBase: ...
def silu_and_mul(input: TensorBase) -> TensorBase: ...
@overload
def silu_backward(grad_output: TensorBase, input: TensorBase) -> TensorBase: ...
@overload
def silu_backward(
    grad_output: TensorBase,
    input: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
def silu_mul(gate: TensorBase, up: TensorBase) -> TensorBase: ...
def sin(input: TensorBase) -> TensorBase: ...
def sin_(input: TensorBase) -> TensorBase: ...
def sinc(input: TensorBase) -> TensorBase: ...
def sinc_(input: TensorBase) -> TensorBase: ...
def sinh(input: TensorBase) -> TensorBase: ...
def sinh_(input: TensorBase) -> TensorBase: ...
def size(input: TensorBase, dim: _int) -> _int: ...
def slice_backward(
    grad_output: TensorBase,
    input: TensorBase,
    dim: _int = 0,
    start: _int | None = None,
    end: _int | None = None,
    step: _int = 1,
) -> TensorBase: ...
def slice_copy(
    input: TensorBase,
    dim: _int = 0,
    start: _int | None = None,
    end: _int | None = None,
    step: _int = 1,
) -> TensorBase: ...
def slice_inverse(
    input: TensorBase,
    src: TensorBase,
    dim: _int = 0,
    start: _int | None = None,
    end: _int | None = None,
    step: _int = 1,
) -> TensorBase: ...
def slice_scatter(
    input: TensorBase,
    src: TensorBase,
    dim: _int = 0,
    start: _int | None = None,
    end: _int | None = None,
    step: _int = 1,
) -> TensorBase: ...
@overload
def slogdet(input: TensorBase) -> tuple[TensorBase, TensorBase]: ...
@overload
def slogdet(
    input: TensorBase,
    sign: TensorBase,
    logabsdet: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def slow_conv3d(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: _int | _size = 0,
) -> TensorBase: ...
@overload
def slow_conv3d_forward(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
    output: TensorBase,
) -> TensorBase: ...
@overload
def slow_conv3d_forward(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None,
    stride: _size,
    padding: _size,
) -> TensorBase: ...
def slow_conv_dilated2d(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
) -> TensorBase: ...
def slow_conv_dilated3d(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: _int | _size = 0,
    dilation: _int | _size = 1,
) -> TensorBase: ...
def slow_conv_transpose2d(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: _int | _size = 0,
    output_padding: _int | _size = 0,
    dilation: _int | _size = 1,
) -> TensorBase: ...
def slow_conv_transpose3d(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: _int | _size = 0,
    output_padding: _int | _size = 0,
    dilation: _int | _size = 1,
) -> TensorBase: ...
def smm(input: TensorBase, mat2: TensorBase) -> TensorBase: ...
def smooth_l1_loss(
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
    beta: _float = 1.0,
) -> TensorBase: ...
@overload
def smooth_l1_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    beta: _float,
) -> TensorBase: ...
@overload
def smooth_l1_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    beta: _float,
    grad_input: TensorBase,
) -> TensorBase: ...
def soft_margin_loss(input: TensorBase, target: TensorBase) -> TensorBase: ...
@overload
def soft_margin_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def soft_margin_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int,
) -> TensorBase: ...
@overload
def softmax(
    input: TensorBase,
    dim: _int,
    dtype: _dtype = DType.undefined,
) -> TensorBase: ...
@overload
def softmax(
    input: TensorBase,
    dim: _int,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def softplus(
    input: TensorBase,
    beta: Number | _complex = 1,
    threshold: Number | _complex = 20,
) -> TensorBase: ...
@overload
def softplus_backward(
    grad_output: TensorBase,
    input: TensorBase,
    beta: Number | _complex,
    threshold: Number | _complex,
) -> TensorBase: ...
@overload
def softplus_backward(
    grad_output: TensorBase,
    input: TensorBase,
    beta: Number | _complex,
    threshold: Number | _complex,
    grad_input: TensorBase,
) -> TensorBase: ...
def softshrink(
    input: TensorBase,
    lambd: Number | _complex = 0.5,
) -> TensorBase: ...
@overload
def softshrink_backward(
    grad_output: TensorBase,
    input: TensorBase,
    lambd: Number | _complex,
) -> TensorBase: ...
@overload
def softshrink_backward(
    grad_output: TensorBase,
    input: TensorBase,
    lambd: Number | _complex,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def sort(
    input: TensorBase,
    dim: _int = -1,
    descending: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def sort(
    input: TensorBase,
    dim: _int = -1,
    descending: _bool = False,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def sort(
    input: TensorBase,
    stable: _bool | None,
    dim: _int = -1,
    *,
    descending: _bool = False,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def sort(
    input: TensorBase,
    *,
    stable: _bool | None,
    dim: _int = -1,
    descending: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
def sparse_add(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def sparse_bsc_tensor(
    ccol_indices: TensorBase,
    row_indices: TensorBase,
    values: TensorBase,
    size: _size,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_bsc_tensor(
    ccol_indices: TensorBase,
    row_indices: TensorBase,
    values: TensorBase,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_bsr_tensor(
    crow_indices: TensorBase,
    col_indices: TensorBase,
    values: TensorBase,
    size: _size,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_bsr_tensor(
    crow_indices: TensorBase,
    col_indices: TensorBase,
    values: TensorBase,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_compressed_tensor(
    compressed_indices: TensorBase,
    plain_indices: TensorBase,
    values: TensorBase,
    size: _size,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_compressed_tensor(
    compressed_indices: TensorBase,
    plain_indices: TensorBase,
    values: TensorBase,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_coo_tensor(
    indices: TensorBase,
    values: TensorBase,
    size: _size | None = None,
    *,
    is_coalesced: _bool = False,
) -> TensorBase: ...
@overload
def sparse_coo_tensor(
    size: _size,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_coo_tensor(
    indices: TensorBase,
    values: TensorBase,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
    is_coalesced: _bool | None = None,
) -> TensorBase: ...
@overload
def sparse_coo_tensor(
    indices: TensorBase,
    values: TensorBase,
    size: _size,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = None,
    is_coalesced: _bool | None = None,
) -> TensorBase: ...
@overload
def sparse_csc_tensor(
    ccol_indices: TensorBase,
    row_indices: TensorBase,
    values: TensorBase,
    size: _size,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_csc_tensor(
    ccol_indices: TensorBase,
    row_indices: TensorBase,
    values: TensorBase,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_csr_tensor(
    crow_indices: TensorBase,
    col_indices: TensorBase,
    values: TensorBase,
    size: _size,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
@overload
def sparse_csr_tensor(
    crow_indices: TensorBase,
    col_indices: TensorBase,
    values: TensorBase,
    *,
    dtype: _dtype | None = None,
    layout: int | None = None,
    device: Device | None = None,
    pin_memory: _bool | None = False,
) -> TensorBase: ...
def sparse_mm(input: TensorBase, dense: TensorBase) -> TensorBase: ...
def sparse_mul(input: TensorBase, other: TensorBase) -> TensorBase: ...
def sparse_sampled_addmm(
    input: TensorBase,
    mat1: TensorBase,
    mat2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def sparse_sum(
    input: TensorBase,
    dim: _size | None = None,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def spdiags(
    diagonals: TensorBase,
    offsets: TensorBase,
    shape: _size,
    layout: _int | None = None,
) -> TensorBase: ...
def special_airy_ai(x: TensorBase) -> TensorBase: ...
def special_bessel_j0(input: TensorBase) -> TensorBase: ...
def special_bessel_j1(input: TensorBase) -> TensorBase: ...
def special_bessel_y0(input: TensorBase) -> TensorBase: ...
def special_bessel_y1(input: TensorBase) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_t(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_t(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_t(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_u(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_u(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_u(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_v(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_v(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_v(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_w(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_w(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_chebyshev_polynomial_w(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
def special_digamma(input: TensorBase) -> TensorBase: ...
def special_entr(input: TensorBase) -> TensorBase: ...
def special_erf(input: TensorBase) -> TensorBase: ...
def special_erfc(input: TensorBase) -> TensorBase: ...
def special_erfcx(input: TensorBase) -> TensorBase: ...
def special_erfinv(input: TensorBase) -> TensorBase: ...
def special_exp2(input: TensorBase) -> TensorBase: ...
def special_expit(input: TensorBase) -> TensorBase: ...
def special_expm1(input: TensorBase) -> TensorBase: ...
def special_gammainc(input: TensorBase, other: TensorBase) -> TensorBase: ...
def special_gammaincc(input: TensorBase, other: TensorBase) -> TensorBase: ...
def special_gammaln(input: TensorBase) -> TensorBase: ...
@overload
def special_hermite_polynomial_h(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_hermite_polynomial_h(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_hermite_polynomial_h(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
@overload
def special_hermite_polynomial_he(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_hermite_polynomial_he(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_hermite_polynomial_he(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
def special_i0(input: TensorBase) -> TensorBase: ...
def special_i0e(input: TensorBase) -> TensorBase: ...
def special_i1(input: TensorBase) -> TensorBase: ...
def special_i1e(input: TensorBase) -> TensorBase: ...
@overload
def special_laguerre_polynomial_l(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_laguerre_polynomial_l(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_laguerre_polynomial_l(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
@overload
def special_legendre_polynomial_p(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_legendre_polynomial_p(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_legendre_polynomial_p(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
def special_log1p(input: TensorBase) -> TensorBase: ...
def special_log_ndtr(input: TensorBase) -> TensorBase: ...
def special_log_softmax(
    input: TensorBase,
    dim: _int,
    *,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def special_logit(
    input: TensorBase,
    eps: _float | None = None,
) -> TensorBase: ...
def special_logsumexp(
    input: TensorBase,
    dim: _size,
    keepdim: _bool = False,
) -> TensorBase: ...
def special_modified_bessel_i0(input: TensorBase) -> TensorBase: ...
def special_modified_bessel_i1(input: TensorBase) -> TensorBase: ...
def special_modified_bessel_k0(input: TensorBase) -> TensorBase: ...
def special_modified_bessel_k1(input: TensorBase) -> TensorBase: ...
def special_multigammaln(input: TensorBase, p: _int) -> TensorBase: ...
def special_ndtr(input: TensorBase) -> TensorBase: ...
def special_ndtri(input: TensorBase) -> TensorBase: ...
def special_polygamma(n: _int, input: TensorBase) -> TensorBase: ...
def special_psi(input: TensorBase) -> TensorBase: ...
def special_round(input: TensorBase, *, decimals: _int = 0) -> TensorBase: ...
def special_scaled_modified_bessel_k0(x: TensorBase) -> TensorBase: ...
def special_scaled_modified_bessel_k1(x: TensorBase) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_t(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_t(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_t(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_u(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_u(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_u(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_v(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_v(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_v(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_w(
    x: TensorBase,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_w(
    x: Number | _complex,
    n: TensorBase,
) -> TensorBase: ...
@overload
def special_shifted_chebyshev_polynomial_w(
    x: TensorBase,
    n: Number | _complex,
) -> TensorBase: ...
def special_sinc(input: TensorBase) -> TensorBase: ...
def special_softmax(
    input: TensorBase,
    dim: _int,
    dtype: _dtype | None = None,
) -> TensorBase: ...
def special_spherical_bessel_j0(x: TensorBase) -> TensorBase: ...
@overload
def special_xlog1py(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def special_xlog1py(
    input: Number | _complex,
    other: TensorBase,
) -> TensorBase: ...
@overload
def special_xlog1py(
    input: TensorBase,
    other: Number | _complex,
) -> TensorBase: ...
@overload
def special_xlogy(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def special_xlogy(
    input: Number | _complex,
    other: TensorBase,
) -> TensorBase: ...
@overload
def special_xlogy(
    input: TensorBase,
    other: Number | _complex,
) -> TensorBase: ...
@overload
def special_zeta(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def special_zeta(input: Number | _complex, other: TensorBase) -> TensorBase: ...
@overload
def special_zeta(input: TensorBase, other: Number | _complex) -> TensorBase: ...
def spherical_bessel_j0(input: TensorBase) -> TensorBase: ...
@overload
def split(
    input: TensorBase,
    split_size: _int,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def split(
    input: TensorBase,
    split_sizes: _size,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def split(
    input: TensorBase,
    split_size: _int,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def split_copy(
    input: TensorBase,
    split_size: _int,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def split_copy(
    input: TensorBase,
    split_size: _size,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def split_copy(
    input: TensorBase,
    split_size: _int,
    dim: _int = 0,
    out: tuple[TensorBase, ...] | list[TensorBase],
) -> None: ...
def split_with_sizes(
    input: TensorBase,
    split_sizes: _size,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def split_with_sizes_copy(
    input: TensorBase,
    split_sizes: _size,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def split_with_sizes_copy(
    input: TensorBase,
    split_sizes: _size,
    dim: _int = 0,
    out: tuple[TensorBase, ...] | list[TensorBase],
) -> None: ...
def sqrt(input: TensorBase) -> TensorBase: ...
def square(input: TensorBase) -> TensorBase: ...
def square_(input: TensorBase) -> TensorBase: ...
@overload
def squeeze(input: TensorBase) -> TensorBase: ...
@overload
def squeeze(input: TensorBase, dim: _int) -> TensorBase: ...
@overload
def squeeze(input: TensorBase, dim: _size) -> TensorBase: ...
def squeeze_backward(
    grad_output: TensorBase,
    input: TensorBase,
) -> TensorBase: ...
def sspaddmm(
    input: TensorBase,
    mat1: TensorBase,
    mat2: TensorBase,
    *,
    beta: Number | _complex = 1,
    alpha: Number | _complex = 1,
) -> TensorBase: ...
def stack(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
    dim: _int = 0,
) -> TensorBase: ...
@overload
def std(input: TensorBase, correction: _int = 1) -> TensorBase: ...
@overload
def std(
    input: TensorBase,
    dim: _size,
    correction: _int = 1,
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def std(
    input: TensorBase,
    dim: _size | None = None,
    *,
    correction: Number | _complex | None = None,
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def std_mean(
    input: TensorBase,
    dim: _size = (),
    unbiased: _bool = True,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def std_mean(
    input: TensorBase,
    dim: _size | None,
    unbiased: _bool = True,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def std_mean(
    input: TensorBase,
    dim: _size | None = None,
    *,
    correction: Number | _complex | None = None,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def stft(
    input: TensorBase,
    n_fft: _int,
    hop_length: _int | None = None,
    win_length: _int | None = None,
    window: TensorBase | None = None,
    center: _bool = True,
    pad_mode: str = "reflect",
    normalized: _bool = False,
    onesided: _bool = True,
    return_complex: _bool = True,
) -> TensorBase: ...
@overload
def stft(
    input: TensorBase,
    n_fft: _int,
    hop_length: _int | None = None,
    win_length: _int | None = None,
    window: TensorBase | None = None,
    center: _bool = True,
    pad_mode: str = "reflect",
    normalized: _bool = False,
    onesided: _bool | None = None,
    return_complex: _bool | None = None,
    align_to_window: _bool | None = None,
) -> TensorBase: ...
def stft_backward(
    grad_output: TensorBase,
    input: TensorBase,
    n_fft: _int,
    hop_length: _int | None = None,
    win_length: _int | None = None,
    window: TensorBase | None = None,
    center: _bool = True,
    pad_mode: str = "reflect",
    normalized: _bool = False,
    onesided: _bool = True,
) -> TensorBase: ...
def stride(input: TensorBase, dim: _int) -> _int: ...
@overload
def sum(
    input: TensorBase,
    *,
    dtype: _dtype = DType.undefined,
) -> TensorBase: ...
@overload
def sum(
    input: TensorBase,
    dim: _size,
    keepdim: _bool = False,
    *,
    dtype: _dtype = DType.undefined,
) -> TensorBase: ...
@overload
def svd(
    input: TensorBase,
    some: _bool = True,
    compute_uv: _bool = True,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
@overload
def svd(
    input: TensorBase,
    some: _bool = True,
    compute_uv: _bool = True,
    U: TensorBase,
    S: TensorBase,
    V: TensorBase,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def swapaxes(input: TensorBase, axis0: _int, axis1: _int) -> TensorBase: ...
def swapdims(input: TensorBase, dim0: _int, dim1: _int) -> TensorBase: ...
def sym_constrain_range(
    size: Number | _complex,
    *,
    min: _int | None = None,
    max: _int | None = None,
) -> None: ...
def sym_constrain_range_for_size(
    size: Number | _complex,
    *,
    min: _int | None = None,
    max: _int | None = None,
) -> None: ...
def sym_is_contiguous(
    input: TensorBase,
    memory_format: MemoryFormat = contiguous_format,
) -> _bool: ...
def sym_numel(input: TensorBase) -> _int: ...
def sym_size(input: TensorBase, dim: _int) -> _int: ...
def sym_storage_offset(input: TensorBase) -> _int: ...
def sym_stride(input: TensorBase, dim: _int) -> _int: ...
def t(input: TensorBase) -> TensorBase: ...
def t_copy(input: TensorBase) -> TensorBase: ...
def take(input: TensorBase, index: TensorBase) -> TensorBase: ...
def take_along_dim(
    input: TensorBase,
    indices: TensorBase,
    dim: _int | None = None,
) -> TensorBase: ...
def tan(input: TensorBase) -> TensorBase: ...
def tan_(input: TensorBase) -> TensorBase: ...
def tanh(input: TensorBase) -> TensorBase: ...
def tanh_(input: TensorBase) -> TensorBase: ...
@overload
def tanh_backward(
    grad_output: TensorBase,
    output: TensorBase,
) -> TensorBase: ...
@overload
def tanh_backward(
    grad_output: TensorBase,
    output: TensorBase,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def tensor_split(
    input: TensorBase,
    sections: _int,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def tensor_split(
    input: TensorBase,
    indices: _size,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
@overload
def tensor_split(
    input: TensorBase,
    tensor_indices_or_sections: TensorBase,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
def tensordot(
    input: TensorBase,
    other: TensorBase,
    dims_self: _size,
    dims_other: _size,
) -> TensorBase: ...
def thnn_conv2d(
    input: TensorBase,
    weight: TensorBase,
    kernel_size: _size,
    bias: TensorBase | None = None,
    stride: _int | _size = 1,
    padding: _int | _size = 0,
) -> TensorBase: ...
def threshold(
    input: TensorBase,
    threshold: Number | _complex,
    value: Number | _complex,
) -> TensorBase: ...
def threshold_(
    input: TensorBase,
    threshold: Number | _complex,
    value: Number | _complex,
) -> TensorBase: ...
@overload
def threshold_backward(
    grad_output: TensorBase,
    input: TensorBase,
    threshold: Number | _complex,
) -> TensorBase: ...
@overload
def threshold_backward(
    grad_output: TensorBase,
    input: TensorBase,
    threshold: Number | _complex,
    grad_input: TensorBase,
) -> TensorBase: ...
def tile(input: TensorBase, dims: _size) -> TensorBase: ...
def to_dense(input: TensorBase) -> TensorBase: ...
def to_dense_backward(
    grad: TensorBase,
    input: TensorBase,
    masked_grad: _bool | None = None,
) -> TensorBase: ...
def to_mkldnn_backward(grad: TensorBase, input: TensorBase) -> TensorBase: ...
def to_sparse(input: TensorBase) -> TensorBase: ...
def to_sparse_csr(input: TensorBase) -> TensorBase: ...
@overload
def topk(
    input: TensorBase,
    k: _int,
    dim: _int = -1,
    largest: _bool = True,
    sorted: _bool = True,
    impl: _int = 0,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def topk(
    input: TensorBase,
    k: _int,
    dim: _int = -1,
    largest: _bool = True,
    sorted: _bool = True,
    values: TensorBase,
    indices: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def tp_cosine_embedding_loss(
    input1: TensorBase,
    input2: TensorBase,
    target: TensorBase,
    margin: _float = 0.0,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_cosine_embedding_loss_backward(
    grad_output: TensorBase,
    input1: TensorBase,
    input2: TensorBase,
    target: TensorBase,
    margin: _float = 0.0,
    reduction: _int = 1,
) -> tuple[TensorBase, TensorBase]: ...
def tp_hinge_embedding_loss(
    input: TensorBase,
    target: TensorBase,
    margin: _float = 1.0,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_hinge_embedding_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    margin: _float = 1.0,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_kl_div(
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
    log_target: _bool = False,
) -> TensorBase: ...
def tp_kl_div_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
    log_target: _bool = False,
) -> TensorBase: ...
def tp_l1_loss(
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_l1_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_margin_ranking_loss(
    input1: TensorBase,
    input2: TensorBase,
    target: TensorBase,
    margin: _float = 0.0,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_margin_ranking_loss_backward(
    grad_output: TensorBase,
    input1: TensorBase,
    input2: TensorBase,
    target: TensorBase,
    margin: _float = 0.0,
    reduction: _int = 1,
) -> tuple[TensorBase, TensorBase]: ...
def tp_poisson_nll_loss(
    input: TensorBase,
    target: TensorBase,
    log_input: _bool = True,
    full: _bool = False,
    eps: _float = 1e-08,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_poisson_nll_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    log_input: _bool = True,
    full: _bool = False,
    eps: _float = 1e-08,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_soft_margin_loss(
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
) -> TensorBase: ...
def tp_soft_margin_loss_backward(
    grad_output: TensorBase,
    input: TensorBase,
    target: TensorBase,
    reduction: _int = 1,
) -> TensorBase: ...
def trace(input: TensorBase) -> TensorBase: ...
def trace_backward(grad: TensorBase, sizes: _size) -> TensorBase: ...
@overload
def transpose(input: TensorBase, dim0: _int, dim1: _int) -> TensorBase: ...
@overload
def transpose(input: TensorBase, dim0: _int, dim1: _int) -> TensorBase: ...
def transpose_copy(input: TensorBase, dim0: _int, dim1: _int) -> TensorBase: ...
@overload
def trapezoid(
    y: TensorBase,
    x: TensorBase | None = None,
    *,
    dx: Number | _complex = 1,
    dim: _int = -1,
) -> TensorBase: ...
@overload
def trapezoid(
    y: TensorBase,
    x: TensorBase,
    *,
    dim: _int = -1,
) -> TensorBase: ...
@overload
def trapezoid(
    y: TensorBase,
    *,
    dx: Number | _complex = 1,
    dim: _int = -1,
) -> TensorBase: ...
@overload
def triangular_solve(
    input: TensorBase,
    A: TensorBase,
    upper: _bool = False,
    transpose: _bool = False,
    unitriangular: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def triangular_solve(
    input: TensorBase,
    A: TensorBase,
    upper: _bool = True,
    transpose: _bool = False,
    unitriangular: _bool = False,
    X: TensorBase,
    M: TensorBase,
) -> tuple[TensorBase, TensorBase]: ...
def tril(input: TensorBase, diagonal: _int = 0) -> TensorBase: ...
def tril_(input: TensorBase, diagonal: _int = 0) -> TensorBase: ...
def tril_indices(
    row: _int,
    col: _int,
    offset: _int = 0,
    *,
    dtype: _dtype = DType.int64,
    device: Device | None = None,
    pin_memory: _bool = False,
) -> TensorBase: ...
def triplet_margin_loss(
    anchor: TensorBase,
    positive: TensorBase,
    negative: TensorBase,
    margin: Number | _complex = 1.0,
    p: _float = 2.0,
) -> TensorBase: ...
def triu(input: TensorBase, diagonal: _int = 0) -> TensorBase: ...
def triu_(input: TensorBase, diagonal: _int = 0) -> TensorBase: ...
def triu_indices(
    row: _int,
    col: _int,
    offset: _int = 0,
    *,
    dtype: _dtype = DType.int64,
    device: Device | None = None,
    pin_memory: _bool = False,
) -> TensorBase: ...
def trunc(input: TensorBase) -> TensorBase: ...
def trunc_(input: TensorBase) -> TensorBase: ...
@overload
def unbind(input: TensorBase, dim: _int = 0) -> tuple[TensorBase, ...]: ...
@overload
def unbind(input: TensorBase, dim: _int = 0) -> tuple[TensorBase, ...]: ...
@overload
def unbind_copy(input: TensorBase, dim: _int = 0) -> tuple[TensorBase, ...]: ...
@overload
def unbind_copy(
    input: TensorBase,
    dim: _int = 0,
    out: tuple[TensorBase, ...] | list[TensorBase],
) -> None: ...
def unflatten(input: TensorBase, dim: _int, sizes: _size) -> TensorBase: ...
def unflatten_dense_tensors(
    flat: TensorBase,
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> tuple[TensorBase, ...]: ...
def unfold(
    input: TensorBase,
    dimension: _int,
    size: _int,
    step: _int,
) -> TensorBase: ...
def unfold_backward(
    grad_output: TensorBase,
    input_sizes: _size,
    dim: _int,
    size: _int,
    step: _int,
) -> TensorBase: ...
def unfold_copy(
    input: TensorBase,
    dimension: _int,
    size: _int,
    step: _int,
) -> TensorBase: ...
def unique(
    input: TensorBase,
    sorted: _bool = True,
    return_inverse: _bool = False,
    return_counts: _bool = False,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def unique_dim(
    input: TensorBase,
    dim: _int,
    sorted: _bool = True,
    return_inverse: _bool = False,
    return_counts: _bool = False,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def unique_dim_consecutive(
    input: TensorBase,
    dim: _int,
    return_inverse: _bool = False,
    return_counts: _bool = False,
) -> tuple[TensorBase, TensorBase, TensorBase]: ...
def unsafe_chunk(
    input: TensorBase,
    chunks: _int,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
def unsafe_split(
    input: TensorBase,
    split_size: _int,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
def unsafe_split_with_sizes(
    input: TensorBase,
    split_sizes: _size,
    dim: _int = 0,
) -> tuple[TensorBase, ...]: ...
def unsqueeze(input: TensorBase, dim: _int) -> TensorBase: ...
def unsqueeze_copy(input: TensorBase, dim: _int) -> TensorBase: ...
@overload
def upsample_bicubic2d(
    input: TensorBase,
    output_size: _size,
    align_corners: _bool,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_bicubic2d(
    input: TensorBase,
    output_size: _size | None,
    align_corners: _bool,
    scale_factors: Sequence[_float] | None,
) -> TensorBase: ...
@overload
def upsample_bicubic2d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    align_corners: _bool,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_bicubic2d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    align_corners: _bool,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def upsample_bilinear2d(
    input: TensorBase,
    output_size: _size,
    align_corners: _bool,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_bilinear2d(
    input: TensorBase,
    output_size: _size | None,
    align_corners: _bool,
    scale_factors: Sequence[_float] | None,
) -> TensorBase: ...
@overload
def upsample_bilinear2d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    align_corners: _bool,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_bilinear2d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    align_corners: _bool,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def upsample_linear1d(
    input: TensorBase,
    output_size: _size,
    align_corners: _bool,
    scales: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_linear1d(
    input: TensorBase,
    output_size: _size | None,
    align_corners: _bool,
    scale_factors: Sequence[_float] | None,
) -> TensorBase: ...
@overload
def upsample_linear1d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    align_corners: _bool,
    scales: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_linear1d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    align_corners: _bool,
    scales: _float | None = None,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def upsample_nearest1d(
    input: TensorBase,
    output_size: _size,
    scales: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_nearest1d(
    input: TensorBase,
    output_size: _size | None,
    scale_factors: Sequence[_float] | None,
) -> TensorBase: ...
@overload
def upsample_nearest1d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    scales: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_nearest1d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    scales: _float | None = None,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def upsample_nearest2d(
    input: TensorBase,
    output_size: _size,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_nearest2d(
    input: TensorBase,
    output_size: _size | None,
    scale_factors: Sequence[_float] | None,
) -> TensorBase: ...
@overload
def upsample_nearest2d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_nearest2d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def upsample_nearest3d(
    input: TensorBase,
    output_size: _size,
    scales_d: _float | None = None,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_nearest3d(
    input: TensorBase,
    output_size: _size | None,
    scale_factors: Sequence[_float] | None,
) -> TensorBase: ...
@overload
def upsample_nearest3d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    scales_d: _float | None = None,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_nearest3d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    scales_d: _float | None = None,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
    grad_input: TensorBase,
) -> TensorBase: ...
@overload
def upsample_trilinear3d(
    input: TensorBase,
    output_size: _size,
    align_corners: _bool,
    scales_d: _float | None = None,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_trilinear3d(
    input: TensorBase,
    output_size: _size | None,
    align_corners: _bool,
    scale_factors: Sequence[_float] | None,
) -> TensorBase: ...
@overload
def upsample_trilinear3d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    align_corners: _bool,
    scales_d: _float | None = None,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
) -> TensorBase: ...
@overload
def upsample_trilinear3d_backward(
    grad_output: TensorBase,
    output_size: _size,
    input_size: _size,
    align_corners: _bool,
    scales_d: _float | None = None,
    scales_h: _float | None = None,
    scales_w: _float | None = None,
    grad_input: TensorBase,
) -> TensorBase: ...
def value_selecting_reduction_backward(
    grad: TensorBase,
    dim: _int,
    indices: TensorBase,
    sizes: _size,
    keepdim: _bool,
) -> TensorBase: ...
def values_copy(input: TensorBase) -> TensorBase: ...
def vander(
    x: TensorBase,
    N: _int | None = None,
    increasing: _bool = False,
) -> TensorBase: ...
@overload
def var(input: TensorBase, correction: _int = 1) -> TensorBase: ...
@overload
def var(
    input: TensorBase,
    dim: _size,
    correction: _int = 1,
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def var(
    input: TensorBase,
    dim: _size | None = None,
    *,
    correction: Number | _complex | None = None,
    keepdim: _bool = False,
) -> TensorBase: ...
@overload
def var_mean(
    input: TensorBase,
    dim: _size = (),
    unbiased: _bool = True,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def var_mean(
    input: TensorBase,
    dim: _size | None,
    unbiased: _bool = True,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
@overload
def var_mean(
    input: TensorBase,
    dim: _size | None = None,
    *,
    correction: Number | _complex | None = None,
    keepdim: _bool = False,
) -> tuple[TensorBase, TensorBase]: ...
def vdot(input: TensorBase, other: TensorBase) -> TensorBase: ...
def view_as_complex(input: TensorBase) -> TensorBase: ...
def view_as_complex_copy(input: TensorBase) -> TensorBase: ...
def view_as_real(input: TensorBase) -> TensorBase: ...
def view_as_real_copy(input: TensorBase) -> TensorBase: ...
@overload
def view_copy(input: TensorBase, size: _size) -> TensorBase: ...
@overload
def view_copy(input: TensorBase, dtype: _dtype) -> TensorBase: ...
@overload
def vsplit(input: TensorBase, sections: _int) -> tuple[TensorBase, ...]: ...
@overload
def vsplit(input: TensorBase, indices: _size) -> tuple[TensorBase, ...]: ...
def vstack(
    tensors: tuple[TensorBase, ...] | list[TensorBase],
) -> TensorBase: ...
def xlog1py(input: TensorBase, other: TensorBase) -> TensorBase: ...
def xlog1py_(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def xlogy(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def xlogy(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def xlogy(input: Number | _complex, other: TensorBase) -> TensorBase: ...
@overload
def xlogy(input: TensorBase, other: Number | _complex) -> TensorBase: ...
@overload
def xlogy_(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def xlogy_(input: TensorBase, other: TensorBase) -> TensorBase: ...
@overload
def xlogy_(input: TensorBase, other: Number | _complex) -> TensorBase: ...
def zeta(input: TensorBase, other: TensorBase) -> TensorBase: ...



__all__ += [
    "abs",
    "abs_",
    "absolute",
    "absolute_",
    "acos",
    "acos_",
    "acosh",
    "acosh_",
    "adaptive_avg_pool1d",
    "adaptive_avg_pool2d",
    "adaptive_avg_pool2d_backward",
    "adaptive_avg_pool3d",
    "adaptive_avg_pool3d_backward",
    "adaptive_max_pool1d",
    "adaptive_max_pool2d",
    "adaptive_max_pool2d_backward",
    "adaptive_max_pool2d_with_indices",
    "adaptive_max_pool2d_with_indices_backward",
    "adaptive_max_pool3d",
    "adaptive_max_pool3d_backward",
    "add",
    "add_",
    "add_relu",
    "addbmm",
    "addbmm_",
    "addcdiv",
    "addcdiv_",
    "addcmul",
    "addcmul_",
    "addmm",
    "addmm_",
    "addmv",
    "addmv_",
    "addr",
    "addr_",
    "adjoint",
    "affine_grid_generator",
    "affine_grid_generator_backward",
    "airy_ai",
    "alias",
    "alias_copy",
    "all",
    "allclose",
    "alpha_dropout",
    "alpha_dropout_",
    "amax",
    "amin",
    "aminmax",
    "angle",
    "any",
    "arange",
    "arccos",
    "arccos_",
    "arccosh",
    "arccosh_",
    "arcsin",
    "arcsin_",
    "arcsinh",
    "arcsinh_",
    "arctan",
    "arctan2",
    "arctan_",
    "arctanh",
    "arctanh_",
    "argmax",
    "argmin",
    "argsort",
    "argwhere",
    "as_strided",
    "as_strided_",
    "as_strided_copy",
    "as_strided_scatter",
    "asin",
    "asin_",
    "asinh",
    "asinh_",
    "atan",
    "atan2",
    "atan2_",
    "atan_",
    "atanh",
    "atanh_",
    "atleast_1d",
    "atleast_2d",
    "atleast_3d",
    "avg_pool1d",
    "avg_pool2d",
    "avg_pool2d_backward",
    "avg_pool3d",
    "avg_pool3d_backward",
    "baddbmm",
    "bartlett_window",
    "batch_norm",
    "batch_norm_backward",
    "batch_norm_backward_elemt",
    "batch_norm_backward_reduce",
    "batch_norm_elemt",
    "batch_norm_gather_stats",
    "batch_norm_gather_stats_with_counts",
    "batch_norm_stats",
    "batch_norm_update_stats",
    "bernoulli",
    "bernoulli_",
    "bessel_j0",
    "bessel_j1",
    "bessel_y0",
    "bessel_y1",
    "bilinear",
    "binary_cross_entropy",
    "binary_cross_entropy_backward",
    "binary_cross_entropy_with_logits",
    "bincount",
    "binomial",
    "bitwise_and",
    "bitwise_left_shift",
    "bitwise_not",
    "bitwise_not_",
    "bitwise_or",
    "bitwise_right_shift",
    "bitwise_xor",
    "blackman_window",
    "block_diag",
    "bmm",
    "broadcast_tensors",
    "broadcast_to",
    "bucketize",
    "can_cast",
    "cartesian_prod",
    "cat",
    "cauchy_",
    "ccol_indices_copy",
    "cdist",
    "ceil",
    "ceil_",
    "celu",
    "celu_",
    "chain_matmul",
    "channel_shuffle",
    "chebyshev_polynomial_t",
    "chebyshev_polynomial_u",
    "chebyshev_polynomial_v",
    "chebyshev_polynomial_w",
    "cholesky",
    "cholesky_inverse",
    "cholesky_solve",
    "choose_qparams_optimized",
    "chunk",
    "circular_pad_nd",
    "circular_pad_nd_backward",
    "clamp",
    "clamp_",
    "clamp_backward",
    "clamp_max",
    "clamp_max_",
    "clamp_min",
    "clamp_min_",
    "clip",
    "clip_",
    "clone",
    "col2im",
    "col2im_backward",
    "col_indices_copy",
    "column_stack",
    "combinations",
    "complex",
    "concat",
    "concatenate",
    "conj",
    "conj_physical",
    "conj_physical_",
    "constant_pad_nd",
    "constant_pad_nd_backward",
    "contiguous",
    "conv1d",
    "conv1d_grad_bias",
    "conv1d_grad_input",
    "conv1d_grad_weight",
    "conv2d",
    "conv2d_grad_bias",
    "conv2d_grad_input",
    "conv2d_grad_weight",
    "conv2d_relu",
    "conv3d",
    "conv3d_grad_bias",
    "conv3d_grad_input",
    "conv3d_grad_weight",
    "conv_depthwise3d",
    "conv_tbc",
    "conv_tbc_backward",
    "conv_transpose1d",
    "conv_transpose1d_grad_bias",
    "conv_transpose1d_grad_input",
    "conv_transpose1d_grad_weight",
    "conv_transpose2d",
    "conv_transpose2d_grad_bias",
    "conv_transpose2d_grad_input",
    "conv_transpose2d_grad_weight",
    "conv_transpose3d",
    "conv_transpose3d_grad_bias",
    "conv_transpose3d_grad_input",
    "conv_transpose3d_grad_weight",
    "convolution",
    "convolution_backward",
    "convolution_backward_overrideable",
    "convolution_overrideable",
    "copy",
    "copy_",
    "copy_sparse_to_sparse_",
    "corrcoef",
    "cos",
    "cos_",
    "cosh",
    "cosh_",
    "cosine_embedding_loss",
    "cosine_similarity",
    "count_nonzero",
    "cov",
    "cross",
    "cross_entropy_loss",
    "crow_indices_copy",
    "ctc_loss",
    "cudnn_affine_grid_generator",
    "cudnn_affine_grid_generator_backward",
    "cudnn_batch_norm",
    "cudnn_batch_norm_backward",
    "cudnn_convolution",
    "cudnn_convolution_add_relu",
    "cudnn_convolution_relu",
    "cudnn_convolution_transpose",
    "cudnn_grid_sampler",
    "cudnn_grid_sampler_backward",
    "cudnn_is_acceptable",
    "cummax",
    "cummaxmin_backward",
    "cummin",
    "cumprod",
    "cumprod_",
    "cumprod_backward",
    "cumsum",
    "cumsum_",
    "cumulative_trapezoid",
    "deg2rad",
    "deg2rad_",
    "dequantize",
    "dequantize_per_channel",
    "dequantize_per_tensor",
    "det",
    "detach",
    "detach_",
    "detach_copy",
    "diag",
    "diag_embed",
    "diagflat",
    "diagonal",
    "diagonal_backward",
    "diagonal_copy",
    "diagonal_scatter",
    "diff",
    "digamma",
    "dist",
    "div",
    "div_",
    "dot",
    "dropout",
    "dropout_",
    "dsplit",
    "dstack",
    "einsum",
    "elu",
    "elu_",
    "elu_backward",
    "embedding",
    "embedding_backward",
    "embedding_bag",
    "embedding_dense_backward",
    "embedding_renorm_",
    "embedding_sparse_backward",
    "empty",
    "empty_like",
    "empty_permuted",
    "empty_quantized",
    "empty_strided",
    "entr",
    "eq",
    "equal",
    "erf",
    "erf_",
    "erfc",
    "erfc_",
    "erfcx",
    "erfinv",
    "erfinv_",
    "exp",
    "exp2",
    "exp2_",
    "exp_",
    "expand",
    "expand_as",
    "expand_copy",
    "expm1",
    "expm1_",
    "exponential_",
    "eye",
    "fake_quantize_per_channel_affine",
    "fake_quantize_per_channel_affine_cachemask",
    "fake_quantize_per_channel_affine_cachemask_backward",
    "fake_quantize_per_tensor_affine",
    "fake_quantize_per_tensor_affine_cachemask",
    "fake_quantize_per_tensor_affine_cachemask_backward",
    "fbgemm_linear_fp16_weight",
    "fbgemm_linear_fp16_weight_fp32_activation",
    "fbgemm_linear_int8_weight",
    "fbgemm_linear_int8_weight_fp32_activation",
    "fbgemm_linear_quantize_weight",
    "fbgemm_pack_gemm_matrix_fp16",
    "fbgemm_pack_quantized_matrix",
    "feature_alpha_dropout",
    "feature_alpha_dropout_",
    "feature_dropout",
    "feature_dropout_",
    "fft_fft",
    "fft_fft2",
    "fft_fft2_backward",
    "fft_fft_backward",
    "fft_fftfreq",
    "fft_fftn",
    "fft_fftshift",
    "fft_hfft",
    "fft_hfft2",
    "fft_hfftn",
    "fft_ifft",
    "fft_ifft2",
    "fft_ifft2_backward",
    "fft_ifft_backward",
    "fft_ifftn",
    "fft_ifftshift",
    "fft_ihfft",
    "fft_ihfft2",
    "fft_ihfftn",
    "fft_irfft",
    "fft_irfft2",
    "fft_irfft2_backward",
    "fft_irfft_backward",
    "fft_irfftn",
    "fft_rfft",
    "fft_rfft2",
    "fft_rfft2_backward",
    "fft_rfft_backward",
    "fft_rfftfreq",
    "fft_rfftn",
    "fill",
    "fill_",
    "fix",
    "fix_",
    "flatten",
    "flatten_dense_tensors",
    "flip",
    "fliplr",
    "flipud",
    "float_power",
    "floor",
    "floor_",
    "fmax",
    "fmin",
    "forward_add",
    "forward_cos",
    "forward_div",
    "forward_exp",
    "forward_log",
    "forward_mm",
    "forward_mul",
    "forward_neg",
    "forward_pow",
    "forward_relu",
    "forward_sigmoid",
    "forward_sin",
    "forward_sqrt",
    "forward_sub",
    "forward_tanh",
    "frac",
    "frac_",
    "fractional_max_pool2d",
    "fractional_max_pool2d_backward",
    "fractional_max_pool3d",
    "fractional_max_pool3d_backward",
    "frexp",
    "frobenius_norm",
    "from_file",
    "full",
    "full_like",
    "fused_moving_avg_obs_fake_quant",
    "fused_mul_add",
    "fused_rope",
    "fused_swiglu",
    "gammainc",
    "gammaincc",
    "gather",
    "gather_backward",
    "gcd",
    "gcd_",
    "ge",
    "gelu",
    "gelu_",
    "gelu_backward",
    "geometric_",
    "geqrf",
    "ger",
    "get_device",
    "glu",
    "glu_backward",
    "glu_backward_jvp",
    "glu_jvp",
    "gradient",
    "greater",
    "greater_equal",
    "grid_sampler",
    "grid_sampler_2d",
    "grid_sampler_2d_backward",
    "grid_sampler_3d",
    "grid_sampler_3d_backward",
    "group_norm",
    "group_norm_backward",
    "grouped_mm",
    "gru",
    "gru_cell",
    "gt",
    "hamming_window",
    "hann_window",
    "hardshrink",
    "hardshrink_backward",
    "hardsigmoid",
    "hardsigmoid_",
    "hardsigmoid_backward",
    "hardswish",
    "hardswish_",
    "hardswish_backward",
    "hardtanh",
    "hardtanh_",
    "hardtanh_backward",
    "hash_tensor",
    "heaviside",
    "heaviside_",
    "hermite_polynomial_h",
    "hermite_polynomial_he",
    "hinge_embedding_loss",
    "histc",
    "histogram",
    "histogramdd",
    "hsplit",
    "hspmm",
    "hstack",
    "huber_loss",
    "huber_loss_backward",
    "hypot",
    "hypot_",
    "i0",
    "i0_",
    "i0e",
    "i1",
    "i1e",
    "igamma",
    "igammac",
    "im2col",
    "im2col_backward",
    "imag",
    "index",
    "index_add",
    "index_add_",
    "index_copy",
    "index_copy_",
    "index_fill",
    "index_fill_",
    "index_put",
    "index_put_",
    "index_reduce",
    "index_reduce_",
    "index_select",
    "index_select_backward",
    "indices_copy",
    "infinitely_differentiable_gelu_backward",
    "inner",
    "inner_backward_other",
    "inner_backward_self",
    "instance_norm",
    "instance_norm_backward",
    "int_repr",
    "inverse",
    "is_complex",
    "is_conj",
    "is_distributed",
    "is_floating_point",
    "is_inference",
    "is_neg",
    "is_nonzero",
    "is_same_size",
    "is_signed",
    "is_vulkan_available",
    "isclose",
    "isfinite",
    "isin",
    "isinf",
    "isnan",
    "isneginf",
    "isposinf",
    "isreal",
    "istft",
    "item",
    "kaiser_window",
    "kl_div",
    "kron",
    "kthvalue",
    "l1_loss",
    "laguerre_polynomial_l",
    "layer_norm",
    "layer_norm_backward",
    "lcm",
    "lcm_",
    "ldexp",
    "ldexp_",
    "le",
    "leaky_relu",
    "leaky_relu_",
    "leaky_relu_backward",
    "legendre_polynomial_p",
    "lerp",
    "lerp_",
    "less",
    "less_equal",
    "lgamma",
    "lgamma_",
    "lift",
    "lift_fresh",
    "lift_fresh_copy",
    "linear",
    "linear_backward",
    "linspace",
    "log",
    "log10",
    "log10_",
    "log1p",
    "log1p_",
    "log2",
    "log2_",
    "log_",
    "log_ndtr",
    "log_normal_",
    "log_sigmoid",
    "log_sigmoid_backward",
    "log_sigmoid_forward",
    "log_softmax",
    "logaddexp",
    "logaddexp2",
    "logcumsumexp",
    "logdet",
    "logical_and",
    "logical_and_",
    "logical_not",
    "logical_not_",
    "logical_or",
    "logical_or_",
    "logical_xor",
    "logical_xor_",
    "logit",
    "logit_",
    "logit_backward",
    "logspace",
    "logsumexp",
    "lstm",
    "lstm_cell",
    "lstm_mps_backward",
    "lt",
    "lu_solve",
    "lu_unpack",
    "margin_ranking_loss",
    "masked_fill",
    "masked_fill_",
    "masked_scatter",
    "masked_scatter_",
    "masked_scatter_backward",
    "masked_select",
    "masked_select_backward",
    "matmul",
    "matmul_backward",
    "matmul_backward_other",
    "matmul_backward_self",
    "matrix_exp",
    "matrix_exp_backward",
    "matrix_power",
    "max",
    "max_pool1d",
    "max_pool1d_with_indices",
    "max_pool2d",
    "max_pool2d_backward",
    "max_pool2d_with_indices",
    "max_pool2d_with_indices_backward",
    "max_pool3d",
    "max_pool3d_backward",
    "max_pool3d_with_indices",
    "max_pool3d_with_indices_backward",
    "max_unpool2d",
    "max_unpool2d_backward",
    "max_unpool3d",
    "max_unpool3d_backward",
    "maximum",
    "mean",
    "mean_dim_backward",
    "median",
    "meshgrid",
    "min",
    "minimum",
    "miopen_batch_norm",
    "miopen_batch_norm_backward",
    "miopen_convolution",
    "miopen_convolution_add_relu",
    "miopen_convolution_relu",
    "miopen_convolution_transpose",
    "miopen_ctc_loss",
    "miopen_depthwise_convolution",
    "miopen_rnn",
    "miopen_rnn_backward",
    "mish",
    "mish_",
    "mish_backward",
    "mkldnn_adaptive_avg_pool2d",
    "mkldnn_adaptive_avg_pool2d_backward",
    "mkldnn_convolution",
    "mkldnn_linear",
    "mkldnn_linear_backward",
    "mkldnn_linear_backward_input",
    "mkldnn_linear_backward_weights",
    "mkldnn_max_pool2d",
    "mkldnn_max_pool2d_backward",
    "mkldnn_max_pool3d",
    "mkldnn_max_pool3d_backward",
    "mkldnn_reorder_conv2d_weight",
    "mkldnn_reorder_conv3d_weight",
    "mkldnn_rnn_layer",
    "mkldnn_rnn_layer_backward",
    "mm",
    "mode",
    "modified_bessel_i0",
    "modified_bessel_i1",
    "modified_bessel_k0",
    "modified_bessel_k1",
    "moveaxis",
    "movedim",
    "mps_convolution_backward",
    "mps_convolution_transpose_backward",
    "mse_loss",
    "mse_loss_backward",
    "msort",
    "mul",
    "mul_",
    "multi_margin_loss",
    "multi_margin_loss_backward",
    "multilabel_margin_loss",
    "multilabel_margin_loss_backward",
    "multilabel_margin_loss_forward",
    "multilabel_soft_margin_loss",
    "multinomial",
    "mv",
    "mvlgamma",
    "nan_to_num",
    "nan_to_num_",
    "nanmean",
    "nanmedian",
    "nanquantile",
    "nansum",
    "narrow",
    "narrow_copy",
    "native_alpha_dropout",
    "native_batch_norm",
    "native_batch_norm_backward",
    "native_channel_shuffle",
    "native_dropout",
    "native_dropout_backward",
    "native_feature_dropout",
    "native_group_norm",
    "native_group_norm_backward",
    "native_layer_norm",
    "native_layer_norm_backward",
    "native_norm",
    "ndtr",
    "ndtri",
    "ne",
    "neg",
    "neg_",
    "negative",
    "negative_",
    "nested_to_padded_tensor",
    "nextafter",
    "nextafter_",
    "nll_loss",
    "nll_loss2d",
    "nll_loss2d_backward",
    "nll_loss2d_forward",
    "nll_loss_backward",
    "nll_loss_forward",
    "nll_loss_nd",
    "nonzero",
    "nonzero_numpy",
    "nonzero_static",
    "norm",
    "norm_except_dim",
    "normal",
    "normal_",
    "normal_functional",
    "not_equal",
    "nuclear_norm",
    "one_hot",
    "ones",
    "ones_like",
    "orgqr",
    "ormqr",
    "outer",
    "pad",
    "pad_sequence",
    "pairwise_distance",
    "pdist",
    "permute",
    "permute_backward",
    "permute_copy",
    "pinverse",
    "pixel_shuffle",
    "pixel_unshuffle",
    "poisson",
    "poisson_nll_loss",
    "polar",
    "polygamma",
    "positive",
    "pow",
    "prelu",
    "prod",
    "promote_types",
    "put",
    "q_per_channel_axis",
    "q_per_channel_scales",
    "q_per_channel_zero_points",
    "q_scale",
    "q_zero_point",
    "quantile",
    "quantize_per_channel",
    "quantize_per_tensor",
    "quantize_per_tensor_dynamic",
    "quantized_batch_norm",
    "quantized_gru_cell",
    "quantized_linear",
    "quantized_lstm_cell",
    "quantized_max_pool1d",
    "quantized_max_pool2d",
    "quantized_max_pool3d",
    "quantized_rnn_relu_cell",
    "quantized_rnn_tanh_cell",
    "rad2deg",
    "rad2deg_",
    "rand",
    "rand_like",
    "randint",
    "randint_like",
    "randn",
    "randn_like",
    "random_",
    "randperm",
    "range",
    "ravel",
    "real",
    "reciprocal",
    "reciprocal_",
    "reflection_pad1d",
    "reflection_pad1d_backward",
    "reflection_pad2d",
    "reflection_pad2d_backward",
    "reflection_pad3d",
    "reflection_pad3d_backward",
    "reflection_pad_nd",
    "reflection_pad_nd_backward",
    "relu",
    "relu6",
    "relu6_",
    "relu_",
    "remainder",
    "renorm",
    "renorm_",
    "repeat",
    "repeat_interleave",
    "replication_pad1d",
    "replication_pad1d_backward",
    "replication_pad2d",
    "replication_pad2d_backward",
    "replication_pad3d",
    "replication_pad3d_backward",
    "replication_pad_nd",
    "replication_pad_nd_backward",
    "reshape",
    "reshape_as",
    "resize_",
    "resize_as_",
    "resize_as_sparse_",
    "resolve_conj",
    "resolve_neg",
    "result_type",
    "rms_norm",
    "rnn_relu",
    "rnn_relu_cell",
    "rnn_tanh",
    "rnn_tanh_cell",
    "roll",
    "rot90",
    "rotary_embedding",
    "round",
    "round_",
    "row_indices_copy",
    "row_stack",
    "rrelu",
    "rrelu_",
    "rrelu_with_noise",
    "rrelu_with_noise_",
    "rrelu_with_noise_backward",
    "rsqrt",
    "rsqrt_",
    "rsub",
    "sample",
    "scalar_tensor",
    "scaled_dot_product_attention",
    "scaled_dot_product_attention_backward",
    "scaled_modified_bessel_k0",
    "scaled_modified_bessel_k1",
    "scatter",
    "scatter_",
    "scatter_add",
    "scatter_add_",
    "scatter_reduce",
    "searchsorted",
    "segment_reduce",
    "select",
    "select_backward",
    "select_copy",
    "select_scatter",
    "selu",
    "selu_",
    "sgn",
    "sgn_",
    "shifted_chebyshev_polynomial_t",
    "shifted_chebyshev_polynomial_u",
    "shifted_chebyshev_polynomial_v",
    "shifted_chebyshev_polynomial_w",
    "sigmoid",
    "sigmoid_",
    "sigmoid_backward",
    "sign",
    "sign_",
    "signbit",
    "silu",
    "silu_",
    "silu_and_mul",
    "silu_backward",
    "silu_mul",
    "sin",
    "sin_",
    "sinc",
    "sinc_",
    "sinh",
    "sinh_",
    "size",
    "slice",
    "slice_backward",
    "slice_copy",
    "slice_inverse",
    "slice_scatter",
    "slogdet",
    "slow_conv3d",
    "slow_conv3d_forward",
    "slow_conv_dilated2d",
    "slow_conv_dilated3d",
    "slow_conv_transpose2d",
    "slow_conv_transpose3d",
    "smm",
    "smooth_l1_loss",
    "smooth_l1_loss_backward",
    "soft_margin_loss",
    "soft_margin_loss_backward",
    "softmax",
    "softplus",
    "softplus_backward",
    "softshrink",
    "softshrink_backward",
    "sort",
    "sparse_add",
    "sparse_bsc_tensor",
    "sparse_bsr_tensor",
    "sparse_compressed_tensor",
    "sparse_coo_tensor",
    "sparse_csc_tensor",
    "sparse_csr_tensor",
    "sparse_mm",
    "sparse_mul",
    "sparse_sampled_addmm",
    "sparse_sum",
    "spdiags",
    "special_airy_ai",
    "special_bessel_j0",
    "special_bessel_j1",
    "special_bessel_y0",
    "special_bessel_y1",
    "special_chebyshev_polynomial_t",
    "special_chebyshev_polynomial_u",
    "special_chebyshev_polynomial_v",
    "special_chebyshev_polynomial_w",
    "special_digamma",
    "special_entr",
    "special_erf",
    "special_erfc",
    "special_erfcx",
    "special_erfinv",
    "special_exp2",
    "special_expit",
    "special_expm1",
    "special_gammainc",
    "special_gammaincc",
    "special_gammaln",
    "special_hermite_polynomial_h",
    "special_hermite_polynomial_he",
    "special_i0",
    "special_i0e",
    "special_i1",
    "special_i1e",
    "special_laguerre_polynomial_l",
    "special_legendre_polynomial_p",
    "special_log1p",
    "special_log_ndtr",
    "special_log_softmax",
    "special_logit",
    "special_logsumexp",
    "special_modified_bessel_i0",
    "special_modified_bessel_i1",
    "special_modified_bessel_k0",
    "special_modified_bessel_k1",
    "special_multigammaln",
    "special_ndtr",
    "special_ndtri",
    "special_polygamma",
    "special_psi",
    "special_round",
    "special_scaled_modified_bessel_k0",
    "special_scaled_modified_bessel_k1",
    "special_shifted_chebyshev_polynomial_t",
    "special_shifted_chebyshev_polynomial_u",
    "special_shifted_chebyshev_polynomial_v",
    "special_shifted_chebyshev_polynomial_w",
    "special_sinc",
    "special_softmax",
    "special_spherical_bessel_j0",
    "special_xlog1py",
    "special_xlogy",
    "special_zeta",
    "spherical_bessel_j0",
    "split",
    "split_copy",
    "split_with_sizes",
    "split_with_sizes_copy",
    "sqrt",
    "sqrt_",
    "square",
    "square_",
    "squeeze",
    "squeeze_backward",
    "squeeze_copy",
    "sspaddmm",
    "stack",
    "std",
    "std_mean",
    "stft",
    "stft_backward",
    "stride",
    "sub",
    "sub_",
    "sum",
    "svd",
    "swapaxes",
    "swapdims",
    "sym_constrain_range",
    "sym_constrain_range_for_size",
    "sym_is_contiguous",
    "sym_numel",
    "sym_size",
    "sym_storage_offset",
    "sym_stride",
    "t",
    "t_copy",
    "take",
    "take_along_dim",
    "tan",
    "tan_",
    "tanh",
    "tanh_",
    "tanh_backward",
    "tensor_split",
    "tensordot",
    "thnn_conv2d",
    "threshold",
    "threshold_",
    "threshold_backward",
    "tile",
    "to_dense",
    "to_dense_backward",
    "to_mkldnn_backward",
    "to_sparse",
    "to_sparse_csr",
    "topk",
    "tp_cosine_embedding_loss",
    "tp_cosine_embedding_loss_backward",
    "tp_hinge_embedding_loss",
    "tp_hinge_embedding_loss_backward",
    "tp_kl_div",
    "tp_kl_div_backward",
    "tp_l1_loss",
    "tp_l1_loss_backward",
    "tp_margin_ranking_loss",
    "tp_margin_ranking_loss_backward",
    "tp_poisson_nll_loss",
    "tp_poisson_nll_loss_backward",
    "tp_soft_margin_loss",
    "tp_soft_margin_loss_backward",
    "trace",
    "trace_backward",
    "transpose",
    "transpose_copy",
    "trapezoid",
    "trapz",
    "triangular_solve",
    "tril",
    "tril_",
    "tril_indices",
    "triplet_margin_loss",
    "triu",
    "triu_",
    "triu_indices",
    "trunc",
    "trunc_",
    "unbind",
    "unbind_copy",
    "unflatten",
    "unflatten_dense_tensors",
    "unfold",
    "unfold_backward",
    "unfold_copy",
    "uniform_",
    "unique",
    "unique_consecutive",
    "unique_dim",
    "unique_dim_consecutive",
    "unsafe_chunk",
    "unsafe_split",
    "unsafe_split_with_sizes",
    "unsqueeze",
    "unsqueeze_copy",
    "upsample_bicubic2d",
    "upsample_bicubic2d_backward",
    "upsample_bilinear2d",
    "upsample_bilinear2d_backward",
    "upsample_linear1d",
    "upsample_linear1d_backward",
    "upsample_nearest1d",
    "upsample_nearest1d_backward",
    "upsample_nearest2d",
    "upsample_nearest2d_backward",
    "upsample_nearest3d",
    "upsample_nearest3d_backward",
    "upsample_trilinear3d",
    "upsample_trilinear3d_backward",
    "value_selecting_reduction_backward",
    "values_copy",
    "vander",
    "var",
    "var_mean",
    "vdot",
    "view",
    "view_as_complex",
    "view_as_complex_copy",
    "view_as_real",
    "view_as_real_copy",
    "view_copy",
    "vsplit",
    "vstack",
    "where",
    "xlog1py",
    "xlog1py_",
    "xlogy",
    "xlogy_",
    "zero_",
    "zeros",
    "zeros_like",
    "zeta",
]
