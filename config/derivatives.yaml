- name: permute(Tensor self, int[] dims) -> Tensor
  self: tensorplay::tpx::ops::permute_backward(grad, self, dims)

- name: squeeze(Tensor self) -> Tensor
  self: tensorplay::tpx::ops::squeeze_backward(grad, self)

- name: squeeze.dim(Tensor self, int dim) -> Tensor
  self: unsqueeze_to(grad, {dim}, self.shape())

- name: squeeze.dims(Tensor self, int[] dim) -> Tensor
  self: unsqueeze_to(grad, dim, self.shape())

- name: unsqueeze(Tensor self, int dim) -> Tensor
  self: grad.squeeze(dim)

- name: transpose(Tensor self, int dim0, int dim1) -> Tensor
  self: grad.transpose(dim0, dim1)

- name: t(Tensor self) -> Tensor
  self: grad.t()

# The complex optimizer path uses the same storage-reinterpretation views as
# Torch.  Their backward operations are the inverse view and must remain
# views, so higher-order optimizer differentiation does not insert a copy.
- name: view_as_real(Tensor self) -> Tensor
  self: tensorplay::tpx::ops::view_as_complex(grad)

- name: view_as_complex(Tensor self) -> Tensor
  self: tensorplay::tpx::ops::view_as_real(grad)

# Fused dropout (torch's native_dropout): the mask output is saved by the
# backward node and the scale folds in here. F.dropout rejects p == 1
# upstream, so 1 - p is never zero.
- name: native_dropout(Tensor input, float p) -> (Tensor output, Tensor mask)
  input: native_dropout_backward(grad, mask, 1.0 / (1.0 - p))

# Alpha dropout keeps the kept-elements slope at `a`; the affine offset b
# contributes no gradient. a = 1/sqrt((alpha^2 p + 1)(1 - p)) is recomputed
# in the backward kernel from the scalar p.
- name: native_alpha_dropout(Tensor input, float p) -> (Tensor output, Tensor mask)
  input: _alpha_dropout_backward(grad, mask, p)

- name: native_feature_dropout(Tensor input, float p) -> (Tensor output, Tensor mask)
  input: _feature_dropout_backward(grad, mask, p)

- name: add.Tensor(Tensor self, Tensor other, *, Scalar alpha=1) -> Tensor
  self: grad
  other: maybe_multiply(grad, alpha)

# In-place arithmetic is used by torch.optim's differentiable single-tensor
# implementations.  Keep the same backward formulas as the functional
# overloads; the generated wrapper attaches the new history to the mutated
# non-leaf tensor and rejects leaf mutation under GradMode.
- name: add_.Tensor(Tensor(a!) self, Tensor other, *, Scalar alpha=1) -> Tensor(a!)
  self: grad
  other: maybe_multiply(grad, alpha)

- name: add_.Scalar(Tensor(a!) self, Scalar other, Scalar alpha=1) -> Tensor(a!)
  self: grad

# TorchInductor keeps residual add+ReLU as one training forward node and
# materializes the ReLU mask from the output in its backward graph.  The
# TensorPlay native primitive has the same output contract for the equal-shape
# residual case used by ResNet.
- name: add_relu(Tensor self, Tensor other) -> Tensor
  self: threshold_backward(grad, result, 0)
  other: threshold_backward(grad, result, 0)

- name: sub.Tensor(Tensor self, Tensor other, *, Scalar alpha=1) -> Tensor
  self: grad
  other: maybe_multiply(grad.neg(), alpha)

- name: sub_.Tensor(Tensor(a!) self, Tensor other, *, Scalar alpha=1) -> Tensor(a!)
  self: grad
  other: maybe_multiply(grad.neg(), alpha)

- name: sub_.Scalar(Tensor(a!) self, Scalar other, Scalar alpha=1) -> Tensor(a!)
  self: grad

- name: mul.Tensor(Tensor self, Tensor other) -> Tensor
  self: mul_tensor_backward(grad, other, self.dtype())
  other: mul_tensor_backward(grad, self, other.dtype())

- name: mul_.Tensor(Tensor(a!) self, Tensor other) -> Tensor(a!)
  self: grad * ops::conj(other)
  other: grad * ops::conj(self)

- name: mul_.Scalar(Tensor(a!) self, Scalar other) -> Tensor(a!)
  self: grad * scalar_conj_if_complex(other)

- name: div.Tensor(Tensor self, Tensor other) -> Tensor
  self: div_tensor_self_backward(grad, other, self.dtype())
  other: div_tensor_other_backward(grad, self, other)

- name: div_.Tensor(Tensor(a!) self, Tensor other) -> Tensor(a!)
  self: grad / ops::conj(other)
  other: -(grad * self) * ops::conj(self / other / other)

- name: div_.Scalar(Tensor(a!) self, Scalar other) -> Tensor(a!)
  self: grad / scalar_conj_if_complex(other)

- name: addcmul(Tensor self, Tensor tensor1, Tensor tensor2, *, Scalar value=1) -> Tensor
  self: grad
  tensor1: grad * tensor2 * value
  tensor2: grad * tensor1 * value

- name: addcmul_(Tensor(a!) self, Tensor tensor1, Tensor tensor2, *, Scalar value=1) -> Tensor(a!)
  self: grad
  tensor1: grad * tensor2 * value
  tensor2: grad * tensor1 * value

- name: addcdiv(Tensor self, Tensor tensor1, Tensor tensor2, *, Scalar value=1) -> Tensor
  self: grad
  tensor1: grad / tensor2 * value
  tensor2: -grad * tensor1 / (tensor2 * tensor2) * value

- name: addcdiv_(Tensor(a!) self, Tensor tensor1, Tensor tensor2, *, Scalar value=1) -> Tensor(a!)
  self: grad
  tensor1: grad / tensor2 * value
  tensor2: -grad * tensor1 / (tensor2 * tensor2) * value

- name: where.self(Tensor condition, Tensor self, Tensor other) -> Tensor
  self: where(condition, grad, zeros_like(self))
  other: where(condition, zeros_like(other), grad)

- name: where.ScalarSelf(Tensor condition, Scalar self, Tensor other) -> Tensor
  other: where(condition, zeros_like(other), grad)

- name: where.ScalarOther(Tensor condition, Tensor self, Scalar other) -> Tensor
  self: where(condition, grad, zeros_like(self))

- name: maximum(Tensor self, Tensor other) -> Tensor
  self: where(eq(self, other), grad / 2, where(gt(self, other), grad, zeros_like(self)))
  other: where(eq(self, other), grad / 2, where(lt(self, other), grad, zeros_like(other)))

- name: minimum(Tensor self, Tensor other) -> Tensor
  self: where(eq(self, other), grad / 2, where(lt(self, other), grad, zeros_like(self)))
  other: where(eq(self, other), grad / 2, where(gt(self, other), grad, zeros_like(other)))

- name: fused_mul_add(Tensor self, Tensor other, Tensor addend) -> Tensor
  self: grad * other
  other: grad * self
  addend: grad

- name: fused_mul_add.Scalar(Tensor self, Scalar other, Scalar addend) -> Tensor
  self: grad * other

- name: neg(Tensor self) -> Tensor
  self: grad.neg()

- name: add.Scalar(Tensor self, Scalar other, Scalar alpha=1) -> Tensor
  self: grad

- name: clamp(Tensor self, Scalar? min=None, Scalar? max=None) -> Tensor
  self: clamp_backward(grad, self, min, max)

# Verbatim upstream formulas (tools/autograd/derivatives.yaml).
- name: clamp_min(Tensor self, Scalar min) -> Tensor
  self: where(self > min, grad, zeros_like(self))

- name: clamp_max(Tensor self, Scalar max) -> Tensor
  self: where(self < max, grad, zeros_like(self))

- name: clamp_min_(Tensor(a!) self, Scalar min) -> Tensor(a!)
  self: where(self > min, grad, zeros_like(self))

- name: clamp_max_(Tensor(a!) self, Scalar max) -> Tensor(a!)
  self: where(self < max, grad, zeros_like(self))

- name: sub.Scalar(Tensor self, Scalar other, Scalar alpha=1) -> Tensor
  self: grad

- name: mul.Scalar(Tensor self, Scalar other) -> Tensor
  self: grad * scalar_conj_if_complex(other)

- name: div.Scalar(Tensor self, Scalar other) -> Tensor
  self: grad / scalar_conj_if_complex(other)

- name: embedding(Tensor weight, Tensor indices, int padding_idx=-1, bool scale_grad_by_freq=false, bool sparse=false) -> Tensor
  weight: tensorplay::tpx::ops::embedding_dense_backward(grad, indices, weight.size(0), padding_idx, scale_grad_by_freq)

# Upstream torch uses `grad.reshape_symint(self.sym_sizes())`
# (tools/autograd/derivatives.yaml): reshape falls back to a copy when the
# incoming grad is not viewable, so non-contiguous grads never error here.
- name: view(Tensor self, int[] shape) -> Tensor
  self: grad.reshape(self.shape())

- name: clone(Tensor self, *, MemoryFormat? memory_format=None) -> Tensor
  self: grad

- name: contiguous(Tensor self, *, MemoryFormat memory_format=Contiguous) -> Tensor
  self: grad

- name: select(Tensor self, int dim, int index) -> Tensor
  self: tensorplay::tpx::ops::select_backward(grad, self, dim, index)

- name: slice(Tensor self, int dim=0, int? start=None, int? end=None, int step=1) -> Tensor
  self: tensorplay::tpx::ops::slice_backward(grad, self, dim, start, end, step)

# Upstream narrow is CompositeImplicitAutograd == slice(dim, start, start+length);
# our kernel materializes the slice view, backward reuses the slice formula.
- name: narrow(Tensor self, int dim, int start, int length) -> Tensor
  self: tensorplay::tpx::ops::slice_backward(grad, self, dim, start, start + length, 1)

# ATen unfold_backward (UnfoldBackward.cpp): zeros(input_sizes) with the
# windows scattered back onto `dim`, accumulating where step < size overlaps.
- name: unfold(Tensor self, int dimension, int size, int step) -> Tensor
  self: unfold_backward(grad, self.shape(), dimension, size, step)

# Upstream unflatten is CompositeImplicitAutograd (a view); backward is the
# inverse reshape, recorded through reshape's own derivative.
- name: unflatten.int(Tensor self, int dim, int[] sizes) -> Tensor
  self: grad.reshape(self.shape())

# matmul's backward branches on the input dims (dot / vec@mat / mat@vec /
# batched), which the formula DSL cannot express.  The hand-written node in
# tpx/include/ManualNodes.h (MatmulBackward, registered via
# MANUAL_DERIVATIVES) composes recordable primitives so create_graph /
# double-backward sees a graph through `@` -- mirroring upstream, whose
# matmul_backward native calls dispatched at:: ops.

- name: mm(Tensor self, Tensor mat2) -> Tensor
  self: grad.mm(mat2.t())
  mat2: self.t().mm(grad)

# Native F.linear (CPU kernel; other backends fall through to the composite).
# Per-slot compositions keep double-backward alive, mirroring upstream's
# linear_backward contract (tools/autograd/derivatives.yaml:2388-2391).
- name: linear(Tensor input, Tensor weight, Tensor? bias=None) -> Tensor
  input: linear_backward_input(grad, input, weight)
  weight: linear_backward_weight(grad, input, weight)
  bias: linear_backward_bias(grad)

- name: addmm(Tensor self, Tensor mat1, Tensor mat2, *, Scalar beta=1, Scalar alpha=1) -> Tensor
  self: maybe_multiply(grad, beta)
  mat1: maybe_multiply(grad.mm(mat2.t()), alpha)
  mat2: maybe_multiply(mat1.t().mm(grad), alpha)

- name: bmm(Tensor self, Tensor mat2) -> Tensor
  self: grad.matmul(mat2.transpose(-2, -1))
  mat2: self.transpose(-2, -1).matmul(grad)

- name: baddbmm(Tensor self, Tensor batch1, Tensor batch2, *, Scalar beta=1, Scalar alpha=1) -> Tensor
  self: maybe_multiply(grad, beta)
  batch1: maybe_multiply(grad.matmul(batch2.transpose(-2, -1)), alpha)
  batch2: maybe_multiply(batch1.transpose(-2, -1).matmul(grad), alpha)

- name: mv(Tensor self, Tensor vec) -> Tensor
  self: grad.outer(vec)
  vec: self.t().mv(grad)

- name: dot(Tensor self, Tensor tensor) -> Tensor
  self: grad * tensor
  tensor: grad * self

- name: outer(Tensor self, Tensor vec2) -> Tensor
  self: grad.mv(vec2)
  vec2: grad.t().mv(self)

- name: inner(Tensor self, Tensor other) -> Tensor
  self: inner_backward_self(grad, self, other)
  other: inner_backward_other(grad, self, other)

- name: movedim(Tensor self, int[] source, int[] destination) -> Tensor
  self: movedim(grad, destination, source)

- name: diagonal(Tensor self, int offset=0, int dim1=0, int dim2=1) -> Tensor
  self: diagonal_backward(grad, self.shape(), offset, dim1, dim2)

- name: sin(Tensor self) -> Tensor
  self: grad * self.cos().conj()

- name: cos(Tensor self) -> Tensor
  self: grad * -self.sin().conj()

- name: abs(Tensor self) -> Tensor
  # |z| maps C->R; conj(conj(z)/|z|) == z/|z| == sgn(z), so the plain sgn form
  # already carries the conjugated Jacobian.
  self: grad * self.sign()

# Rounding ops are piecewise-constant: zero gradient, but the entry keeps the
# graph connected so consumers (e.g. grid_sample nearest d/dgrid) still get a
# defined grad instead of silently detaching.
- name: floor(Tensor self) -> Tensor
  self: zeros_like(grad)

- name: ceil(Tensor self) -> Tensor
  self: zeros_like(grad)

- name: round(Tensor self) -> Tensor
  self: zeros_like(grad)

- name: trunc(Tensor self) -> Tensor
  self: zeros_like(grad)


- name: pow.Tensor_Scalar(Tensor self, Scalar exponent) -> Tensor
  self: pow_backward(grad, self, exponent)

- name: exp(Tensor self) -> Tensor
  self: grad * result.conj()

- name: log(Tensor self) -> Tensor
  self: grad / self.conj()

- name: square(Tensor self) -> Tensor
  # analytic J = 2*self; torch convention stores grad * conj(J)
  self: grad * 2 * self.conj()

- name: sqrt(Tensor self) -> Tensor
  self: grad / (2 * result.conj())

- name: rsqrt(Tensor self) -> Tensor
  self: -0.5 * grad * result.pow(3).conj()

- name: sum(Tensor self, *, ScalarType dtype=Undefined) -> Tensor
  # zeros_like + broadcast add == grad expanded to self.shape(); written this
  # way to avoid the ambiguous expand overloads in the generated header.
  self: zeros_like(self) + grad

- name: mean(Tensor self, *, ScalarType dtype=Undefined) -> Tensor
  # grad.expand(...) / N is equivalent; scalar div keeps broadcasting and
  # avoids the ambiguous expand overloads in the generated autograd header.
  self: grad / self.numel()

- name: mean.dim(Tensor self, int[] dim, bool keepdim=false, *, ScalarType dtype=Undefined) -> Tensor
  # Recordable composition (unsqueeze reduced dims back in + scale by the
  # kept-element count); the former opaque mean_dim_backward kernel broke
  # double-backward the same way matmul's did.
  self: tensorplay::tpx::broadcast_mean_backward(grad, self, dim, keepdim)

# torch routes these through value_selecting_reduction_backward (an opaque
# native that composes dispatched ops).  Same structure here: a recordable
# tpx-level helper so create_graph sees through max/min(dim).
- name: max.dim(Tensor self, int64_t dim, bool keepdim=False) -> (Tensor values, Tensor indices)
  self: tensorplay::tpx::value_selecting_reduction_backward(grad, dim, indices, self, keepdim)

- name: min.dim(Tensor self, int64_t dim, bool keepdim=False) -> (Tensor values, Tensor indices)
  self: tensorplay::tpx::value_selecting_reduction_backward(grad, dim, indices, self, keepdim)

- name: sum.dim_IntList(Tensor self, int[] dim, bool keepdim=false, *, ScalarType dtype=Undefined) -> Tensor
  self: _sum_dim_backward(grad, self, dim, keepdim)

- name: reshape(Tensor self, int[] shape) -> Tensor
  self: grad.reshape(self.shape())

- name: log_softmax(Tensor self, int dim, ScalarType dtype=Undefined) -> Tensor
  self: grad - self.log_softmax(dim, dtype).exp() * grad.sum({dim}, true)

- name: softmax(Tensor self, int dim, ScalarType dtype=Undefined) -> Tensor
  self: (grad - (grad * result).sum({dim}, true)) * result

- name: nll_loss(Tensor self, Tensor target, Tensor? weight=None, int reduction=1, int ignore_index=-100) -> (Tensor output, Tensor total_weight)
  self: nll_loss_backward(grad, self, target, weight, reduction, ignore_index, total_weight)

- name: nll_loss2d(Tensor self, Tensor target, Tensor? weight=None, int reduction=1, int ignore_index=-100) -> (Tensor output, Tensor total_weight)
  self: nll_loss2d_backward(grad, self, target, weight, reduction, ignore_index, total_weight)

# ATen derivatives.yaml:2099-2105: multi_margin_loss differentiates through
# its native backward (target is class indices, non-differentiable);
# multilabel_margin_loss is composite over multilabel_margin_loss_forward,
# whose backward consumes the saved is_target mask.
- name: multi_margin_loss(Tensor self, Tensor target, Scalar p=1, Scalar margin=1, Tensor? weight=None, int reduction=1) -> Tensor
  self: multi_margin_loss_backward(grad, self, target, p, margin, weight, reduction)

- name: multilabel_margin_loss_forward(Tensor self, Tensor target, int reduction) -> (Tensor output, Tensor is_target)
  self: multilabel_margin_loss_backward(grad, self, target, reduction, is_target)

# CTC: grad flows only to log_probs; the backward consumes the saved forward
# outputs (raw per-sequence NLL keeps +inf for impossible alignments so the
# kernel's zero_infinity gating works) and applies aten LossCTC.cpp eq(16).
- name: _ctc_loss(Tensor log_probs, Tensor targets, Tensor input_lengths, Tensor target_lengths, int blank=0, bool zero_infinity=False) -> (Tensor neg_log_likelihood, Tensor log_alpha)
  log_probs: _ctc_loss_backward(grad, log_probs, targets, input_lengths, target_lengths, neg_log_likelihood, log_alpha, blank, zero_infinity)

- name: mse_loss(Tensor self, Tensor target, int reduction=1) -> Tensor
  self: mse_loss_backward(grad, self, target, reduction)

- name: relu(Tensor self) -> Tensor
  self: threshold_backward(grad, result, 0)

# max_pool2d / max_pool3d are Composite over *_with_indices (see
# native_functions.yaml); autograd records the with_indices node and uses its
# indices-scatter derivative below, so no recompute formula is needed here.
- name: max_pool2d_with_indices(Tensor input, int[] kernel_size, int[] stride=[], int[] padding=[], int[] dilation=[], bool ceil_mode=false) -> (Tensor output, Tensor indices)
  input: max_pool2d_with_indices_backward(grad, input, kernel_size, stride, padding, dilation, ceil_mode, indices)

- name: max_pool3d_with_indices(Tensor input, int[] kernel_size, int[] stride=[], int[] padding=[], int[] dilation=[], bool ceil_mode=false) -> (Tensor output, Tensor indices)
  input: max_pool3d_with_indices_backward(grad, input, kernel_size, stride, padding, dilation, ceil_mode, indices)

- name: avg_pool2d(Tensor input, int[] kernel_size, int[] stride=[], int[] padding=[], bool ceil_mode=false, bool count_include_pad=true, int? divisor_override=None) -> Tensor
  input: avg_pool2d_backward(grad, input, kernel_size, stride, padding, ceil_mode, count_include_pad, divisor_override)

- name: avg_pool3d(Tensor input, int[] kernel_size, int[] stride=[], int[] padding=[], bool ceil_mode=false, bool count_include_pad=true, int? divisor_override=None) -> Tensor
  input: avg_pool3d_backward(grad, input, kernel_size, stride, padding, ceil_mode, count_include_pad, divisor_override)

- name: adaptive_avg_pool3d(Tensor input, int[] output_size) -> Tensor
  input: adaptive_avg_pool3d_backward(grad, input)

- name: adaptive_avg_pool2d(Tensor input, int[] output_size) -> Tensor
  input: adaptive_avg_pool2d_backward(grad, input)

# ATen wires adaptive_max_pool2d through the (values, indices) op so the
# backward scatters onto the saved indices instead of recomputing the argmax.
- name: adaptive_max_pool2d_with_indices(Tensor input, int[] output_size) -> (Tensor output, Tensor indices)
  input: adaptive_max_pool2d_with_indices_backward(grad, input, output_size, indices)

- name: adaptive_max_pool3d(Tensor input, int[] output_size) -> Tensor
  input: adaptive_max_pool3d_backward(grad, input)

# ATen derivatives.yaml: fractional max pool scatters grad onto the recorded
# argmax positions; kernel_size is unused by the backward.
- name: fractional_max_pool2d(Tensor self, int[] kernel_size, int[] output_size, Tensor random_samples) -> (Tensor output, Tensor indices)
  self: fractional_max_pool2d_backward(grad, self, kernel_size, output_size, indices)

- name: fractional_max_pool3d(Tensor self, int[] kernel_size, int[] output_size, Tensor random_samples) -> (Tensor output, Tensor indices)
  self: fractional_max_pool3d_backward(grad, self, kernel_size, output_size, indices)

# ATen derivatives.yaml:2413-2421 differentiates max_unpool through
# max_pool_double_backward (a gather of grad at the recorded indices); the
# native backward kernels implement exactly that gather. indices is int64 and
# therefore not differentiable.
- name: max_unpool2d(Tensor self, Tensor indices, int[] output_size) -> Tensor
  self: max_unpool2d_backward(grad, indices, output_size)

- name: max_unpool3d(Tensor self, Tensor indices, int[] output_size, int[] stride, int[] padding) -> Tensor
  self: max_unpool3d_backward(grad, indices, output_size)

# ATen derivatives.yaml:770-780 wires grid_sampler_{2d,3d} through the
# corresponding *_backward with an output_mask selecting the needed result.
# The masked-out slot is returned as zeros and never consumed.
- name: grid_sampler_2d(Tensor input, Tensor grid, int interpolation_mode, int padding_mode, bool align_corners) -> Tensor
  input: std::get<0>(grid_sampler_2d_backward(grad, input, grid, interpolation_mode, padding_mode, align_corners, {true, false}))
  grid: std::get<1>(grid_sampler_2d_backward(grad, input, grid, interpolation_mode, padding_mode, align_corners, {false, true}))

- name: grid_sampler_3d(Tensor input, Tensor grid, int interpolation_mode, int padding_mode, bool align_corners) -> Tensor
  input: std::get<0>(grid_sampler_3d_backward(grad, input, grid, interpolation_mode, padding_mode, align_corners, {true, false}))
  grid: std::get<1>(grid_sampler_3d_backward(grad, input, grid, interpolation_mode, padding_mode, align_corners, {false, true}))

- name: batch_norm(Tensor input, Tensor? weight, Tensor? bias, Tensor? running_mean, Tensor? running_var, bool training, float momentum, float eps) -> Tensor
  input: std::get<0>(batch_norm_backward(grad, input, weight, running_mean, running_var, training, eps))
  weight: std::get<1>(batch_norm_backward(grad, input, weight, running_mean, running_var, training, eps))
  bias: std::get<2>(batch_norm_backward(grad, input, weight, running_mean, running_var, training, eps))

- name: layer_norm(Tensor input, int[] normalized_shape, Tensor? weight=None, Tensor? bias=None, float eps=1e-5) -> Tensor
  input: std::get<0>(layer_norm_backward(grad, input, normalized_shape, weight, bias, eps))
  weight: std::get<1>(layer_norm_backward(grad, input, normalized_shape, weight, bias, eps))
  bias: std::get<2>(layer_norm_backward(grad, input, normalized_shape, weight, bias, eps))

- name: group_norm(Tensor input, int num_groups, Tensor? weight=None, Tensor? bias=None, float eps=1e-5) -> Tensor
  input: std::get<0>(group_norm_backward(grad, input, num_groups, weight, bias, eps))
  weight: std::get<1>(group_norm_backward(grad, input, num_groups, weight, bias, eps))
  bias: std::get<2>(group_norm_backward(grad, input, num_groups, weight, bias, eps))

- name: instance_norm(Tensor input, Tensor? weight, Tensor? bias, Tensor? running_mean, Tensor? running_var, bool use_input_stats, float momentum, float eps) -> Tensor
  input: std::get<0>(instance_norm_backward(grad, input, weight, bias, running_mean, running_var, use_input_stats, eps))
  weight: std::get<1>(instance_norm_backward(grad, input, weight, bias, running_mean, running_var, use_input_stats, eps))
  bias: std::get<2>(instance_norm_backward(grad, input, weight, bias, running_mean, running_var, use_input_stats, eps))

- name: constant_pad_nd(Tensor self, int[] pad, Scalar value=0) -> Tensor
  self: constant_pad_nd_backward(grad, pad)

- name: conv2d(Tensor input, Tensor weight, Tensor? bias=None, int[] stride=[], int[] padding=[], int[] dilation=[], int groups=1) -> Tensor
  input: conv2d_grad_input(grad, input, weight, stride, padding, dilation, groups)
  weight: conv2d_grad_weight(grad, input, weight, stride, padding, dilation, groups)
  bias: conv2d_grad_bias(grad, input, weight, stride, padding, dilation, groups)

- name: conv1d(Tensor input, Tensor weight, Tensor? bias=None, int[] stride=[], int[] padding=[], int[] dilation=[], int groups=1) -> Tensor
  input: conv1d_grad_input(grad, input, weight, stride, padding, dilation, groups)
  weight: conv1d_grad_weight(grad, input, weight, stride, padding, dilation, groups)
  bias: conv1d_grad_bias(grad, input, weight, stride, padding, dilation, groups)

- name: conv3d(Tensor input, Tensor weight, Tensor? bias=None, int[] stride=[], int[] padding=[], int[] dilation=[], int groups=1) -> Tensor
  input: conv3d_grad_input(grad, input, weight, stride, padding, dilation, groups)
  weight: conv3d_grad_weight(grad, input, weight, stride, padding, dilation, groups)
  bias: conv3d_grad_bias(grad, input, weight, stride, padding, dilation, groups)

- name: conv_transpose2d(Tensor input, Tensor weight, Tensor? bias=None, int[] stride=[], int[] padding=[], int[] output_padding=[], int groups=1, int[] dilation=[]) -> Tensor
  input: conv_transpose2d_grad_input(grad, input, weight, stride, padding, output_padding, groups, dilation)
  weight: conv_transpose2d_grad_weight(grad, input, weight, stride, padding, output_padding, groups, dilation)
  bias: conv_transpose2d_grad_bias(grad, input, weight, stride, padding, output_padding, groups, dilation)

- name: conv_transpose3d(Tensor input, Tensor weight, Tensor? bias=None, int[] stride=[], int[] padding=[], int[] output_padding=[], int groups=1, int[] dilation=[]) -> Tensor
  input: conv_transpose3d_grad_input(grad, input, weight, stride, padding, output_padding, groups, dilation)
  weight: conv_transpose3d_grad_weight(grad, input, weight, stride, padding, output_padding, groups, dilation)
  bias: conv_transpose3d_grad_bias(grad, input, weight, stride, padding, output_padding, groups, dilation)

- name: conv_transpose1d(Tensor input, Tensor weight, Tensor? bias=None, int[] stride=[], int[] padding=[], int[] output_padding=[], int groups=1, int[] dilation=[]) -> Tensor
  input: conv_transpose1d_grad_input(grad, input, weight, stride, padding, output_padding, groups, dilation)
  weight: conv_transpose1d_grad_weight(grad, input, weight, stride, padding, output_padding, groups, dilation)
  bias: conv_transpose1d_grad_bias(grad, input, weight, stride, padding, output_padding, groups, dilation)

# ATen expresses the unfold (im2col) derivative through unfold_backward, which
# is the col2im kernel plus input-shape bookkeeping; same for col2im_backward.
- name: im2col(Tensor self, int[] kernel_size, int[] dilation, int[] padding, int[] stride) -> Tensor
  self: im2col_backward(grad, self.shape(), kernel_size, dilation, padding, stride)

- name: col2im(Tensor self, int[] output_size, int[] kernel_size, int[] dilation, int[] padding, int[] stride) -> Tensor
  self: col2im_backward(grad, self.shape(), output_size, kernel_size, dilation, padding, stride)

- name: reflection_pad_nd(Tensor self, int[] pad) -> Tensor
  self: reflection_pad_nd_backward(grad, self, pad)

- name: replication_pad_nd(Tensor self, int[] pad) -> Tensor
  self: replication_pad_nd_backward(grad, self, pad)

- name: circular_pad_nd(Tensor self, int[] pad) -> Tensor
  self: circular_pad_nd_backward(grad, self, pad)

- name: sigmoid(Tensor self) -> Tensor
  self: sigmoid_backward(grad, result)

- name: silu(Tensor self) -> Tensor
  self: silu_backward(grad, self)

- name: tanh(Tensor self) -> Tensor
  self: tanh_backward(grad, result)

- name: atan2(Tensor self, Tensor other) -> Tensor
  self: grad * other / (self * self + other * other)
  other: grad * -self / (self * self + other * other)

- name: lerp(Tensor self, Tensor end, Scalar weight) -> Tensor
  self: grad * (Scalar(1) - weight)
  end: grad * weight

- name: lerp.Tensor(Tensor self, Tensor end, Tensor weight) -> Tensor
  self: grad * (1 - weight)
  end: grad * weight
  weight: grad * (end - self)

- name: var(Tensor self, int correction=1) -> Tensor
  self: grad * 2 * (self - self.mean()) / (self.numel() - correction)

- name: std(Tensor self, int correction=1) -> Tensor
  self: grad * (self - self.mean()) / (result * (self.numel() - correction))

- name: scaled_dot_product_attention(Tensor query, Tensor key, Tensor value, bool is_causal=false, int impl=0) -> Tensor
  query: std::get<0>(scaled_dot_product_attention_backward(grad, query, key, value, is_causal, impl))
  key: std::get<1>(scaled_dot_product_attention_backward(grad, query, key, value, is_causal, impl))
  value: std::get<2>(scaled_dot_product_attention_backward(grad, query, key, value, is_causal, impl))

# ---------------------------------------------------------------------------
# Activations / upsample — derivative formulas mirror
# third_party/pytorch/tools/autograd/derivatives.yaml (SymInt -> int64_t,
# self.sym_sizes() -> self.shape()).
# ---------------------------------------------------------------------------

# ATen: - name: gelu(Tensor self, *, str approximate='none') -> Tensor
- name: gelu(Tensor self, str approximate="none") -> Tensor
  self: tensorplay::tpx::ops::gelu_backward(grad, self, approximate)

# ATen: hardtanh(Tensor self, Scalar min_val=-1, Scalar max_val=1)
- name: hardtanh(Tensor self, Scalar min_val=-1, Scalar max_val=1) -> Tensor
  self: hardtanh_backward(grad, self, min_val, max_val)

# ATen: relu6 is sugar for hardtanh(0, 6); its entry uses the same backward.
- name: relu6(Tensor self) -> Tensor
  self: hardtanh_backward(grad, self, 0, 6)

# ATen: - name: hardswish(Tensor self) -> Tensor
- name: hardswish(Tensor self) -> Tensor
  self: hardswish_backward(grad, self)

# ATen: - name: hardsigmoid(Tensor self) -> Tensor
- name: hardsigmoid(Tensor self) -> Tensor
  self: hardsigmoid_backward(grad, self)

# ATen: leaky_relu(Tensor self, Scalar negative_slope=0.01)
- name: leaky_relu(Tensor self, Scalar negative_slope=0.01) -> Tensor
  self: leaky_relu_backward(grad, self, negative_slope, false)

# ATen: rrelu_with_noise(Tensor self, Tensor noise, Scalar lower=0.125,
# Scalar upper=0.3333333333333333, bool training=False, bool inplace=False)
- name: rrelu_with_noise(Tensor self, Tensor noise, Scalar lower=0.125, Scalar upper=0.3333333333333333, bool training=False) -> Tensor
  self: rrelu_with_noise_backward(grad, self, noise, lower, upper, training, false)

# ATen: log_sigmoid backward is grad * sigmoid(-self); the native kernel keeps
# the numerically stable branch split (no exp overflow for large |self|).
- name: log_sigmoid(Tensor self) -> Tensor
  self: log_sigmoid_backward(grad, self)

# ATen: elu(Tensor self, Scalar alpha=1, Scalar scale=1, Scalar input_scale=1)
- name: elu(Tensor self, Scalar alpha=1, Scalar scale=1, Scalar input_scale=1) -> Tensor
  self: elu_backward(grad, alpha, scale, input_scale, false, self)

# ATen: celu routes through elu_backward with input_scale = 1/alpha.
- name: celu(Tensor self, Scalar alpha=1.0) -> Tensor
  self: elu_backward(grad, alpha, 1, 1.0/alpha, false, self)

# ATen: - name: mish(Tensor self) -> Tensor (GradMode branch handled by the
# generated node; TensorPlay always uses the finite kernel).
- name: mish(Tensor self) -> Tensor
  self: mish_backward(grad, self)

# ATen: selu is elu with fixed constants; torch's derivatives.yaml expresses it
# through elu_backward(scale=lambda, alpha=lambda*alpha_, input_scale=1).
- name: selu(Tensor self) -> Tensor
  self: elu_backward(grad, 1.6732632423543772848170429916717, 1.0507009873554804934193349852946, 1, false, self)

# ATen: - name: softplus(Tensor self, Scalar beta=1, Scalar threshold=20)
- name: softplus(Tensor self, Scalar beta=1, Scalar threshold=20) -> Tensor
  self: softplus_backward(grad, self, beta, threshold)

# ATen: - name: glu(Tensor self, int dim=-1) -> Tensor
- name: glu(Tensor self, int dim=-1) -> Tensor
  self: glu_backward(grad, self, dim)

# ATen: - name: upsample_nearest1d(Tensor self, SymInt[1] output_size, float? scales=None)
- name: upsample_nearest1d(Tensor self, int[] output_size, float? scales=None) -> Tensor
  self: upsample_nearest1d_backward(grad, output_size, self.shape(), scales)

- name: upsample_nearest2d(Tensor self, int[] output_size, float? scales_h=None, float? scales_w=None) -> Tensor
  self: upsample_nearest2d_backward(grad, output_size, self.shape(), scales_h, scales_w)

- name: upsample_nearest3d(Tensor self, int[] output_size, float? scales_d=None, float? scales_h=None, float? scales_w=None) -> Tensor
  self: upsample_nearest3d_backward(grad, output_size, self.shape(), scales_d, scales_h, scales_w)

- name: upsample_linear1d(Tensor self, int[] output_size, bool align_corners, float? scales=None) -> Tensor
  self: upsample_linear1d_backward(grad, output_size, self.shape(), align_corners, scales)

- name: upsample_bilinear2d(Tensor self, int[] output_size, bool align_corners, float? scales_h=None, float? scales_w=None) -> Tensor
  self: upsample_bilinear2d_backward(grad, output_size, self.shape(), align_corners, scales_h, scales_w)

- name: upsample_bicubic2d(Tensor self, int[] output_size, bool align_corners, float? scales_h=None, float? scales_w=None) -> Tensor
  self: upsample_bicubic2d_backward(grad, output_size, self.shape(), align_corners, scales_h, scales_w)

- name: upsample_trilinear3d(Tensor self, int[] output_size, bool align_corners, float? scales_d=None, float? scales_h=None, float? scales_w=None) -> Tensor
  self: upsample_trilinear3d_backward(grad, output_size, self.shape(), align_corners, scales_d, scales_h, scales_w)

# ATen expresses masked_fill's derivative as masked_fill(grad, mask, 0).
- name: masked_fill(Tensor self, Tensor mask, Scalar value) -> Tensor
  self: masked_fill(grad, mask, 0)

# ===========================================================================
# Tier-1 indexing/masking/scan autograd rules. Formulas follow
# third_party/pytorch/tools/autograd/derivatives.yaml (cited per entry),
# expressed with TensorPlay-native operators.
# ===========================================================================

# derivatives.yaml:1096 grad.masked_fill(mask, 0)
- name: masked_fill(Tensor self, Tensor mask, Scalar value) -> Tensor
  self: grad.masked_fill(mask, 0)

# derivatives.yaml:1096 (in-place shares the functional rule)
- name: masked_fill_(Tensor(a!) self, Tensor mask, Scalar value) -> Tensor(a!)
  self: grad.masked_fill(mask, 0)

# derivatives.yaml:1101 (value grad omitted: needs masked_fill_backward helper)
- name: masked_fill.Tensor(Tensor self, Tensor mask, Tensor value) -> Tensor
  self: grad.masked_fill(mask, 0)

# derivatives.yaml:1818/1822
- name: tril(Tensor self, int diagonal=0) -> Tensor
  self: grad.tril(diagonal)

- name: triu(Tensor self, int diagonal=0) -> Tensor
  self: grad.triu(diagonal)

# derivatives.yaml:531 + torch/csrc/autograd/FunctionsManual.cpp:1136
# cumsum_backward = grad.flip(dim).cumsum(dim).flip(dim)
- name: cumsum(Tensor self, int dim, ScalarType? dtype=None) -> Tensor
  self: tensorplay::tpx::ops::flip(tensorplay::tpx::ops::cumsum(tensorplay::tpx::ops::flip(grad, {dim}), dim), {dim})

# gather_backward composite (TensorAdvancedIndexing.cpp:2118):
# new_zeros(self.sizes()).scatter_add_(dim, index, grad)
- name: gather(Tensor self, int dim, Tensor index) -> Tensor
  self: zeros_like(self).scatter_add(dim, index, grad)

# derivatives.yaml:1529-1531
- name: scatter_add(Tensor self, int dim, Tensor index, Tensor src) -> Tensor
  self: grad
  src: grad.gather(dim, index)

# index_select_backward composite: zeros(input_sizes).index_add(dim, index, grad)
- name: index_select(Tensor self, int dim, Tensor index) -> Tensor
  self: zeros_like(self).index_add(dim, index, grad)

# derivatives.yaml:864+ self: grad; source: grad.index_select(dim, index)
- name: index_add(Tensor self, int dim, Tensor index, Tensor source) -> Tensor
  self: grad
  source: tensorplay::tpx::ops::index_select(grad, dim, index)

# ===========================================================================
# Tier 2-4 autograd rules. Formulas follow
# third_party/pytorch/tools/autograd/derivatives.yaml where expressible with
# TensorPlay-native operators; otherwise omitted (no entry = no autograd).
# ===========================================================================

# derivatives.yaml rsub: other - alpha*self
- name: rsub.Tensor(Tensor self, Tensor other, Scalar alpha=1) -> Tensor
  self: neg(grad) * alpha
  other: grad

- name: rsub.Scalar(Tensor self, Scalar other, Scalar alpha=1) -> Tensor
  self: neg(grad) * alpha

# BinaryOps.cpp true_divide shares div's rules.
- name: true_divide.Tensor(Tensor self, Tensor other) -> Tensor
  self: grad / other
  other: -(grad * result) / other

- name: divide.Tensor(Tensor self, Tensor other) -> Tensor
  self: div_tensor_self_backward(grad, other, self.dtype())
  other: div_tensor_other_backward(grad, self, other)

- name: multiply.Tensor(Tensor self, Tensor other) -> Tensor
  self: mul_tensor_backward(grad, other, self.dtype())
  other: mul_tensor_backward(grad, self, other.dtype())

- name: multiply.Scalar(Tensor self, Scalar other) -> Tensor
  self: grad * scalar_conj_if_complex(other)

- name: negative(Tensor self) -> Tensor
  self: -grad

- name: positive(Tensor self) -> Tensor
  self: grad

# BinaryOps.cpp fmod: d/dother = -grad * trunc(self/other)
- name: fmod.Tensor(Tensor self, Tensor other) -> Tensor
  self: grad
  other: -(trunc(self / other) * grad)

# UnaryOps.cpp reciprocal: -grad / self^2
- name: reciprocal(Tensor self) -> Tensor
  self: -grad * (result * result).conj()

- name: exp2(Tensor self) -> Tensor
  self: grad * (result.conj() * 0.6931471805599453)

- name: xlogy.Tensor(Tensor self, Tensor other) -> Tensor
  self: grad * log(other)
  other: self * grad / other

# BinaryOps.cpp logaddexp
- name: logaddexp(Tensor self, Tensor other) -> Tensor
  self: grad * sigmoid(self - other)
  other: grad * sigmoid(other - self)

- name: hypot(Tensor self, Tensor other) -> Tensor
  self: grad * self / result
  other: grad * other / result

- name: deg2rad(Tensor self) -> Tensor
  self: grad * 0.01745329251994329577

- name: rad2deg(Tensor self) -> Tensor
  self: grad * 57.29577951308232087680

# Activation.cpp hardshrink/softshrink: both route through ATen's shared
# shrink_backward_kernel ((self >= -lambd && self <= lambd) ? 0 : grad),
# exposed here as the native hardshrink_backward / softshrink_backward ops.
- name: hardshrink(Tensor self, Scalar lambd=0.5) -> Tensor
  self: hardshrink_backward(grad, self, lambd)

- name: softshrink(Tensor self, Scalar lambd=0.5) -> Tensor
  self: softshrink_backward(grad, self, lambd)

# Activation.cpp:525 selu / :541 celu
- name: selu(Tensor self) -> Tensor
  self: grad * (self > 0) * 1.0507009873554804934193349852946 + grad * (self <= 0) * (self * 1.0507009873554804934193349852946 * 1.6732632423543772848170429916717).exp()

- name: celu(Tensor self, Scalar alpha=1.0) -> Tensor
  self: grad * (self > 0) + grad * (self <= 0) * ((self / alpha).exp() * alpha / alpha)

# threshold_backward(grad, self, threshold), mirrors clamp_backward style
- name: threshold(Tensor self, Scalar threshold, Scalar value) -> Tensor
  self: threshold_backward(grad, self, threshold)

# clamp_min/clamp_max via comparisons (NaN caveat vs ATen helpers noted)
- name: clamp_min.Scalar(Tensor self, Scalar min) -> Tensor
  self: grad * (self >= min)

- name: clamp_max.Scalar(Tensor self, Scalar max) -> Tensor
  self: grad * (self <= max)

- name: clamp_min.Tensor(Tensor self, Tensor min) -> Tensor
  self: grad * (self >= min)

- name: clamp_max.Tensor(Tensor self, Tensor max) -> Tensor
  self: grad * (self <= max)

# TensorTransformations.cpp:36 flip is an involution.
- name: flip(Tensor self, int[] dims) -> Tensor
  self: grad.flip(dims)

# PixelShuffle.cpp pair are inverses of each other.
- name: pixel_shuffle(Tensor self, int upscale_factor) -> Tensor
  self: tensorplay::tpx::ops::pixel_unshuffle(grad, upscale_factor)

- name: pixel_unshuffle(Tensor self, int downscale_factor) -> Tensor
  self: tensorplay::tpx::ops::pixel_shuffle(grad, downscale_factor)

# ReduceOps.cpp:1578 logsumexp
- name: logsumexp(Tensor self, int dim, bool keepdim=false) -> Tensor
  self: grad * exp(self - unsqueeze(result, dim))

# ===========================================================================
# Tier 5 autograd rules (only formulas expressible with native ops; linalg
# decompositions and RNN cells have no derivative entries yet).
# ===========================================================================

# ComplexHelper: conj is its own inverse on the gradient.
- name: conj(Tensor self) -> Tensor
  self: grad.conj()

# adjoint is transpose(-2,-1) composed with conj; both are self-inverse on
# the gradient (torch: grad.adjoint()).
- name: adjoint(Tensor self) -> Tensor
  self: grad.adjoint()

# polar(r, theta) = r * exp(i*theta): dr = cos(theta)*grad; dtheta = -r*sin(theta)*grad
- name: polar(Tensor abs_, Tensor angle) -> Tensor
  abs_: grad * cos(angle)
  angle: -(abs_ * grad * sin(angle))

# ---------------------------------------------------------------------------
# Torch-aligned loss family derivatives.
# ---------------------------------------------------------------------------

- name: tp_l1_loss(Tensor input, Tensor target, int64_t reduction=1) -> Tensor
  input: tp_l1_loss_backward(grad, input, target, reduction)

- name: smooth_l1_loss(Tensor self, Tensor target, int reduction=1, float beta=1.0) -> Tensor
  self: smooth_l1_loss_backward(grad, self, target, reduction, beta)

- name: huber_loss(Tensor self, Tensor target, int reduction=1, float delta=1.0) -> Tensor
  self: huber_loss_backward(grad, self, target, reduction, delta)

- name: tp_kl_div(Tensor input, Tensor target, int64_t reduction=1, bool log_target=false) -> Tensor
  input: tp_kl_div_backward(grad, input, target, reduction, log_target)

- name: binary_cross_entropy(Tensor self, Tensor target, Tensor? weight=None, int reduction=1) -> Tensor
  self: binary_cross_entropy_backward(grad, self, target, weight, reduction)

- name: tp_margin_ranking_loss(Tensor input1, Tensor input2, Tensor target, float margin=0.0, int64_t reduction=1) -> Tensor
  input1: std::get<0>(tp_margin_ranking_loss_backward(grad, input1, input2, target, margin, reduction))
  input2: std::get<1>(tp_margin_ranking_loss_backward(grad, input1, input2, target, margin, reduction))

- name: tp_hinge_embedding_loss(Tensor input, Tensor target, float margin=1.0, int64_t reduction=1) -> Tensor
  input: tp_hinge_embedding_loss_backward(grad, input, target, margin, reduction)

- name: tp_soft_margin_loss(Tensor input, Tensor target, int64_t reduction=1) -> Tensor
  input: tp_soft_margin_loss_backward(grad, input, target, reduction)

- name: tp_cosine_embedding_loss(Tensor input1, Tensor input2, Tensor target, float margin=0.0, int64_t reduction=1) -> Tensor
  input1: std::get<0>(tp_cosine_embedding_loss_backward(grad, input1, input2, target, margin, reduction))
  input2: std::get<1>(tp_cosine_embedding_loss_backward(grad, input1, input2, target, margin, reduction))

- name: tp_poisson_nll_loss(Tensor input, Tensor target, bool log_input=true, bool full=false, float eps=1e-08, int64_t reduction=1) -> Tensor
  input: tp_poisson_nll_loss_backward(grad, input, target, log_input, full, eps, reduction)

# ===========================================================================
# Spectral / audio ops — adjoints per ATen conventions (see SpectralKernels)
# ===========================================================================
- name: fft_fft(Tensor self, SymInt n=-1, int dim=-1, str norm="backward") -> Tensor
  self: tensorplay::tpx::ops::fft_fft_backward(grad, self, dim, norm)

- name: fft_ifft(Tensor self, SymInt n=-1, int dim=-1, str norm="backward") -> Tensor
  self: tensorplay::tpx::ops::fft_ifft_backward(grad, self, dim, norm)

- name: fft_rfft(Tensor self, SymInt n=-1, int dim=-1, str norm="backward") -> Tensor
  self: tensorplay::tpx::ops::fft_rfft_backward(grad, self, dim, norm)

- name: fft_irfft(Tensor self, SymInt n=-1, int dim=-1, str norm="backward") -> Tensor
  self: tensorplay::tpx::ops::fft_irfft_backward(grad, self, dim, norm)

- name: stft(Tensor self, int n_fft, int? hop_length=None, int? win_length=None, Tensor? window=None, bool center=True, str pad_mode="reflect", bool normalized=False, bool onesided=True, bool return_complex=True) -> Tensor
  self: tensorplay::tpx::ops::stft_backward(grad, self, n_fft, hop_length, win_length, window, center, pad_mode, normalized, onesided)

- name: flip(Tensor self, int[] dims=[]) -> Tensor
  self: tensorplay::tpx::ops::flip(grad, dims)

# TensorTransformations.cpp rot90 is an involution up to sign: upstream
# differentiates as grad.rot90(-k, dims).
- name: rot90(Tensor self, int k=1, int[] dims=[0,1]) -> Tensor
  self: tensorplay::tpx::ops::rot90(grad, -k, dims)

- name: index_select(Tensor self, int dim, Tensor index) -> Tensor
  self: tensorplay::tpx::ops::index_add(tensorplay::tpx::ops::zeros_like(self), dim, index, grad)

- name: log10(Tensor self) -> Tensor
  self: grad / self * 0.43429448190325176

- name: log1p(Tensor self) -> Tensor
  self: log1p_backward(grad, self)

- name: frac(Tensor self) -> Tensor
  self: grad

- name: diff(Tensor self, int n=1, int dim=-1, Tensor? prepend=None, Tensor? append=None) -> Tensor
  self: tensorplay::tpx::ops::neg(grad)

- name: to_dense(Tensor self) -> Tensor
  self: grad.sparse_mask(self)

# --- 2026-08-24: torch 对齐补齐（silu 公式修正 + 双曲/反双曲/logit/expm1 新增）---
# 公式逐条对照 third_party/pytorch/tools/autograd/derivatives.yaml（去 complex 分支）。
# silu 旧式 `grad * self.sigmoid() * (self + 1)` 漏 (1 - sigmoid) 因子，
# 有限差分验证: x=0.49 处旧值 0.9239 vs 真值 0.7354。

- name: sinh(Tensor self) -> Tensor
  self: grad * self.cosh().conj()

# --- torch 对齐补齐(第 2 批):反三角/误差函数/lgamma/log2 ---
# 公式逐条对照 third_party/pytorch/tools/autograd/derivatives.yaml
# (去 .conj() 复数分支;M_2_SQRTPI/M_LN2 常量内联为数值)。

- name: acos(Tensor self) -> Tensor
  self: grad * -((-self * self + 1).rsqrt()).conj()

- name: asin(Tensor self) -> Tensor
  self: grad * (-self * self + 1).rsqrt().conj()

- name: atan(Tensor self) -> Tensor
  self: grad / (self * self + 1).conj()

- name: tan(Tensor self) -> Tensor
  self: grad * (1 + result.pow(2)).conj()

- name: erf(Tensor self) -> Tensor
  self: 1.1283791670205586 * exp(-(self.pow(2))) * grad

- name: erfc(Tensor self) -> Tensor
  self: -1.1283791670205586 * exp(-(self.pow(2))) * grad

- name: lgamma(Tensor self) -> Tensor
  self: grad * digamma(self)

- name: log2(Tensor self) -> Tensor
  self: grad / (self.conj() * 0.6931471805599453)

- name: cosh(Tensor self) -> Tensor
  self: grad * self.sinh().conj()

- name: asinh(Tensor self) -> Tensor
  self: grad * (self.pow(2) + 1).rsqrt().conj()

- name: acosh(Tensor self) -> Tensor
  self: acosh_backward(grad, self)

- name: atanh(Tensor self) -> Tensor
  self: grad * 1 / (1 - self.pow(2)).conj()

# In-place arc variants reuse the out-of-place formula.  The saved `self` is
# captured before the mutation (gen_tpx hoists a clone, matching upstream
# gen_variable_type's `original_self = self.clone()`), so the slope is
# evaluated at the pre-update value like torch's auto-derived in-place
# backwards.  torch itself leaves acosh_/asinh_/atanh_ unimplemented; TP
# provides them via the closed-form slope (spec: test_composite_funcs).
- name: acos_(Tensor(a!) self) -> Tensor(a!)
  self: grad * -((-self * self + 1).rsqrt()).conj()

- name: asin_(Tensor(a!) self) -> Tensor(a!)
  self: grad * (-self * self + 1).rsqrt().conj()

- name: atan_(Tensor(a!) self) -> Tensor(a!)
  self: grad / (self * self + 1).conj()

- name: acosh_(Tensor(a!) self) -> Tensor(a!)
  self: acosh_backward(grad, self)

- name: asinh_(Tensor(a!) self) -> Tensor(a!)
  self: grad * (self.pow(2) + 1).rsqrt().conj()

- name: atanh_(Tensor(a!) self) -> Tensor(a!)
  self: grad * 1 / (1 - self.pow(2)).conj()

- name: logit(Tensor self, Scalar? eps=None) -> Tensor
  self: logit_backward(grad, self, eps)

- name: expm1(Tensor self) -> Tensor
  self: grad * (result.conj() + 1)

# torch derivatives.yaml: angle backward is zero at z == 0, else grad * i*z/|z|^2
- name: angle(Tensor self) -> Tensor
  self: angle_backward(grad, self)

# torch: complex(r, i) routes the stored (conjugated) gradient components
# straight back to the parts: dL/dr = g.real, dL/di = g.imag.
- name: complex(Tensor real, Tensor imag) -> Tensor
  real: grad.real()
  imag: grad.imag()

# torch: pow.Tensor_Tensor uses pow_backward_self / pow_backward_exponent with
# conjugated Jacobians; see FunctionsManual.cpp.
- name: pow.Tensor_Tensor(Tensor self, Tensor exponent) -> Tensor
  self: pow_backward_self(grad, self, exponent)
  exponent: pow_backward_exponent(grad, self, exponent, result)

- name: prod(Tensor self, *, ScalarType dtype=Undefined) -> Tensor
  # torch prod_backward: exact including the single-zero case
  self: prod_backward(grad, self, result)

# ---- Shape & view alignment batch: formulas mirror upstream
# tools/autograd/derivatives.yaml (`at::sum_to(grad, self.sym_sizes())`,
# `repeat_backward(grad, repeats, self.sym_sizes())`); the helpers are ports
# of at::sum_to_size / autograd::repeat_backward and live in ManualNodes.h.

- name: expand(Tensor self, int[] size, *, bool implicit=False) -> Tensor
  self: sum_to_size(grad, self.shape())

- name: repeat(Tensor self, int[] repeats) -> Tensor
  self: repeat_backward(grad, repeats, self.shape())

- name: fill.Scalar(Tensor self, Scalar value) -> Tensor
  self: tensorplay::tpx::ops::zeros_like(grad)

- name: fill.Tensor(Tensor self, Tensor value) -> Tensor
  self: tensorplay::tpx::ops::zeros_like(grad)
  value: grad.sum()

# Trapezoid family: weights w_j = dx * [0.5, 1, ..., 1, 0.5] for the sum
# form (x form uses coordinate differences); the cumulative form backprops a
# reversed cumsum of grad times the same weights.
- name: trapezoid(Tensor y, Tensor? x=None, *, Scalar dx=1, int dim=-1) -> Tensor
  y: _trapezoid_backward(grad, x, y.shape(), dx, dim)

- name: cumulative_trapezoid(Tensor y, Tensor? x=None, *, Scalar dx=1, int dim=-1) -> Tensor
  y: _cumulative_trapezoid_backward(grad, x, dx, dim)

- name: scatter_reduce(Tensor self, int dim, Tensor index, Tensor src, str reduce, *, bool include_self=True) -> Tensor
  self: _scatter_reduce_backward_self(grad, self, dim, index, src, reduce, include_self)
  src: _scatter_reduce_backward_src(grad, self, dim, index, src, reduce, include_self)

- name: index_reduce(Tensor self, int dim, Tensor index, Tensor source, str reduce, *, bool include_self=True) -> Tensor
  self: _index_reduce_backward_self(grad, self, dim, index, source, reduce, include_self)
  source: _index_reduce_backward_src(grad, self, dim, index, source, reduce, include_self)

# Covariance / correlation (ATen Correlation.cpp): C = M diag(w) M^T / fact
# with M = X - weighted_avg. dL/dX = G_M - rowsum(G_M) * (w / wsum) where
# G_M = ((grad + grad^T) M diag(w)) / fact. corrcoef chains through the clip
# mask, the D^-1 C D^-1 normalization and cov's formula.
- name: cov(Tensor self, *, int correction=1, Tensor? fweights=None, Tensor? aweights=None) -> Tensor
  self: _cov_backward(grad, self, correction, fweights, aweights)

- name: corrcoef(Tensor self) -> Tensor
  self: _corrcoef_backward(grad, self)

# ===========================================================================
# Native backward completion batch (ports of upstream derivatives.yaml +
# FunctionsManual.cpp).  Helper functions live in tpx/include/ManualNodes.h;
# every formula lowers to dispatched recordable ops (torch's own backward
# implementations are the same compositions), so values and performance
# follow ATen.
# ===========================================================================

# --- scatter / index family (derivatives.yaml:856-880, 1090-1110) ---------

- name: scatter.src(Tensor self, int dim, Tensor index, Tensor src) -> Tensor
  self: scatter(grad, dim, index, Scalar(0))
  src: gather(grad, dim, index)

- name: scatter.value(Tensor self, int dim, Tensor index, Scalar value) -> Tensor
  self: scatter(grad, dim, index, Scalar(0))

- name: index_copy(Tensor self, int dim, Tensor index, Tensor source) -> Tensor
  self: index_fill(grad, dim, index, Scalar(0))
  source: index_select(grad, dim, index)

- name: index_fill.Scalar(Tensor self, int dim, Tensor index, Scalar value) -> Tensor
  self: index_fill(grad, dim, index, Scalar(0))

- name: index_fill.Tensor(Tensor self, int dim, Tensor index, Tensor value) -> Tensor
  self: index_fill(grad, dim, index, Scalar(0))
  value: sum(index_select(grad, dim, std::get<0>(unique(index, false))))

- name: take(Tensor self, Tensor index) -> Tensor
  self: take_backward(grad, self, index)

- name: masked_select(Tensor self, Tensor mask) -> Tensor
  self: masked_select_backward(grad, self, mask)

- name: masked_scatter(Tensor self, Tensor mask, Tensor source) -> Tensor
  self: masked_fill(grad, mask, Scalar(0))
  source: masked_scatter_backward(grad, mask, source)

# --- scatter-family view ops (derivatives.yaml:1240-1270) ------------------

- name: diag_embed(Tensor self, int offset=0, int dim1=0, int dim2=1) -> Tensor
  self: diagonal(grad, offset, dim1, dim2)

- name: channel_shuffle(Tensor self, int groups) -> Tensor
  self: channel_shuffle(grad, grad.size(1) / groups)

- name: select_scatter(Tensor self, Tensor src, int dim, int index) -> Tensor
  self: select_scatter(grad, zeros_like(src), dim, index)
  src: select(grad, dim, index)

- name: slice_scatter(Tensor self, Tensor src, int dim=0, int? start=None, int? end=None, int step=1) -> Tensor
  self: slice_scatter(grad, zeros_like(src), dim, start, end, step)
  src: slice(grad, dim, start, end, step)

- name: diagonal_scatter(Tensor self, Tensor src, int offset=0, int dim1=0, int dim2=1) -> Tensor
  self: diagonal_scatter(grad, zeros_like(src), offset, dim1, dim2)
  src: diagonal(grad, offset, dim1, dim2)

# --- blas-family (derivatives.yaml:190-210): beta/alpha elide via
# maybe_multiply exactly like upstream. -------------------------------------

- name: addbmm(Tensor self, Tensor batch1, Tensor batch2, *, Scalar beta=1, Scalar alpha=1) -> Tensor
  self: maybe_multiply(grad, beta)
  batch1: maybe_multiply(bmm(expand(unsqueeze(grad, 0), {batch1.size(0), batch1.size(1), batch2.size(2)}), transpose(batch2, 1, 2)), alpha)
  batch2: maybe_multiply(bmm(transpose(batch1, 1, 2), expand(unsqueeze(grad, 0), {batch1.size(0), batch1.size(1), batch2.size(2)})), alpha)

- name: addmv(Tensor self, Tensor mat, Tensor vec, *, Scalar beta=1, Scalar alpha=1) -> Tensor
  self: maybe_multiply(grad, beta)
  mat: maybe_multiply(outer(grad, vec), alpha)
  vec: maybe_multiply(mv(t(mat), grad), alpha)

- name: addr(Tensor self, Tensor vec1, Tensor vec2, *, Scalar beta=1, Scalar alpha=1) -> Tensor
  self: maybe_multiply(grad, beta)
  vec1: maybe_multiply(mv(grad, vec2), alpha)
  vec2: maybe_multiply(mv(t(grad), vec1), alpha)

# --- pointwise / special (derivatives.yaml:1300-1560) ----------------------

- name: logaddexp2(Tensor self, Tensor other) -> Tensor
  self: grad * sigmoid((self - other) * 0.6931471805599453)
  other: grad * sigmoid((other - self) * 0.6931471805599453)

- name: nan_to_num(Tensor self, Scalar nan=0.0, Scalar? posinf=None, Scalar? neginf=None) -> Tensor
  self: grad * isfinite(self).to(grad.dtype())

- name: sinc(Tensor self) -> Tensor
  self: sinc_backward(grad, self)

- name: erfinv(Tensor self) -> Tensor
  self: 0.886226925452758 * exp(erfinv(self).pow(2)) * grad

- name: digamma(Tensor self) -> Tensor
  self: grad * polygamma(1, self)

- name: polygamma(int n, Tensor self) -> Tensor
  self: grad * polygamma(n + 1, self)

- name: i0(Tensor self) -> Tensor
  self: grad * i1(self)

- name: sgn(Tensor self) -> Tensor
  self: zeros_like(self)

- name: nextafter(Tensor self, Tensor other) -> Tensor
  self: where(ne(self, other), grad, zeros_like(grad))
  other: zeros_like(other)

# --- reductions (derivatives.yaml:1138-1230) --------------------------------

- name: max(Tensor self) -> Tensor
  self: evenly_distribute_backward(grad, self, result)

- name: min(Tensor self) -> Tensor
  self: evenly_distribute_backward(grad, self, result)

- name: median(Tensor self) -> Tensor
  self: evenly_distribute_backward(grad, self, result)

- name: nanmedian(Tensor self) -> Tensor
  self: evenly_distribute_backward(grad, self, result)

- name: amax(Tensor self, int[] dim=[], bool keepdim=False) -> Tensor
  self: amax_amin_backward(grad, self, result, dim, keepdim)

- name: amin(Tensor self, int[] dim=[], bool keepdim=False) -> Tensor
  self: amax_amin_backward(grad, self, result, dim, keepdim)

- name: topk(Tensor self, int k, int dim=-1, bool largest=true, bool sorted=true, int impl=0) -> (Tensor values, Tensor indices)
  self: value_selecting_backward(grad, dim, indices, self, true)

- name: sort(Tensor self, int dim=-1, bool descending=False) -> (Tensor values, Tensor indices)
  self: value_selecting_backward(grad, dim, indices, self, true)

- name: mode(Tensor self, int dim=-1, bool keepdim=False) -> (Tensor values, Tensor indices)
  self: value_selecting_backward(grad, dim, indices, self, keepdim)

- name: kthvalue(Tensor self, int k, int dim=-1, bool keepdim=False) -> (Tensor values, Tensor indices)
  self: value_selecting_backward(grad, dim, indices, self, keepdim)

- name: cummax(Tensor self, int dim) -> (Tensor values, Tensor indices)
  self: cummaxmin_backward(grad, self, indices, dim)

- name: cummin(Tensor self, int dim) -> (Tensor values, Tensor indices)
  self: cummaxmin_backward(grad, self, indices, dim)

- name: cumprod(Tensor self, int dim, ScalarType? dtype=None) -> Tensor
  self: cumprod_backward(grad, self, dim, result)

- name: logcumsumexp(Tensor self, int dim, ScalarType? dtype=None) -> Tensor
  self: logcumsumexp_backward(grad, self, result, dim)

- name: nansum(Tensor self, int[] dim=[], bool keepdim=False) -> Tensor
  self: nansum_backward(grad, self, dim, keepdim)

- name: nanmean(Tensor self, int? dim=None, bool keepdim=False, *, ScalarType? dtype=None) -> Tensor
  self: nanmean_backward(grad, self, dim, keepdim)

- name: norm(Tensor self, float p=2.0) -> Tensor
  self: norm_backward(grad, self, p, result)

- name: norm.dim(Tensor self, int[] dim, float p=2.0, bool keepdim=False) -> Tensor
  self: norm_backward(grad, self, p, result, dim, keepdim)

- name: prod.dim_IntList(Tensor self, int[] dim, bool keepdim=false, *, ScalarType dtype=Undefined) -> Tensor
  self: prod_backward(grad, self, result, dim, keepdim)

- name: var.dim(Tensor self, int[] dim, int correction=1, bool keepdim=false) -> Tensor
  self: var_backward(grad, self, dim, correction, keepdim)

- name: std.dim(Tensor self, int[] dim, int correction=1, bool keepdim=false) -> Tensor
  self: std_backward(result, grad, self, dim, correction, keepdim)

- name: trace(Tensor self) -> Tensor
  self: trace_backward(grad, self)

- name: vdot(Tensor self, Tensor other) -> Tensor
  self: grad * other
  other: grad * self

- name: dist(Tensor self, Tensor other, Scalar p=2) -> Tensor
  self: norm_backward(grad, self - other, p, result)
  other: neg(norm_backward(grad, self - other, p, result))

- name: renorm(Tensor self, Scalar p, int dim, Scalar maxnorm) -> Tensor
  self: renorm_backward(grad, self, p, dim, maxnorm)
