// Generated by tools/codegen/main.py -- DO NOT EDIT
#pragma once
#include "Node.h"
#include "Autograd.h"
#include "ManualNodes.h"
#include "SavedVariable.h"
#include "tensorplay/ops/TPXOpsGenerated.h"
#include <algorithm>
#include <utility>
#include "Scalar.h"
#include <vector>
#include <cstdint>
#include <cstdio>

namespace tensorplay {
namespace tpx {
using namespace ops;

struct PermuteBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> dims_;

    explicit PermuteBackward(Tensor self, std::vector<int64_t> dims)
        : self_(self), dims_(dims) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::permute_backward(grad, self_sv, dims_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SqueezeBackward : public Node {
    SavedVariable self_;

    explicit SqueezeBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::squeeze_backward(grad, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SqueezeDimBackward : public Node {
    int64_t dim_;

    explicit SqueezeDimBackward(int64_t dim)
        : dim_(dim) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = unsqueeze(grad, dim_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct UnsqueezeBackward : public Node {
    int64_t dim_;

    explicit UnsqueezeBackward(int64_t dim)
        : dim_(dim) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = squeeze(grad, dim_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct TransposeBackward : public Node {
    int64_t dim0_;
    int64_t dim1_;

    explicit TransposeBackward(int64_t dim0, int64_t dim1)
        : dim0_(dim0), dim1_(dim1) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = transpose(grad, dim0_, dim1_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct TBackward : public Node {

    explicit TBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = t(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct ViewAsRealBackward : public Node {

    explicit ViewAsRealBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::view_as_complex(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct ViewAsComplexBackward : public Node {

    explicit ViewAsComplexBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::view_as_real(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct NativeDropoutBackward : public Node {
    double p_;
    SavedVariable mask_;

    explicit NativeDropoutBackward(double p, Tensor mask)
        : p_(p), mask_(mask) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor mask_sv = mask_.unpack();

        variable_list grads;
        auto __grad_input = div(mul(grad, mask_sv), 1 - p_);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        mask_.reset_data();
    }
};

struct NativeAlphaDropoutBackward : public Node {
    double p_;
    SavedVariable mask_;

    explicit NativeAlphaDropoutBackward(double p, Tensor mask)
        : p_(p), mask_(mask) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor mask_sv = mask_.unpack();

        variable_list grads;
        auto __grad_input = _alpha_dropout_backward(grad, mask_sv, p_);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        mask_.reset_data();
    }
};

struct NativeFeatureDropoutBackward : public Node {
    double p_;
    SavedVariable mask_;

    explicit NativeFeatureDropoutBackward(double p, Tensor mask)
        : p_(p), mask_(mask) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor mask_sv = mask_.unpack();

        variable_list grads;
        auto __grad_input = _feature_dropout_backward(grad, mask_sv, p_);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        mask_.reset_data();
    }
};

struct AddTensorBackward : public Node {
    Scalar alpha_;

    explicit AddTensorBackward(Scalar alpha)
        : alpha_(alpha) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        auto __grad_other = mul(grad, alpha_);
        grads.push_back(__grad_other);
        return grads;
    }
};

struct AddScalarBackward : public Node {

    explicit AddScalarBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        return grads;
    }
};

struct AddReluBackward : public Node {
    SavedVariable result_;

    explicit AddReluBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __shared_0 = threshold_backward(grad, result_sv, 0);
        auto __grad_self = __shared_0;
        grads.push_back(__grad_self);
        auto __grad_other = __shared_0;
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct SubTensorBackward : public Node {
    Scalar alpha_;

    explicit SubTensorBackward(Scalar alpha)
        : alpha_(alpha) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        auto __grad_other = mul(neg(grad), alpha_);
        grads.push_back(__grad_other);
        return grads;
    }
};

struct SubScalarBackward : public Node {

    explicit SubScalarBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        return grads;
    }
};

struct MulTensorBackward : public Node {
    SavedVariable self_;
    SavedVariable other_;

    explicit MulTensorBackward(Tensor self, Tensor other)
        : self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        auto __grad_self = mul_tensor_backward(grad, other_sv, self_sv.dtype());
        grads.push_back(__grad_self);
        auto __grad_other = mul_tensor_backward(grad, self_sv, other_sv.dtype());
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
    }
};

struct MulScalarBackward : public Node {
    Scalar other_;

    explicit MulScalarBackward(Scalar other)
        : other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = mul(grad, scalar_conj_if_complex(other_));
        grads.push_back(__grad_self);
        return grads;
    }
};

struct DivTensorBackward : public Node {
    SavedVariable self_;
    SavedVariable other_;

    explicit DivTensorBackward(Tensor self, Tensor other)
        : self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        auto __grad_self = div_tensor_self_backward(grad, other_sv, self_sv.dtype());
        grads.push_back(__grad_self);
        auto __grad_other = div_tensor_other_backward(grad, self_sv, other_sv);
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
    }
};

struct DivScalarBackward : public Node {
    Scalar other_;

    explicit DivScalarBackward(Scalar other)
        : other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = div(grad, scalar_conj_if_complex(other_));
        grads.push_back(__grad_self);
        return grads;
    }
};

struct AddcmulBackward : public Node {
    SavedVariable tensor1_;
    SavedVariable tensor2_;
    Scalar value_;

    explicit AddcmulBackward(Tensor tensor1, Tensor tensor2, Scalar value)
        : tensor1_(tensor1), tensor2_(tensor2), value_(value) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor tensor1_sv = tensor1_.unpack();
        const Tensor tensor2_sv = tensor2_.unpack();

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        auto __grad_tensor1 = mul(mul(grad, tensor2_sv), value_);
        grads.push_back(__grad_tensor1);
        auto __grad_tensor2 = mul(mul(grad, tensor1_sv), value_);
        grads.push_back(__grad_tensor2);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        tensor1_.reset_data();
        tensor2_.reset_data();
    }
};

struct AddcdivBackward : public Node {
    SavedVariable tensor1_;
    SavedVariable tensor2_;
    Scalar value_;

    explicit AddcdivBackward(Tensor tensor1, Tensor tensor2, Scalar value)
        : tensor1_(tensor1), tensor2_(tensor2), value_(value) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor tensor1_sv = tensor1_.unpack();
        const Tensor tensor2_sv = tensor2_.unpack();

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        auto __grad_tensor1 = mul(div(grad, tensor2_sv), value_);
        grads.push_back(__grad_tensor1);
        auto __grad_tensor2 = mul(div(mul(neg(grad), tensor1_sv), mul(tensor2_sv, tensor2_sv)), value_);
        grads.push_back(__grad_tensor2);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        tensor1_.reset_data();
        tensor2_.reset_data();
    }
};

struct WhereSelfBackward : public Node {
    SavedVariable condition_;
    SavedVariable self_;
    SavedVariable other_;

    explicit WhereSelfBackward(Tensor condition, Tensor self, Tensor other)
        : condition_(condition), self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor condition_sv = condition_.unpack();
        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        grads.push_back(Tensor());
        auto __grad_self = where(condition_sv, grad, zeros_like(self_sv));
        grads.push_back(__grad_self);
        auto __grad_other = where(condition_sv, zeros_like(other_sv), grad);
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        condition_.reset_data();
        self_.reset_data();
        other_.reset_data();
    }
};

struct WhereScalarselfBackward : public Node {
    SavedVariable condition_;
    SavedVariable other_;

    explicit WhereScalarselfBackward(Tensor condition, Tensor other)
        : condition_(condition), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor condition_sv = condition_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        grads.push_back(Tensor());
        auto __grad_other = where(condition_sv, zeros_like(other_sv), grad);
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        condition_.reset_data();
        other_.reset_data();
    }
};

struct WhereScalarotherBackward : public Node {
    SavedVariable condition_;
    SavedVariable self_;

    explicit WhereScalarotherBackward(Tensor condition, Tensor self)
        : condition_(condition), self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor condition_sv = condition_.unpack();
        const Tensor self_sv = self_.unpack();

        variable_list grads;
        grads.push_back(Tensor());
        auto __grad_self = where(condition_sv, grad, zeros_like(self_sv));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        condition_.reset_data();
        self_.reset_data();
    }
};

struct MaximumBackward : public Node {
    SavedVariable self_;
    SavedVariable other_;

    explicit MaximumBackward(Tensor self, Tensor other)
        : self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        auto __shared_0 = eq(self_sv, other_sv);
        auto __grad_self = where(__shared_0, div(grad, 2), where(gt(self_sv, other_sv), grad, zeros_like(self_sv)));
        grads.push_back(__grad_self);
        auto __grad_other = where(__shared_0, div(grad, 2), where(lt(self_sv, other_sv), grad, zeros_like(other_sv)));
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
    }
};

struct MinimumBackward : public Node {
    SavedVariable self_;
    SavedVariable other_;

    explicit MinimumBackward(Tensor self, Tensor other)
        : self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        auto __shared_0 = eq(self_sv, other_sv);
        auto __grad_self = where(__shared_0, div(grad, 2), where(lt(self_sv, other_sv), grad, zeros_like(self_sv)));
        grads.push_back(__grad_self);
        auto __grad_other = where(__shared_0, div(grad, 2), where(gt(self_sv, other_sv), grad, zeros_like(other_sv)));
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
    }
};

struct FusedMulAddBackward : public Node {
    SavedVariable self_;
    SavedVariable other_;

    explicit FusedMulAddBackward(Tensor self, Tensor other)
        : self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, other_sv);
        grads.push_back(__grad_self);
        auto __grad_other = mul(grad, self_sv);
        grads.push_back(__grad_other);
        auto __grad_addend = grad;
        grads.push_back(__grad_addend);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
    }
};

struct FusedMulAddScalarBackward : public Node {
    Scalar other_;

    explicit FusedMulAddScalarBackward(Scalar other)
        : other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = mul(grad, other_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct NegBackward : public Node {

    explicit NegBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = neg(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct ClampBackward : public Node {
    SavedVariable self_;
    std::optional<Scalar> min_;
    std::optional<Scalar> max_;

    explicit ClampBackward(Tensor self, std::optional<Scalar> min, std::optional<Scalar> max)
        : self_(self), min_(min), max_(max) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = clamp_backward(grad, self_sv, min_, max_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ClampMinBackward : public Node {
    SavedVariable self_;
    Scalar min_;

    explicit ClampMinBackward(Tensor self, Scalar min)
        : self_(self), min_(min) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = where(gt(self_sv, min_), grad, zeros_like(self_sv));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ClampMaxBackward : public Node {
    SavedVariable self_;
    Scalar max_;

    explicit ClampMaxBackward(Tensor self, Scalar max)
        : self_(self), max_(max) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = where(lt(self_sv, max_), grad, zeros_like(self_sv));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct EmbeddingBackward : public Node {
    SavedVariable weight_;
    SavedVariable indices_;
    int64_t padding_idx_;
    bool scale_grad_by_freq_;

    explicit EmbeddingBackward(Tensor weight, Tensor indices, int64_t padding_idx, bool scale_grad_by_freq)
        : weight_(weight), indices_(indices), padding_idx_(padding_idx), scale_grad_by_freq_(scale_grad_by_freq) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor weight_sv = weight_.unpack();
        const Tensor indices_sv = indices_.unpack();

        variable_list grads;
        auto __grad_weight = tensorplay::tpx::ops::embedding_dense_backward(grad, indices_sv, weight_sv.size(0), padding_idx_, scale_grad_by_freq_);
        grads.push_back(__grad_weight);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        weight_.reset_data();
        indices_.reset_data();
    }
};

struct ViewBackward : public Node {
    SavedVariable self_;

    explicit ViewBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = reshape(grad, self_sv.shape());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct CloneBackward : public Node {

    explicit CloneBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        return grads;
    }
};

struct ContiguousBackward : public Node {

    explicit ContiguousBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        return grads;
    }
};

struct SelectBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    int64_t index_;

    explicit SelectBackward(Tensor self, int64_t dim, int64_t index)
        : self_(self), dim_(dim), index_(index) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::select_backward(grad, self_sv, dim_, index_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SliceBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    std::optional<int64_t> start_;
    std::optional<int64_t> end_;
    int64_t step_;

    explicit SliceBackward(Tensor self, int64_t dim, std::optional<int64_t> start, std::optional<int64_t> end, int64_t step)
        : self_(self), dim_(dim), start_(start), end_(end), step_(step) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::slice_backward(grad, self_sv, dim_, start_, end_, step_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct NarrowBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    int64_t start_;
    int64_t length_;

    explicit NarrowBackward(Tensor self, int64_t dim, int64_t start, int64_t length)
        : self_(self), dim_(dim), start_(start), length_(length) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::slice_backward(grad, self_sv, dim_, start_, start_ + length_, 1);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct MmBackward : public Node {
    SavedVariable self_;
    SavedVariable mat2_;

    explicit MmBackward(Tensor self, Tensor mat2)
        : self_(self), mat2_(mat2) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor mat2_sv = mat2_.unpack();

        variable_list grads;
        auto __grad_self = mm(grad, t(mat2_sv));
        grads.push_back(__grad_self);
        auto __grad_mat2 = mm(t(self_sv), grad);
        grads.push_back(__grad_mat2);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        mat2_.reset_data();
    }
};

struct AddmmBackward : public Node {
    SavedVariable mat1_;
    SavedVariable mat2_;
    Scalar beta_;
    Scalar alpha_;

    explicit AddmmBackward(Tensor mat1, Tensor mat2, Scalar beta, Scalar alpha)
        : mat1_(mat1), mat2_(mat2), beta_(beta), alpha_(alpha) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor mat1_sv = mat1_.unpack();
        const Tensor mat2_sv = mat2_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, beta_);
        grads.push_back(__grad_self);
        auto __grad_mat1 = mul(mm(grad, t(mat2_sv)), alpha_);
        grads.push_back(__grad_mat1);
        auto __grad_mat2 = mul(mm(t(mat1_sv), grad), alpha_);
        grads.push_back(__grad_mat2);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        mat1_.reset_data();
        mat2_.reset_data();
    }
};

struct BmmBackward : public Node {
    SavedVariable self_;
    SavedVariable mat2_;

    explicit BmmBackward(Tensor self, Tensor mat2)
        : self_(self), mat2_(mat2) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor mat2_sv = mat2_.unpack();

        variable_list grads;
        auto __grad_self = matmul(grad, transpose(mat2_sv, -2, -1));
        grads.push_back(__grad_self);
        auto __grad_mat2 = matmul(transpose(self_sv, -2, -1), grad);
        grads.push_back(__grad_mat2);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        mat2_.reset_data();
    }
};

struct BaddbmmBackward : public Node {
    SavedVariable batch1_;
    SavedVariable batch2_;
    Scalar beta_;
    Scalar alpha_;

    explicit BaddbmmBackward(Tensor batch1, Tensor batch2, Scalar beta, Scalar alpha)
        : batch1_(batch1), batch2_(batch2), beta_(beta), alpha_(alpha) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor batch1_sv = batch1_.unpack();
        const Tensor batch2_sv = batch2_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, beta_);
        grads.push_back(__grad_self);
        auto __grad_batch1 = mul(matmul(grad, transpose(batch2_sv, -2, -1)), alpha_);
        grads.push_back(__grad_batch1);
        auto __grad_batch2 = mul(matmul(transpose(batch1_sv, -2, -1), grad), alpha_);
        grads.push_back(__grad_batch2);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        batch1_.reset_data();
        batch2_.reset_data();
    }
};

struct MvBackward : public Node {
    SavedVariable self_;
    SavedVariable vec_;

    explicit MvBackward(Tensor self, Tensor vec)
        : self_(self), vec_(vec) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor vec_sv = vec_.unpack();

        variable_list grads;
        auto __grad_self = grad.outer(vec_sv);
        grads.push_back(__grad_self);
        auto __grad_vec = t(self_sv).mv(grad);
        grads.push_back(__grad_vec);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        vec_.reset_data();
    }
};

struct DotBackward : public Node {
    SavedVariable self_;
    SavedVariable tensor_;

    explicit DotBackward(Tensor self, Tensor tensor)
        : self_(self), tensor_(tensor) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor tensor_sv = tensor_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, tensor_sv);
        grads.push_back(__grad_self);
        auto __grad_tensor = mul(grad, self_sv);
        grads.push_back(__grad_tensor);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        tensor_.reset_data();
    }
};

struct OuterBackward : public Node {
    SavedVariable self_;
    SavedVariable vec2_;

    explicit OuterBackward(Tensor self, Tensor vec2)
        : self_(self), vec2_(vec2) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor vec2_sv = vec2_.unpack();

        variable_list grads;
        auto __grad_self = grad.mv(vec2_sv);
        grads.push_back(__grad_self);
        auto __grad_vec2 = t(grad).mv(self_sv);
        grads.push_back(__grad_vec2);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        vec2_.reset_data();
    }
};

struct InnerBackward : public Node {
    SavedVariable self_;
    SavedVariable other_;

    explicit InnerBackward(Tensor self, Tensor other)
        : self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        auto __grad_self = inner_backward_self(grad, self_sv, other_sv);
        grads.push_back(__grad_self);
        auto __grad_other = inner_backward_other(grad, self_sv, other_sv);
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
    }
};

struct MovedimBackward : public Node {
    std::vector<int64_t> source_;
    std::vector<int64_t> destination_;

    explicit MovedimBackward(std::vector<int64_t> source, std::vector<int64_t> destination)
        : source_(source), destination_(destination) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = movedim(grad, destination_, source_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct DiagonalBackward : public Node {
    SavedVariable self_;
    int64_t offset_;
    int64_t dim1_;
    int64_t dim2_;

    explicit DiagonalBackward(Tensor self, int64_t offset, int64_t dim1, int64_t dim2)
        : self_(self), offset_(offset), dim1_(dim1), dim2_(dim2) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = diagonal_backward(grad, self_sv.shape(), offset_, dim1_, dim2_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SinBackward : public Node {
    SavedVariable self_;

    explicit SinBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, cos(self_sv).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct CosBackward : public Node {
    SavedVariable self_;

    explicit CosBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, -sin(self_sv).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct AbsBackward : public Node {
    SavedVariable self_;

    explicit AbsBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, sign(self_sv));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct FloorBackward : public Node {

    explicit FloorBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = zeros_like(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct CeilBackward : public Node {

    explicit CeilBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = zeros_like(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct RoundBackward : public Node {

    explicit RoundBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = zeros_like(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct TruncBackward : public Node {

    explicit TruncBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = zeros_like(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct PowTensorScalarBackward : public Node {
    SavedVariable self_;
    Scalar exponent_;

    explicit PowTensorScalarBackward(Tensor self, Scalar exponent)
        : self_(self), exponent_(exponent) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = pow_backward(grad, self_sv, exponent_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ExpBackward : public Node {
    SavedVariable result_;

    explicit ExpBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, result_sv.conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct LogBackward : public Node {
    SavedVariable self_;

    explicit LogBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = div(grad, self_sv.conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SquareBackward : public Node {
    SavedVariable self_;

    explicit SquareBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(mul(grad, 2), self_sv.conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SqrtBackward : public Node {
    SavedVariable result_;

    explicit SqrtBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = div(grad, 2 * result_sv.conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct RsqrtBackward : public Node {
    SavedVariable result_;

    explicit RsqrtBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(mul(grad, -0.5), pow(result_sv, 3).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct SumBackward : public Node {
    SavedVariable self_;

    explicit SumBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = add(zeros_like(self_sv), grad);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct MeanDimBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> dim_;
    bool keepdim_;

    explicit MeanDimBackward(Tensor self, std::vector<int64_t> dim, bool keepdim)
        : self_(self), dim_(dim), keepdim_(keepdim) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::broadcast_mean_backward(grad, self_sv, dim_, keepdim_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct MaxDimBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    bool keepdim_;
    SavedVariable indices_;

    explicit MaxDimBackward(Tensor self, int64_t dim, bool keepdim, Tensor indices)
        : self_(self), dim_(dim), keepdim_(keepdim), indices_(indices) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor indices_sv = indices_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::value_selecting_reduction_backward(grad, dim_, indices_sv, self_sv, keepdim_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        indices_.reset_data();
    }
};

struct MinDimBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    bool keepdim_;
    SavedVariable indices_;

    explicit MinDimBackward(Tensor self, int64_t dim, bool keepdim, Tensor indices)
        : self_(self), dim_(dim), keepdim_(keepdim), indices_(indices) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor indices_sv = indices_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::value_selecting_reduction_backward(grad, dim_, indices_sv, self_sv, keepdim_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        indices_.reset_data();
    }
};

struct SumDimIntlistBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> dim_;
    bool keepdim_;

    explicit SumDimIntlistBackward(Tensor self, std::vector<int64_t> dim, bool keepdim)
        : self_(self), dim_(dim), keepdim_(keepdim) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = _sum_dim_backward(grad, self_sv, dim_, keepdim_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ReshapeBackward : public Node {
    SavedVariable self_;

    explicit ReshapeBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = reshape(grad, self_sv.shape());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct LogSoftmaxBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    DType dtype_;

    explicit LogSoftmaxBackward(Tensor self, int64_t dim, DType dtype)
        : self_(self), dim_(dim), dtype_(dtype) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = sub(grad, mul(exp(log_softmax(self_sv, dim_, dtype_)), sum(grad, {dim_}, true)));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SoftmaxBackward : public Node {
    int64_t dim_;
    SavedVariable result_;

    explicit SoftmaxBackward(int64_t dim, Tensor result)
        : dim_(dim), result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(sub(grad, sum(mul(grad, result_sv), {dim_}, true)), result_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct NllLossBackward : public Node {
    SavedVariable self_;
    SavedVariable target_;
    std::optional<Tensor> weight_;
    int64_t reduction_;
    int64_t ignore_index_;
    SavedVariable total_weight_;

    explicit NllLossBackward(Tensor self, Tensor target, std::optional<Tensor> weight, int64_t reduction, int64_t ignore_index, Tensor total_weight)
        : self_(self), target_(target), weight_(weight), reduction_(reduction), ignore_index_(ignore_index), total_weight_(total_weight) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor target_sv = target_.unpack();
        const Tensor total_weight_sv = total_weight_.unpack();

        variable_list grads;
        auto __grad_self = nll_loss_backward(grad, self_sv, target_sv, weight_, reduction_, ignore_index_, total_weight_sv);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        target_.reset_data();
        total_weight_.reset_data();
    }
};

struct CtcLossBackward : public Node {
    SavedVariable log_probs_;
    SavedVariable targets_;
    SavedVariable input_lengths_;
    SavedVariable target_lengths_;
    int64_t blank_;
    bool zero_infinity_;
    SavedVariable neg_log_likelihood_;
    SavedVariable log_alpha_;

    explicit CtcLossBackward(Tensor log_probs, Tensor targets, Tensor input_lengths, Tensor target_lengths, int64_t blank, bool zero_infinity, Tensor neg_log_likelihood, Tensor log_alpha)
        : log_probs_(log_probs), targets_(targets), input_lengths_(input_lengths), target_lengths_(target_lengths), blank_(blank), zero_infinity_(zero_infinity), neg_log_likelihood_(neg_log_likelihood), log_alpha_(log_alpha) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor log_probs_sv = log_probs_.unpack();
        const Tensor targets_sv = targets_.unpack();
        const Tensor input_lengths_sv = input_lengths_.unpack();
        const Tensor target_lengths_sv = target_lengths_.unpack();
        const Tensor neg_log_likelihood_sv = neg_log_likelihood_.unpack();
        const Tensor log_alpha_sv = log_alpha_.unpack();

        variable_list grads;
        auto __grad_log_probs = _ctc_loss_backward(grad, log_probs_sv, targets_sv, input_lengths_sv, target_lengths_sv, neg_log_likelihood_sv, log_alpha_sv, blank_, zero_infinity_);
        grads.push_back(__grad_log_probs);
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        log_probs_.reset_data();
        targets_.reset_data();
        input_lengths_.reset_data();
        target_lengths_.reset_data();
        neg_log_likelihood_.reset_data();
        log_alpha_.reset_data();
    }
};

struct MseLossBackward : public Node {
    SavedVariable self_;
    SavedVariable target_;
    int64_t reduction_;

    explicit MseLossBackward(Tensor self, Tensor target, int64_t reduction)
        : self_(self), target_(target), reduction_(reduction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_self = mse_loss_backward(grad, self_sv, target_sv, reduction_);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        target_.reset_data();
    }
};

struct ReluBackward : public Node {
    SavedVariable result_;

    explicit ReluBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = threshold_backward(grad, result_sv, 0);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct MaxPool2DBackward : public Node {
    SavedVariable input_;
    std::vector<int64_t> kernel_size_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> dilation_;
    bool ceil_mode_;

    explicit MaxPool2DBackward(Tensor input, std::vector<int64_t> kernel_size, std::vector<int64_t> stride, std::vector<int64_t> padding, std::vector<int64_t> dilation, bool ceil_mode)
        : input_(input), kernel_size_(kernel_size), stride_(stride), padding_(padding), dilation_(dilation), ceil_mode_(ceil_mode) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __grad_input = max_pool2d_backward(grad, input_sv, kernel_size_, stride_, padding_, dilation_, ceil_mode_);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct AvgPool2DBackward : public Node {
    SavedVariable input_;
    std::vector<int64_t> kernel_size_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    bool ceil_mode_;
    bool count_include_pad_;
    std::optional<int64_t> divisor_override_;

    explicit AvgPool2DBackward(Tensor input, std::vector<int64_t> kernel_size, std::vector<int64_t> stride, std::vector<int64_t> padding, bool ceil_mode, bool count_include_pad, std::optional<int64_t> divisor_override)
        : input_(input), kernel_size_(kernel_size), stride_(stride), padding_(padding), ceil_mode_(ceil_mode), count_include_pad_(count_include_pad), divisor_override_(divisor_override) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __grad_input = avg_pool2d_backward(grad, input_sv, kernel_size_, stride_, padding_, ceil_mode_, count_include_pad_, divisor_override_);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct AvgPool3DBackward : public Node {
    SavedVariable input_;
    std::vector<int64_t> kernel_size_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    bool ceil_mode_;
    bool count_include_pad_;
    std::optional<int64_t> divisor_override_;

    explicit AvgPool3DBackward(Tensor input, std::vector<int64_t> kernel_size, std::vector<int64_t> stride, std::vector<int64_t> padding, bool ceil_mode, bool count_include_pad, std::optional<int64_t> divisor_override)
        : input_(input), kernel_size_(kernel_size), stride_(stride), padding_(padding), ceil_mode_(ceil_mode), count_include_pad_(count_include_pad), divisor_override_(divisor_override) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __grad_input = avg_pool3d_backward(grad, input_sv, kernel_size_, stride_, padding_, ceil_mode_, count_include_pad_, divisor_override_);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct AdaptiveAvgPool3DBackward : public Node {
    SavedVariable input_;

    explicit AdaptiveAvgPool3DBackward(Tensor input)
        : input_(input) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __grad_input = adaptive_avg_pool3d_backward(grad, input_sv);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct AdaptiveAvgPool2DBackward : public Node {
    SavedVariable input_;

    explicit AdaptiveAvgPool2DBackward(Tensor input)
        : input_(input) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __grad_input = adaptive_avg_pool2d_backward(grad, input_sv);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct AdaptiveMaxPool2DBackward : public Node {
    SavedVariable input_;

    explicit AdaptiveMaxPool2DBackward(Tensor input)
        : input_(input) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __grad_input = adaptive_max_pool2d_backward(grad, input_sv);
        grads.push_back(__grad_input);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct BatchNormBackward : public Node {
    SavedVariable input_;
    std::optional<Tensor> weight_;
    std::optional<Tensor> running_mean_;
    std::optional<Tensor> running_var_;
    bool training_;
    double eps_;

    explicit BatchNormBackward(Tensor input, std::optional<Tensor> weight, std::optional<Tensor> running_mean, std::optional<Tensor> running_var, bool training, double eps)
        : input_(input), weight_(weight), running_mean_(running_mean), running_var_(running_var), training_(training), eps_(eps) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __shared_0 = batch_norm_backward(grad, input_sv, weight_, running_mean_, running_var_, training_, eps_);
        auto __grad_input = std::get<0>(__shared_0);
        grads.push_back(__grad_input);
        auto __grad_weight = std::get<1>(__shared_0);
        grads.push_back(__grad_weight);
        auto __grad_bias = std::get<2>(__shared_0);
        grads.push_back(__grad_bias);
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct LayerNormBackward : public Node {
    SavedVariable input_;
    std::vector<int64_t> normalized_shape_;
    std::optional<Tensor> weight_;
    std::optional<Tensor> bias_;
    double eps_;

    explicit LayerNormBackward(Tensor input, std::vector<int64_t> normalized_shape, std::optional<Tensor> weight, std::optional<Tensor> bias, double eps)
        : input_(input), normalized_shape_(normalized_shape), weight_(weight), bias_(bias), eps_(eps) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __shared_0 = layer_norm_backward(grad, input_sv, normalized_shape_, weight_, bias_, eps_);
        auto __grad_input = std::get<0>(__shared_0);
        grads.push_back(__grad_input);
        auto __grad_weight = std::get<1>(__shared_0);
        grads.push_back(__grad_weight);
        auto __grad_bias = std::get<2>(__shared_0);
        grads.push_back(__grad_bias);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct GroupNormBackward : public Node {
    SavedVariable input_;
    int64_t num_groups_;
    std::optional<Tensor> weight_;
    std::optional<Tensor> bias_;
    double eps_;

    explicit GroupNormBackward(Tensor input, int64_t num_groups, std::optional<Tensor> weight, std::optional<Tensor> bias, double eps)
        : input_(input), num_groups_(num_groups), weight_(weight), bias_(bias), eps_(eps) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __shared_0 = group_norm_backward(grad, input_sv, num_groups_, weight_, bias_, eps_);
        auto __grad_input = std::get<0>(__shared_0);
        grads.push_back(__grad_input);
        auto __grad_weight = std::get<1>(__shared_0);
        grads.push_back(__grad_weight);
        auto __grad_bias = std::get<2>(__shared_0);
        grads.push_back(__grad_bias);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct InstanceNormBackward : public Node {
    SavedVariable input_;
    std::optional<Tensor> weight_;
    std::optional<Tensor> bias_;
    std::optional<Tensor> running_mean_;
    std::optional<Tensor> running_var_;
    bool use_input_stats_;
    double eps_;

    explicit InstanceNormBackward(Tensor input, std::optional<Tensor> weight, std::optional<Tensor> bias, std::optional<Tensor> running_mean, std::optional<Tensor> running_var, bool use_input_stats, double eps)
        : input_(input), weight_(weight), bias_(bias), running_mean_(running_mean), running_var_(running_var), use_input_stats_(use_input_stats), eps_(eps) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();

        variable_list grads;
        auto __shared_0 = instance_norm_backward(grad, input_sv, weight_, bias_, running_mean_, running_var_, use_input_stats_, eps_);
        auto __grad_input = std::get<0>(__shared_0);
        grads.push_back(__grad_input);
        auto __grad_weight = std::get<1>(__shared_0);
        grads.push_back(__grad_weight);
        auto __grad_bias = std::get<2>(__shared_0);
        grads.push_back(__grad_bias);
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
    }
};

struct ConstantPadNdBackward : public Node {
    std::vector<int64_t> pad_;

    explicit ConstantPadNdBackward(std::vector<int64_t> pad)
        : pad_(pad) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = constant_pad_nd_backward(grad, pad_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct Conv2DBackward : public Node {
    SavedVariable input_;
    SavedVariable weight_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> dilation_;
    int64_t groups_;

    explicit Conv2DBackward(Tensor input, Tensor weight, std::vector<int64_t> stride, std::vector<int64_t> padding, std::vector<int64_t> dilation, int64_t groups)
        : input_(input), weight_(weight), stride_(stride), padding_(padding), dilation_(dilation), groups_(groups) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor weight_sv = weight_.unpack();

        variable_list grads;
        auto __grad_input = conv2d_grad_input(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_input);
        auto __grad_weight = conv2d_grad_weight(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_weight);
        auto __grad_bias = conv2d_grad_bias(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_bias);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        weight_.reset_data();
    }
};

struct Conv1DBackward : public Node {
    SavedVariable input_;
    SavedVariable weight_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> dilation_;
    int64_t groups_;

    explicit Conv1DBackward(Tensor input, Tensor weight, std::vector<int64_t> stride, std::vector<int64_t> padding, std::vector<int64_t> dilation, int64_t groups)
        : input_(input), weight_(weight), stride_(stride), padding_(padding), dilation_(dilation), groups_(groups) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor weight_sv = weight_.unpack();

        variable_list grads;
        auto __grad_input = conv1d_grad_input(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_input);
        auto __grad_weight = conv1d_grad_weight(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_weight);
        auto __grad_bias = conv1d_grad_bias(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_bias);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        weight_.reset_data();
    }
};

struct Conv3DBackward : public Node {
    SavedVariable input_;
    SavedVariable weight_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> dilation_;
    int64_t groups_;

    explicit Conv3DBackward(Tensor input, Tensor weight, std::vector<int64_t> stride, std::vector<int64_t> padding, std::vector<int64_t> dilation, int64_t groups)
        : input_(input), weight_(weight), stride_(stride), padding_(padding), dilation_(dilation), groups_(groups) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor weight_sv = weight_.unpack();

        variable_list grads;
        auto __grad_input = conv3d_grad_input(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_input);
        auto __grad_weight = conv3d_grad_weight(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_weight);
        auto __grad_bias = conv3d_grad_bias(grad, input_sv, weight_sv, stride_, padding_, dilation_, groups_);
        grads.push_back(__grad_bias);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        weight_.reset_data();
    }
};

struct ConvTranspose2DBackward : public Node {
    SavedVariable input_;
    SavedVariable weight_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> output_padding_;
    int64_t groups_;
    std::vector<int64_t> dilation_;

    explicit ConvTranspose2DBackward(Tensor input, Tensor weight, std::vector<int64_t> stride, std::vector<int64_t> padding, std::vector<int64_t> output_padding, int64_t groups, std::vector<int64_t> dilation)
        : input_(input), weight_(weight), stride_(stride), padding_(padding), output_padding_(output_padding), groups_(groups), dilation_(dilation) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor weight_sv = weight_.unpack();

        variable_list grads;
        auto __grad_input = conv_transpose2d_grad_input(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_input);
        auto __grad_weight = conv_transpose2d_grad_weight(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_weight);
        auto __grad_bias = conv_transpose2d_grad_bias(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_bias);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        weight_.reset_data();
    }
};

struct ConvTranspose3DBackward : public Node {
    SavedVariable input_;
    SavedVariable weight_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> output_padding_;
    int64_t groups_;
    std::vector<int64_t> dilation_;

    explicit ConvTranspose3DBackward(Tensor input, Tensor weight, std::vector<int64_t> stride, std::vector<int64_t> padding, std::vector<int64_t> output_padding, int64_t groups, std::vector<int64_t> dilation)
        : input_(input), weight_(weight), stride_(stride), padding_(padding), output_padding_(output_padding), groups_(groups), dilation_(dilation) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor weight_sv = weight_.unpack();

        variable_list grads;
        auto __grad_input = conv_transpose3d_grad_input(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_input);
        auto __grad_weight = conv_transpose3d_grad_weight(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_weight);
        auto __grad_bias = conv_transpose3d_grad_bias(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_bias);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        weight_.reset_data();
    }
};

struct ConvTranspose1DBackward : public Node {
    SavedVariable input_;
    SavedVariable weight_;
    std::vector<int64_t> stride_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> output_padding_;
    int64_t groups_;
    std::vector<int64_t> dilation_;

    explicit ConvTranspose1DBackward(Tensor input, Tensor weight, std::vector<int64_t> stride, std::vector<int64_t> padding, std::vector<int64_t> output_padding, int64_t groups, std::vector<int64_t> dilation)
        : input_(input), weight_(weight), stride_(stride), padding_(padding), output_padding_(output_padding), groups_(groups), dilation_(dilation) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor weight_sv = weight_.unpack();

        variable_list grads;
        auto __grad_input = conv_transpose1d_grad_input(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_input);
        auto __grad_weight = conv_transpose1d_grad_weight(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_weight);
        auto __grad_bias = conv_transpose1d_grad_bias(grad, input_sv, weight_sv, stride_, padding_, output_padding_, groups_, dilation_);
        grads.push_back(__grad_bias);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        weight_.reset_data();
    }
};

struct Im2ColBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> kernel_size_;
    std::vector<int64_t> dilation_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> stride_;

    explicit Im2ColBackward(Tensor self, std::vector<int64_t> kernel_size, std::vector<int64_t> dilation, std::vector<int64_t> padding, std::vector<int64_t> stride)
        : self_(self), kernel_size_(kernel_size), dilation_(dilation), padding_(padding), stride_(stride) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = im2col_backward(grad, self_sv.shape(), kernel_size_, dilation_, padding_, stride_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct Col2ImBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> output_size_;
    std::vector<int64_t> kernel_size_;
    std::vector<int64_t> dilation_;
    std::vector<int64_t> padding_;
    std::vector<int64_t> stride_;

    explicit Col2ImBackward(Tensor self, std::vector<int64_t> output_size, std::vector<int64_t> kernel_size, std::vector<int64_t> dilation, std::vector<int64_t> padding, std::vector<int64_t> stride)
        : self_(self), output_size_(output_size), kernel_size_(kernel_size), dilation_(dilation), padding_(padding), stride_(stride) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = col2im_backward(grad, self_sv.shape(), output_size_, kernel_size_, dilation_, padding_, stride_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ReflectionPadNdBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> pad_;

    explicit ReflectionPadNdBackward(Tensor self, std::vector<int64_t> pad)
        : self_(self), pad_(pad) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = reflection_pad_nd_backward(grad, self_sv, pad_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ReplicationPadNdBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> pad_;

    explicit ReplicationPadNdBackward(Tensor self, std::vector<int64_t> pad)
        : self_(self), pad_(pad) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = replication_pad_nd_backward(grad, self_sv, pad_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct CircularPadNdBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> pad_;

    explicit CircularPadNdBackward(Tensor self, std::vector<int64_t> pad)
        : self_(self), pad_(pad) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = circular_pad_nd_backward(grad, self_sv, pad_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SigmoidBackward : public Node {
    SavedVariable result_;

    explicit SigmoidBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, mul(result_sv, neg(sub(result_sv, 1))).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct SiluBackward : public Node {
    SavedVariable self_;

    explicit SiluBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __shared_0 = sigmoid(self_sv);
        auto __grad_self = mul(mul(grad, __shared_0), 1 + mul(self_sv, neg(sub(__shared_0, 1))));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct TanhBackward : public Node {
    SavedVariable result_;

    explicit TanhBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, neg(sub(mul(result_sv, result_sv), 1)).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct Atan2Backward : public Node {
    SavedVariable self_;
    SavedVariable other_;

    explicit Atan2Backward(Tensor self, Tensor other)
        : self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        auto __grad_self = div(mul(grad, other_sv), add(mul(self_sv, self_sv), mul(other_sv, other_sv)));
        grads.push_back(__grad_self);
        auto __grad_other = div(mul(grad, neg(self_sv)), add(mul(self_sv, self_sv), mul(other_sv, other_sv)));
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
    }
};

struct LerpBackward : public Node {
    Scalar weight_;

    explicit LerpBackward(Scalar weight)
        : weight_(weight) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = mul(grad, Scalar(1) - weight_);
        grads.push_back(__grad_self);
        auto __grad_end = mul(grad, weight_);
        grads.push_back(__grad_end);
        return grads;
    }
};

struct LerpTensorBackward : public Node {
    SavedVariable self_;
    SavedVariable end_;
    SavedVariable weight_;

    explicit LerpTensorBackward(Tensor self, Tensor end, Tensor weight)
        : self_(self), end_(end), weight_(weight) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor end_sv = end_.unpack();
        const Tensor weight_sv = weight_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, neg(sub(weight_sv, 1)));
        grads.push_back(__grad_self);
        auto __grad_end = mul(grad, weight_sv);
        grads.push_back(__grad_end);
        auto __grad_weight = mul(grad, sub(end_sv, self_sv));
        grads.push_back(__grad_weight);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        end_.reset_data();
        weight_.reset_data();
    }
};

struct VarBackward : public Node {
    SavedVariable self_;
    int64_t correction_;

    explicit VarBackward(Tensor self, int64_t correction)
        : self_(self), correction_(correction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = div(mul(mul(grad, 2), sub(self_sv, mean(self_sv))), self_sv.numel() - correction_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct StdBackward : public Node {
    SavedVariable self_;
    int64_t correction_;
    SavedVariable result_;

    explicit StdBackward(Tensor self, int64_t correction, Tensor result)
        : self_(self), correction_(correction), result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = div(mul(grad, sub(self_sv, mean(self_sv))), mul(result_sv, self_sv.numel() - correction_));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        result_.reset_data();
    }
};

struct ScaledDotProductAttentionBackward : public Node {
    SavedVariable query_;
    SavedVariable key_;
    SavedVariable value_;
    bool is_causal_;
    int64_t impl_;

    explicit ScaledDotProductAttentionBackward(Tensor query, Tensor key, Tensor value, bool is_causal, int64_t impl)
        : query_(query), key_(key), value_(value), is_causal_(is_causal), impl_(impl) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor query_sv = query_.unpack();
        const Tensor key_sv = key_.unpack();
        const Tensor value_sv = value_.unpack();

        variable_list grads;
        auto __shared_0 = scaled_dot_product_attention_backward(grad, query_sv, key_sv, value_sv, is_causal_, impl_);
        auto __grad_query = std::get<0>(__shared_0);
        grads.push_back(__grad_query);
        auto __grad_key = std::get<1>(__shared_0);
        grads.push_back(__grad_key);
        auto __grad_value = std::get<2>(__shared_0);
        grads.push_back(__grad_value);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        query_.reset_data();
        key_.reset_data();
        value_.reset_data();
    }
};

struct GeluBackward : public Node {
    SavedVariable self_;
    std::string approximate_;

    explicit GeluBackward(Tensor self, std::string approximate)
        : self_(self), approximate_(approximate) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::gelu_backward(grad, self_sv, approximate_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct HardtanhBackward : public Node {
    SavedVariable self_;
    Scalar min_val_;
    Scalar max_val_;

    explicit HardtanhBackward(Tensor self, Scalar min_val, Scalar max_val)
        : self_(self), min_val_(min_val), max_val_(max_val) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = hardtanh_backward(grad, self_sv, min_val_, max_val_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct Relu6Backward : public Node {
    SavedVariable self_;

    explicit Relu6Backward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = hardtanh_backward(grad, self_sv, 0, 6);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct HardswishBackward : public Node {
    SavedVariable self_;

    explicit HardswishBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = hardswish_backward(grad, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct HardsigmoidBackward : public Node {
    SavedVariable self_;

    explicit HardsigmoidBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = hardsigmoid_backward(grad, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct LeakyReluBackward : public Node {
    SavedVariable self_;
    Scalar negative_slope_;

    explicit LeakyReluBackward(Tensor self, Scalar negative_slope)
        : self_(self), negative_slope_(negative_slope) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = leaky_relu_backward(grad, self_sv, negative_slope_, false);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct EluBackward : public Node {
    SavedVariable self_;
    Scalar alpha_;
    Scalar scale_;
    Scalar input_scale_;

    explicit EluBackward(Tensor self, Scalar alpha, Scalar scale, Scalar input_scale)
        : self_(self), alpha_(alpha), scale_(scale), input_scale_(input_scale) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = elu_backward(grad, alpha_, scale_, input_scale_, false, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct CeluBackward : public Node {
    SavedVariable self_;
    Scalar alpha_;

    explicit CeluBackward(Tensor self, Scalar alpha)
        : self_(self), alpha_(alpha) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = add(mul(grad, gt(self_sv, 0)), mul(mul(grad, le(self_sv, 0)), div(mul(exp(div(self_sv, alpha_)), alpha_), alpha_)));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct MishBackward : public Node {
    SavedVariable self_;

    explicit MishBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mish_backward(grad, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SeluBackward : public Node {
    SavedVariable self_;

    explicit SeluBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = add(mul(mul(grad, gt(self_sv, 0)), 1.0507009873554804934193349852946), mul(mul(grad, le(self_sv, 0)), exp(mul(mul(self_sv, 1.0507009873554804934193349852946), 1.6732632423543772848170429916717))));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SoftplusBackward : public Node {
    SavedVariable self_;
    Scalar beta_;
    Scalar threshold_;

    explicit SoftplusBackward(Tensor self, Scalar beta, Scalar threshold)
        : self_(self), beta_(beta), threshold_(threshold) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = softplus_backward(grad, self_sv, beta_, threshold_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct GluBackward : public Node {
    SavedVariable self_;
    int64_t dim_;

    explicit GluBackward(Tensor self, int64_t dim)
        : self_(self), dim_(dim) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = glu_backward(grad, self_sv, dim_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct UpsampleNearest1DBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> output_size_;
    std::optional<double> scales_;

    explicit UpsampleNearest1DBackward(Tensor self, std::vector<int64_t> output_size, std::optional<double> scales)
        : self_(self), output_size_(output_size), scales_(scales) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = upsample_nearest1d_backward(grad, output_size_, self_sv.shape(), scales_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct UpsampleNearest2DBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> output_size_;
    std::optional<double> scales_h_;
    std::optional<double> scales_w_;

    explicit UpsampleNearest2DBackward(Tensor self, std::vector<int64_t> output_size, std::optional<double> scales_h, std::optional<double> scales_w)
        : self_(self), output_size_(output_size), scales_h_(scales_h), scales_w_(scales_w) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = upsample_nearest2d_backward(grad, output_size_, self_sv.shape(), scales_h_, scales_w_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct UpsampleNearest3DBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> output_size_;
    std::optional<double> scales_d_;
    std::optional<double> scales_h_;
    std::optional<double> scales_w_;

    explicit UpsampleNearest3DBackward(Tensor self, std::vector<int64_t> output_size, std::optional<double> scales_d, std::optional<double> scales_h, std::optional<double> scales_w)
        : self_(self), output_size_(output_size), scales_d_(scales_d), scales_h_(scales_h), scales_w_(scales_w) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = upsample_nearest3d_backward(grad, output_size_, self_sv.shape(), scales_d_, scales_h_, scales_w_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct UpsampleLinear1DBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> output_size_;
    bool align_corners_;
    std::optional<double> scales_;

    explicit UpsampleLinear1DBackward(Tensor self, std::vector<int64_t> output_size, bool align_corners, std::optional<double> scales)
        : self_(self), output_size_(output_size), align_corners_(align_corners), scales_(scales) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = upsample_linear1d_backward(grad, output_size_, self_sv.shape(), align_corners_, scales_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct UpsampleBilinear2DBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> output_size_;
    bool align_corners_;
    std::optional<double> scales_h_;
    std::optional<double> scales_w_;

    explicit UpsampleBilinear2DBackward(Tensor self, std::vector<int64_t> output_size, bool align_corners, std::optional<double> scales_h, std::optional<double> scales_w)
        : self_(self), output_size_(output_size), align_corners_(align_corners), scales_h_(scales_h), scales_w_(scales_w) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = upsample_bilinear2d_backward(grad, output_size_, self_sv.shape(), align_corners_, scales_h_, scales_w_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct UpsampleBicubic2DBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> output_size_;
    bool align_corners_;
    std::optional<double> scales_h_;
    std::optional<double> scales_w_;

    explicit UpsampleBicubic2DBackward(Tensor self, std::vector<int64_t> output_size, bool align_corners, std::optional<double> scales_h, std::optional<double> scales_w)
        : self_(self), output_size_(output_size), align_corners_(align_corners), scales_h_(scales_h), scales_w_(scales_w) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = upsample_bicubic2d_backward(grad, output_size_, self_sv.shape(), align_corners_, scales_h_, scales_w_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct UpsampleTrilinear3DBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> output_size_;
    bool align_corners_;
    std::optional<double> scales_d_;
    std::optional<double> scales_h_;
    std::optional<double> scales_w_;

    explicit UpsampleTrilinear3DBackward(Tensor self, std::vector<int64_t> output_size, bool align_corners, std::optional<double> scales_d, std::optional<double> scales_h, std::optional<double> scales_w)
        : self_(self), output_size_(output_size), align_corners_(align_corners), scales_d_(scales_d), scales_h_(scales_h), scales_w_(scales_w) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = upsample_trilinear3d_backward(grad, output_size_, self_sv.shape(), align_corners_, scales_d_, scales_h_, scales_w_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct MaskedFillBackward : public Node {
    SavedVariable mask_;

    explicit MaskedFillBackward(Tensor mask)
        : mask_(mask) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor mask_sv = mask_.unpack();

        variable_list grads;
        auto __grad_self = grad.masked_fill(mask_sv, 0);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        mask_.reset_data();
    }
};

struct MaskedFillTensorBackward : public Node {
    SavedVariable mask_;

    explicit MaskedFillTensorBackward(Tensor mask)
        : mask_(mask) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor mask_sv = mask_.unpack();

        variable_list grads;
        auto __grad_self = grad.masked_fill(mask_sv, 0);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        mask_.reset_data();
    }
};

struct TrilBackward : public Node {
    int64_t diagonal_;

    explicit TrilBackward(int64_t diagonal)
        : diagonal_(diagonal) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad.tril(diagonal_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct TriuBackward : public Node {
    int64_t diagonal_;

    explicit TriuBackward(int64_t diagonal)
        : diagonal_(diagonal) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad.triu(diagonal_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct CumsumBackward : public Node {
    int64_t dim_;

    explicit CumsumBackward(int64_t dim)
        : dim_(dim) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::flip(tensorplay::tpx::ops::cumsum(tensorplay::tpx::ops::flip(grad, {dim_}), dim_), {dim_});
        grads.push_back(__grad_self);
        return grads;
    }
};

struct GatherBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    SavedVariable index_;

    explicit GatherBackward(Tensor self, int64_t dim, Tensor index)
        : self_(self), dim_(dim), index_(index) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor index_sv = index_.unpack();

        variable_list grads;
        auto __grad_self = zeros_like(self_sv).scatter_add(dim_, index_sv, grad);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        index_.reset_data();
    }
};

struct ScatterAddBackward : public Node {
    int64_t dim_;
    SavedVariable index_;

    explicit ScatterAddBackward(int64_t dim, Tensor index)
        : dim_(dim), index_(index) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor index_sv = index_.unpack();

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        auto __grad_src = grad.gather(dim_, index_sv);
        grads.push_back(__grad_src);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        index_.reset_data();
    }
};

struct IndexSelectBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    SavedVariable index_;

    explicit IndexSelectBackward(Tensor self, int64_t dim, Tensor index)
        : self_(self), dim_(dim), index_(index) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor index_sv = index_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::index_add(tensorplay::tpx::ops::zeros_like(self_sv), dim_, index_sv, grad);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        index_.reset_data();
    }
};

struct IndexAddBackward : public Node {
    int64_t dim_;
    SavedVariable index_;

    explicit IndexAddBackward(int64_t dim, Tensor index)
        : dim_(dim), index_(index) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor index_sv = index_.unpack();

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        auto __grad_source = tensorplay::tpx::ops::index_select(grad, dim_, index_sv);
        grads.push_back(__grad_source);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        index_.reset_data();
    }
};

struct NegativeBackward : public Node {

    explicit NegativeBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = neg(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct PositiveBackward : public Node {

    explicit PositiveBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        return grads;
    }
};

struct ReciprocalBackward : public Node {
    SavedVariable result_;

    explicit ReciprocalBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(neg(grad), mul(result_sv, result_sv).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct Exp2Backward : public Node {
    SavedVariable result_;

    explicit Exp2Backward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, result_sv.conj() * 0.6931471805599453);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct LogaddexpBackward : public Node {
    SavedVariable self_;
    SavedVariable other_;

    explicit LogaddexpBackward(Tensor self, Tensor other)
        : self_(self), other_(other) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, sigmoid(sub(self_sv, other_sv)));
        grads.push_back(__grad_self);
        auto __grad_other = mul(grad, sigmoid(sub(other_sv, self_sv)));
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
    }
};

struct HypotBackward : public Node {
    SavedVariable self_;
    SavedVariable other_;
    SavedVariable result_;

    explicit HypotBackward(Tensor self, Tensor other, Tensor result)
        : self_(self), other_(other), result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor other_sv = other_.unpack();
        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = div(mul(grad, self_sv), result_sv);
        grads.push_back(__grad_self);
        auto __grad_other = div(mul(grad, other_sv), result_sv);
        grads.push_back(__grad_other);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        other_.reset_data();
        result_.reset_data();
    }
};

struct Deg2RadBackward : public Node {

    explicit Deg2RadBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = mul(grad, 0.01745329251994329577);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct Rad2DegBackward : public Node {

    explicit Rad2DegBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = mul(grad, 57.29577951308232087680);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct HardshrinkBackward : public Node {
    SavedVariable self_;
    Scalar lambd_;

    explicit HardshrinkBackward(Tensor self, Scalar lambd)
        : self_(self), lambd_(lambd) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, relu(sign(sub(abs(self_sv), lambd_))));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SoftshrinkBackward : public Node {
    SavedVariable self_;
    Scalar lambd_;

    explicit SoftshrinkBackward(Tensor self, Scalar lambd)
        : self_(self), lambd_(lambd) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(mul(grad, sign(self_sv)), relu(sign(sub(abs(self_sv), lambd_))));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ThresholdBackward : public Node {
    SavedVariable self_;
    Scalar threshold_;

    explicit ThresholdBackward(Tensor self, Scalar threshold)
        : self_(self), threshold_(threshold) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = threshold_backward(grad, self_sv, threshold_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct FlipBackward : public Node {
    std::vector<int64_t> dims_;

    explicit FlipBackward(std::vector<int64_t> dims)
        : dims_(dims) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::flip(grad, dims_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct PixelShuffleBackward : public Node {
    int64_t upscale_factor_;

    explicit PixelShuffleBackward(int64_t upscale_factor)
        : upscale_factor_(upscale_factor) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::pixel_unshuffle(grad, upscale_factor_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct PixelUnshuffleBackward : public Node {
    int64_t downscale_factor_;

    explicit PixelUnshuffleBackward(int64_t downscale_factor)
        : downscale_factor_(downscale_factor) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::pixel_shuffle(grad, downscale_factor_);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct LogsumexpBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    SavedVariable result_;

    explicit LogsumexpBackward(Tensor self, int64_t dim, Tensor result)
        : self_(self), dim_(dim), result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, exp(sub(self_sv, unsqueeze(result_sv, dim_))));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        result_.reset_data();
    }
};

struct ConjBackward : public Node {

    explicit ConjBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad.conj();
        grads.push_back(__grad_self);
        return grads;
    }
};

struct AdjointBackward : public Node {

    explicit AdjointBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad.adjoint();
        grads.push_back(__grad_self);
        return grads;
    }
};

struct PolarBackward : public Node {
    SavedVariable angle_;

    explicit PolarBackward(Tensor angle)
        : angle_(angle) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor angle_sv = angle_.unpack();

        variable_list grads;
        grads.push_back(Tensor());
        auto __grad_angle = neg(mul(mul(grad, abs_), sin(angle_sv)));
        grads.push_back(__grad_angle);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        angle_.reset_data();
    }
};

struct TpL1LossBackward : public Node {
    SavedVariable input_;
    SavedVariable target_;
    int64_t reduction_;

    explicit TpL1LossBackward(Tensor input, Tensor target, int64_t reduction)
        : input_(input), target_(target), reduction_(reduction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_input = tp_l1_loss_backward(grad, input_sv, target_sv, reduction_);
        grads.push_back(__grad_input);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        target_.reset_data();
    }
};

struct TpSmoothL1LossBackward : public Node {
    SavedVariable input_;
    SavedVariable target_;
    int64_t reduction_;
    double beta_;

    explicit TpSmoothL1LossBackward(Tensor input, Tensor target, int64_t reduction, double beta)
        : input_(input), target_(target), reduction_(reduction), beta_(beta) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_input = tp_smooth_l1_loss_backward(grad, input_sv, target_sv, reduction_, beta_);
        grads.push_back(__grad_input);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        target_.reset_data();
    }
};

struct TpHuberLossBackward : public Node {
    SavedVariable input_;
    SavedVariable target_;
    int64_t reduction_;
    double delta_;

    explicit TpHuberLossBackward(Tensor input, Tensor target, int64_t reduction, double delta)
        : input_(input), target_(target), reduction_(reduction), delta_(delta) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_input = tp_huber_loss_backward(grad, input_sv, target_sv, reduction_, delta_);
        grads.push_back(__grad_input);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        target_.reset_data();
    }
};

struct TpKlDivBackward : public Node {
    SavedVariable input_;
    SavedVariable target_;
    int64_t reduction_;
    bool log_target_;

    explicit TpKlDivBackward(Tensor input, Tensor target, int64_t reduction, bool log_target)
        : input_(input), target_(target), reduction_(reduction), log_target_(log_target) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_input = tp_kl_div_backward(grad, input_sv, target_sv, reduction_, log_target_);
        grads.push_back(__grad_input);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        target_.reset_data();
    }
};

struct TpBinaryCrossEntropyBackward : public Node {
    SavedVariable input_;
    SavedVariable target_;
    std::optional<Tensor> weight_;
    int64_t reduction_;

    explicit TpBinaryCrossEntropyBackward(Tensor input, Tensor target, std::optional<Tensor> weight, int64_t reduction)
        : input_(input), target_(target), weight_(weight), reduction_(reduction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_input = tp_binary_cross_entropy_backward(grad, input_sv, target_sv, weight_, reduction_);
        grads.push_back(__grad_input);
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        target_.reset_data();
    }
};

struct TpMarginRankingLossBackward : public Node {
    SavedVariable input1_;
    SavedVariable input2_;
    SavedVariable target_;
    double margin_;
    int64_t reduction_;

    explicit TpMarginRankingLossBackward(Tensor input1, Tensor input2, Tensor target, double margin, int64_t reduction)
        : input1_(input1), input2_(input2), target_(target), margin_(margin), reduction_(reduction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input1_sv = input1_.unpack();
        const Tensor input2_sv = input2_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __shared_0 = tp_margin_ranking_loss_backward(grad, input1_sv, input2_sv, target_sv, margin_, reduction_);
        auto __grad_input1 = std::get<0>(__shared_0);
        grads.push_back(__grad_input1);
        auto __grad_input2 = std::get<1>(__shared_0);
        grads.push_back(__grad_input2);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input1_.reset_data();
        input2_.reset_data();
        target_.reset_data();
    }
};

struct TpHingeEmbeddingLossBackward : public Node {
    SavedVariable input_;
    SavedVariable target_;
    double margin_;
    int64_t reduction_;

    explicit TpHingeEmbeddingLossBackward(Tensor input, Tensor target, double margin, int64_t reduction)
        : input_(input), target_(target), margin_(margin), reduction_(reduction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_input = tp_hinge_embedding_loss_backward(grad, input_sv, target_sv, margin_, reduction_);
        grads.push_back(__grad_input);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        target_.reset_data();
    }
};

struct TpSoftMarginLossBackward : public Node {
    SavedVariable input_;
    SavedVariable target_;
    int64_t reduction_;

    explicit TpSoftMarginLossBackward(Tensor input, Tensor target, int64_t reduction)
        : input_(input), target_(target), reduction_(reduction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_input = tp_soft_margin_loss_backward(grad, input_sv, target_sv, reduction_);
        grads.push_back(__grad_input);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        target_.reset_data();
    }
};

struct TpCosineEmbeddingLossBackward : public Node {
    SavedVariable input1_;
    SavedVariable input2_;
    SavedVariable target_;
    double margin_;
    int64_t reduction_;

    explicit TpCosineEmbeddingLossBackward(Tensor input1, Tensor input2, Tensor target, double margin, int64_t reduction)
        : input1_(input1), input2_(input2), target_(target), margin_(margin), reduction_(reduction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input1_sv = input1_.unpack();
        const Tensor input2_sv = input2_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __shared_0 = tp_cosine_embedding_loss_backward(grad, input1_sv, input2_sv, target_sv, margin_, reduction_);
        auto __grad_input1 = std::get<0>(__shared_0);
        grads.push_back(__grad_input1);
        auto __grad_input2 = std::get<1>(__shared_0);
        grads.push_back(__grad_input2);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input1_.reset_data();
        input2_.reset_data();
        target_.reset_data();
    }
};

struct TpPoissonNllLossBackward : public Node {
    SavedVariable input_;
    SavedVariable target_;
    bool log_input_;
    bool full_;
    double eps_;
    int64_t reduction_;

    explicit TpPoissonNllLossBackward(Tensor input, Tensor target, bool log_input, bool full, double eps, int64_t reduction)
        : input_(input), target_(target), log_input_(log_input), full_(full), eps_(eps), reduction_(reduction) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor input_sv = input_.unpack();
        const Tensor target_sv = target_.unpack();

        variable_list grads;
        auto __grad_input = tp_poisson_nll_loss_backward(grad, input_sv, target_sv, log_input_, full_, eps_, reduction_);
        grads.push_back(__grad_input);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        input_.reset_data();
        target_.reset_data();
    }
};

struct FftFftBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    std::string norm_;

    explicit FftFftBackward(Tensor self, int64_t dim, std::string norm)
        : self_(self), dim_(dim), norm_(norm) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::fft_fft_backward(grad, self_sv, dim_, norm_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct FftIfftBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    std::string norm_;

    explicit FftIfftBackward(Tensor self, int64_t dim, std::string norm)
        : self_(self), dim_(dim), norm_(norm) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::fft_ifft_backward(grad, self_sv, dim_, norm_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct FftRfftBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    std::string norm_;

    explicit FftRfftBackward(Tensor self, int64_t dim, std::string norm)
        : self_(self), dim_(dim), norm_(norm) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::fft_rfft_backward(grad, self_sv, dim_, norm_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct FftIrfftBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    std::string norm_;

    explicit FftIrfftBackward(Tensor self, int64_t dim, std::string norm)
        : self_(self), dim_(dim), norm_(norm) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::fft_irfft_backward(grad, self_sv, dim_, norm_);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct StftBackward : public Node {
    SavedVariable self_;
    int64_t n_fft_;
    std::optional<int64_t> hop_length_;
    std::optional<int64_t> win_length_;
    std::optional<Tensor> window_;
    bool center_;
    std::string pad_mode_;
    bool normalized_;
    bool onesided_;

    explicit StftBackward(Tensor self, int64_t n_fft, std::optional<int64_t> hop_length, std::optional<int64_t> win_length, std::optional<Tensor> window, bool center, std::string pad_mode, bool normalized, bool onesided)
        : self_(self), n_fft_(n_fft), hop_length_(hop_length), win_length_(win_length), window_(window), center_(center), pad_mode_(pad_mode), normalized_(normalized), onesided_(onesided) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::stft_backward(grad, self_sv, n_fft_, hop_length_, win_length_, window_, center_, pad_mode_, normalized_, onesided_);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct Log10Backward : public Node {
    SavedVariable self_;

    explicit Log10Backward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(div(grad, self_sv), 0.43429448190325176);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct Log1PBackward : public Node {
    SavedVariable self_;

    explicit Log1PBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = log1p_backward(grad, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct FracBackward : public Node {

    explicit FracBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = grad;
        grads.push_back(__grad_self);
        return grads;
    }
};

struct DiffBackward : public Node {

    explicit DiffBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::neg(grad);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        return grads;
    }
};

struct ToDenseBackward : public Node {
    SavedVariable self_;

    explicit ToDenseBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = grad.sparse_mask(self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct SinhBackward : public Node {
    SavedVariable self_;

    explicit SinhBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, self_sv.cosh().conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct AcosBackward : public Node {
    SavedVariable self_;

    explicit AcosBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, -add(mul(neg(self_sv), self_sv), 1).rsqrt().conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct AsinBackward : public Node {
    SavedVariable self_;

    explicit AsinBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, add(mul(neg(self_sv), self_sv), 1).rsqrt().conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct AtanBackward : public Node {
    SavedVariable self_;

    explicit AtanBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = div(grad, add(mul(self_sv, self_sv), 1).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct TanBackward : public Node {
    SavedVariable result_;

    explicit TanBackward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, 1 + pow(result_sv, 2).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct ErfBackward : public Node {
    SavedVariable self_;

    explicit ErfBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(mul(exp(neg(pow(self_sv, 2))), 1.1283791670205586), grad);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ErfcBackward : public Node {
    SavedVariable self_;

    explicit ErfcBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(mul(exp(neg(pow(self_sv, 2))), -1.1283791670205586), grad);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct LgammaBackward : public Node {
    SavedVariable self_;

    explicit LgammaBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, digamma(self_sv));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct Log2Backward : public Node {
    SavedVariable self_;

    explicit Log2Backward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = div(grad, self_sv.conj() * 0.6931471805599453);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct CoshBackward : public Node {
    SavedVariable self_;

    explicit CoshBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, self_sv.sinh().conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct AsinhBackward : public Node {
    SavedVariable self_;

    explicit AsinhBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, add(pow(self_sv, 2), 1).rsqrt().conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct AcoshBackward : public Node {
    SavedVariable self_;

    explicit AcoshBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = acosh_backward(grad, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct AtanhBackward : public Node {
    SavedVariable self_;

    explicit AtanhBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = div(mul(grad, 1), neg(sub(pow(self_sv, 2), 1)).conj());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct LogitBackward : public Node {
    SavedVariable self_;

    explicit LogitBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = div(grad, mul(self_sv, neg(sub(self_sv, 1))));
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct Expm1Backward : public Node {
    SavedVariable result_;

    explicit Expm1Backward(Tensor result)
        : result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = mul(grad, result_sv.conj() + 1);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        result_.reset_data();
    }
};

struct AngleBackward : public Node {
    SavedVariable self_;

    explicit AngleBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = angle_backward(grad, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct ComplexBackward : public Node {

    explicit ComplexBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_real = grad.real();
        grads.push_back(__grad_real);
        auto __grad_imag = grad.imag();
        grads.push_back(__grad_imag);
        return grads;
    }
};

struct PowTensorTensorBackward : public Node {
    SavedVariable self_;
    SavedVariable exponent_;
    SavedVariable result_;

    explicit PowTensorTensorBackward(Tensor self, Tensor exponent, Tensor result)
        : self_(self), exponent_(exponent), result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor exponent_sv = exponent_.unpack();
        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = pow_backward_self(grad, self_sv, exponent_sv);
        grads.push_back(__grad_self);
        auto __grad_exponent = pow_backward_exponent(grad, self_sv, exponent_sv, result_sv);
        grads.push_back(__grad_exponent);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        exponent_.reset_data();
        result_.reset_data();
    }
};

struct ProdBackward : public Node {
    SavedVariable self_;
    SavedVariable result_;

    explicit ProdBackward(Tensor self, Tensor result)
        : self_(self), result_(result) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor result_sv = result_.unpack();

        variable_list grads;
        auto __grad_self = prod_backward_fast(grad, self_sv, result_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        result_.reset_data();
    }
};

struct ExpandBackward : public Node {
    SavedVariable self_;

    explicit ExpandBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = sum_to_size(grad, self_sv.shape());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct RepeatBackward : public Node {
    SavedVariable self_;
    std::vector<int64_t> repeats_;

    explicit RepeatBackward(Tensor self, std::vector<int64_t> repeats)
        : self_(self), repeats_(repeats) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = repeat_backward(grad, repeats_, self_sv.shape());
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct FillScalarBackward : public Node {

    explicit FillScalarBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::zeros_like(grad);
        grads.push_back(__grad_self);
        return grads;
    }
};

struct FillTensorBackward : public Node {

    explicit FillTensorBackward()
        {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_self = tensorplay::tpx::ops::zeros_like(grad);
        grads.push_back(__grad_self);
        auto __grad_value = sum(grad);
        grads.push_back(__grad_value);
        return grads;
    }
};

struct TrapezoidBackward : public Node {
    SavedVariable y_;
    std::optional<Tensor> x_;
    Scalar dx_;
    int64_t dim_;

    explicit TrapezoidBackward(Tensor y, std::optional<Tensor> x, Scalar dx, int64_t dim)
        : y_(y), x_(x), dx_(dx), dim_(dim) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor y_sv = y_.unpack();

        variable_list grads;
        auto __grad_y = _trapezoid_backward(grad, x_, y_sv.shape(), dx_, dim_);
        grads.push_back(__grad_y);
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        y_.reset_data();
    }
};

struct CumulativeTrapezoidBackward : public Node {
    std::optional<Tensor> x_;
    Scalar dx_;
    int64_t dim_;

    explicit CumulativeTrapezoidBackward(std::optional<Tensor> x, Scalar dx, int64_t dim)
        : x_(x), dx_(dx), dim_(dim) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        variable_list grads;
        auto __grad_y = _cumulative_trapezoid_backward(grad, x_, dx_, dim_);
        grads.push_back(__grad_y);
        grads.push_back(Tensor());
        return grads;
    }
};

struct ScatterReduceBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    SavedVariable index_;
    SavedVariable src_;
    std::string reduce_;
    bool include_self_;

    explicit ScatterReduceBackward(Tensor self, int64_t dim, Tensor index, Tensor src, std::string reduce, bool include_self)
        : self_(self), dim_(dim), index_(index), src_(src), reduce_(reduce), include_self_(include_self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor index_sv = index_.unpack();
        const Tensor src_sv = src_.unpack();

        variable_list grads;
        auto __grad_self = _scatter_reduce_backward_self(grad, self_sv, dim_, index_sv, src_sv, reduce_, include_self_);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        auto __grad_src = _scatter_reduce_backward_src(grad, self_sv, dim_, index_sv, src_sv, reduce_, include_self_);
        grads.push_back(__grad_src);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        index_.reset_data();
        src_.reset_data();
    }
};

struct IndexReduceBackward : public Node {
    SavedVariable self_;
    int64_t dim_;
    SavedVariable index_;
    SavedVariable source_;
    std::string reduce_;
    bool include_self_;

    explicit IndexReduceBackward(Tensor self, int64_t dim, Tensor index, Tensor source, std::string reduce, bool include_self)
        : self_(self), dim_(dim), index_(index), source_(source), reduce_(reduce), include_self_(include_self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();
        const Tensor index_sv = index_.unpack();
        const Tensor source_sv = source_.unpack();

        variable_list grads;
        auto __grad_self = _index_reduce_backward_self(grad, self_sv, dim_, index_sv, source_sv, reduce_, include_self_);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        auto __grad_source = _index_reduce_backward_src(grad, self_sv, dim_, index_sv, source_sv, reduce_, include_self_);
        grads.push_back(__grad_source);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
        index_.reset_data();
        source_.reset_data();
    }
};

struct CovBackward : public Node {
    SavedVariable self_;
    int64_t correction_;
    std::optional<Tensor> fweights_;
    std::optional<Tensor> aweights_;

    explicit CovBackward(Tensor self, int64_t correction, std::optional<Tensor> fweights, std::optional<Tensor> aweights)
        : self_(self), correction_(correction), fweights_(fweights), aweights_(aweights) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor(), Tensor(), Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = _cov_backward(grad, self_sv, correction_, fweights_, aweights_);
        grads.push_back(__grad_self);
        grads.push_back(Tensor());
        grads.push_back(Tensor());
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

struct CorrcoefBackward : public Node {
    SavedVariable self_;

    explicit CorrcoefBackward(Tensor self)
        : self_(self) {}

    variable_list apply(variable_list&& inputs) override {
        if (inputs.empty() || !inputs[0].defined()) return {Tensor()};
        const Tensor& grad = inputs[0];

        const Tensor self_sv = self_.unpack();

        variable_list grads;
        auto __grad_self = _corrcoef_backward(grad, self_sv);
        grads.push_back(__grad_self);
        return grads;
    }

    void release_variables() override {
        Node::release_variables();
        self_.reset_data();
    }
};

} // namespace tpx
} // namespace tensorplay
