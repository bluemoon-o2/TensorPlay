// Generated by tools/codegen/main.py -- DO NOT EDIT
#pragma once
#include <tuple>

static Tensor embedding(const Tensor& weight, const Tensor& indices, int64_t padding_idx = -1, bool scale_grad_by_freq = false, bool sparse = false);

static Tensor embedding_dense_backward(const Tensor& grad_output, const Tensor& indices, int64_t num_weights, int64_t padding_idx, bool scale_grad_by_freq);

static Tensor conv1d(const Tensor& input, const Tensor& weight, const std::optional<Tensor>& bias = std::nullopt, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& dilation = {}, int64_t groups = 1);

static Tensor conv1d_grad_input(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv1d_grad_weight(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv1d_grad_bias(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv2d(const Tensor& input, const Tensor& weight, const std::optional<Tensor>& bias = std::nullopt, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& dilation = {}, int64_t groups = 1);

static Tensor conv2d_relu(const Tensor& input, const Tensor& weight, const std::optional<Tensor>& bias = std::nullopt, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& dilation = {}, int64_t groups = 1);

static Tensor add_relu(const Tensor& self, const Tensor& other);

static Tensor conv2d_grad_input(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv2d_grad_weight(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv2d_grad_bias(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv3d(const Tensor& input, const Tensor& weight, const std::optional<Tensor>& bias = std::nullopt, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& dilation = {}, int64_t groups = 1);

static Tensor conv3d_grad_input(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv3d_grad_weight(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv3d_grad_bias(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& dilation, int64_t groups);

static Tensor conv_transpose2d(const Tensor& input, const Tensor& weight, const std::optional<Tensor>& bias = std::nullopt, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& output_padding = {}, int64_t groups = 1, const std::vector<int64_t>& dilation = {});

static Tensor conv_transpose2d_grad_input(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

static Tensor conv_transpose2d_grad_weight(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

static Tensor conv_transpose2d_grad_bias(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

static Tensor conv_transpose3d(const Tensor& input, const Tensor& weight, const std::optional<Tensor>& bias = std::nullopt, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& output_padding = {}, int64_t groups = 1, const std::vector<int64_t>& dilation = {});

static Tensor conv_transpose3d_grad_input(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

static Tensor constant_pad_nd(const Tensor& self, const std::vector<int64_t>& pad, Scalar value);

static Tensor constant_pad_nd_backward(const Tensor& grad_output, const std::vector<int64_t>& pad);

static Tensor conv_transpose3d_grad_weight(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

static Tensor conv_transpose3d_grad_bias(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

Tensor add(const Tensor& other, Scalar alpha = 1) const;

Tensor add(Scalar other, Scalar alpha = 1) const;

Tensor& add_(const Tensor& other, Scalar alpha = 1);

Tensor& add_(Scalar other, Scalar alpha = 1);

Tensor sub(const Tensor& other, Scalar alpha = 1) const;

Tensor sub(Scalar other, Scalar alpha = 1) const;

Tensor& sub_(const Tensor& other, Scalar alpha = 1);

Tensor& sub_(Scalar other, Scalar alpha = 1);

Tensor mul(const Tensor& other) const;

Tensor mul(Scalar other) const;

Tensor& mul_(const Tensor& other);

Tensor& mul_(Scalar other);

Tensor div(const Tensor& other) const;

static Tensor fused_mul_add(const Tensor& self, const Tensor& other, const Tensor& addend);

static Tensor fused_mul_add(const Tensor& self, Scalar other, Scalar addend);

static std::vector<Tensor> _foreach_sgd(const std::vector<Tensor>& params, const std::vector<Tensor>& grads, const std::vector<Tensor>& momentum_buffers, double lr, double momentum, double dampening, double weight_decay, bool nesterov, bool first_momentum_step);

static std::vector<Tensor> _foreach_adam(const std::vector<Tensor>& params, const std::vector<Tensor>& grads, const std::vector<Tensor>& exp_avgs, const std::vector<Tensor>& exp_avg_sqs, const std::vector<Tensor>& max_exp_avg_sqs, const std::vector<int64_t>& steps, double lr, double beta1, double beta2, double eps, double weight_decay, bool amsgrad);

static void _amp_foreach_non_finite_check_and_unscale_(std::vector<Tensor> self, Tensor& found_inf, const Tensor& inv_scale);

static Tensor& _amp_update_scale_(Tensor& self, const Tensor& growth_tracker, const Tensor& found_inf, double growth_factor, double backoff_factor, int64_t growth_interval);

static void _fused_adam_(std::vector<Tensor> self, std::vector<Tensor> grads, std::vector<Tensor> exp_avgs, std::vector<Tensor> exp_avg_sqs, std::vector<Tensor> max_exp_avg_sqs, const std::vector<Tensor>& state_steps, double lr, double beta1, double beta2, double weight_decay, double eps, bool amsgrad, bool maximize, const std::optional<Tensor>& grad_scale = std::nullopt, const std::optional<Tensor>& found_inf = std::nullopt);

static void _fused_adam_(std::vector<Tensor> self, std::vector<Tensor> grads, std::vector<Tensor> exp_avgs, std::vector<Tensor> exp_avg_sqs, std::vector<Tensor> max_exp_avg_sqs, const std::vector<Tensor>& state_steps, const Tensor& lr, double beta1, double beta2, double weight_decay, double eps, bool amsgrad, bool maximize, const std::optional<Tensor>& grad_scale = std::nullopt, const std::optional<Tensor>& found_inf = std::nullopt);

static void _fused_adamw_(std::vector<Tensor> self, std::vector<Tensor> grads, std::vector<Tensor> exp_avgs, std::vector<Tensor> exp_avg_sqs, std::vector<Tensor> max_exp_avg_sqs, const std::vector<Tensor>& state_steps, double lr, double beta1, double beta2, double weight_decay, double eps, bool amsgrad, bool maximize, const std::optional<Tensor>& grad_scale = std::nullopt, const std::optional<Tensor>& found_inf = std::nullopt);

static void _fused_adamw_(std::vector<Tensor> self, std::vector<Tensor> grads, std::vector<Tensor> exp_avgs, std::vector<Tensor> exp_avg_sqs, std::vector<Tensor> max_exp_avg_sqs, const std::vector<Tensor>& state_steps, const Tensor& lr, double beta1, double beta2, double weight_decay, double eps, bool amsgrad, bool maximize, const std::optional<Tensor>& grad_scale = std::nullopt, const std::optional<Tensor>& found_inf = std::nullopt);

static void _fused_sgd_(std::vector<Tensor> self, std::vector<Tensor> grads, std::vector<Tensor> momentum_buffer_list, double weight_decay, double momentum, double lr, double dampening, bool nesterov, bool maximize, bool is_first_step, const std::optional<Tensor>& grad_scale = std::nullopt, const std::optional<Tensor>& found_inf = std::nullopt);

static void _fused_sgd_(std::vector<Tensor> self, std::vector<Tensor> grads, std::vector<Tensor> momentum_buffer_list, double weight_decay, double momentum, const Tensor& lr, double dampening, bool nesterov, bool maximize, bool is_first_step, const std::optional<Tensor>& grad_scale = std::nullopt, const std::optional<Tensor>& found_inf = std::nullopt);

static void _fused_adagrad_(std::vector<Tensor> self, std::vector<Tensor> grads, std::vector<Tensor> state_sums, std::vector<Tensor> state_steps, double lr, double lr_decay, double weight_decay, double eps, bool maximize, const std::optional<Tensor>& grad_scale = std::nullopt, const std::optional<Tensor>& found_inf = std::nullopt);

static void _fused_adagrad_(std::vector<Tensor> self, std::vector<Tensor> grads, std::vector<Tensor> state_sums, std::vector<Tensor> state_steps, const Tensor& lr, double lr_decay, double weight_decay, double eps, bool maximize, const std::optional<Tensor>& grad_scale = std::nullopt, const std::optional<Tensor>& found_inf = std::nullopt);

static std::vector<Tensor> _foreach_add(const std::vector<Tensor>& self, Scalar scalar);

static std::vector<Tensor> _foreach_add(const std::vector<Tensor>& self, const std::vector<Tensor>& other, Scalar alpha = 1);

static std::vector<Tensor> _foreach_add(const std::vector<Tensor>& self, const std::vector<Scalar>& scalars);

static std::vector<Tensor> _foreach_add(const std::vector<Tensor>& self, const Tensor& other, Scalar alpha = 1);

static void _foreach_add_(std::vector<Tensor> self, Scalar scalar);

static void _foreach_add_(std::vector<Tensor> self, const std::vector<Tensor>& other, Scalar alpha = 1);

static void _foreach_add_(std::vector<Tensor> self, const std::vector<Scalar>& scalars);

static void _foreach_add_(std::vector<Tensor> self, const Tensor& other, Scalar alpha = 1);

static std::vector<Tensor> _foreach_sub(const std::vector<Tensor>& self, Scalar scalar);

static std::vector<Tensor> _foreach_sub(const std::vector<Tensor>& self, const std::vector<Tensor>& other, Scalar alpha = 1);

static std::vector<Tensor> _foreach_sub(const std::vector<Tensor>& self, const std::vector<Scalar>& scalars);

static std::vector<Tensor> _foreach_sub(const std::vector<Tensor>& self, const Tensor& other, Scalar alpha = 1);

static void _foreach_sub_(std::vector<Tensor> self, Scalar scalar);

static void _foreach_sub_(std::vector<Tensor> self, const std::vector<Tensor>& other, Scalar alpha = 1);

static void _foreach_sub_(std::vector<Tensor> self, const std::vector<Scalar>& scalars);

static void _foreach_sub_(std::vector<Tensor> self, const Tensor& other, Scalar alpha = 1);

static std::vector<Tensor> _foreach_mul(const std::vector<Tensor>& self, Scalar scalar);

static std::vector<Tensor> _foreach_mul(const std::vector<Tensor>& self, const std::vector<Tensor>& other);

static std::vector<Tensor> _foreach_mul(const std::vector<Tensor>& self, const std::vector<Scalar>& scalars);

static std::vector<Tensor> _foreach_mul(const std::vector<Tensor>& self, const Tensor& other);

static void _foreach_mul_(std::vector<Tensor> self, Scalar scalar);

static void _foreach_mul_(std::vector<Tensor> self, const std::vector<Tensor>& other);

static void _foreach_mul_(std::vector<Tensor> self, const std::vector<Scalar>& scalars);

static void _foreach_mul_(std::vector<Tensor> self, const Tensor& other);

static std::vector<Tensor> _foreach_div(const std::vector<Tensor>& self, Scalar scalar);

static std::vector<Tensor> _foreach_div(const std::vector<Tensor>& self, const std::vector<Tensor>& other);

static std::vector<Tensor> _foreach_div(const std::vector<Tensor>& self, const std::vector<Scalar>& scalars);

static std::vector<Tensor> _foreach_div(const std::vector<Tensor>& self, const Tensor& other);

static void _foreach_div_(std::vector<Tensor> self, Scalar scalar);

static void _foreach_div_(std::vector<Tensor> self, const std::vector<Tensor>& other);

static void _foreach_div_(std::vector<Tensor> self, const std::vector<Scalar>& scalars);

static void _foreach_div_(std::vector<Tensor> self, const Tensor& other);

static std::vector<Tensor> _foreach_addcmul(const std::vector<Tensor>& self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, Scalar value = 1);

static void _foreach_addcmul_(std::vector<Tensor> self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, Scalar value = 1);

static std::vector<Tensor> _foreach_addcmul(const std::vector<Tensor>& self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, const std::vector<Scalar>& scalars);

static void _foreach_addcmul_(std::vector<Tensor> self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, const std::vector<Scalar>& scalars);

static std::vector<Tensor> _foreach_addcmul(const std::vector<Tensor>& self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, const Tensor& scalars);

static void _foreach_addcmul_(std::vector<Tensor> self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, const Tensor& scalars);

static std::vector<Tensor> _foreach_addcdiv(const std::vector<Tensor>& self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, Scalar value = 1);

static void _foreach_addcdiv_(std::vector<Tensor> self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, Scalar value = 1);

static std::vector<Tensor> _foreach_addcdiv(const std::vector<Tensor>& self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, const std::vector<Scalar>& scalars);

static void _foreach_addcdiv_(std::vector<Tensor> self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, const std::vector<Scalar>& scalars);

static std::vector<Tensor> _foreach_addcdiv(const std::vector<Tensor>& self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, const Tensor& scalars);

static void _foreach_addcdiv_(std::vector<Tensor> self, const std::vector<Tensor>& tensor1, const std::vector<Tensor>& tensor2, const Tensor& scalars);

static std::vector<Tensor> _foreach_lerp(const std::vector<Tensor>& self, const std::vector<Tensor>& tensors1, Scalar weight);

static std::vector<Tensor> _foreach_lerp(const std::vector<Tensor>& self, const std::vector<Tensor>& tensors1, const std::vector<Tensor>& weights);

static void _foreach_lerp_(std::vector<Tensor> self, const std::vector<Tensor>& tensors1, Scalar weight);

static void _foreach_lerp_(std::vector<Tensor> self, const std::vector<Tensor>& tensors1, const std::vector<Tensor>& weights);

static std::vector<Tensor> _foreach_lerp(const std::vector<Tensor>& self, const std::vector<Tensor>& tensors1, const std::vector<Scalar>& weight);

static void _foreach_lerp_(std::vector<Tensor> self, const std::vector<Tensor>& tensors1, const std::vector<Scalar>& weight);

static std::vector<Tensor> _foreach_sqrt(const std::vector<Tensor>& self);

static void _foreach_sqrt_(std::vector<Tensor> self);

static std::vector<Tensor> _foreach_rsqrt(const std::vector<Tensor>& self);

static void _foreach_rsqrt_(std::vector<Tensor> self);

static std::vector<Tensor> _foreach_neg(const std::vector<Tensor>& self);

static void _foreach_neg_(std::vector<Tensor> self);

static std::vector<Tensor> _foreach_abs(const std::vector<Tensor>& self);

static void _foreach_abs_(std::vector<Tensor> self);

static std::vector<Tensor> _foreach_reciprocal(const std::vector<Tensor>& self);

static void _foreach_reciprocal_(std::vector<Tensor> self);

static std::vector<Tensor> _foreach_sign(const std::vector<Tensor>& self);

static void _foreach_sign_(std::vector<Tensor> self);

static std::vector<Tensor> _foreach_pow(const std::vector<Tensor>& self, Scalar exponent);

static std::vector<Tensor> _foreach_pow(Scalar self, const std::vector<Tensor>& exponent);

static std::vector<Tensor> _foreach_pow(const std::vector<Tensor>& self, const std::vector<Tensor>& exponent);

static void _foreach_pow_(std::vector<Tensor> self, Scalar exponent);

static void _foreach_pow_(std::vector<Tensor> self, const std::vector<Tensor>& exponent);

static std::vector<Tensor> _foreach_pow(const std::vector<Tensor>& self, const std::vector<Scalar>& exponent);

static void _foreach_pow_(std::vector<Tensor> self, const std::vector<Scalar>& exponent);

static std::vector<Tensor> _foreach_clamp_min(const std::vector<Tensor>& self, Scalar scalar);

static std::vector<Tensor> _foreach_clamp_max(const std::vector<Tensor>& self, Scalar scalar);

static std::vector<Tensor> _foreach_clamp_min(const std::vector<Tensor>& self, const std::vector<Tensor>& other);

static void _foreach_clamp_min_(std::vector<Tensor> self, const std::vector<Tensor>& other);

static std::vector<Tensor> _foreach_clamp_min(const std::vector<Tensor>& self, const std::vector<Scalar>& scalars);

static void _foreach_clamp_min_(std::vector<Tensor> self, const std::vector<Scalar>& scalars);

static std::vector<Tensor> _foreach_clamp_max(const std::vector<Tensor>& self, const std::vector<Tensor>& other);

static void _foreach_clamp_max_(std::vector<Tensor> self, const std::vector<Tensor>& other);

static std::vector<Tensor> _foreach_clamp_max(const std::vector<Tensor>& self, const std::vector<Scalar>& scalars);

static void _foreach_clamp_max_(std::vector<Tensor> self, const std::vector<Scalar>& scalars);

static void _foreach_clamp_min_(std::vector<Tensor> self, Scalar scalar);

static void _foreach_clamp_max_(std::vector<Tensor> self, Scalar scalar);

static std::vector<Tensor> _foreach_maximum(const std::vector<Tensor>& self, Scalar scalar);

static std::vector<Tensor> _foreach_minimum(const std::vector<Tensor>& self, Scalar scalar);

static std::vector<Tensor> _foreach_maximum(const std::vector<Tensor>& self, const std::vector<Tensor>& other);

static void _foreach_maximum_(std::vector<Tensor> self, const std::vector<Tensor>& other);

static std::vector<Tensor> _foreach_maximum(const std::vector<Tensor>& self, const std::vector<Scalar>& scalars);

static void _foreach_maximum_(std::vector<Tensor> self, const std::vector<Scalar>& scalars);

static std::vector<Tensor> _foreach_minimum(const std::vector<Tensor>& self, const std::vector<Tensor>& other);

static void _foreach_minimum_(std::vector<Tensor> self, const std::vector<Tensor>& other);

static std::vector<Tensor> _foreach_minimum(const std::vector<Tensor>& self, const std::vector<Scalar>& scalars);

static void _foreach_minimum_(std::vector<Tensor> self, const std::vector<Scalar>& scalars);

static void _foreach_maximum_(std::vector<Tensor> self, Scalar scalar);

static void _foreach_minimum_(std::vector<Tensor> self, Scalar scalar);

static void _foreach_copy_(std::vector<Tensor> self, const std::vector<Tensor>& src, bool non_blocking = false);

static void _foreach_zero_(std::vector<Tensor> self);

Tensor div(Scalar other) const;

Tensor& div_(const Tensor& other);

Tensor& div_(Scalar other);

static Tensor addcmul(const Tensor& self, const Tensor& tensor1, const Tensor& tensor2, Scalar value = 1);

Tensor addcmul(const Tensor& tensor1, const Tensor& tensor2, Scalar value = 1) const;

Tensor& addcmul_(const Tensor& tensor1, const Tensor& tensor2, Scalar value = 1);

static Tensor addcdiv(const Tensor& self, const Tensor& tensor1, const Tensor& tensor2, Scalar value = 1);

Tensor addcdiv(const Tensor& tensor1, const Tensor& tensor2, Scalar value = 1) const;

Tensor& addcdiv_(const Tensor& tensor1, const Tensor& tensor2, Scalar value = 1);

static Tensor where(const Tensor& condition, const Tensor& self, const Tensor& other);

static Tensor where(const Tensor& condition, Scalar self, const Tensor& other);

static Tensor where(const Tensor& condition, const Tensor& self, Scalar other);

static Tensor where(const Tensor& condition, Scalar self, Scalar other);

static Tensor maximum(const Tensor& self, const Tensor& other);

static Tensor minimum(const Tensor& self, const Tensor& other);

static Tensor view_as_real(const Tensor& self);

Tensor view_as_real() const;

static Tensor view_as_complex(const Tensor& self);

Tensor view_as_complex() const;

static bool is_complex(const Tensor& self);

static Tensor mm(const Tensor& self, const Tensor& mat2);

Tensor mm(const Tensor& mat2) const;

static Tensor addmm(const Tensor& self, const Tensor& mat1, const Tensor& mat2, Scalar beta = 1, Scalar alpha = 1);

static Tensor matmul(const Tensor& self, const Tensor& other);

Tensor matmul(const Tensor& other) const;

static Tensor& matmul(const Tensor& self, const Tensor& other, Tensor& out);

static Tensor matmul_backward_self(const Tensor& grad_output, const Tensor& self, const Tensor& other);

static Tensor matmul_backward_other(const Tensor& grad_output, const Tensor& self, const Tensor& other);

static Tensor bmm(const Tensor& self, const Tensor& mat2);

Tensor bmm(const Tensor& mat2) const;

static Tensor baddbmm(const Tensor& self, const Tensor& batch1, const Tensor& batch2, Scalar beta = 1, Scalar alpha = 1);

static Tensor mv(const Tensor& self, const Tensor& vec);

Tensor mv(const Tensor& vec) const;

static Tensor dot(const Tensor& self, const Tensor& tensor);

Tensor dot(const Tensor& tensor) const;

static Tensor inner(const Tensor& self, const Tensor& other);

Tensor inner(const Tensor& other) const;

static Tensor inner_backward_self(const Tensor& grad_output, const Tensor& self, const Tensor& other);

static Tensor inner_backward_other(const Tensor& grad_output, const Tensor& self, const Tensor& other);

static Tensor outer(const Tensor& self, const Tensor& vec2);

Tensor outer(const Tensor& vec2) const;

static Tensor einsum(std::string equation, const std::vector<Tensor>& operands, const std::vector<int64_t>& path = {});

static Tensor eq(const Tensor& self, const Tensor& other);

Tensor eq(const Tensor& other) const;

static Tensor eq(const Tensor& self, Scalar other);

Tensor eq(Scalar other) const;

static Tensor ne(const Tensor& self, const Tensor& other);

Tensor ne(const Tensor& other) const;

static Tensor ne(const Tensor& self, Scalar other);

Tensor ne(Scalar other) const;

static Tensor lt(const Tensor& self, const Tensor& other);

Tensor lt(const Tensor& other) const;

static Tensor lt(const Tensor& self, Scalar other);

Tensor lt(Scalar other) const;

static Tensor le(const Tensor& self, const Tensor& other);

Tensor le(const Tensor& other) const;

static Tensor le(const Tensor& self, Scalar other);

Tensor le(Scalar other) const;

static Tensor gt(const Tensor& self, const Tensor& other);

Tensor gt(const Tensor& other) const;

static Tensor gt(const Tensor& self, Scalar other);

Tensor gt(Scalar other) const;

static Tensor ge(const Tensor& self, const Tensor& other);

Tensor ge(const Tensor& other) const;

static Tensor ge(const Tensor& self, Scalar other);

Tensor ge(Scalar other) const;

Tensor& copy_(const Tensor& src, bool non_blocking = false);

Tensor& fill_(Scalar value);

static Tensor transpose(const Tensor& self, int64_t dim0, int64_t dim1);

Tensor transpose(int64_t dim0, int64_t dim1) const;

static Tensor t(const Tensor& self);

Tensor t() const;

static Tensor permute(const Tensor& self, const std::vector<int64_t>& dims);

Tensor permute(const std::vector<int64_t>& dims) const;

static Tensor permute_backward(const Tensor& grad_output, const Tensor& self, const std::vector<int64_t>& dims);

static Tensor squeeze(const Tensor& self);

Tensor squeeze() const;

static Tensor squeeze_backward(const Tensor& grad_output, const Tensor& self);

static Tensor squeeze(const Tensor& self, int64_t dim);

Tensor squeeze(int64_t dim) const;

static Tensor unsqueeze(const Tensor& self, int64_t dim);

Tensor unsqueeze(int64_t dim) const;

static Tensor diagonal(const Tensor& self, int64_t offset = 0, int64_t dim1 = 0, int64_t dim2 = 1);

Tensor diagonal(int64_t offset = 0, int64_t dim1 = 0, int64_t dim2 = 1) const;

static Tensor diagonal_backward(const Tensor& grad_output, const std::vector<int64_t>& input_sizes, int64_t offset, int64_t dim1, int64_t dim2);

static Tensor movedim(const Tensor& self, const std::vector<int64_t>& source, const std::vector<int64_t>& destination);

Tensor movedim(const std::vector<int64_t>& source, const std::vector<int64_t>& destination) const;

static Tensor cat(const std::vector<Tensor>& tensors, int64_t dim = 0);

static Tensor stack(const std::vector<Tensor>& tensors, int64_t dim = 0);

static std::vector<Tensor> split(const Tensor& self, int64_t split_size, int64_t dim = 0);

std::vector<Tensor> split(int64_t split_size, int64_t dim = 0) const;

static std::vector<Tensor> split(const Tensor& self, const std::vector<int64_t>& split_sizes, int64_t dim = 0);

std::vector<Tensor> split(const std::vector<int64_t>& split_sizes, int64_t dim = 0) const;

static std::vector<Tensor> chunk(const Tensor& self, int64_t chunks, int64_t dim = 0);

std::vector<Tensor> chunk(int64_t chunks, int64_t dim = 0) const;

static Tensor reshape(const Tensor& self, const std::vector<int64_t>& shape);

Tensor reshape(const std::vector<int64_t>& shape) const;

static std::vector<Tensor> unbind(const Tensor& self, int64_t dim = 0);

std::vector<Tensor> unbind(int64_t dim = 0) const;

Tensor slice(int64_t dim = 0, std::optional<int64_t> start = std::nullopt, std::optional<int64_t> end = std::nullopt, int64_t step = 1) const;

Tensor clone(std::optional<int64_t> memory_format = std::nullopt) const;

Tensor contiguous(int64_t memory_format = 0) const;

static Tensor select_backward(const Tensor& grad_output, const Tensor& self, int64_t dim, int64_t index);

static Tensor slice_backward(const Tensor& grad_output, const Tensor& self, int64_t dim = 0, std::optional<int64_t> start = std::nullopt, std::optional<int64_t> end = std::nullopt, int64_t step = 1);

static Tensor rand(const std::vector<int64_t>& size, std::optional<DType> dtype = std::nullopt, std::optional<Device> device = std::nullopt);

static Tensor rand_like(const Tensor& self, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor randint(int64_t low, int64_t high, const std::vector<int64_t>& size, DType dtype = DType::Int64, std::optional<Device> device = std::nullopt);

static Tensor randint_like(const Tensor& self, int64_t low, int64_t high, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor randn(const std::vector<int64_t>& size, std::optional<DType> dtype = std::nullopt, std::optional<Device> device = std::nullopt);

static Tensor randn_like(const Tensor& self, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor randperm(int64_t n, DType dtype = DType::Int64, std::optional<Device> device = std::nullopt);

Tensor bernoulli() const;

static Tensor bernoulli(const Tensor& self);

static std::tuple<Tensor, Tensor> native_dropout(const Tensor& input, double p);

static std::tuple<Tensor, Tensor> native_alpha_dropout(const Tensor& input, double p);

static Tensor _alpha_dropout_backward(const Tensor& grad_output, const Tensor& mask, double p);

static std::tuple<Tensor, Tensor> native_feature_dropout(const Tensor& input, double p);

static Tensor _feature_dropout_backward(const Tensor& grad_output, const Tensor& mask, double p);

static Tensor trapezoid(const Tensor& y, const std::optional<Tensor>& x = std::nullopt, Scalar dx = 1, int64_t dim = -1);

static Tensor cumulative_trapezoid(const Tensor& y, const std::optional<Tensor>& x = std::nullopt, Scalar dx = 1, int64_t dim = -1);

static Tensor _trapezoid_backward(const Tensor& grad, const std::optional<Tensor>& x, const std::vector<int64_t>& ysizes, Scalar dx, int64_t dim);

static Tensor _cumulative_trapezoid_backward(const Tensor& grad, const std::optional<Tensor>& x, Scalar dx, int64_t dim);

static std::vector<Tensor> gradient(const Tensor& self, const std::vector<Tensor>& spacing = {}, const std::vector<int64_t>& dim = {}, int64_t edge_order = 1);

static Tensor cov(const Tensor& self, int64_t correction = 1, const std::optional<Tensor>& fweights = std::nullopt, const std::optional<Tensor>& aweights = std::nullopt);

Tensor cov(int64_t correction = 1, const std::optional<Tensor>& fweights = std::nullopt, const std::optional<Tensor>& aweights = std::nullopt) const;

static Tensor corrcoef(const Tensor& self);

Tensor corrcoef() const;

static Tensor _cov_backward(const Tensor& grad, const Tensor& self, int64_t correction, const std::optional<Tensor>& fweights, const std::optional<Tensor>& aweights);

static Tensor _corrcoef_backward(const Tensor& grad, const Tensor& self);

Tensor& resize_(const std::vector<int64_t>& size);

static Tensor normal(const Tensor& mean, const Tensor& std);

static Tensor abs(const Tensor& self);

Tensor abs() const;

static Tensor acos(const Tensor& self);

Tensor acos() const;

static Tensor acosh(const Tensor& self);

Tensor acosh() const;

static Tensor angle(const Tensor& self);

Tensor angle() const;

static Tensor asin(const Tensor& self);

Tensor asin() const;

static Tensor asinh(const Tensor& self);

Tensor asinh() const;

static Tensor atan(const Tensor& self);

Tensor atan() const;

static Tensor atan2(const Tensor& self, const Tensor& other);

Tensor atan2(const Tensor& other) const;

static Tensor poisson(const Tensor& self);

Tensor poisson() const;

static Tensor atanh(const Tensor& self);

Tensor atanh() const;

static Tensor ceil(const Tensor& self);

Tensor ceil() const;

static Tensor clamp(const Tensor& self, std::optional<Scalar> min = std::nullopt, std::optional<Scalar> max = std::nullopt);

Tensor clamp(std::optional<Scalar> min = std::nullopt, std::optional<Scalar> max = std::nullopt) const;

static Tensor clamp_backward(const Tensor& grad_output, const Tensor& self, std::optional<Scalar> min = std::nullopt, std::optional<Scalar> max = std::nullopt);

static Tensor cos(const Tensor& self);

Tensor cos() const;

static Tensor cosh(const Tensor& self);

Tensor cosh() const;

static Tensor erf(const Tensor& self);

Tensor erf() const;

static Tensor erfc(const Tensor& self);

Tensor erfc() const;

static Tensor expm1(const Tensor& self);

Tensor expm1() const;

static Tensor lgamma(const Tensor& self);

Tensor lgamma() const;

static Tensor log2(const Tensor& self);

Tensor log2() const;

static Tensor reciprocal(const Tensor& self);

Tensor reciprocal() const;

static Tensor trunc(const Tensor& self);

Tensor trunc() const;

static Tensor exp(const Tensor& self);

Tensor exp() const;

static Tensor floor(const Tensor& self);

Tensor floor() const;

static Tensor lerp(const Tensor& self, const Tensor& end, Scalar weight);

Tensor lerp(const Tensor& end, Scalar weight) const;

static Tensor lerp(const Tensor& self, const Tensor& end, const Tensor& weight);

Tensor lerp(const Tensor& end, const Tensor& weight) const;

Tensor& lerp_(const Tensor& end, Scalar weight);

Tensor& lerp_(const Tensor& end, const Tensor& weight);

Tensor& abs_();

Tensor& neg_();

Tensor& sqrt_();

Tensor& rsqrt_();

static Tensor log(const Tensor& self);

Tensor log() const;

static Tensor neg(const Tensor& self);

Tensor neg() const;

static Tensor pow(const Tensor& self, Scalar exponent);

Tensor pow(Scalar exponent) const;

static Tensor pow(const Tensor& self, const Tensor& exponent);

Tensor pow(const Tensor& exponent) const;

static Tensor round(const Tensor& self);

Tensor round() const;

static Tensor rsqrt(const Tensor& self);

Tensor rsqrt() const;

static Tensor sigmoid(const Tensor& self);

Tensor sigmoid() const;

static Tensor sign(const Tensor& self);

Tensor sign() const;

static Tensor sin(const Tensor& self);

Tensor sin() const;

static Tensor sinh(const Tensor& self);

Tensor sinh() const;

static Tensor softmax(const Tensor& self, int64_t dim, DType dtype = DType::Undefined);

Tensor softmax(int64_t dim, DType dtype = DType::Undefined) const;

static Tensor log_softmax(const Tensor& self, int64_t dim, DType dtype = DType::Undefined);

Tensor log_softmax(int64_t dim, DType dtype = DType::Undefined) const;

static Tensor sqrt(const Tensor& self);

Tensor sqrt() const;

static Tensor square(const Tensor& self);

Tensor square() const;

static Tensor tan(const Tensor& self);

Tensor tan() const;

static Tensor tanh(const Tensor& self);

Tensor tanh() const;

static Tensor relu(const Tensor& self);

Tensor relu() const;

static Tensor& relu_(Tensor& self);

Tensor& relu_();

static Tensor threshold_backward(const Tensor& grad_output, const Tensor& self, Scalar threshold);

static Tensor gelu(const Tensor& self, std::string approximate = "none");

Tensor gelu(std::string approximate = "none") const;

static Tensor gelu_backward(const Tensor& grad_output, const Tensor& self, std::string approximate = "none");

static Tensor silu(const Tensor& self);

Tensor silu() const;

static Tensor empty(const std::vector<int64_t>& size, std::optional<DType> dtype = std::nullopt, std::optional<Device> device = std::nullopt, bool pin_memory = false);

static Tensor full(const std::vector<int64_t>& size, Scalar fill_value, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt, bool pin_memory = false);

static Tensor zeros(const std::vector<int64_t>& size, std::optional<DType> dtype = std::nullopt, std::optional<Device> device = std::nullopt, bool pin_memory = false);

static Tensor ones(const std::vector<int64_t>& size, std::optional<DType> dtype = std::nullopt, std::optional<Device> device = std::nullopt, bool pin_memory = false);

static Tensor eye(int64_t n, int64_t m = -1, DType dtype = DType::Float32, std::optional<Device> device = std::nullopt);

static Tensor arange(Scalar start, Scalar end, Scalar step = 1, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor arange(Scalar end, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor linspace(Scalar start, Scalar end, int64_t steps, DType dtype = DType::Float32, std::optional<Device> device = std::nullopt);

static Tensor logspace(Scalar start, Scalar end, int64_t steps, double base = 10.0, DType dtype = DType::Float32, std::optional<Device> device = std::nullopt);

static Tensor sum(const Tensor& self, DType dtype = DType::Undefined);

Tensor sum(DType dtype = DType::Undefined) const;

static Tensor sum(const Tensor& self, const std::vector<int64_t>& dim, bool keepdim = false, DType dtype = DType::Undefined);

Tensor sum(const std::vector<int64_t>& dim, bool keepdim = false, DType dtype = DType::Undefined) const;

static Tensor mean(const Tensor& self, DType dtype = DType::Undefined);

Tensor mean(DType dtype = DType::Undefined) const;

static Tensor mean(const Tensor& self, const std::vector<int64_t>& dim, bool keepdim = false, DType dtype = DType::Undefined);

Tensor mean(const std::vector<int64_t>& dim, bool keepdim = false, DType dtype = DType::Undefined) const;

static Tensor _sum_dim_backward(const Tensor& grad_output, const Tensor& self, const std::vector<int64_t>& dim, bool keepdim = false);

static Tensor mean_dim_backward(const Tensor& grad_output, const Tensor& self, const std::vector<int64_t>& dim, bool keepdim = false);

static Tensor max(const Tensor& self);

Tensor max() const;

static std::tuple<Tensor, Tensor> max(const Tensor& self, int64_t dim, bool keepdim = false);

std::tuple<Tensor, Tensor> max(int64_t dim, bool keepdim = false) const;

static Tensor min(const Tensor& self);

Tensor min() const;

static std::tuple<Tensor, Tensor> min(const Tensor& self, int64_t dim, bool keepdim = false);

std::tuple<Tensor, Tensor> min(int64_t dim, bool keepdim = false) const;

static Tensor prod(const Tensor& self, DType dtype = DType::Undefined);

Tensor prod(DType dtype = DType::Undefined) const;

static Tensor prod(const Tensor& self, const std::vector<int64_t>& dim, bool keepdim = false, DType dtype = DType::Undefined);

Tensor prod(const std::vector<int64_t>& dim, bool keepdim = false, DType dtype = DType::Undefined) const;

static Tensor argmax(const Tensor& self, std::optional<int64_t> dim = std::nullopt, bool keepdim = false);

Tensor argmax(std::optional<int64_t> dim = std::nullopt, bool keepdim = false) const;

static Tensor argmin(const Tensor& self, std::optional<int64_t> dim = std::nullopt, bool keepdim = false);

Tensor argmin(std::optional<int64_t> dim = std::nullopt, bool keepdim = false) const;

static Tensor all(const Tensor& self);

Tensor all() const;

static Tensor all(const Tensor& self, const std::vector<int64_t>& dim, bool keepdim = false);

Tensor all(const std::vector<int64_t>& dim, bool keepdim = false) const;

static Tensor any(const Tensor& self);

Tensor any() const;

static Tensor any(const Tensor& self, const std::vector<int64_t>& dim, bool keepdim = false);

Tensor any(const std::vector<int64_t>& dim, bool keepdim = false) const;

static Tensor var(const Tensor& self, int64_t correction = 1);

Tensor var(int64_t correction = 1) const;

static Tensor var(const Tensor& self, const std::vector<int64_t>& dim, int64_t correction = 1, bool keepdim = false);

Tensor var(const std::vector<int64_t>& dim, int64_t correction = 1, bool keepdim = false) const;

static Tensor std(const Tensor& self, int64_t correction = 1);

Tensor std(int64_t correction = 1) const;

static Tensor std(const Tensor& self, const std::vector<int64_t>& dim, int64_t correction = 1, bool keepdim = false);

Tensor std(const std::vector<int64_t>& dim, int64_t correction = 1, bool keepdim = false) const;

static Tensor median(const Tensor& self);

Tensor median() const;

static Tensor norm(const Tensor& self, double p = 2.0);

Tensor norm(double p = 2.0) const;

static Tensor norm(const Tensor& self, const std::vector<int64_t>& dim, double p = 2.0, bool keepdim = false);

Tensor norm(const std::vector<int64_t>& dim, double p = 2.0, bool keepdim = false) const;

Tensor& exponential_(double lambd = 1.0);

Tensor& geometric_(double p);

Tensor& log_normal_(double mean = 1.0, double std = 2.0);

Tensor& normal_(double mean = 0.0, double std = 1.0);

Tensor& random_(int64_t low = 0, int64_t high = 0);

Tensor& uniform_(double from = 0.0, double to = 1.0);

Tensor& bernoulli_();

Tensor& cauchy_(double median = 0.0, double sigma = 1.0);

static Tensor empty_like(const Tensor& self, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor zeros_like(const Tensor& self, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor ones_like(const Tensor& self, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor full_like(const Tensor& self, Scalar fill_value, DType dtype = DType::Undefined, std::optional<Device> device = std::nullopt);

static Tensor masked_select(const Tensor& self, const Tensor& mask);

Tensor masked_select(const Tensor& mask) const;

Tensor& zero_();

static Tensor max_pool2d(const Tensor& input, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& dilation = {}, bool ceil_mode = false);

static Tensor avg_pool2d(const Tensor& input, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, bool ceil_mode = false, bool count_include_pad = true, std::optional<int64_t> divisor_override = std::nullopt);

static Tensor avg_pool3d(const Tensor& input, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, bool ceil_mode = false, bool count_include_pad = true, std::optional<int64_t> divisor_override = std::nullopt);

static Tensor avg_pool3d_backward(const Tensor& grad_output, const Tensor& input, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, bool ceil_mode = false, bool count_include_pad = true, std::optional<int64_t> divisor_override = std::nullopt);

static Tensor adaptive_avg_pool3d(const Tensor& input, const std::vector<int64_t>& output_size);

static Tensor adaptive_avg_pool3d_backward(const Tensor& grad_output, const Tensor& input);

static Tensor adaptive_avg_pool2d(const Tensor& input, const std::vector<int64_t>& output_size);

static Tensor adaptive_max_pool2d(const Tensor& input, const std::vector<int64_t>& output_size);

static std::tuple<Tensor, Tensor> nll_loss(const Tensor& self, const Tensor& target, const std::optional<Tensor>& weight = std::nullopt, int64_t reduction = 1, int64_t ignore_index = -100);

static Tensor nll_loss_backward(const Tensor& grad_output, const Tensor& self, const Tensor& target, const std::optional<Tensor>& weight = std::nullopt, int64_t reduction = 1, int64_t ignore_index = -100, const std::optional<Tensor>& total_weight = std::nullopt);

static std::tuple<Tensor, Tensor> _ctc_loss(const Tensor& log_probs, const Tensor& targets, const Tensor& input_lengths, const Tensor& target_lengths, int64_t blank = 0, bool zero_infinity = false);

static Tensor _ctc_loss_backward(const Tensor& grad, const Tensor& log_probs, const Tensor& targets, const Tensor& input_lengths, const Tensor& target_lengths, const Tensor& neg_log_likelihood, const Tensor& log_alpha, int64_t blank, bool zero_infinity);

static Tensor mse_loss(const Tensor& self, const Tensor& target, int64_t reduction = 1);

static Tensor mse_loss_backward(const Tensor& grad_output, const Tensor& self, const Tensor& target, int64_t reduction = 1);

static Tensor max_pool2d_backward(const Tensor& grad_output, const Tensor& input, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& dilation = {}, bool ceil_mode = false);

static Tensor avg_pool2d_backward(const Tensor& grad_output, const Tensor& input, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, bool ceil_mode = false, bool count_include_pad = true, std::optional<int64_t> divisor_override = std::nullopt);

static Tensor adaptive_avg_pool2d_backward(const Tensor& grad_output, const Tensor& input);

static Tensor adaptive_max_pool2d_backward(const Tensor& grad_output, const Tensor& input);

static Tensor batch_norm(const Tensor& input, const std::optional<Tensor>& weight, const std::optional<Tensor>& bias, const std::optional<Tensor>& running_mean, const std::optional<Tensor>& running_var, bool training, double momentum, double eps);

static Tensor layer_norm(const Tensor& input, const std::vector<int64_t>& normalized_shape, const std::optional<Tensor>& weight = std::nullopt, const std::optional<Tensor>& bias = std::nullopt, double eps = 1e-5);

static Tensor group_norm(const Tensor& input, int64_t num_groups, const std::optional<Tensor>& weight = std::nullopt, const std::optional<Tensor>& bias = std::nullopt, double eps = 1e-5);

static Tensor instance_norm(const Tensor& input, const std::optional<Tensor>& weight = std::nullopt, const std::optional<Tensor>& bias = std::nullopt, const std::optional<Tensor>& running_mean = std::nullopt, const std::optional<Tensor>& running_var = std::nullopt, bool use_input_stats = true, double momentum = 0.1, double eps = 1e-5);

static std::tuple<Tensor, Tensor, Tensor> batch_norm_backward(const Tensor& grad_output, const Tensor& input, const std::optional<Tensor>& weight = std::nullopt, const std::optional<Tensor>& running_mean = std::nullopt, const std::optional<Tensor>& running_var = std::nullopt, bool training = true, double eps = 1e-5);

static std::tuple<Tensor, Tensor, Tensor> layer_norm_backward(const Tensor& grad_output, const Tensor& input, const std::vector<int64_t>& normalized_shape, const std::optional<Tensor>& weight = std::nullopt, const std::optional<Tensor>& bias = std::nullopt, double eps = 1e-5);

static std::tuple<Tensor, Tensor, Tensor> group_norm_backward(const Tensor& grad_output, const Tensor& input, int64_t num_groups, const std::optional<Tensor>& weight = std::nullopt, const std::optional<Tensor>& bias = std::nullopt, double eps = 1e-5);

static std::tuple<Tensor, Tensor, Tensor> instance_norm_backward(const Tensor& grad_output, const Tensor& input, const std::optional<Tensor>& weight = std::nullopt, const std::optional<Tensor>& bias = std::nullopt, const std::optional<Tensor>& running_mean = std::nullopt, const std::optional<Tensor>& running_var = std::nullopt, bool use_input_stats = true, double eps = 1e-5);

static Tensor multinomial(const Tensor& self, int64_t num_samples, bool replacement = false, int64_t impl = 0);

static std::tuple<Tensor, Tensor> topk(const Tensor& self, int64_t k, int64_t dim = -1, bool largest = true, bool sorted = true, int64_t impl = 0);

static Tensor sample(const Tensor& logits, double temperature = 1.0, int64_t top_k = 0, double top_p = 1.0, int64_t impl = 0);

static Tensor scaled_dot_product_attention(const Tensor& query, const Tensor& key, const Tensor& value, bool is_causal = false, int64_t impl = 0);

static std::tuple<Tensor, Tensor, Tensor> scaled_dot_product_attention_backward(const Tensor& grad_output, const Tensor& query, const Tensor& key, const Tensor& value, bool is_causal = false, int64_t impl = 0);

static Tensor tp_l1_loss(const Tensor& input, const Tensor& target, int64_t reduction = 1);

static Tensor tp_l1_loss_backward(const Tensor& grad_output, const Tensor& input, const Tensor& target, int64_t reduction = 1);

static Tensor tp_smooth_l1_loss(const Tensor& input, const Tensor& target, int64_t reduction = 1, double beta = 1.0);

static Tensor tp_smooth_l1_loss_backward(const Tensor& grad_output, const Tensor& input, const Tensor& target, int64_t reduction = 1, double beta = 1.0);

static Tensor tp_huber_loss(const Tensor& input, const Tensor& target, int64_t reduction = 1, double delta = 1.0);

static Tensor tp_huber_loss_backward(const Tensor& grad_output, const Tensor& input, const Tensor& target, int64_t reduction = 1, double delta = 1.0);

static Tensor tp_kl_div(const Tensor& input, const Tensor& target, int64_t reduction = 1, bool log_target = false);

static Tensor tp_kl_div_backward(const Tensor& grad_output, const Tensor& input, const Tensor& target, int64_t reduction = 1, bool log_target = false);

static Tensor tp_binary_cross_entropy(const Tensor& input, const Tensor& target, const std::optional<Tensor>& weight = std::nullopt, int64_t reduction = 1);

static Tensor tp_binary_cross_entropy_backward(const Tensor& grad_output, const Tensor& input, const Tensor& target, const std::optional<Tensor>& weight = std::nullopt, int64_t reduction = 1);

static Tensor tp_margin_ranking_loss(const Tensor& input1, const Tensor& input2, const Tensor& target, double margin = 0.0, int64_t reduction = 1);

static std::tuple<Tensor, Tensor> tp_margin_ranking_loss_backward(const Tensor& grad_output, const Tensor& input1, const Tensor& input2, const Tensor& target, double margin = 0.0, int64_t reduction = 1);

static Tensor tp_hinge_embedding_loss(const Tensor& input, const Tensor& target, double margin = 1.0, int64_t reduction = 1);

static Tensor tp_hinge_embedding_loss_backward(const Tensor& grad_output, const Tensor& input, const Tensor& target, double margin = 1.0, int64_t reduction = 1);

static Tensor tp_soft_margin_loss(const Tensor& input, const Tensor& target, int64_t reduction = 1);

static Tensor tp_soft_margin_loss_backward(const Tensor& grad_output, const Tensor& input, const Tensor& target, int64_t reduction = 1);

static Tensor tp_cosine_embedding_loss(const Tensor& input1, const Tensor& input2, const Tensor& target, double margin = 0.0, int64_t reduction = 1);

static std::tuple<Tensor, Tensor> tp_cosine_embedding_loss_backward(const Tensor& grad_output, const Tensor& input1, const Tensor& input2, const Tensor& target, double margin = 0.0, int64_t reduction = 1);

static Tensor tp_poisson_nll_loss(const Tensor& input, const Tensor& target, bool log_input = true, bool full = false, double eps = 1e-08, int64_t reduction = 1);

static Tensor tp_poisson_nll_loss_backward(const Tensor& grad_output, const Tensor& input, const Tensor& target, bool log_input = true, bool full = false, double eps = 1e-08, int64_t reduction = 1);

static Tensor fft_fft(const Tensor& self, int64_t n = -1, int64_t dim = -1, std::string norm = "backward");

static Tensor fft_ifft(const Tensor& self, int64_t n = -1, int64_t dim = -1, std::string norm = "backward");

static Tensor fft_rfft(const Tensor& self, int64_t n = -1, int64_t dim = -1, std::string norm = "backward");

static Tensor fft_irfft(const Tensor& self, int64_t n = -1, int64_t dim = -1, std::string norm = "backward");

static Tensor fft_fft_backward(const Tensor& grad, const Tensor& self, int64_t dim, std::string norm);

static Tensor fft_ifft_backward(const Tensor& grad, const Tensor& self, int64_t dim, std::string norm);

static Tensor fft_rfft_backward(const Tensor& grad, const Tensor& self, int64_t dim, std::string norm);

static Tensor fft_irfft_backward(const Tensor& grad, const Tensor& self, int64_t dim, std::string norm);

static Tensor hann_window(int64_t window_length, bool periodic = true, std::optional<DType> dtype = std::nullopt);

static Tensor hamming_window(int64_t window_length, bool periodic = true, double alpha = 0.54, double beta = 0.46, std::optional<DType> dtype = std::nullopt);

static Tensor bartlett_window(int64_t window_length, bool periodic = true, std::optional<DType> dtype = std::nullopt);

static Tensor blackman_window(int64_t window_length, bool periodic = true, std::optional<DType> dtype = std::nullopt);

static Tensor stft(const Tensor& self, int64_t n_fft, std::optional<int64_t> hop_length = std::nullopt, std::optional<int64_t> win_length = std::nullopt, const std::optional<Tensor>& window = std::nullopt, bool center = true, std::string pad_mode = "reflect", bool normalized = false, bool onesided = true, bool return_complex = true);

static Tensor istft(const Tensor& input, int64_t n_fft, std::optional<int64_t> hop_length = std::nullopt, std::optional<int64_t> win_length = std::nullopt, const std::optional<Tensor>& window = std::nullopt, bool center = true, bool normalized = false, bool onesided = true, std::optional<int64_t> length = std::nullopt, bool return_complex = false);

static Tensor stft_backward(const Tensor& grad_output, const Tensor& self, int64_t n_fft, std::optional<int64_t> hop_length = std::nullopt, std::optional<int64_t> win_length = std::nullopt, const std::optional<Tensor>& window = std::nullopt, bool center = true, std::string pad_mode = "reflect", bool normalized = false, bool onesided = true);

static Tensor cumsum(const Tensor& self, int64_t dim = 0, std::optional<DType> dtype = std::nullopt);

Tensor cumsum(int64_t dim = 0, std::optional<DType> dtype = std::nullopt) const;

static Tensor flip(const Tensor& self, const std::vector<int64_t>& dims = {});

static Tensor index_select(const Tensor& self, int64_t dim, const Tensor& index);

Tensor index_select(int64_t dim, const Tensor& index) const;

static Tensor index_add(const Tensor& self, int64_t dim, const Tensor& index, const Tensor& source);

Tensor index_add(int64_t dim, const Tensor& index, const Tensor& source) const;

static Tensor nonzero(const Tensor& self);

static std::tuple<Tensor, Tensor, Tensor> unique(const Tensor& self, bool sorted = true, bool return_inverse = false, bool return_counts = false);

static Tensor count_nonzero(const Tensor& self, const std::vector<int64_t>& dim = {});

static Tensor log10(const Tensor& self);

Tensor log10() const;

static Tensor log1p(const Tensor& self);

Tensor log1p() const;

static Tensor frac(const Tensor& self);

Tensor frac() const;

static Tensor i0(const Tensor& self);

static Tensor polar(const Tensor& abs, const Tensor& angle);

static Tensor real(const Tensor& self);

Tensor real() const;

static Tensor diff(const Tensor& self, int64_t n = 1, int64_t dim = -1, const std::optional<Tensor>& prepend = std::nullopt, const std::optional<Tensor>& append = std::nullopt);

static Tensor logical_and(const Tensor& self, const Tensor& other);

static Tensor linalg_cholesky(const Tensor& A, bool upper = false);

static std::tuple<Tensor, Tensor> linalg_cholesky_ex(const Tensor& A, bool upper = false, bool check_errors = false);

static Tensor linalg_inv(const Tensor& A);

static std::tuple<Tensor, Tensor> linalg_inv_ex(const Tensor& A, bool check_errors = false);

static Tensor linalg_det(const Tensor& A);

static std::tuple<Tensor, Tensor> linalg_slogdet(const Tensor& A);

static Tensor linalg_solve(const Tensor& A, const Tensor& B, bool left = true);

static std::tuple<Tensor, Tensor> linalg_solve_ex(const Tensor& A, const Tensor& B, bool left = true, bool check_errors = false);

static std::tuple<Tensor, Tensor> linalg_lu_factor(const Tensor& A, bool pivot = true);

static std::tuple<Tensor, Tensor, Tensor> linalg_lu_factor_ex(const Tensor& A, bool pivot = true, bool check_errors = false);

static std::tuple<Tensor, Tensor, Tensor> linalg_lu(const Tensor& A, bool pivot = true);

static Tensor linalg_lu_solve(const Tensor& LU, const Tensor& pivots, const Tensor& B, bool left = true, bool adjoint = false);

static Tensor linalg_solve_triangular(const Tensor& A, const Tensor& B, bool upper, bool left = true, bool unitriangular = false);

static std::tuple<Tensor, Tensor> linalg_eigh(const Tensor& A, std::string UPLO = "L");

static Tensor linalg_eigvalsh(const Tensor& A, std::string UPLO = "L");

static std::tuple<Tensor, Tensor> linalg_eig(const Tensor& A);

static Tensor linalg_eigvals(const Tensor& A);

static std::tuple<Tensor, Tensor, Tensor> linalg_svd(const Tensor& A, bool full_matrices = true, std::optional<std::string> driver = std::nullopt);

static Tensor linalg_svdvals(const Tensor& A, std::optional<std::string> driver = std::nullopt);

static std::tuple<Tensor, Tensor, Tensor, Tensor> linalg_lstsq(const Tensor& A, const Tensor& B, std::optional<double> rcond = std::nullopt, std::optional<std::string> driver = std::nullopt);

static std::tuple<Tensor, Tensor> linalg_qr(const Tensor& A, std::string mode = "reduced");

static Tensor linalg_householder_product(const Tensor& input, const Tensor& tau);

static std::tuple<Tensor, Tensor> linalg_ldl_factor(const Tensor& A, bool hermitian = false);

static std::tuple<Tensor, Tensor, Tensor> linalg_ldl_factor_ex(const Tensor& A, bool hermitian = false, bool check_errors = false);

static Tensor linalg_ldl_solve(const Tensor& LD, const Tensor& pivots, const Tensor& B, bool hermitian = false);

static Tensor linalg_diagonal(const Tensor& A, int64_t offset = 0, int64_t dim1 = -2, int64_t dim2 = -1);

static Tensor roll(const Tensor& self, const std::vector<int64_t>& shifts, const std::vector<int64_t>& dims = {});

static Tensor unfold(const Tensor& self, int64_t dimension, int64_t size, int64_t step);

static Tensor threshold(const Tensor& self, Scalar threshold, Scalar value);

static Tensor relu6(const Tensor& self);

Tensor relu6() const;

static Tensor leaky_relu(const Tensor& self, Scalar negative_slope = 0.01);

Tensor leaky_relu(Scalar negative_slope = 0.01) const;

static Tensor leaky_relu_backward(const Tensor& grad_output, const Tensor& self, Scalar negative_slope, bool self_is_result);

static Tensor hardtanh(const Tensor& self, Scalar min_val = -1, Scalar max_val = 1);

Tensor hardtanh(Scalar min_val = -1, Scalar max_val = 1) const;

static Tensor hardtanh_backward(const Tensor& grad_output, const Tensor& self, Scalar min_val, Scalar max_val);

static Tensor hardswish(const Tensor& self);

Tensor hardswish() const;

static Tensor hardswish_backward(const Tensor& grad_output, const Tensor& self);

static Tensor hardsigmoid(const Tensor& self);

Tensor hardsigmoid() const;

static Tensor hardsigmoid_backward(const Tensor& grad_output, const Tensor& self);

static Tensor softplus(const Tensor& self, Scalar beta = 1, Scalar threshold = 20);

Tensor softplus(Scalar beta = 1, Scalar threshold = 20) const;

static Tensor softplus_backward(const Tensor& grad_output, const Tensor& self, Scalar beta, Scalar threshold);

static Tensor elu(const Tensor& self, Scalar alpha = 1, Scalar scale = 1, Scalar input_scale = 1);

Tensor elu(Scalar alpha = 1, Scalar scale = 1, Scalar input_scale = 1) const;

static Tensor elu_backward(const Tensor& grad_output, Scalar alpha, Scalar scale, Scalar input_scale, bool is_result, const Tensor& self_or_result);

static Tensor selu(const Tensor& self);

Tensor selu() const;

static Tensor celu(const Tensor& self, Scalar alpha = 1.0);

Tensor celu(Scalar alpha = 1.0) const;

static Tensor mish(const Tensor& self);

Tensor mish() const;

static Tensor mish_backward(const Tensor& grad_output, const Tensor& self);

static Tensor softshrink(const Tensor& self, Scalar lambd = 0.5);

Tensor softshrink(Scalar lambd = 0.5) const;

static Tensor hardshrink(const Tensor& self, Scalar lambd = 0.5);

Tensor hardshrink(Scalar lambd = 0.5) const;

static Tensor glu(const Tensor& self, int64_t dim = -1);

Tensor glu(int64_t dim = -1) const;

static Tensor glu_backward(const Tensor& grad_output, const Tensor& self, int64_t dim = -1);

static Tensor one_hot(const Tensor& self, int64_t num_classes = -1);

static Tensor narrow(const Tensor& self, int64_t dim, int64_t start, int64_t length);

Tensor narrow(int64_t dim, int64_t start, int64_t length) const;

static Tensor pixel_shuffle(const Tensor& self, int64_t upscale_factor);

static Tensor pixel_unshuffle(const Tensor& self, int64_t downscale_factor);

static Tensor reflection_pad_nd(const Tensor& self, const std::vector<int64_t>& pad);

static Tensor reflection_pad_nd_backward(const Tensor& grad_output, const Tensor& self, const std::vector<int64_t>& pad);

static Tensor replication_pad_nd(const Tensor& self, const std::vector<int64_t>& pad);

static Tensor replication_pad_nd_backward(const Tensor& grad_output, const Tensor& self, const std::vector<int64_t>& pad);

static Tensor circular_pad_nd(const Tensor& self, const std::vector<int64_t>& pad);

static Tensor circular_pad_nd_backward(const Tensor& grad_output, const Tensor& self, const std::vector<int64_t>& pad);

static Tensor im2col(const Tensor& self, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& dilation = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& stride = {});

static Tensor im2col_backward(const Tensor& grad_output, const std::vector<int64_t>& input_size, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& dilation, const std::vector<int64_t>& padding, const std::vector<int64_t>& stride);

static Tensor col2im(const Tensor& self, const std::vector<int64_t>& output_size, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& dilation = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& stride = {});

static Tensor col2im_backward(const Tensor& grad_output, const std::vector<int64_t>& input_size, const std::vector<int64_t>& output_size, const std::vector<int64_t>& kernel_size, const std::vector<int64_t>& dilation, const std::vector<int64_t>& padding, const std::vector<int64_t>& stride);

static Tensor conv_transpose1d(const Tensor& input, const Tensor& weight, const std::optional<Tensor>& bias = std::nullopt, const std::vector<int64_t>& stride = {}, const std::vector<int64_t>& padding = {}, const std::vector<int64_t>& output_padding = {}, int64_t groups = 1, const std::vector<int64_t>& dilation = {});

static Tensor conv_transpose1d_grad_input(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

static Tensor conv_transpose1d_grad_weight(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

static Tensor conv_transpose1d_grad_bias(const Tensor& grad_output, const Tensor& input, const Tensor& weight, const std::vector<int64_t>& stride, const std::vector<int64_t>& padding, const std::vector<int64_t>& output_padding, int64_t groups, const std::vector<int64_t>& dilation);

static Tensor upsample_nearest1d(const Tensor& self, const std::vector<int64_t>& output_size, std::optional<double> scales = std::nullopt);

static Tensor upsample_nearest1d_backward(const Tensor& grad_output, const std::vector<int64_t>& output_size, const std::vector<int64_t>& input_size, std::optional<double> scales = std::nullopt);

static Tensor upsample_nearest2d(const Tensor& self, const std::vector<int64_t>& output_size, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_nearest2d_backward(const Tensor& grad_output, const std::vector<int64_t>& output_size, const std::vector<int64_t>& input_size, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_nearest3d(const Tensor& self, const std::vector<int64_t>& output_size, std::optional<double> scales_d = std::nullopt, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_nearest3d_backward(const Tensor& grad_output, const std::vector<int64_t>& output_size, const std::vector<int64_t>& input_size, std::optional<double> scales_d = std::nullopt, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_linear1d(const Tensor& self, const std::vector<int64_t>& output_size, bool align_corners, std::optional<double> scales = std::nullopt);

static Tensor upsample_linear1d_backward(const Tensor& grad_output, const std::vector<int64_t>& output_size, const std::vector<int64_t>& input_size, bool align_corners, std::optional<double> scales = std::nullopt);

static Tensor upsample_bilinear2d(const Tensor& self, const std::vector<int64_t>& output_size, bool align_corners, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_bilinear2d_backward(const Tensor& grad_output, const std::vector<int64_t>& output_size, const std::vector<int64_t>& input_size, bool align_corners, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_bicubic2d(const Tensor& self, const std::vector<int64_t>& output_size, bool align_corners, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_bicubic2d_backward(const Tensor& grad_output, const std::vector<int64_t>& output_size, const std::vector<int64_t>& input_size, bool align_corners, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_trilinear3d(const Tensor& self, const std::vector<int64_t>& output_size, bool align_corners, std::optional<double> scales_d = std::nullopt, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor upsample_trilinear3d_backward(const Tensor& grad_output, const std::vector<int64_t>& output_size, const std::vector<int64_t>& input_size, bool align_corners, std::optional<double> scales_d = std::nullopt, std::optional<double> scales_h = std::nullopt, std::optional<double> scales_w = std::nullopt);

static Tensor addbmm(const Tensor& self, const Tensor& batch1, const Tensor& batch2, Scalar beta = 1, Scalar alpha = 1);

Tensor addbmm(const Tensor& batch1, const Tensor& batch2, Scalar beta = 1, Scalar alpha = 1) const;

static Tensor addmv(const Tensor& self, const Tensor& mat, const Tensor& vec, Scalar beta = 1, Scalar alpha = 1);

Tensor addmv(const Tensor& mat, const Tensor& vec, Scalar beta = 1, Scalar alpha = 1) const;

static Tensor addr(const Tensor& self, const Tensor& vec1, const Tensor& vec2, Scalar beta = 1, Scalar alpha = 1);

Tensor addr(const Tensor& vec1, const Tensor& vec2, Scalar beta = 1, Scalar alpha = 1) const;

static Tensor amax(const Tensor& self, const std::vector<int64_t>& dim = {}, bool keepdim = false);

static Tensor amin(const Tensor& self, const std::vector<int64_t>& dim = {}, bool keepdim = false);

static std::tuple<Tensor, Tensor> aminmax(const Tensor& self, const std::vector<int64_t>& dim = {}, bool keepdim = false);

static Tensor argsort(const Tensor& self, int64_t dim = -1, bool descending = false);

Tensor argsort(int64_t dim = -1, bool descending = false) const;

static Tensor bincount(const Tensor& self, const std::optional<Tensor>& weights = std::nullopt, int64_t minlength = 0);

Tensor bincount(const std::optional<Tensor>& weights = std::nullopt, int64_t minlength = 0) const;

static Tensor block_diag(const std::vector<Tensor>& tensors);

static std::vector<Tensor> broadcast_tensors(const std::vector<Tensor>& tensors);

static Tensor channel_shuffle(const Tensor& self, int64_t groups);

Tensor channel_shuffle(int64_t groups) const;

static Tensor cholesky(const Tensor& self, bool upper = false);

Tensor cholesky(bool upper = false) const;

static Tensor cholesky_inverse(const Tensor& self, bool upper = false);

Tensor cholesky_inverse(bool upper = false) const;

static Tensor cholesky_solve(const Tensor& self, const Tensor& input2, bool upper = false);

Tensor cholesky_solve(const Tensor& input2, bool upper = false) const;

static Tensor clip(const Tensor& self, std::optional<Scalar> min = std::nullopt, std::optional<Scalar> max = std::nullopt);

Tensor clip(std::optional<Scalar> min = std::nullopt, std::optional<Scalar> max = std::nullopt) const;

static Tensor& clamp_(Tensor& self, std::optional<Scalar> min = std::nullopt, std::optional<Scalar> max = std::nullopt);

Tensor& clamp_(std::optional<Scalar> min = std::nullopt, std::optional<Scalar> max = std::nullopt);

static Tensor clamp_min(const Tensor& self, Scalar min);

Tensor clamp_min(Scalar min) const;

static Tensor clamp_max(const Tensor& self, Scalar max);

Tensor clamp_max(Scalar max) const;

static Tensor& clamp_min_(Tensor& self, Scalar min);

Tensor& clamp_min_(Scalar min);

static Tensor& clamp_max_(Tensor& self, Scalar max);

Tensor& clamp_max_(Scalar max);

static Tensor complex(const Tensor& real, const Tensor& imag);

static Tensor conj(const Tensor& self);

Tensor conj() const;

static Tensor adjoint(const Tensor& self);

Tensor adjoint() const;

static Tensor imag(const Tensor& self);

Tensor imag() const;

static std::tuple<Tensor, Tensor> cummax(const Tensor& self, int64_t dim);

std::tuple<Tensor, Tensor> cummax(int64_t dim) const;

static std::tuple<Tensor, Tensor> cummin(const Tensor& self, int64_t dim);

std::tuple<Tensor, Tensor> cummin(int64_t dim) const;

static Tensor cumprod(const Tensor& self, int64_t dim, std::optional<DType> dtype = std::nullopt);

Tensor cumprod(int64_t dim, std::optional<DType> dtype = std::nullopt) const;

static Tensor logcumsumexp(const Tensor& self, int64_t dim, std::optional<DType> dtype = std::nullopt);

Tensor logcumsumexp(int64_t dim, std::optional<DType> dtype = std::nullopt) const;

static Tensor diag(const Tensor& self, int64_t diagonal = 0);

Tensor diag(int64_t diagonal = 0) const;

static Tensor diag_embed(const Tensor& self, int64_t offset = 0, int64_t dim1 = 0, int64_t dim2 = 1);

Tensor diag_embed(int64_t offset = 0, int64_t dim1 = 0, int64_t dim2 = 1) const;

static Tensor dist(const Tensor& self, const Tensor& other, Scalar p = 2);

Tensor dist(const Tensor& other, Scalar p = 2) const;

static Tensor gather(const Tensor& self, int64_t dim, const Tensor& index);

Tensor gather(int64_t dim, const Tensor& index) const;

static Tensor take(const Tensor& self, const Tensor& index);

Tensor take(const Tensor& index) const;

static Tensor take_along_dim(const Tensor& self, const Tensor& indices, std::optional<int64_t> dim = std::nullopt);

Tensor take_along_dim(const Tensor& indices, std::optional<int64_t> dim = std::nullopt) const;

static Tensor select_scatter(const Tensor& self, const Tensor& src, int64_t dim, int64_t index);

Tensor select_scatter(const Tensor& src, int64_t dim, int64_t index) const;

static Tensor slice_scatter(const Tensor& self, const Tensor& src, int64_t dim = 0, std::optional<int64_t> start = std::nullopt, std::optional<int64_t> end = std::nullopt, int64_t step = 1);

Tensor slice_scatter(const Tensor& src, int64_t dim = 0, std::optional<int64_t> start = std::nullopt, std::optional<int64_t> end = std::nullopt, int64_t step = 1) const;

static Tensor diagonal_scatter(const Tensor& self, const Tensor& src, int64_t offset = 0, int64_t dim1 = 0, int64_t dim2 = 1);

Tensor diagonal_scatter(const Tensor& src, int64_t offset = 0, int64_t dim1 = 0, int64_t dim2 = 1) const;

static Tensor msort(const Tensor& self);

Tensor msort() const;

static Tensor nanmean(const Tensor& self, std::optional<int64_t> dim = std::nullopt, bool keepdim = false, std::optional<DType> dtype = std::nullopt);

Tensor nanmean(std::optional<int64_t> dim = std::nullopt, bool keepdim = false, std::optional<DType> dtype = std::nullopt) const;

static Tensor isclose(const Tensor& self, const Tensor& other, double rtol = 1e-05, double atol = 1e-08, bool equal_nan = false);

Tensor isclose(const Tensor& other, double rtol = 1e-05, double atol = 1e-08, bool equal_nan = false) const;

static Tensor isreal(const Tensor& self);

Tensor isreal() const;

static Tensor bitwise_not(const Tensor& self);

Tensor bitwise_not() const;

static Tensor bitwise_and(const Tensor& self, const Tensor& other);

Tensor bitwise_and(const Tensor& other) const;

static Tensor bitwise_and(const Tensor& self, Scalar other);

Tensor bitwise_and(Scalar other) const;

static Tensor bitwise_or(const Tensor& self, const Tensor& other);

Tensor bitwise_or(const Tensor& other) const;

static Tensor bitwise_or(const Tensor& self, Scalar other);

Tensor bitwise_or(Scalar other) const;

static Tensor bitwise_xor(const Tensor& self, const Tensor& other);

Tensor bitwise_xor(const Tensor& other) const;

static Tensor bitwise_xor(const Tensor& self, Scalar other);

Tensor bitwise_xor(Scalar other) const;

static Tensor bitwise_left_shift(const Tensor& self, const Tensor& other);

Tensor bitwise_left_shift(const Tensor& other) const;

static Tensor bitwise_left_shift(const Tensor& self, Scalar other);

Tensor bitwise_left_shift(Scalar other) const;

static Tensor bitwise_right_shift(const Tensor& self, const Tensor& other);

Tensor bitwise_right_shift(const Tensor& other) const;

static Tensor bitwise_right_shift(const Tensor& self, Scalar other);

Tensor bitwise_right_shift(Scalar other) const;

static Tensor index_copy(const Tensor& self, int64_t dim, const Tensor& index, const Tensor& source);

Tensor index_copy(int64_t dim, const Tensor& index, const Tensor& source) const;

static Tensor index_fill(const Tensor& self, int64_t dim, const Tensor& index, Scalar value);

Tensor index_fill(int64_t dim, const Tensor& index, Scalar value) const;

static Tensor index_fill(const Tensor& self, int64_t dim, const Tensor& index, const Tensor& value);

Tensor index_fill(int64_t dim, const Tensor& index, const Tensor& value) const;

static Tensor& index_fill_(Tensor& self, int64_t dim, const Tensor& index, Scalar value);

Tensor& index_fill_(int64_t dim, const Tensor& index, Scalar value);

static Tensor& index_fill_(Tensor& self, int64_t dim, const Tensor& index, const Tensor& value);

Tensor& index_fill_(int64_t dim, const Tensor& index, const Tensor& value);

static Tensor searchsorted(const Tensor& sorted_sequence, const Tensor& self, bool out_int32 = false, bool right = false);

static Tensor bucketize(const Tensor& self, const Tensor& boundaries, bool out_int32 = false, bool right = false);

static Tensor scatter(const Tensor& self, int64_t dim, const Tensor& index, const Tensor& src);

Tensor scatter(int64_t dim, const Tensor& index, const Tensor& src) const;

static Tensor scatter(const Tensor& self, int64_t dim, const Tensor& index, Scalar value);

Tensor scatter(int64_t dim, const Tensor& index, Scalar value) const;

static Tensor& scatter_(Tensor& self, int64_t dim, const Tensor& index, const Tensor& src);

Tensor& scatter_(int64_t dim, const Tensor& index, const Tensor& src);

static Tensor& scatter_(Tensor& self, int64_t dim, const Tensor& index, Scalar value);

Tensor& scatter_(int64_t dim, const Tensor& index, Scalar value);

static Tensor masked_fill(const Tensor& self, const Tensor& mask, const Tensor& value);

Tensor masked_fill(const Tensor& mask, const Tensor& value) const;

static Tensor& masked_fill_(Tensor& self, const Tensor& mask, const Tensor& value);

Tensor& masked_fill_(const Tensor& mask, const Tensor& value);

static Tensor index_put(const Tensor& self, const std::vector<Tensor>& indices, const Tensor& values, bool accumulate = false);

Tensor index_put(const std::vector<Tensor>& indices, const Tensor& values, bool accumulate = false) const;

static Tensor& index_put_(Tensor& self, const std::vector<Tensor>& indices, const Tensor& values, bool accumulate = false);

Tensor& index_put_(const std::vector<Tensor>& indices, const Tensor& values, bool accumulate = false);

static Tensor masked_fill(const Tensor& self, const Tensor& mask, Scalar value);

Tensor masked_fill(const Tensor& mask, Scalar value) const;

static Tensor& masked_fill_(Tensor& self, const Tensor& mask, Scalar value);

Tensor& masked_fill_(const Tensor& mask, Scalar value);

static Tensor masked_scatter(const Tensor& self, const Tensor& mask, const Tensor& source);

Tensor masked_scatter(const Tensor& mask, const Tensor& source) const;

static Tensor scatter_add(const Tensor& self, int64_t dim, const Tensor& index, const Tensor& src);

Tensor scatter_add(int64_t dim, const Tensor& index, const Tensor& src) const;

static Tensor& scatter_add_(Tensor& self, int64_t dim, const Tensor& index, const Tensor& src);

Tensor& scatter_add_(int64_t dim, const Tensor& index, const Tensor& src);

static std::tuple<Tensor, Tensor> sort(const Tensor& self, int64_t dim = -1, bool descending = false);

std::tuple<Tensor, Tensor> sort(int64_t dim = -1, bool descending = false) const;

static std::tuple<Tensor, Tensor> mode(const Tensor& self, int64_t dim = -1, bool keepdim = false);

std::tuple<Tensor, Tensor> mode(int64_t dim = -1, bool keepdim = false) const;

static std::tuple<Tensor, Tensor> kthvalue(const Tensor& self, int64_t k, int64_t dim = -1, bool keepdim = false);

std::tuple<Tensor, Tensor> kthvalue(int64_t k, int64_t dim = -1, bool keepdim = false) const;

static Tensor nanmedian(const Tensor& self);

Tensor nanmedian() const;

static Tensor nansum(const Tensor& self, const std::vector<int64_t>& dim = {}, bool keepdim = false);

Tensor nansum(const std::vector<int64_t>& dim = {}, bool keepdim = false) const;

static std::tuple<Tensor, Tensor> std_mean(const Tensor& self, const std::vector<int64_t>& dim = {}, bool unbiased = true, bool keepdim = false);

static std::tuple<Tensor, Tensor> var_mean(const Tensor& self, const std::vector<int64_t>& dim = {}, bool unbiased = true, bool keepdim = false);

static Tensor trace(const Tensor& self);

Tensor trace() const;

static Tensor renorm(const Tensor& self, Scalar p, int64_t dim, Scalar maxnorm);

Tensor renorm(Scalar p, int64_t dim, Scalar maxnorm) const;

static Tensor rot90(const Tensor& self, int64_t k = 1, const std::vector<int64_t>& dims = {});

Tensor rot90(int64_t k = 1, const std::vector<int64_t>& dims = {}) const;

static std::vector<Tensor> split_with_sizes(const Tensor& self, const std::vector<int64_t>& split_sizes, int64_t dim = 0);

std::vector<Tensor> split_with_sizes(const std::vector<int64_t>& split_sizes, int64_t dim = 0) const;

static std::vector<Tensor> meshgrid(const std::vector<Tensor>& tensors, std::string indexing = "ij");

static Tensor greater(const Tensor& self, const Tensor& other);

Tensor greater(const Tensor& other) const;

static Tensor greater_equal(const Tensor& self, const Tensor& other);

Tensor greater_equal(const Tensor& other) const;

static Tensor less(const Tensor& self, const Tensor& other);

Tensor less(const Tensor& other) const;

static Tensor less_equal(const Tensor& self, const Tensor& other);

Tensor less_equal(const Tensor& other) const;

static Tensor not_equal(const Tensor& self, const Tensor& other);

Tensor not_equal(const Tensor& other) const;

static Tensor logical_not(const Tensor& self);

Tensor logical_not() const;

static Tensor logical_or(const Tensor& self, const Tensor& other);

Tensor logical_or(const Tensor& other) const;

static Tensor logical_xor(const Tensor& self, const Tensor& other);

Tensor logical_xor(const Tensor& other) const;

static Tensor deg2rad(const Tensor& self);

Tensor deg2rad() const;

static Tensor rad2deg(const Tensor& self);

Tensor rad2deg() const;

static Tensor exp2(const Tensor& self);

Tensor exp2() const;

static Tensor fix(const Tensor& self);

Tensor fix() const;

static Tensor digamma(const Tensor& self);

Tensor digamma() const;

static Tensor erfinv(const Tensor& self);

Tensor erfinv() const;

static Tensor sgn(const Tensor& self);

Tensor sgn() const;

static Tensor signbit(const Tensor& self);

Tensor signbit() const;

static Tensor sinc(const Tensor& self);

Tensor sinc() const;

static Tensor heaviside(const Tensor& self, const Tensor& values);

Tensor heaviside(const Tensor& values) const;

static Tensor hypot(const Tensor& self, const Tensor& other);

Tensor hypot(const Tensor& other) const;

static Tensor logaddexp(const Tensor& self, const Tensor& other);

Tensor logaddexp(const Tensor& other) const;

static Tensor logaddexp2(const Tensor& self, const Tensor& other);

Tensor logaddexp2(const Tensor& other) const;

static Tensor logit(const Tensor& self, std::optional<Scalar> eps = std::nullopt);

Tensor logit(std::optional<Scalar> eps = std::nullopt) const;

static Tensor gcd(const Tensor& self, const Tensor& other);

Tensor gcd(const Tensor& other) const;

static Tensor lcm(const Tensor& self, const Tensor& other);

Tensor lcm(const Tensor& other) const;

static Tensor nextafter(const Tensor& self, const Tensor& other);

Tensor nextafter(const Tensor& other) const;

static Tensor isfinite(const Tensor& self);

Tensor isfinite() const;

static Tensor isinf(const Tensor& self);

Tensor isinf() const;

static Tensor isnan(const Tensor& self);

Tensor isnan() const;

static Tensor isneginf(const Tensor& self);

Tensor isneginf() const;

static Tensor isposinf(const Tensor& self);

Tensor isposinf() const;

static Tensor nan_to_num(const Tensor& self, Scalar nan = 0.0, std::optional<Scalar> posinf = std::nullopt, std::optional<Scalar> neginf = std::nullopt);

Tensor nan_to_num(Scalar nan = 0.0, std::optional<Scalar> posinf = std::nullopt, std::optional<Scalar> neginf = std::nullopt) const;

static Tensor negative(const Tensor& self);

Tensor negative() const;

static Tensor positive(const Tensor& self);

Tensor positive() const;

static std::tuple<Tensor, Tensor, Tensor> svd(const Tensor& self, bool some = true, bool compute_uv = true);

std::tuple<Tensor, Tensor, Tensor> svd(bool some = true, bool compute_uv = true) const;

static std::tuple<Tensor, Tensor> triangular_solve(const Tensor& self, const Tensor& A, bool upper = false, bool transpose = false, bool unitriangular = false);

std::tuple<Tensor, Tensor> triangular_solve(const Tensor& A, bool upper = false, bool transpose = false, bool unitriangular = false) const;

static Tensor vdot(const Tensor& self, const Tensor& other);

Tensor vdot(const Tensor& other) const;

static Tensor prelu(const Tensor& self, const Tensor& weight);

Tensor prelu(const Tensor& weight) const;

static Tensor pdist(const Tensor& self, double p = 2.0);

Tensor pdist(double p = 2.0) const;

static Tensor pairwise_distance(const Tensor& x1, const Tensor& x2, double p = 2.0, double eps = 1e-06, bool keepdim = false);

static Tensor l1_loss(const Tensor& input, const Tensor& target);

static Tensor smooth_l1_loss(const Tensor& input, const Tensor& target, Scalar beta = 1.0);

static Tensor huber_loss(const Tensor& input, const Tensor& target, Scalar delta = 1.0);

static Tensor kl_div(const Tensor& input, const Tensor& target);

static Tensor binary_cross_entropy(const Tensor& input, const Tensor& target);

static Tensor binary_cross_entropy_with_logits(const Tensor& self, const Tensor& target, const std::optional<Tensor>& weight = std::nullopt, const std::optional<Tensor>& pos_weight = std::nullopt);

static Tensor poisson_nll_loss(const Tensor& input, const Tensor& target, bool log_input = true, bool full = false, double eps = 1e-08);

static Tensor soft_margin_loss(const Tensor& input, const Tensor& target);

static Tensor cosine_embedding_loss(const Tensor& x1, const Tensor& x2, const Tensor& target, Scalar margin = 0);

static Tensor margin_ranking_loss(const Tensor& input1, const Tensor& input2, const Tensor& target, Scalar margin = 0);

static Tensor hinge_embedding_loss(const Tensor& input, const Tensor& target, Scalar margin = 1.0);

static Tensor multi_margin_loss(const Tensor& input, const Tensor& target, Scalar margin = 1.0, double p = 1);

static Tensor multilabel_margin_loss(const Tensor& input, const Tensor& target, Scalar margin = 1.0);

static Tensor multilabel_soft_margin_loss(const Tensor& input, const Tensor& target);

static Tensor triplet_margin_loss(const Tensor& anchor, const Tensor& positive, const Tensor& negative, Scalar margin = 1.0, double p = 2.0);

static std::tuple<Tensor, Tensor, Tensor> lstm(const Tensor& input, const std::vector<Tensor>& hx, const std::vector<Tensor>& params, bool has_biases = true, int64_t num_layers = 1, double dropout_p = 0.0, bool training = false, bool bidirectional = false, bool batch_first = false);

static std::tuple<Tensor, Tensor> gru(const Tensor& input, const std::vector<Tensor>& hx, const std::vector<Tensor>& params, bool has_biases = true, int64_t num_layers = 1, double dropout_p = 0.0, bool training = false, bool bidirectional = false, bool batch_first = false);

static std::tuple<Tensor, Tensor> rnn_relu(const Tensor& input, const std::vector<Tensor>& hx, const std::vector<Tensor>& params, bool has_biases = true, int64_t num_layers = 1, double dropout_p = 0.0, bool training = false, bool bidirectional = false, bool batch_first = false);

static std::tuple<Tensor, Tensor> rnn_tanh(const Tensor& input, const std::vector<Tensor>& hx, const std::vector<Tensor>& params, bool has_biases = true, int64_t num_layers = 1, double dropout_p = 0.0, bool training = false, bool bidirectional = false, bool batch_first = false);

static Tensor logsumexp(const Tensor& self, int64_t dim, bool keepdim = false);

Tensor logsumexp(int64_t dim, bool keepdim = false) const;

static Tensor tril(const Tensor& self, int64_t diagonal = 0);

Tensor tril(int64_t diagonal = 0) const;

static Tensor triu(const Tensor& self, int64_t diagonal = 0);

Tensor triu(int64_t diagonal = 0) const;

static Tensor bessel_j0(const Tensor& self);

Tensor bessel_j0() const;

static Tensor bessel_j1(const Tensor& self);

Tensor bessel_j1() const;

static Tensor bessel_y0(const Tensor& self);

Tensor bessel_y0() const;

static Tensor bessel_y1(const Tensor& self);

Tensor bessel_y1() const;

static Tensor airy_ai(const Tensor& self);

Tensor airy_ai() const;

static Tensor spherical_bessel_j0(const Tensor& self);

Tensor spherical_bessel_j0() const;

static Tensor modified_bessel_i1(const Tensor& self);

Tensor modified_bessel_i1() const;

static Tensor modified_bessel_k0(const Tensor& self);

Tensor modified_bessel_k0() const;

static Tensor modified_bessel_k1(const Tensor& self);

Tensor modified_bessel_k1() const;

static Tensor scaled_modified_bessel_k0(const Tensor& self);

Tensor scaled_modified_bessel_k0() const;

static Tensor scaled_modified_bessel_k1(const Tensor& self);

Tensor scaled_modified_bessel_k1() const;

static Tensor i0e(const Tensor& self);

Tensor i0e() const;

static Tensor i1(const Tensor& self);

Tensor i1() const;

static Tensor i1e(const Tensor& self);

Tensor i1e() const;

static Tensor chebyshev_polynomial_t(const Tensor& x, const Tensor& n);

static Tensor chebyshev_polynomial_u(const Tensor& x, const Tensor& n);

static Tensor chebyshev_polynomial_v(const Tensor& x, const Tensor& n);

static Tensor chebyshev_polynomial_w(const Tensor& x, const Tensor& n);

static Tensor shifted_chebyshev_polynomial_t(const Tensor& x, const Tensor& n);

static Tensor shifted_chebyshev_polynomial_u(const Tensor& x, const Tensor& n);

static Tensor shifted_chebyshev_polynomial_v(const Tensor& x, const Tensor& n);

static Tensor shifted_chebyshev_polynomial_w(const Tensor& x, const Tensor& n);

static Tensor hermite_polynomial_h(const Tensor& x, const Tensor& n);

static Tensor hermite_polynomial_he(const Tensor& x, const Tensor& n);

static Tensor laguerre_polynomial_l(const Tensor& x, const Tensor& n);

static Tensor legendre_polynomial_p(const Tensor& x, const Tensor& n);

static Tensor zeta(const Tensor& self, const Tensor& other);

Tensor zeta(const Tensor& other) const;

static Tensor gammainc(const Tensor& a, const Tensor& x);

static Tensor gammaincc(const Tensor& a, const Tensor& x);

static Tensor polygamma(int64_t n, const Tensor& self);

Tensor polygamma(int64_t n) const;

static Tensor sparse_coo_tensor(const Tensor& indices, const Tensor& values, std::optional<std::vector<int64_t>> size = std::nullopt, bool is_coalesced = false);

static Tensor to_dense(const Tensor& self);

Tensor to_dense() const;

static Tensor to_sparse(const Tensor& self);

Tensor to_sparse() const;

static Tensor to_sparse_csr(const Tensor& self);

Tensor to_sparse_csr() const;

static int64_t _nnz(const Tensor& self);

int64_t _nnz() const;

static Tensor sparse_mm(const Tensor& self, const Tensor& dense);

static Tensor sparse_sum(const Tensor& self, std::optional<std::vector<int64_t>> dim = std::nullopt, std::optional<DType> dtype = std::nullopt);

Tensor sparse_sum(std::optional<std::vector<int64_t>> dim = std::nullopt, std::optional<DType> dtype = std::nullopt) const;

static Tensor sparse_add(const Tensor& self, const Tensor& other);

static Tensor sparse_mul(const Tensor& self, const Tensor& other);

static Tensor spdiags(const Tensor& diagonals, const Tensor& offsets, const std::vector<int64_t>& shape, std::optional<int64_t> layout = std::nullopt);

static Tensor quantize_per_tensor(const Tensor& self, double scale, int64_t zero_point, int64_t quant_min = -128, int64_t quant_max = 127);

Tensor quantize_per_tensor(double scale, int64_t zero_point, int64_t quant_min = -128, int64_t quant_max = 127) const;

static Tensor dequantize_per_tensor(const Tensor& self, double scale, int64_t zero_point);

Tensor dequantize_per_tensor(double scale, int64_t zero_point) const;

static Tensor quantize_per_channel(const Tensor& self, const Tensor& scales, const Tensor& zero_points, int64_t axis = 0);

Tensor quantize_per_channel(const Tensor& scales, const Tensor& zero_points, int64_t axis = 0) const;

static Tensor dequantize_per_channel(const Tensor& self, const Tensor& scales, const Tensor& zero_points, int64_t axis = 0);

Tensor dequantize_per_channel(const Tensor& scales, const Tensor& zero_points, int64_t axis = 0) const;

static Tensor quantized_linear(const Tensor& input, const Tensor& weight, double input_scale, int64_t input_zero_point, const Tensor& weight_scales, const Tensor& weight_zero_points, const std::optional<Tensor>& bias = std::nullopt);

static std::tuple<Tensor, Tensor> forward_neg(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_exp(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_log(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_sin(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_cos(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_sqrt(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_tanh(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_sigmoid(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_relu(const Tensor& a, const Tensor& da);

static std::tuple<Tensor, Tensor> forward_add(const Tensor& a, const Tensor& da, const Tensor& b, const Tensor& db);

static std::tuple<Tensor, Tensor> forward_sub(const Tensor& a, const Tensor& da, const Tensor& b, const Tensor& db);

static std::tuple<Tensor, Tensor> forward_mul(const Tensor& a, const Tensor& da, const Tensor& b, const Tensor& db);

static std::tuple<Tensor, Tensor> forward_div(const Tensor& a, const Tensor& da, const Tensor& b, const Tensor& db);

static std::tuple<Tensor, Tensor> forward_pow(const Tensor& a, const Tensor& da, const Tensor& b, const Tensor& db);

static std::tuple<Tensor, Tensor> forward_mm(const Tensor& a, const Tensor& da, const Tensor& b, const Tensor& db);

Tensor expand(const std::vector<int64_t>& size, bool implicit = false) const;

Tensor expand_as(const Tensor& other) const;

static Tensor broadcast_to(const Tensor& self, const std::vector<int64_t>& size);

Tensor broadcast_to(const std::vector<int64_t>& size) const;

Tensor repeat(const std::vector<int64_t>& repeats) const;

static Tensor tile(const Tensor& self, const std::vector<int64_t>& dims);

Tensor tile(const std::vector<int64_t>& dims) const;

static Tensor hstack(const std::vector<Tensor>& tensors);

static Tensor& hstack(const std::vector<Tensor>& tensors, Tensor& out);

static Tensor vstack(const std::vector<Tensor>& tensors);

static Tensor& vstack(const std::vector<Tensor>& tensors, Tensor& out);

static Tensor dstack(const std::vector<Tensor>& tensors);

static Tensor& dstack(const std::vector<Tensor>& tensors, Tensor& out);

static Tensor row_stack(const std::vector<Tensor>& tensors);

static Tensor& row_stack(const std::vector<Tensor>& tensors, Tensor& out);

static Tensor column_stack(const std::vector<Tensor>& tensors);

static Tensor& column_stack(const std::vector<Tensor>& tensors, Tensor& out);

static std::vector<Tensor> tensor_split(const Tensor& self, int64_t sections, int64_t dim = 0);

std::vector<Tensor> tensor_split(int64_t sections, int64_t dim = 0) const;

static std::vector<Tensor> tensor_split(const Tensor& self, const std::vector<int64_t>& indices, int64_t dim = 0);

std::vector<Tensor> tensor_split(const std::vector<int64_t>& indices, int64_t dim = 0) const;

static std::vector<Tensor> tensor_split(const Tensor& self, const Tensor& tensor_indices_or_sections, int64_t dim = 0);

std::vector<Tensor> tensor_split(const Tensor& tensor_indices_or_sections, int64_t dim = 0) const;

static std::vector<Tensor> hsplit(const Tensor& self, int64_t sections);

std::vector<Tensor> hsplit(int64_t sections) const;

static std::vector<Tensor> hsplit(const Tensor& self, const std::vector<int64_t>& indices);

std::vector<Tensor> hsplit(const std::vector<int64_t>& indices) const;

static std::vector<Tensor> vsplit(const Tensor& self, int64_t sections);

std::vector<Tensor> vsplit(int64_t sections) const;

static std::vector<Tensor> vsplit(const Tensor& self, const std::vector<int64_t>& indices);

std::vector<Tensor> vsplit(const std::vector<int64_t>& indices) const;

static std::vector<Tensor> dsplit(const Tensor& self, int64_t sections);

std::vector<Tensor> dsplit(int64_t sections) const;

static std::vector<Tensor> dsplit(const Tensor& self, const std::vector<int64_t>& indices);

std::vector<Tensor> dsplit(const std::vector<int64_t>& indices) const;

static Tensor atleast_1d(const Tensor& self);

static std::vector<Tensor> atleast_1d(const std::vector<Tensor>& tensors);

static Tensor atleast_2d(const Tensor& self);

static std::vector<Tensor> atleast_2d(const std::vector<Tensor>& tensors);

static Tensor atleast_3d(const Tensor& self);

static std::vector<Tensor> atleast_3d(const std::vector<Tensor>& tensors);

static Tensor flatten(const Tensor& self, int64_t start_dim = 0, int64_t end_dim = -1);

Tensor flatten(int64_t start_dim = 0, int64_t end_dim = -1) const;

static Tensor unflatten(const Tensor& self, int64_t dim, const std::vector<int64_t>& sizes);

Tensor unflatten(int64_t dim, const std::vector<int64_t>& sizes) const;

static Tensor ravel(const Tensor& self);

Tensor ravel() const;

static Tensor moveaxis(const Tensor& self, const std::vector<int64_t>& source, const std::vector<int64_t>& destination);

Tensor moveaxis(const std::vector<int64_t>& source, const std::vector<int64_t>& destination) const;

static Tensor moveaxis(const Tensor& self, int64_t source, int64_t destination);

Tensor moveaxis(int64_t source, int64_t destination) const;

static Tensor swapaxes(const Tensor& self, int64_t axis0, int64_t axis1);

Tensor swapaxes(int64_t axis0, int64_t axis1) const;

static Tensor swapdims(const Tensor& self, int64_t dim0, int64_t dim1);

Tensor swapdims(int64_t dim0, int64_t dim1) const;

static Tensor argwhere(const Tensor& self);

Tensor argwhere() const;

static Tensor fill(const Tensor& self, Scalar value);

static Tensor fill(const Tensor& self, const Tensor& value);

static bool equal(const Tensor& self, const Tensor& other);

bool equal(const Tensor& other) const;

static bool allclose(const Tensor& self, const Tensor& other, double rtol = 1e-05, double atol = 1e-08, bool equal_nan = false);

bool allclose(const Tensor& other, double rtol = 1e-05, double atol = 1e-08, bool equal_nan = false) const;

static Tensor scatter_reduce(const Tensor& self, int64_t dim, const Tensor& index, const Tensor& src, std::string reduce, bool include_self = true);

Tensor scatter_reduce(int64_t dim, const Tensor& index, const Tensor& src, std::string reduce, bool include_self = true) const;

static Tensor index_reduce(const Tensor& self, int64_t dim, const Tensor& index, const Tensor& source, std::string reduce, bool include_self = true);

Tensor index_reduce(int64_t dim, const Tensor& index, const Tensor& source, std::string reduce, bool include_self = true) const;

static Tensor _scatter_reduce_backward_self(const Tensor& grad, const Tensor& self, int64_t dim, const Tensor& index, const Tensor& src, std::string reduce, bool include_self);

static Tensor _scatter_reduce_backward_src(const Tensor& grad, const Tensor& self, int64_t dim, const Tensor& index, const Tensor& src, std::string reduce, bool include_self);

static Tensor _index_reduce_backward_self(const Tensor& grad, const Tensor& self, int64_t dim, const Tensor& index, const Tensor& source, std::string reduce, bool include_self);

static Tensor _index_reduce_backward_src(const Tensor& grad, const Tensor& self, int64_t dim, const Tensor& index, const Tensor& source, std::string reduce, bool include_self);

static Tensor grouped_mm(const Tensor& self, const Tensor& mat2, const Tensor& offs);
