# 远端构建机（勿提交）

## 当前（2026-08-25 更新，RTX 4090 D 新机）

- 连接: `ssh cc`
  （HostName `10.10.49.28`，port `8088`，User `jovyan`）
- GPU: RTX 4090 D 24GB（**sm_89**，configure 需 `-DCMAKE_CUDA_ARCHITECTURES=89`,
  旧 P4 的 61 已作废）
- CUDA: 12.4，nvcc 在 `/usr/local/cuda/bin`（**不在默认 PATH**,需 export）
- 工具链: cmake 3.25 / ninja / g++ 11.4；64 核 503G 内存
- **Python 用 tp 环境**: `/opt/conda/envs/tp/bin/python`（3.12，
  pybind11/pyyaml/typing_extensions/numpy 已装）；configure 加
  `-DPython_EXECUTABLE=/opt/conda/envs/tp/bin/python`。
  系统默认 python3 是 3.8 且缺依赖,勿用
- **存储布局**: `~`（/home/jovyan）与 /opt 是 **NFS 持久盘——慢,只用来
  存东西**;`/` 与 `/tmp` 是 overlay(本地快盘 877G)。**构建一律放
  overlay**(如 `/tmp/TensorPlay`),NFS 上编译会极慢且状态易失踪;
  要保留的产物/源码包再落回 ~
- oneDNN 走 `third_party/v3.4.1.zip`(zip 内单一顶层目录 `oneDNN-3.4.1/`,
  FetchContent 正常)

## 配置期三坑与解法（2026-08-25 下午实测，全在新机踩到）

1. **cmake 版本二选一都有坑**:
   - conda 自带 3.25:`check_language(CUDA)` 的 ABI 探测产物 bin 在 NFS 上
     读不到(报 file STRINGS cannot be read),换 overlay 盘 + 新 cmake 才过;
   - tp env 里 pip 装 4.x:硬拒 oneDNN 的老 `cmake_minimum_required`,
     加 `-DCMAKE_POLICY_VERSION_MINIMUM=3.5`;
   - 稳妥组合:**cmake≥4 + `-DCMAKE_CUDA_COMPILER=/usr/local/cuda/bin/nvcc`
     显式指定**(跳过 check_language)+ 上面 POLICY 开关。
2. **vendored OpenBLAS 0.3.29 必炸**:`NOFORTRAN=ON` 时其
   `ctest/CMakeLists.txt:47` 的 `${CMAKE_Fortran_COMPILER_ID}` 展开为空,
   if() 语法错。`-DBUILD_TESTING=OFF` **挡不住**(ctest 只受 NO_CBLAS 门控,
   关了会断 cblas 链接)。解法:首次 configure 解包后对
   `build/_deps/openblas_vendor-src/ctest/CMakeLists.txt` 该行给两个变量
   加引号(`"${VAR}" STREQUAL "GNU"` 形式),原地重跑 configure 即可。
3. **codegen 缺 torchgen**:源码 tar 若不含 `third_party/pytorch` 子模块,
   generate_code 报 "vendored torchgen not found"。只需同步
   `third_party/pytorch/torchgen/`(~1.5M)。已补进 /tmp/TensorPlay。

## 协作约定

- **远端唯一工作树 `/tmp/TensorPlay`**,单目录单构建(AGENTS.md 共识),
  勿另起 /tmp/tpwork 之类副本树;
- ssh 备注:host key 已轮换(重装),旧记录用 `ssh-keygen -R '[10.10.49.28]:8088'`
  清掉;私钥在本地 `/root/.ssh/id_ed25519`(自 WSL C 盘拷贝,原 config 是
  Windows 路径已改);远端无 scp,传文件用 `ssh cc 'cat > f' < f` 管道。

## torch 共存(2026-08-25)

- tp 环境 torch 2.8.0+cu128(pip 自带 libcudart.so.12=12.8)与 tp 的 _C
  (链 /usr/local/cuda 12.4)**单进程只能先导入 torch**:若 tensorplay 先
  import,12.4 cudart 先占位,torch 随即报
  `cudaGetDriverEntryPointByVersion ... libcudart.so.12`。
  验证脚本一律 `import torch` 在前;
- 跑测试/脚本:cd /tmp/TensorPlay 或 PYTHONPATH=/tmp/TensorPlay(包非安装式);
- GEN_BASE_DIR 是源码树相对路径 → build/ 与 buildcuda/ 共享 build/generated,
  改 yaml/codegen 后两个构建目录的生成物天然同步。
